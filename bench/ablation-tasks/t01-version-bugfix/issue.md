## Task (t01-version-bugfix, quality-ablation arm A — issue #2130)

The CLI's --version flag currently crashes with a stack trace instead of printing the version — fix it, and make sure the fix is tested.

## Acceptance
check: adjudicated against a hidden answer key by the operator after the run — the run is graded on delivered behavior, not on self-report.
empty state: the fixture as checked out.
provenance: executed-unit.
