# Deviation log — issue #2451 (execution-observation role)

- 2026-08-26T01:15:00Z | filed | issue-2451/execution-observation |
  canonical: `gh pr view 2470 --repo tokenmaxxxer/on-the-record --json state` (this turn) — result: `{"state":"OPEN"}`, same transcript quoted in `docs/issue-2451/reports/execution-observation.md` under `## What was done` bullet 3.
  the `pr-preflight` gate required `Closes #2451` in this PR's body
  (build-now/CORE_BUILD_NOW=1 treats it as phase-2-equivalent), but PR
  #2470 — the PR actually carrying the merge-gates.md fix, itself already
  carrying its own `Closes #2451` — was still OPEN at observation time
  per the command above. Not called for by this task's scope (observe
  and record, not decide PR sequencing), and required a role-shaped
  judgment call outside the frozen write set: whether to merge PR #2470
  myself (with `--delete-branch`, which would also make acceptance
  bullet 3 observable) to avoid the issue closing before its actual fix
  lands.
  Declined to merge PR #2470: execution-observation holds no merge
  authority under the role-handoff contract, and merging another role's
  deliverable to force a gate/acceptance-bullet into a favorable state is
  exactly the kind of action the contract's "never approve or merge
  yourself" boundary and this session's own safety directives (ask before
  hard-to-reverse, shared-state actions) argue against. Resolved by
  complying with the gate literally (added `Closes #2451`, matching this
  repo's own established convention from PR #2477/issue-2379) while
  adding an explicit sequencing note in the PR body asking whoever merges
  next to land PR #2470 first, and by keeping the record's own frontmatter
  `result` field set to bullet 3's own outcome rather than the
  implementation record's claimed one, so the gap stays visible
  regardless of merge order. A role session never spawns a peer role or
  opens an issue on its own initiative mid-task, so this is reported here
  and in the session's final reply rather than filed as a new issue.
