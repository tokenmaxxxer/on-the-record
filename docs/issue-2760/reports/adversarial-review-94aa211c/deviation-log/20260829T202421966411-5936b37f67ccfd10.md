2026-08-30T00:00:00Z, inline, first probe-harness fixture used `tempfile.mkdtemp()` under `/tmp` and hit a pre-existing unrelated empty `/tmp/.git` directory, briefly contradicting the merge-base acceptance-check-3 result; fixed by moving fixtures to `/dev/shm` after confirming clean `.git` ancestry — see docs/issue-2760/reports/adversarial-review-94aa211c.md, "What did not work" and "Harness construction and one real deviation" sections.
