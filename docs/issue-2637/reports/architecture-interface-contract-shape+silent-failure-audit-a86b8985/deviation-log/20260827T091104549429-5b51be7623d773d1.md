canonical: before-landing warrant-hunter agent output (stance 0, "assume the gate just touched is bypassable"), and this session's own re-verification commands against `on-the-record/hooks/deliverable-guard.sh`.

- 2026-08-27T09:11:04Z: expected the first-draft `PRODUCT_CAPTURE_PRIORITIES_DIR_RE`
  regex added to `on-the-record/hooks/deliverable-guard.sh` to hold as written;
  it did not — the hunter found it was unanchored (`re.search`, no `^`), so
  a deliverable path shaped like

  ```
  src/docs/reports/product/priorities/hack.md
  ```

  was silently exempted merely by ending with the shard suffix, even
  though it is not actually inside the real shard directory. Fixed by
  anchoring both alternatives of the regex with `^`; re-verified live
  against the real hook script before commit — exploit-shaped path now
  denies (exit 2), legitimate shard path still exempts (exit 0). Full
  reproduction sits in this session's own hunt record, same commit as
  this entry. Left unfixed, out of scope for issue #2637: two
  pre-existing checks in the same function (`n.endswith(EXEMPT_SUFFIXES)`,
  `PRODUCT_CAPTURE_ISSUE_RE.search(n)`) share the identical
  unanchored-suffix-match class and predate this change.
