canonical: this session's own re-verification commands against `on-the-record/hooks/deliverable-guard.sh` and `on-the-record/hooks/product-capture-stopgate.sh`, reproduced in full in `docs/issue-2637/reports/execution-observation.md`'s "Check 3" and "What did not work" sections.

- 2026-08-27T09:24Z: expected the first `deliverable-guard.sh` functional
  re-test to exercise the orchestrator code path; it did not — this
  observer session's own ambient `TOKENMAXXXER_SPAWNED=1` made the guard
  no-op for every payload, including a plain, untracked `src/foo.py`
  synthetic `file_path` value (never written to disk). Fixed by
  re-running with `env -u TOKENMAXXXER_SPAWNED`; re-verified the
  anchor-bypass fix and a genuine unrelated-`src/` denial both hold
  (exit 2 for the bypass attempt and for an unrelated `src/` write, exit
  0 for a real shard path).
- 2026-08-27T09:24Z: derived: a first attempt at the
  `product-capture-stopgate.sh` check used a single-line JSON array
  (`[{"type":"user",...}]`) as the transcript file instead of JSONL,
  producing no nudge in either state. Root cause reproduced directly:

  ```
  $ python3 -c "import json; print(isinstance(json.loads('[{\"type\":\"user\"}]'), dict))"
  False
  ```

  The hook's transcript reader calls `json.loads(line)` once per line and
  only processes `dict` results — a whole-array line parses to a `list`,
  so every line was silently skipped. Fixed by writing the transcript as
  JSONL (one JSON object per line); re-verified the nudge fires before a
  shard exists and goes silent once one does (see Check 3 in the main
  record).

Both dead ends were caught and corrected within this session, before
either was reported as a finding against the reviewed code.
