- 2026-08-29T07:15:18Z, inline, secure-coding-authorization-access-control+silent-failure-audit-604a6e6b(issue-2669): applied both mounted skills' framing during design and wrote `skill-verdict: ... — applied: invoked;` lines into the record (commit 76177caa) before ever calling the Skill tool for either — violates invoke-before-apply (issue #2062: applicable skills must be loaded via the Skill tool *before* applying). Caught by the Stop hook's skill-verdict-guard notice. Invoked both skills via the Skill tool this same session and re-checked the delivered fix against their actual rule text (silent-failure-audit's Handled/Silently-Absorbed/Unreachable classification; secure-coding-authorization-access-control's rule 7, multi-entry-path parity). That re-check surfaced a real gap the original design pass missed: the `cd`-prefix regex only matches a single leading `cd <dir> &&`/`cd <dir>;` — `pushd`, a subshell `(cd dir && ...)`, or a second chained `cd` all still fall back to the payload cwd. Follow-up: fixed the record's skill-verdict lines and added this as a new Open finding rather than silently absorbing it. derived: `python3 -m pytest test/test_upstream_defect_scope_guard_cross_repo_cwd.py -v` — result: 4 passed, 1 xfailed (behavior unchanged by this record-only correction).
