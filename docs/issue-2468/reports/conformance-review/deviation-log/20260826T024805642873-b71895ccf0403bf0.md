- 2026-08-26T02:48:05Z inline conformance-review(issue-2468): this session's housekeeping-landing commit first tried a heredoc-shaped `git commit -m "$(cat <<'EOF' ... EOF)"` body and was refused by `heredoc-command-refusal-gate` (issue #1976). Expected the heredoc form to commit; instead retried with the required repeated `-m` flag form (one `-m` per paragraph, ending with a `Subject: issue-2468` trailer paragraph) and it succeeded.
  acceptance: `git log -1 --format=%B 50568763` (this session) — result:
```
issue-2468: log deviation -- stale PID/worktree drift, land hook-fires shards

This session found the prior checkpoint's REQ-4 blocker (background pytest PID 3813978) belonged to the prior session's own shell and had died with it; also found the /tmp/pr2483-review worktree had drifted to an unrelated issue-2403 commit via /tmp path reuse. Both are logged in the deviation-log entry committed here, along with two read-only Bash commands this session that were refused by pretooluse-dispatcher.sh's approval-gate despite being read-only. No review verdict or requirement content changed this commit -- REQ-4 remains open per the existing record. Also landing five hook-fires shards left untracked since the last commit.

skill-verdict: other mounted skills (conformance-review-*) -- not triggered this session; this commit is housekeeping/deviation-logging only, no new requirement-extraction, sampling, verdict-assignment, evidence, or finding-record work was performed.

Subject: issue-2468
```
  No review content was affected -- the commit landed on the retry, same message body, correctly formed.
