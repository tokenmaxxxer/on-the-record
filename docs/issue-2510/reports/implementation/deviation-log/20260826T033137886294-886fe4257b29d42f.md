# Deviation log — issue-2510 / implementation

canonical: python3 gates/test_requirement_met.py (42/42 passed, includes
t_summarize_empty_advisory_not_blocked_is_not_all_unknown; see
docs/issue-2510/reports/implementation/2026-08-26-hunt-fix-inline-print-into-summarize.md
for the hunt transcript)

- 2026-08-26T00:00:00Z, inline, a before-landing warrant-hunt pass on
  the delivery PR found `summarize()`'s `unknown == total` check is
  vacuously true at `total == 0`, so a hand-built
  `{"empty_state": False, "blocking_reasons": [], "advisory": []}` dict
  (not reachable via `check()`/`grade()` today, but callable directly
  since `summarize()` is a pure function) read "0 criteria" as "0
  criteria, all UNKNOWN" — the exact not-evaluated/evaluated-and-fine
  conflation this issue exists to remove, just at a not-yet-reachable
  edge of the new function's own contract. Stayed inside this issue's
  frozen write set (`gates/requirement_met.py`,
  `gates/test_requirement_met.py`); mechanical guard (`total == 0` check
  ahead of the UNKNOWN branch), no design/product judgment, doesn't
  change what the deliverable claims to do, one-off. Fixed inline with a
  pinning test. gates/requirement_met.py:559-567 (guard added before the
  `unknown == total` branch) /
  gates/test_requirement_met.py:t_summarize_empty_advisory_not_blocked_is_not_all_unknown.
