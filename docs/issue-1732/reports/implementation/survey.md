# Survey — issue #1732

Write surfaces:
- `on-the-record/monitors/poll-heartbeat.sh:326-341` — the `to_emit`-empty branch of the delta-diff heredoc (the same inline `python3 -` block #1220/#1719 already own). When `to_emit` is empty and the tick has crossed the 1800s `last_emit_epoch` bound, it unconditionally builds `heartbeat_lines` starting with `f"[heartbeat] monitoring active, {healthy} session(s) tracked, no changes"` (line 335) and then extends it with any `returned-pr:`-keyed lines from `curr` (line 339, the #1719 req#1 re-surfacing), writing the whole list to stdout and setting `emitted_now = True` unconditionally — so the bound always fires something, even when the only content is the now-to-be-removed heartbeat line. `healthy` (line 333) is computed only to feed that one f-string and has no other reader.
  canonical: on-the-record/monitors/poll-heartbeat.sh:326-341 (this session, read verbatim)
- `on-the-record/monitors/poll-heartbeat.sh:343` — `new_state = {"lines": new_lines, "last_emit_epoch": now if emitted_now else prev.get("last_emit_epoch", 0)}`. `last_emit_epoch` already only advances when `emitted_now` is `True`; the fix's "leave `last_emit_epoch` untouched" requirement falls out for free once the no-returned-pr case stops setting `emitted_now = True` — no separate change needed at this line.
  canonical: on-the-record/monitors/poll-heartbeat.sh:343 (this session, read verbatim)
- `grep -n "monitoring active" on-the-record/monitors/poll-heartbeat.sh` — one hit, line 335 (cited above). The issue's Acceptance check 3 (`grep -n "monitoring active" monitors/poll-heartbeat.sh`) targets this same file at its abbreviated path; the checkout root for that grep is `on-the-record/monitors/poll-heartbeat.sh` in this repo layout — confirmed via `find . -iname poll-heartbeat.sh` returning exactly one match at that path.
  canonical: `grep -n "monitoring active" on-the-record/monitors/poll-heartbeat.sh` (this session) — output `335:            f"[heartbeat] monitoring active, {healthy} session(s) tracked, no changes"`
- Test ownership — no existing suite exercises the 1800s bound at all. `grep -rln "1800\|monitoring active\|last_emit_epoch" . --include="*.py" --include="*.sh"` (excluding `.git/`) hits only `spawn.py` (unrelated `1800` literal) and `on-the-record/monitors/poll-heartbeat.sh` itself — no test file references the bound today. `on-the-record/monitors/test_poll_heartbeat.py` already owns the closest neighboring coverage (`t_returned_pr_unchanged_set_produces_no_output_on_due_tick`, `t_returned_pr_new_item_emits_on_due_tick`, both added for #1719) via its `_run_tick(checkout, home, report)` two-tick-against-the-same-checkout harness, which drives the real script with `FAKE_POLL_DUE=1` and persists `runs/poll_heartbeat_last_state.json` across calls. `gates/test_poll_heartbeat_delta.py` (#1117) covers whole-report delta suppression generally but has no returned-pr or bound-specific fixtures. The bound itself (1800s) cannot be crossed by real wall-clock waiting in a test; it must be simulated by writing `runs/poll_heartbeat_last_state.json`'s `last_emit_epoch` directly between two `_run_tick` calls, the same on-disk state file `poll-heartbeat.sh` itself reads/writes, so a new test helper following `_run_tick`'s established pattern belongs in `on-the-record/monitors/test_poll_heartbeat.py`.
  canonical: `grep -rln "1800\|monitoring active\|last_emit_epoch" . --include="*.py" --include="*.sh" | grep -v /.git/` (this session) — output `spawn.py` and `on-the-record/monitors/poll-heartbeat.sh` only
- `docs/handbooks/monitor-liveness.md:88-90` — the "Quiet ticks" section already states "A not-due tick, and a fully-suppressed due tick, print nothing to the Monitor channel" as the general delta-suppression contract; it never names the removed heartbeat line or the 1800s bound at all (`grep -n "monitoring active\|1800\|30.min" docs/handbooks/monitor-liveness.md` has no hits for those terms). This fix makes that existing sentence accurate for the no-returned-pr bound case (it previously undersold what the bound actually did); no wording in the handbook becomes wrong, and no new env var/config key/dependency/migration/setup step is introduced, so no handbook edit is required by the doc-placement ladder.
  canonical: docs/handbooks/monitor-liveness.md:88-90 (this session, read verbatim); `grep -n "monitoring active\|1800\|30.min" docs/handbooks/monitor-liveness.md` (this session) — empty output
- `docs/issue-1220/**` and `docs/issue-1719/**` mentions of "monitoring active" are past-tense frozen records of the state at the time those issues landed, not live documentation — out of this write set, same convention #1730's survey applied to `docs/issue-1587/**`/`docs/issue-1605/**`.

canonical: `gh issue view 1732` output (this session, read verbatim) — issue body carries `validity-consult-skip: trivial` and `design-research-skip: mechanical` as closed-vocabulary skip tags, and its Acceptance section lists the fix's four checks (empty-bound-no-output, returned-pr-only-bound-output, grep-clean, existing suites unaffected).

Skip condition: pure bugfix — per the canonical citations above, the fix is fully specified by the issue's own Acceptance section and confirmed by reading the `to_emit`-empty branch and the `new_state` write above (no design decision is open: the diff-keying logic, the always-emit category, and the returned-pr re-surfacing #1719 built are all unchanged; only the heartbeat-line emission is deleted and the emit condition for the bound narrows from "always" to "only when there are returned-pr lines"). Scouting/full survey round skipped per scout-directive's bugfix skip condition.
