# Deviation log — issue-2158 (implementation role)

- 2026-08-24T06:16:05Z | inline | after landing the `pr-preflight.sh` build-now bypass and its own new tests, running `python3 -m pytest gates/ test/ on-the-record/hooks/ -q` surfaced two pre-existing end-to-end test helpers outside the issue's literally named two-file scope (`pr-preflight.sh` + `test_pr_preflight.py`) that build their subprocess `env` via `dict(os.environ)` without popping `CORE_BUILD_NOW` — they inherited this session's own real `CORE_BUILD_NOW=1` and failed once the new bypass legitimately honored it. Mechanical one-line `env.pop("CORE_BUILD_NOW", None)` fix in each, same isolation `test_approval_gate.py` and the updated `test_pr_preflight.py::_run_preflight` already had; no design/architecture/product judgment, no change to what the deliverable claims to do. canonical: `on-the-record/hooks/test_pr_preflight_delegation.py:81-82` and `test/test_branch_role_field.py:319-322`, this session — diff landed in the same commit as the feature (`42f2d920`), documented in `docs/issue-2158/reports/implementation.md`'s "What did not work"/"Rationale for deviations" sections.
