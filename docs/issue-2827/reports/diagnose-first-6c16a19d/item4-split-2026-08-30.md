---
kind: evidence
subject: issue-2827
doc-type: reference
---

# issue-2827 — item 4 split (2026-08-30)

## Scope

canonical: `gh pr view 2825 --repo tokenmaxxxer/on-the-record` (merged;
title "issue-2135: re-measure standing context post-diet -- 44,840
tokens, still over 25K") and its committed file
`docs/issue-2135/reports/diagnose-first+technical-writing-minimalism-scoping-5676d1d0/composition-breakdown-2026-08-30.md`
(read this session) — that file measured a live spawn's standing context
at 44,840 tokens and put on-the-record's own share (its items 1-3) at
4,479 tok, ~10%, leaving item 4 ("CLI baseline + tool schemas + core
SessionStart hook injection", 40,361 tok) as one unattributed lump. This
file splits item 4 by source, per this issue's Acceptance, using this
session's own live first-turn spawn as the instrument (same shape PR
#2825 used: `sonnet`, single-phase via `CORE_BUILD_NOW=1`, launched
through `spawn.py` against the live board). Session log:
`on-the-record-issue-2827-diagnose-first-6c16a19d.session.20260830T135512.515671.log`
under `$MUSTER_WORKSPACE_ROOT`.

Byte-to-token ratio used throughout: `bytes / 4 ≈ tokens` — the same
convention PR #2825's breakdown used — derived: in that file, items 1-3
were 2563/4=640.75≈641 tok, 12384/4=3096 tok, 2969/4=742.25≈742 tok, each
matching its stated token figure.

## Total, this spawn

**44,860 tokens** (9,817 cache-creation + 35,043 cache-read).

derived:
```
python3 -c "
import json
p='/home/jwjung/.tokenmaxxxer/work/on-the-record-issue-2827-diagnose-first-6c16a19d.session.20260830T135512.515671.log'
with open(p) as f:
    for l in f:
        d = json.loads(l)
        if d.get('type') == 'assistant':
            u = d.get('message', {}).get('usage', {})
            if u:
                print(u.get('cache_creation_input_tokens'), u.get('cache_read_input_tokens'))
                break
"
```
result: `9817 35043` → 9817+35043=44860.

## Items 1-3 (re-measured this spawn only to compute item 4 as a remainder)

#### Item 1 — spawn-assembled directive index

2,086 B = 521.5 tok. derived: `wc -c
/home/jwjung/.tokenmaxxxer/work/on-the-record-issue-2827-diagnose-first-6c16a19d.task.txt`
→ `2086`, 2086/4=521.5.

#### Item 2 — on-demand section files via `--append-system-prompt`

12,384 B = 3,096 tok. derived:
```
cd /home/jwjung/.tokenmaxxxer/work/on-the-record-issue-2827-diagnose-first-6c16a19d
python3 -c "
import spawn, directive_assembly as da
files = da.directive_section_files(skills_mounted=True, checkpoint_block=None, code_scoped=True)
print(sum(len(v.encode('utf-8')) for v in files.values()), 'B files')
print(len(da._directive_system_prompt_block(files).encode('utf-8')), 'B system-prompt block')
"
```
result: `12203 B files` / `12384 B system-prompt block`, 12384/4=3096 —
byte-identical to PR #2825's own figure for this item (canonical: the
composition-breakdown file cited in Scope above), confirming this code
path is unchanged.

#### Item 3 — per-turn UserPromptSubmit re-injection

3,451 B = 862.75 tok. derived: this turn's 7 UserPromptSubmit reminders
(proposal-shape, survey-order, freelunch, warrant, record-shape, terse,
scout), copied verbatim from this session's own turn (incl. their
`<system-reminder>` wrapper tags) to a scratch file, then `wc -c
/tmp/ups_reminders_2827.txt` → `3451`, 3451/4=862.75. (PR #2825's own
item-3 figure, 2,969 B, excluded the wrapper tags — canonical: the
composition-breakdown file cited in Scope above; either way this item is
tokenmaxxxer-core-owned and already dieted per core#278, a non-goal of
this issue, re-measured here only so item 4 below is arithmetically
correct for this spawn.)

#### Sum 1-3 and item 4 as remainder

2086+12384+3451=17921 B = 521.5+3096+862.75=4480.25 tok ≈ 4,480 tok.
4480/44860=0.0999 → **9.99%** of this spawn's total — derived: arithmetic
on the three figures above, matching PR #2825's own 4479/44840=0.0999
(canonical: the composition-breakdown file cited in Scope above).

**Item 4 (remainder) = 44,860 − 4,480 = 40,380 tokens** — derived:
arithmetic on the total (44,860, cited above) minus the items-1-3 sum
(4,480, cited above).

## Item 4, split by source

#### (a) core-plugin SessionStart hook injection

10,805 B = 2,701 tok (10805/4=2701.25). derived: this session's own log,
the `SessionStart:startup` `hook_response` events (5 total — one per
mounted plugin registering a `SessionStart` hook; 3 return empty
stdout):
```
python3 -c "
import json
LOG='/home/jwjung/.tokenmaxxxer/work/on-the-record-issue-2827-diagnose-first-6c16a19d.session.20260830T135512.515671.log'
with open(LOG) as f:
    lines = f.readlines()
for i in range(0, 10):
    d = json.loads(lines[i])
    if d.get('type') == 'system' and d.get('subtype') == 'hook_response':
        out = d.get('stdout') or ''
        print(d.get('hook_id'), len(out.encode('utf-8')), out[:40].replace(chr(10),' '))
"
```
result:
```
e0b889bf-ac55-468a-9691-c2e377f198e8 0
75af69a4-5401-481d-b8a7-484f9bb28500 0
24bce977-e769-49cb-96f8-59b3eb984e84 1026 warrant: open work units in this repo
2e40a947-955c-4110-a73b-b1619121166f 0
35801644-92a2-4ff2-a1d1-026ac889f3db 10805 [core] Interaction protocol for role d
```
The 10,805 B entry (`hook_id` `35801644...`) is the block this session
saw as a `<persisted-output>` system-reminder at turn 1 ("Output too
large (10.5KB)... [core] Interaction protocol for role
diagnose-first-6c16a19d...") — this is "the core SessionStart hook's own
injection" this issue names. Owner: tokenmaxxxer-core (`core` plugin).

#### (b) warrant-plugin SessionStart hook injection

1,026 B = 257 tok (1026/4=256.5). derived: same command and result as
(a) above, the `24bce977...` entry — "warrant: open work units in this
repository..." A second, smaller SessionStart injection, also
core-family, not individually named in this issue's original item-4
description; surfaced here as part of the split. Owner:
tokenmaxxxer-core (`warrant` plugin).

#### (c) deferred-tool name-list overhead

459 B = 115 tok (459/4=114.75). derived: the "following deferred tools
are now available via ToolSearch" reminder this session saw at turn 1,
copied verbatim to a scratch file: `wc -c
/tmp/deferred_tools_reminder_2827.txt` → `459`. Cross-check: this
session's own `system/init` event lists 26 tools total —
```
python3 -c "
import json
LOG='/home/jwjung/.tokenmaxxxer/work/on-the-record-issue-2827-diagnose-first-6c16a19d.session.20260830T135512.515671.log'
with open(LOG) as f:
    for l in f:
        d = json.loads(l)
        if d.get('type')=='system' and d.get('subtype')=='init':
            print(len(d['tools']), d['tools']); break
"
```
result: `26` tools, of which 11 carry a full JSON-schema definition in
this session's own turn-1 context (`Agent, Bash, Edit, ListAgents, Read,
ReportFindings, ScheduleWakeup, Skill, ToolSearch, Workflow, Write`) —
26−11=15, matching the deferred-list reminder's own count. Owner: harness
(Claude Code CLI `ToolSearch` deferral mechanism).

#### (d) skill-backed slash-command listing

8,833 B = 2,208 tok (8833/4=2208.25). derived: the "The following skills
are available for use with the Skill tool" reminder this session saw at
turn 1, copied verbatim to a scratch file: `wc -c
/tmp/skills_reminder_2827.txt` → `8833`. Owner: mounted plugins (core,
terse, freelunch, scout, warrant, diagnose-first, work-in-english,
research-evidence-discipline) — derived: this session's own
`system/init` event field `plugins` (read this session) lists exactly
these 8 plugins and no `on-the-record` entry among them — 0 of the 8 is
on-the-record.

#### (e) agent-type listing for the Agent tool

2,261 B = 565 tok (2261/4=565.25). derived: the "Available agent types
for the Agent tool" reminder this session saw at turn 1, copied verbatim
to a scratch file: `wc -c /tmp/agent_types_reminder_2827.txt` → `2261`.
Owner: mounted plugins (core, freelunch, warrant) — same `plugins` field
cited in (d) — 0 on-the-record.

#### Sum (a)-(e)

2701+257+115+2208+565=5846 tok — derived: arithmetic on the five figures
above.

#### (f) residual

40380−5846=34534 tok (85.5%: 34534/40380=0.855; 77.0% of the 44,860
total: 34534/44860=0.770) — derived: arithmetic on item 4's total (cited
above) minus the sum (a)-(e) (cited above). This is the harness CLI
baseline system prompt (the fixed "You are Claude Code..." /
tool-use / tone / auto-memory / environment / context-management prose)
plus the 11 eagerly-loaded tool schemas named in (c) plus the 33
non-skill slash-command registrations (`clear, model, config, doctor,
usage, ...` — derived: this session's own `system/init` event field
`slash_commands` has 54 entries, `skills` has 21 entries, 54−21=33 slash
commands with no corresponding descriptive text anywhere in this
session's context).

unverifiable: splitting (f) further into "harness baseline prose" vs.
"the 11 eagerly-loaded tool schemas" vs. "the 33 non-skill slash-command
registrations" — reason: this session's own log's `system/init` event
(read above) records only structural counts and name lists for `tools`
and `slash_commands`, not the literal wire-format system-prompt string or
`tools`-array bytes the Claude Code CLI actually sends to the model API.
That payload is assembled and transmitted by the CLI process itself and
is not captured anywhere a spawned session can read — unlike (a)-(e),
there is no log field, hook stdout, or re-fetchable tool call that
isolates it. What would be needed: either (i) the Claude Code CLI /
agent-SDK's own instrumentation of the literal system-prompt string and
`tools` array it sends per request (owned by Anthropic / the harness, not
this repo), or (ii) a per-content-block token count on a single raw API
request captured outside the CLI (e.g. a proxy capturing the literal
request body) — neither is available to a session spawned through
`spawn.py`.

## One calibration point on (c)/(f) (illustrative only, not summed into item 4)

Fetching one currently-deferred tool's full schema live via `ToolSearch`
mid-session (`WebSearch`) returns 1,906 B = 476 tok (1906/4=476.5).
derived: the literal JSON object `ToolSearch` returned this session for
`select:WebSearch`, copied verbatim to a file: `wc -c
/tmp/toolsearch_websearch_2827.json` → `1906`.

That fetch happened mid-session (after first turn), so it is not part of
the 44,860-token total measured above — it sizes the deferral mechanism
only. If the 15 tools in (c) were instead eagerly loaded at roughly this
size each, standing context would grow by roughly 15×476=7140 tok
relative to today (derived: arithmetic, 15*476=7140) — an order of
magnitude more than items 1-3 combined (4,480 tok, cited above). Offered
as context for why (f)'s residual likely skews toward "the 11
eagerly-loaded tool schemas" rather than harness prose alone — not a
claim this session can prove without the wire payload (see the
unverifiable note above).

## Actionable share, stated as a single number

Every sub-part of item 4 identified above — (a) and (b) tokenmaxxxer-core
plugins, (c)-(e) harness mechanism plus plugin registrations with zero
on-the-record-owned plugins among them (derived: `plugins` field cited in
(d)/(e) above), (f) harness CLI baseline — is outside on-the-record's
ownership. **on-the-record's actionable share of item 4 is 0 tokens,
0%.**

That leaves on-the-record's total actionable share exactly where items
1-3 already put it:
- strict per-item ownership (items 1+2 only — item 3's owner is
  tokenmaxxxer-core per PR #2825's own table, canonical: the
  composition-breakdown file cited in Scope above): 521.5+3096=3617.5 tok
  = 3617.5/44860=0.0807 → **8.07%** of the total.
- the issue's own established aggregate framing (items 1-3 bundled, per
  PR #2825's "4,479 tok / 10%" conclusion — canonical: the
  composition-breakdown file cited in Scope above, and this file's own
  item-3 re-measurement above, which reproduces it to within 1.99 tok):
  4,480/44,860=0.0999 → **9.99%** of the total.

Both readings are at or under this issue's own 10% line (derived:
0.0807 < 0.10 and 0.0999 < 0.10). Splitting item 4 into five attributed
parts plus one explicitly unattributable residual adds nothing to either
number — it confirms PR #2825's Amdahl ceiling rather than finding a new
lever inside it. Per this issue's Acceptance ("if the actionable share is
under 10% of the total, say so plainly — that is a finding, and it ends
this line of work rather than starting another round"): **it is under
10%. This ends this line of work.** No further on-the-record-scoped
context diet is proposed by this session.

## What was not attempted, and why

- No tool or slash command was unregistered, and none is proposed for
  removal — the issue's `must not` forbids buying a smaller number by
  removing capability sessions use, and this measurement did not surface
  any on-the-record-owned lever to cut in the first place.
- The `--append-system-prompt` section files (item 2) were not touched.
  canonical: `gh issue view 2204 --repo tokenmaxxxer/on-the-record`
  (closed; its Acceptance and Fix sections document why item 2 rides the
  system prompt instead of being lazy-loaded — a live-spawn session log
  showed sessions reading every pointed-at file sequentially before their
  first task action, ~46s, under the prior "Read `<file>` when
  `<condition>`" design). Cross-checked against the current code:
  `directive_assembly.py` lines 480-490 (read this session) carry the
  same rationale verbatim in a comment. Re-opening that question is out
  of scope per this issue's `must not`.
- No ablation/re-run bench was executed — this issue's Acceptance is a
  measurement-and-attribution contract (instrument, split, state the
  actionable share), not a re-run of #2135's ablation leg. derived: `git
  status --short` for this session (see the record's Acceptance
  verification section) shows no `bench/ablation.py` output path among
  the changed files.
