# Deviation log — issue #2463 (conformance-review role)

- 2026-08-26T00:00:00Z | inline | issue-2463/conformance-review |
  landing hit three mechanical repo gates not anticipated in the original
  task brief. (1) `record-claim-guard` refused the first `Write` of the
  review record: a sentence in the "Why" section restated test-run counts
  without an adjacent `canonical:`/`derived:` tag in that same section,
  and `loop_state: reported` triggered an `open-finding-resolution-path`
  requirement even though "Open findings" was already "None." — resolved
  by pointing the restated counts back at their own already-tagged
  transcripts under "What was done" instead of re-asserting them bare,
  and by adding explicit "no resolution path is needed" language to the
  empty Open findings line. (2) `heredoc-command-refusal-gate` (issue
  #1976) refused the first `git commit` (`-m "$(cat <<'EOF' ... EOF)"`)
  and the first `gh pr create --body "$(cat <<'EOF' ... EOF)"` — resolved
  by switching to one `-m` flag per paragraph for the commit, and to
  `--body-file` for the PR, no content change either time. (3)
  `pr-preflight` refused the first `gh pr create` because the PR body
  lacked a `Closes #2463` trailer — under this session's `CORE_BUILD_NOW=1`
  build-now bypass, the gate treats this branch as a phase-2-equivalent
  delivery PR, which this repo's convention requires to carry that
  trailer regardless of role, contrary to this session's initial
  assumption (drawn from the pre-build-now precedent PR #2456, which used
  a plain issue reference with no Closes trailer). Resolved by adding
  `Closes #2463` to the PR body (PR #2464, the actual code-fix PR, also
  carries this same trailer for the same issue — both referencing it is
  expected under this convention, not a conflict). Not filed as new
  issues: all three are one-off, mechanical corrections to match
  existing, already-documented repo conventions (the same
  heredoc-refusal/pr-preflight pair recurred verbatim from the issue-2417
  execution-observation session's own deviation log), no
  design/architecture/product judgment involved.
  canonical: the `PreToolUse:Write` hook error transcript (record-claim-guard
  + open-finding-resolution-path), and the two `PreToolUse:Bash` hook
  error transcripts (heredoc-command-refusal-gate on commit, then on
  `gh pr create`; pr-preflight's exact `expected: 'Closes #2463'`
  message) are all in this session's own transcript; every retried
  command succeeded after the fix (commit `f589cfab`, PR
  https://github.com/tokenmaxxxer/on-the-record/pull/2466).
