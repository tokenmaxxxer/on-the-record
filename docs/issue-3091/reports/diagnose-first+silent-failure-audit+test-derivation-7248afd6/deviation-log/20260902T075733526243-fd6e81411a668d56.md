# Deviations

- Expected the 4 dead-`checkout_issue_branch`-mock fixes to fully
  resolve their respective tests on the first edit. Two needed a
  second, independent stale-mock fix (`gh_rest.fetch_issue` missing
  `owner`/`repo` keys, issue #2395) once the branch-checkout crash
  stopped masking it.
- Expected the 15 per-file fixes to leave the full `test/` directory
  green without further changes. Running it together exposed a 16th,
  order-fragile ledger assertion in
  `test_ledger_entry_records_not_run_when_skill_source_is_not_skill_repo`
  (`recorded[-1]` assumed a fixed write order between two independently
  scheduled `ledger_write` calls), fixed by searching for the tagged
  entry instead of indexing positionally.

Full reproduction commands and evidence for both are in
`docs/issue-3091/reports/diagnose-first+silent-failure-audit+test-derivation-7248afd6.md`'s
"What did not work" section (same-commit).
