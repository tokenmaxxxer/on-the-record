- 2026-08-30T01:01:31Z — inline — first Write attempt at this record was rejected twice by pretooluse-dispatcher.sh gates: (1) record-claim-guard flagged a state/defect claim in the "Next steps" section lacking a `canonical:`/`derived:` tag in that same section, and a citation to `docs/issue-2811/reports/technical-writing-style-guide-compliance-ea5a2771.md` (PR #2816's own record) as an unreachable path since it is untracked in this branch's working tree (PR #2816 is unmerged); (2) the frontmatter `sha:` was a short 8-char sha instead of the required 40-char form. Corrected in place before any content landed: added a `derived:` tag re-citing the checks already shown earlier in the same "Next steps" paragraph, added "(untracked in this branch's working tree — ... read via `gh pr diff`)" next to the path reference, and expanded the sha to the full 40-char form (`c4f762a9ced3a97d04b8ae957edf8b526cbaa108`) — canonical: committed record `docs/issue-2811/reports/independent-verification-2.md` at commit `84a66b89` carries the corrected frontmatter sha and the `derived:`-tagged "Next steps" paragraph — checked: `git show 84a66b89:docs/issue-2811/reports/independent-verification-2.md | grep -n "sha: c4f762a9ced3a97d04b8ae957edf8b526cbaa108\|derived: this record's own"` — result: both lines present. Separately, the first commit's trailer read `Subject: 2811` instead of `Subject: issue-2811`; trailer-gate.sh flagged it after the commit landed locally (unpushed), fixed via `git commit --amend` before push — canonical: `git log -1 --format=%B` on the pushed commit shows `Subject: issue-2811`.
