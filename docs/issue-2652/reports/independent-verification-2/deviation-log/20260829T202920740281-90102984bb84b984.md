- Expected: the record file's first `Write` to `docs/issue-2652/reports/independent-verification-2.md` would pass `record-claim-guard.sh` on the first attempt. Actual: it was rejected four times across two drafts — an unfounded-outcome-claim trigger on "requires" in prose, a `canonical:` tag placed too far from an outcome claim to count, `acceptance: ... — result:` lines with parenthetical annotations inserted between the command and `result:` (breaking the exact lead-in shape the guard's regex expects), and a "Next steps" section whose `acceptance:`-lines contained the word "passed" with no `derived:`/`canonical:` tag anywhere in that section's bounds. Fixed by reading `gates/record_lint.py`'s actual regexes (`_OUTCOME_CLAIM_MARKER`, `_ACCEPTANCE_RESULT_LEADIN`, `_CLAIM_DERIVED_TAG`) instead of guessing, then adding a `derived:` tag to the "Next steps" section and removing the inline parentheticals — no claims were weakened, only citation placement/formatting changed.
- Expected: the PR author's own record path (`docs/issue-2652/reports/adversarial-review+architecture-coupling-classification-3b733611.md`, on PR #2768's unmerged branch) could be cited by path alone. Actual: `record-claim-guard.sh`'s orphaned-path check rejected it because the path does not exist in this branch's own tracked history. Fixed by adding an explicit "untracked on this branch" note next to the citation, per the gate's own stated escape hatch.
