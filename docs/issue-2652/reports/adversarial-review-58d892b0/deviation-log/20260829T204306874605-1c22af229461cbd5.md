Two full `Write` attempts on `docs/issue-2652/reports/adversarial-review-58d892b0.md` were rejected outright by `record-claim-guard.sh` (elided code-fence quotes using `...` inside cited file:line ranges failed the exact-match requirement, and one long-path reference lacked an adjacent `untracked` annotation). Replaced the elided quotes with literal `grep -n`/`sed -n` command-and-output blocks and added inline `untracked` annotations, rather than the hand-copied-and-trimmed quotes originally drafted. No findings or conclusions changed — only the citation mechanics of the record.
