- Expected: the record file's first `Write` to `docs/issue-2652/reports/adversarial-review+architecture-coupling-classification-3b733611.md` would pass `record-claim-guard.sh` on the first attempt. Actual: it was rejected twice — once for a bare rule-number count lacking a `derived:`/code-fence citation, once for a false-positive "passed" trigger inside prose describing stamp coupling, and once for a `canonical:` tag positioned outside the section it was meant to support. Fixed by wrapping the count claim in a `derived: <command>` tag, rephrasing the prose to avoid the trigger word, and moving the `canonical:` tag inside the correct section — no claims were weakened, only citation placement/formatting changed.
- Expected: `gh pr create --body-file` would pass `pr-preflight.sh` on the first attempt. Actual: it was rejected because the body opened with a bullet list instead of prose. Fixed by adding a plain prose lead sentence before the bullets.
