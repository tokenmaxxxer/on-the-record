- Expected: the record file's first `Write` to `docs/issue-2652/reports/independent-verification-1.md` would pass `record-claim-guard.sh` on the first attempt. Actual: it was rejected twice -- once for several status/defect claims lacking a `canonical:`/`derived:` tag in the same section (watchdog-marker behavior, template-record moot-ness, `gh`-fallback claim, and an OUTCOME claim in "Next steps"), and once for a bare reference to `docs/issue-2652/reports/adversarial-review+architecture-coupling-classification-3b733611.md` -- a path that exists only on the unmerged PR #2768 branch, not in this worktree. Fixed by adding `canonical:`/`acceptance:` tags to each flagged claim and by annotating every mention of the cross-branch record path as "untracked in this worktree, read via `git show` from the PR branch" (frontmatter comment + inline prose) per the gate's suggested workaround. No claims were weakened, only citation placement/annotation changed.
