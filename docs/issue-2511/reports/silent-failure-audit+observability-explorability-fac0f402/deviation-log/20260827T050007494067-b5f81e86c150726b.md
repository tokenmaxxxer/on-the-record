# issue-2511 silent-failure-audit+observability-explorability-fac0f402 — deviation log

- 2026-08-27: this session's first draft of `test/test_spawn_attempt_staleness.py`'s
  `SpawnAttemptSweepSupersededFixTest::test_cwd_invalid_superseded_by_later_successful_attempt_stops_replaying`
  (and the matching prose in the record's "What was done"/"Why" sections)
  fabricated a `cwd` field (`"tokenmaxxxer/on-the-record"`) on the
  `issue-2576` halted attempt fixture, reasoning from the halt's `reason`
  text (which does contain that string as the bad `-C` argument) without
  checking whether the live `spawn_attempt` event itself actually recorded
  a `cwd` field. A mid-session issue comment (issuecomment-5434456805)
  corrected the record: every attempt currently in the canonical
  `spawn-attempts.jsonl` predates PR #2594's addition of `cwd` recording,
  so `cwd` is `None`/absent on all three live entries, not merely a
  bad-but-present value. Re-verified independently against the live file
  before trusting the comment. Fixed before landing: removed the
  fabricated `cwd` key from the test fixture, corrected the record's
  prose to state the missing-field fact, and added an
  `amendments-reconciled` note citing the comment. No code change was
  needed — `spawn._attempt_superseded()` never reads `attempt.get("cwd")`,
  so the fix itself was unaffected by the error; only the test fixture and
  descriptive prose were wrong.
  canonical: `git show b47c24df -- docs/issue-2511/reports/silent-failure-audit+observability-explorability-fac0f402.md test/test_spawn_attempt_staleness.py` — the correcting commit's diff (removes the fabricated `"cwd": "tokenmaxxxer/on-the-record"` key, adds the `amendments-reconciled` line)
