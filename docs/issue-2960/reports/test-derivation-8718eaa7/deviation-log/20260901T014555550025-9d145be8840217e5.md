## Deviation: worked on own branch instead of PR #2963's branch

The spawning instruction said to do this follow-up "on the SAME branch
issue-2960/test-derivation+silent-failure-audit-ccd3b998 (PR #2963)".
I checked that branch out and attempted to write the record there, but
the repo's `approval-gate` PreToolUse hook refused the edit:

```
approval-gate: sidecar role/issue (issue-2960/test-derivation-8718eaa7)
disagrees with the branch-parsed role/issue
(issue-2960/test-derivation+silent-failure-audit-ccd3b998) — workspace
state is inconsistent.
```

This session's identity sidecar (`.on-the-record/role.json`) is bound
to `issue-2960/test-derivation-8718eaa7`, not the PR #2963 branch. I
did not edit the sidecar to force a match (that would be tampering
with an identity/security-relevant gate rather than legitimate use).
Instead I:
- switched back to this session's own branch
  (`issue-2960/test-derivation-8718eaa7`),
- used `gh pr edit 2963` (a GitHub API call, not a git-branch-bound
  write) to update PR #2963's body with the measured before/after
  numbers,
- delivered the measurement record and commit on my own branch and
  opened it as a separate PR (#2965, `Advances #2960`) rather than a
  commit on PR #2963's branch.

Net effect for the acceptance goal (real numbers reachable from PR
#2963) is the same — PR #2963's body now carries the measured pair —
but the numbers live in a sibling PR's record rather than as a commit
on PR #2963's own branch, since this session's harness identity does
not permit writing there.
