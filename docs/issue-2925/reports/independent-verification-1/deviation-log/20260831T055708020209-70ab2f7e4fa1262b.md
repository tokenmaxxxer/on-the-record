- 2026-08-31 | issue=2925 | skill=independent-verification-1 | type=scope-expansion
  The issue named exactly ten referencing files to check individually.
  Resolving item 1 (`consult.py`) required removing its entire `judge_cmd()`
  producer chain (the only thing it fed was the deleted findings queue), and
  that removal cascaded into three files the issue never named — none of
  which contain the literal string "patrol": `spawn.py` (re-exports of the
  removed names, the `role == "judge"` CLI branch, the `--merge` argparse
  flag), `test/test_spawn_model_override.py` (`JudgeModelGuardTest`, whose
  only subject was the removed `_judge_cmd_and_env`), and
  `test/test_consult_no_rulebook_identity_regression.py`
  (`ReadonlyPluginDirsAlwaysSkillRepoTest`, whose only subject was the
  removed `_readonly_plugin_dirs`).
  derived: `git grep -niI patrol -- spawn.py test/test_spawn_model_override.py test/test_consult_no_rulebook_identity_regression.py` — result: no output in any of the three, confirming the expansion tracks judge_cmd's call graph, not the literal grep population.
  Judgment: leaving that chain in place, half-alive and unreachable, would
  have been the "dormant copy... with none of the function" the issue's own
  must-not list forbids, and would have left `consult.py`/`spawn.py` broken
  the first time anyone ran `spawn.py judge` (a bare `ModuleNotFoundError`
  on the deleted `patrol_queue` import). No approved phase-1 proposal exists
  to diverge from (CORE_BUILD_NOW=1 build-now bypass, single-session
  delivery) — logging this as the record's own judgment call rather than a
  deviation from prior direction, per the record's "Upstream basis"
  section which states the same reasoning in full.
