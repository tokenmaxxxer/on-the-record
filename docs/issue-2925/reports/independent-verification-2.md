---
issue: 2925
role: independent-verification-2
author: independent-verification-2
verifies_subject: true
code_under_review: 53a4f5f1:gates/patrol_board.py(deleted), 53a4f5f1:gates/patrol_promote.py(deleted), 53a4f5f1:gates/patrol_queue.py(deleted), 53a4f5f1:gates/patrol_trigger.py(deleted), 53a4f5f1:gates/patrol_wiring.py(deleted), 53a4f5f1:gates/precision_measure.py(deleted), 53a4f5f1:consult.py, 53a4f5f1:gates/gh_rest.py, 53a4f5f1:gates/record_lint.py, 53a4f5f1:on-the-record/gates/record_lint.py, 53a4f5f1:on-the-record/hooks/gh-write-allow-gate.sh, 53a4f5f1:on-the-record/monitors/poll-heartbeat.sh, 53a4f5f1:on-the-record/monitors/test_poll_heartbeat.py, 53a4f5f1:on-the-record/commands/run.md, 53a4f5f1:test/test_retirement_count.py
type: verification-record
breaking: false
verdict: claims-confirmed-no-regressions-one-open-finding-merge-conflict-risk
loop_state: landed
upstream:
  - path: PR #2932 (issue-2925/refactoring-legacy-seam-selection+silent-failure-audit-63ccc91e)
    sha: 53a4f5f10580543354c8b446621da1cd5ca09930
skill-verdict: work-in-english — applied: invoked; repo/commit work done in English, final user-facing summary is in Korean per project convention
skill-verdict: model-routing — applied: invoked; orchestrator delegated this entire investigate+verify+land task to one executor-tier worker
other mounted skills: not triggered — this is a single-PR read-and-reproduce audit (checkout the branch, re-run cited tests/greps/tick, verify each of the ten referenced files individually), not a multi-file build; freelunch's fan-out threshold did not apply.
---

# issue-2925 — independent-verification-2 record

## What was done

canonical: `printenv CORE_BUILD_NOW` — result: `1`, confirming the build-now bypass is authorized for this session (set by the spawner, per contract v3 s19a).

canonical: `gh pr list --search "2925"` — result: PR #2932 "issue-2925: remove the patrol program" (branch `issue-2925/refactoring-legacy-seam-selection+silent-failure-audit-63ccc91e`), state OPEN, is the separate implementation PR that did the actual patrol-program removal in code. This session's own working tree at start held only the uncommitted `docs/issue-2925/reports/independent-verification-2.md` skeleton (frontmatter placeholders, no filled sections) — the code deliverable was not in this workspace; it lives entirely in PR #2932. This session's job is therefore to independently verify PR #2932's claims against issue #2925's acceptance criteria, then land this record.

Fetched PR #2932 (`git fetch origin pull/2932/head:pr-2932-review`, tip `53a4f5f10580543354c8b446621da1cd5ca09930`) into a separate worktree (`/tmp/pr-2932-review`) and re-ran every acceptance check from scratch rather than trusting the PR's own record.

### Claims checked against the actual diff and live command output

1. **Zero remaining `patrol` references in live code, excluding `docs/`.** derived: `git grep -rni patrol -- . ':!docs'` on `53a4f5f1` — result: exactly one hit, `test/test_retirement_count.py:44: self.assertFalse(retirement_count.line_hits("patrol the controller"))` — an English test string checking a string-matcher doesn't false-positive on "patrol" containing "role", not a patrol-program reference. Bounded as tracked files only, case-insensitive, `docs/` excluded per the issue's must-not list. CONFIRMED.

2. **The five named modules (1,326 lines) plus `gates/precision_measure.py` are fully deleted, not stubbed.** derived: on `53a4f5f1`, `ls gates/patrol_*.py gates/precision_measure.py` — result: no such file for any of the six paths. derived: line counts of each of the five named modules as they existed on the pre-PR base (`git show 6db165ce:gates/patrol_board.py | wc -l` etc.) — result: `381 + 365 + 366 + 72 + 142 = 1326`, matching the issue's own count exactly. `gates/precision_measure.py` is not one of the five named modules but its only functional half (`cmd_sample`) called `patrol_queue.scan_record_lint`/`patrol_queue._finding_rule_id`/`patrol_queue.SWEEP_DISABLED_RULES` directly and had zero other importers repo-wide (derived: `git grep -rn "precision_measure" -- . ':!docs' ':!gates/precision_measure.py'` on `53a4f5f1` — result: no output) — deleting rather than leaving a dead CLI subcommand behind is consistent with the issue's "must not leave a stub" rule. CONFIRMED.

3. **A Monitor tick runs correctly with the patrol machinery removed, default bash, exit 0, normal output.** acceptance: `bash --version` on this host — result: `GNU bash, 버전 5.1.16(1)-release`. acceptance: `POLL_HEARTBEAT_MAX_TICKS=1 POLL_HEARTBEAT_SLEEP_SECONDS=0 bash on-the-record/monitors/poll-heartbeat.sh` on `53a4f5f1` — result: exit 0, normal `[new-returned-pr]`/`[returned-pr]`/`[standing-red]` output, zero `[patrol-poll]` lines anywhere in the output. CONFIRMED.

4. **A Monitor tick runs under bash 3.2.** acceptance: `docker run --rm -v /tmp/pr-2932-review:/repo -v /tmp/fakehome:/root -w /repo -e POLL_HEARTBEAT_MAX_TICKS=1 -e POLL_HEARTBEAT_SLEEP_SECONDS=0 bash:3.2 bash on-the-record/monitors/poll-heartbeat.sh` — result: `GNU bash, version 3.2.57(1)-release`, exit 0, `bash -n` syntax-clean. **Caveat, stated plainly rather than fabricated**: the `bash:3.2` Docker image (Alpine/musl-based) has no `python3` or `git` installed, so this run exercises the shell-syntax/control-flow path (including the now-absent patrol array-expansion code, which no longer executes at all since it was deleted) but not the full `git`/`python3`-backed tick logic the way the default-bash run in claim 3 did. derived: `docker run --rm bash:3.2 bash -c "which python3 git"` — result: neither found (empty output, non-zero exit). This is an environment limitation of the available `bash:3.2` image, not a defect — noted here rather than overclaiming a full functional-equivalent run. PARTIALLY CONFIRMED (syntax/exit-0 confirmed; full functional parity with the default-bash run not reproducible in this environment).

5. **The ten files the issue named as referencing patrol were each individually inspected, not blindly find-replaced.** derived: `grep -in patrol <file>` on `53a4f5f1` for each of the ten: `consult.py`, `gates/gh_rest.py`, `gates/precision_measure.py`, `gates/record_lint.py`, `on-the-record/gates/record_lint.py`, `on-the-record/hooks/gh-write-allow-gate.sh`, `on-the-record/monitors/poll-heartbeat.sh`, `on-the-record/monitors/test_poll_heartbeat.py`, `on-the-record/commands/run.md`, `test/test_retirement_count.py` — result: zero hits in nine of the ten (patrol resolved/removed), one hit in `test/test_retirement_count.py` (the unrelated string in claim 1). Read the actual diff (`git diff 6db165ce pr-2932-review -- consult.py gates/gh_rest.py gates/record_lint.py on-the-record/gates/record_lint.py on-the-record/hooks/gh-write-allow-gate.sh on-the-record/commands/run.md`) directly rather than trusting the PR body's characterization: `consult.py`'s `judge_cmd()` had a real functional dependency (`import patrol_queue`, `load_queue`/`verify`/`fingerprint`/`enqueue`/`save_queue`) that was replaced with an inline re-verification (`Path.read_text()` + substring check) at the same call site — a resolve, not an amputation, since the one safeguard worth keeping (re-reading the cited path to catch hallucinated excerpts) is preserved. `gates/gh_rest.py`, `gates/record_lint.py` (both copies), `on-the-record/hooks/gh-write-allow-gate.sh` had only stale comment/docstring citations of deleted `patrol_*.py` files or issue numbers, reworded without changing functional code. `on-the-record/commands/run.md` had a real workflow step (`python3 gates/patrol_wiring.py run <repo-root> <merge-sha>` post-merge) that is correctly removed since that script no longer exists. CONFIRMED for all ten.

6. **The two Monitor-visible lines `#2919` added must not be re-silenced or weakened, except the patrol-query-report line.** derived: `git merge-base --is-ancestor acd74b3b HEAD` on `pr-2932-review` (`acd74b3b` = the `#2919` commit "fix poll-heartbeat.sh exit-1 on macOS", which added both lines: `[patrol-poll] skills query failed at startup, patrol skipped this tick` and the unrelated `[alive-stamp-lock] stale lockdir ... reclaimed` mkdir-mutex line) — result: `acd74b3b` is NOT an ancestor of PR #2932's HEAD. derived: `git merge-base --is-ancestor acd74b3b origin/main` — result: also NOT an ancestor of `origin/main` (tip `46779718`). So `#2919`'s own fix (PR #2923, still OPEN per `gh pr list --search "2919"`) had not landed on `main` by the time PR #2932 branched — the base file PR #2932 edited still had the pre-`#2919` `2>/dev/null`-swallowing version with neither visibility line present. derived: `grep -c "patrol-poll\|alive-stamp-lock" on-the-record/monitors/poll-heartbeat.sh` on `53a4f5f1` — result: `0`, confirming neither line exists to weaken on this branch. CONFIRMED as accurately vacuous — this criterion was satisfiable only because its premise (that `#2919`'s fix had already landed) did not hold. See Open findings for the resulting merge-conflict risk this creates.

7. **No regression in the broader test suite.** derived: `python3 -m pytest test/ -q` on `pr-2932-review` (`53a4f5f1`) — result: `15 failed, 513 passed, 3 xfailed`. Same command on `origin/main` (`46779718`, via a second worktree `/tmp/main-check`) — result: `15 failed, 513 passed, 3 xfailed`. The two `FAILED` name lists are identical (`test_convention_equivalence.py::BranchRoleFieldDualReadEquivalenceTest::test_hooks_retain_original_fallback_regex_verbatim`, `test_convention_equivalence.py::ApprovalGateEquivalenceTest::test_hook_file_exists_and_has_expected_shape`, `test_local_dependency_env.py::CallSiteWiringTest::test_origin_captured_before_workspace_reassignment`, five `test_spawn_cross_family_skill_selection.py` cases, two `test_spawn_artifact_skill_pairing.py` cases, four `test_spawn_skill_judge_haiku_timeout_overlap.py` cases) — pre-existing standing-red failures unrelated to patrol, not regressions this PR introduced. CONFIRMED.

8. **The specifically-touched test suites pass, matching the PR body's own count.** derived: `python3 -m pytest on-the-record/monitors/test_poll_heartbeat.py test/test_retirement_count.py -q` on `53a4f5f1` — result: `43 passed`, matching the PR body's claim exactly. CONFIRMED.

9. **No retired-axis identifier (`role_data`) reintroduced by this diff.** derived: `git grep -rn "role_data" -- . ':!docs'` on `53a4f5f1` — result: 4 hits, all in `bench/run.py` and `directive_assembly.py`, both files untouched by this PR (derived: `git diff 6db165ce HEAD -- bench/run.py directive_assembly.py` — result: empty diff). These are pre-existing, out-of-scope references (the issue itself names this as "same root as #2920", a separate issue). CONFIRMED — this PR did not reintroduce or newly touch any `role_data` site.

10. **`py_compile`/`bash -n` clean on touched files.** derived: `python3 -m py_compile consult.py gates/gh_rest.py gates/record_lint.py on-the-record/gates/record_lint.py on-the-record/monitors/poll_heartbeat_delta.py` and `bash -n on-the-record/hooks/gh-write-allow-gate.sh on-the-record/monitors/poll-heartbeat.sh` on `53a4f5f1` — result: both clean. CONFIRMED.

## Why

This session's own workspace held only an unfilled record skeleton, not the code deliverable — `gh pr list --search "2925"` showed a separate, already-open implementation PR (#2932) authored under a different role slug on the same issue. Per this role's purpose (independent-verification-2), the correct action was to check out and independently re-run every acceptance check against PR #2932's actual diff and live command output, not to redo the removal work a second time in this workspace (which would either duplicate #2932 or diverge from it) and not to rubber-stamp the subject's own record's claims without reproduction. Each claim above was re-derived from a fresh command run in a separate worktree rather than restated from the PR body or the subject's record.

## What did not work

None. The one place this verification could not fully reproduce the subject's own claim was the bash-3.2 tick run's functional parity (claim 4 above) — the available `bash:3.2` Docker image lacks `python3`/`git`, so only the syntax/control-flow path (not the full git/python3-backed logic) could be exercised here. Stated as a partial confirmation with its cause named, not silently upgraded to a full match or hidden.

## Upstream basis

PR #2932 (`issue-2925/refactoring-legacy-seam-selection+silent-failure-audit-63ccc91e`, tip `53a4f5f10580543354c8b446621da1cd5ca09930`) is the deliverable under review; `sha: same-commit` does not apply since none of the reviewed code lands in this record's own commit — all cited paths carry the real PR tip sha per contract §1. canonical: PR #2932's own report file, added under its branch's own `docs/issue-2925/reports/` directory (not present on this record's own branch, since PR #2932 is unmerged; that branch's tree was read directly in the `/tmp/pr-2932-review` worktree checked out from `53a4f5f1`), was read for context, but every claim it makes that this record also asserts was independently re-derived from fresh command output (see numbered claims above), not copied from that file.

## Open findings

1. **New here: merge-conflict risk between PR #2932 and issue #2919's still-open fixes.** derived: `gh pr list --search "2919"` — result: PR #2923 ("fix poll-heartbeat.sh exit-1 on macOS", commit `acd74b3b`) and PR #2935 ("fix live-holder eviction in poll-heartbeat.sh mkdir mutex", commit `e2441a9b`) are both OPEN. Both touch the same region of `on-the-record/monitors/poll-heartbeat.sh` that PR #2932 deletes (the patrol roster query, the `_alive_stamp_write` flock block, the per-tick patrol loop — see claim 6 above), and neither PR is based on the other's changes (see claim 6's ancestor checks). Whichever of {#2919's PRs, #2932} merges last will hit a real (not cosmetic) merge conflict in this file, and naive conflict resolution risks either reintroducing deleted patrol code or dropping `#2919`'s flock-absence/unbound-array fix. Resolution path: whoever lands second rebases onto the first and manually confirms the merged file has both the patrol removal and the `#2919` fix, with a fresh `bash -n` + tick run (both bash 5.1 and 3.2) as the acceptance check — not a mechanical `git rebase --continue` accepted without re-reading the hunk.

## Next steps

None — `loop_state` is terminal (`landed`). The one open finding above is a handoff for whichever of the `#2919`/`#2925` PRs lands second, not in-progress work on this record.
