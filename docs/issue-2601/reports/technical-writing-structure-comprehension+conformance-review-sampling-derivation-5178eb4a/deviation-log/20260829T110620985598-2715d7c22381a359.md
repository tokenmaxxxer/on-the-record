---
issue: 2601
---

derived: the classification+edit script's diff, reviewed inline this turn —
result: the mechanical `role`->`skill` substitution had been run, on the
first pass, over two lines that should have been excluded as
historical-citation exceptions: `release-engineering-readiness-checklist/SKILL.md:25`
and `release-engineering-error-budget-policy/SKILL.md:64`, both citing the
dated research file `docs/reports/research/2026-07-27-role-practice/ops.md`
by name (untracked in both skill-repository and the on-the-record checkout
this session has access to — cited only as-is by the skill files, never
independently verified to exist under that name).

The same historical-citation exception was already correctly applied to
sibling citations in `conformance-review-severity-classification`,
`product-discovery-assumption-mapping`, `product-discovery-guardrail-metrics`,
`product-discovery-one-pager`, `release-engineering-postmortem`, and
`release-engineering-rollout-plan` — these two were missed when the
per-file exception list was first assembled.

canonical: a targeted repo-wide re-grep for `skill-practice`/`skill-interaction`,
run immediately after the bulk mechanical edit and before any commit, is
what surfaced both misses. Both lines were reverted to the original
`role-practice` text via direct `Edit` calls, and the classification map
was updated to mark both lines `historical` so the verification script's
per-occurrence check would catch any recurrence.

derived: `cd ~/skill-registry/skills && grep -rn 'skill-practice\|skill-interaction' .` — result:
```
(no output — 0 matches)
```

Full detail (rationale and the acceptance-check cross-reference) is in this
issue's delivery record's "What did not work" section:
`docs/issue-2601/reports/technical-writing-structure-comprehension+conformance-review-sampling-derivation-5178eb4a.md`.
