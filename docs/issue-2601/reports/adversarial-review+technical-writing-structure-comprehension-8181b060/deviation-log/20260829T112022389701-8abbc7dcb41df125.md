---
issue: 2601
---

derived: this session's own tool-call transcript this turn — result: the
first two `Write` attempts at this record's own path
(`docs/issue-2601/reports/adversarial-review+technical-writing-structure-comprehension-8181b060.md`)
were rejected by `record-claim-guard.sh` before landing, for citing
untracked-in-this-checkout paths (`on-the-record/hooks/role-spec-reference-guard.sh`,
`docs/specs/role-handoff-contract.md`, and the companion PR's own record/
deviation-log paths) without an `untracked`/`renamed from/to` marker
adjacent to each literal mention, and separately for asserting an
Open-findings residual and a `loop_state: complete` outcome without an
in-section `canonical:`/`derived:`/`acceptance:` tag.

I expected the first draft to pass because every cited path was one I had
actually read moments earlier (via a separate `/tmp` clone of
`tokenmaxxxer/on-the-record`, not this checkout) — that expectation did
not hold: the gate checks reachability from this checkout specifically,
not "was this read live this session." Fixed by adding an `untracked in
this checkout` annotation next to every literal path pointing outside
this repo's own working tree, and by adding an explicit `derived:`/
`acceptance:` tag inside the Open findings and Next steps sections
themselves rather than relying on citations already present earlier in
the document. Third attempt passed.

canonical: `git log --oneline -1` on this branch — result: `d9e4ce0d
issue-2601: independent verification of skill-repository PR #117 +
companion PR #2716` — the record that eventually landed is the third
draft described above.
