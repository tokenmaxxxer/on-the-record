# Deviation log — issue-2268 (conformance-review role)

- 2026-08-25T01:00:00Z | inline | first `Edit` attempt on `docs/issue-2268/reports/conformance-review.md` (filling the skeleton with the full per-requirement record) was refused by `on-the-record/hooks/pretooluse-dispatcher.sh`'s record-claim-guard (issue #793): several prose sentences asserted status/defect claims ("independently confirmed...", "read, not modified...", a skill-verdict line) with no `canonical:`/`derived:` tag in the same section. canonical: the `PreToolUse:Edit hook error` returned by the tool call itself, this turn, listing each flagged sentence. Resolved by rewriting the record (via `Write`, since the file was still the untouched skeleton after the rejected `Edit`) with an explicit `canonical: <command>` + code-fenced live output attached in every section that makes a claim (What was done, Upstream basis, Open findings), reusing outputs already gathered this session (`git cat-file -e`, `git log --oneline`, `diff <(git show ...)`, `grep -rn`). Stayed inside this role's own frozen write set (its own record file only), mechanical (citation formatting, not a content change), one-off.
- 2026-08-25T01:03:20Z | inline | the first `git commit` attempt for this record used a heredoc-style `-m "$(cat <<'EOF' ... EOF)"` body (per this session's own general commit-message guidance) and was refused by `on-the-record/hooks/pretooluse-dispatcher.sh`'s heredoc-command-refusal-gate (issue #1976): "heredoc-shaped commit message body detected ... the host's write-capable-command classifier refuses this shape as un-analyzable." canonical: the `PreToolUse:Bash hook error` returned by the tool call itself, this turn. Resolved by re-running the same commit with one `-m` flag per paragraph (title / body / `Subject: issue-2268` trailer) instead of a heredoc, no change to the intended commit content. Stayed inside this role's own frozen write set, mechanical (shell invocation shape only), one-off.
- 2026-08-25T01:03:20Z | inline | initial attempt to record these two deviations at `docs/issue-2268/reports/deviation-log.md` (untracked, never created — the write itself was refused before any content landed there) was refused by `on-the-record/hooks/pretooluse-dispatcher.sh`'s board-gate (contract v3 s11): "docs/issue-2268/reports/deviation-log.md belongs to another role. conformance-review writes only conformance-review.md, conformance-review/** — never a foreign record." canonical: the `PreToolUse:Write hook error` returned by the tool call itself, this turn. Resolved by writing this same content to `docs/issue-2268/reports/conformance-review/deviation-log.md` (untracked prior to this write; this file, being created now) instead, matching the nested-path precedent already on `main` (e.g. `docs/issue-2039/reports/conformance-review/deviation-log.md`, `docs/issue-1163/reports/conformance-review/deviation-log.md` — canonical: `git log --all --oneline --name-only | grep -i "reports/conformance-review/"`, this turn). Mechanical (path correction only), one-off.
