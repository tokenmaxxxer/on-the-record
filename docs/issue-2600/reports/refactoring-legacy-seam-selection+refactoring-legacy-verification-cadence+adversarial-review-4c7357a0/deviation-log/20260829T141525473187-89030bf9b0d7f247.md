- First adversarial-review dispatch used `isolation: "worktree"` -> that worktree checks out only committed history, so it saw zero diff (nothing was committed yet); redispatched without isolation, directly against the live working directory. canonical: the evaluator agent's own returned result (that dispatch), quoted verbatim in this session's record's "What did not work" section.
- Round-1 rename fan-out expected all 10 file-ownership groups to each finish their file list -> 3 of 10 hit per-worker turn-budget cutoffs mid-group (self-reported "INCOMPLETE" by each worker); required a second, smaller-batched round (12 groups of 1-4 files) to finish the remaining files. canonical: workflow runs `wf_1363e367-f52` (round 1) and `wf_65388dd9-667` (round 2), full derived: counts (8+5+12=25 files) in this session's record's "What was done" section.
- Expected file-ownership-partitioned parallel workers to be internally consistent -> several produced real cross-file desyncs invisible to any single owner (e.g. `events.py`'s `found_role`->`found_skill` renamed on one line but not 5 sibling references in the same function; `consult.py` calling `_sp.resolve_role_family_source`/`_sp.role_settings`/`_sp.resolved_role_model` after `spawn.py`/`pipeline.py` had already renamed those definitions in the same wave) -> found via `ruff --select F821,F841` and full-suite diffing against the `origin/main` baseline (both re-run this session, output in the record's "What did not work" section), fixed directly rather than re-dispatching.
- Attempted renaming `spawn.py`'s positional argparse dest from `role` to `skill` for consistency -> reverted after finding it collides with the existing `--skill` optional flag (both derive dest `skill`, and argparse silently lets the positional's `None` default clobber the flag's parsed value) -> kept the dest `role`, documented as an argparse-dest-trap special case. derived: `python3 -c "import argparse; p=argparse.ArgumentParser(); p.add_argument('skill', nargs='?'); p.add_argument('--skill', default=None); print(p.parse_args(['--skill','bar']))"` — result: `Namespace(skill=None)`, reproduced in the record.
