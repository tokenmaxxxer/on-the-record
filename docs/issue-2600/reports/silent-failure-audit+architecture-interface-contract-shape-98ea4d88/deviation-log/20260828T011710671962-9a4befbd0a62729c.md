2026-08-28T01:17:49Z | filed | `upstream-defect-scope-guard.sh` denies `gh pr create --repo tokenmaxxxer/tokenmaxxxer-core` (and the `gh api`/GraphQL equivalents) from this session regardless of `cd`, because it resolves "this session's own origin repo" from the harness's fixed per-session `cwd`, not the shell's actual working directory — the core repo's half of #2600's env-var rename is committed and pushed (`79983f8` on branch `issue-2600/silent-failure-audit+architecture-interface-contract-shape-98ea4d88`) but its PR could not be opened from here; reported, not spawned. canonical: docs/issue-2600/reports/silent-failure-audit+architecture-interface-contract-shape-98ea4d88.md, Open findings #3.
2026-08-28T01:17:49Z | filed | `CLAUDE_ROLE` (65 call sites across both repos, set by this session's own spawner in `pipeline.py:722`) was found in scope for #2600's environment-variable-kind rename but deliberately not renamed — a role-shaped judgment call (sibling issue #2593 lists it as an explicit Non-goal with no settled replacement name; renaming the read side without renaming the write side atomically would break this session's own remaining gates mid-task), not a mechanical one, so it needed weighing rather than an inline fix; reported, not spawned. canonical: docs/issue-2600/reports/silent-failure-audit+architecture-interface-contract-shape-98ea4d88.md, Open findings #1.
