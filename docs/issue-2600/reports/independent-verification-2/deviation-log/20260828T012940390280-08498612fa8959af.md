2026-08-28T01:29:40Z inline: `git checkout FETCH_HEAD -- .` + `git clean -fd` against this session's own tracked tree (attempting to inspect the subject PR's diff) staged the subject's entire diff and deleted this record's own still-untracked skeleton file; caught via `git status`, recovered with `git reset --hard HEAD` plus a `Write` reconstructing the skeleton from the earlier `Read`. All later acceptance reproduction moved to `git worktree add --detach` / a scratch `gh repo clone` instead. See docs/issue-2600/reports/independent-verification-2.md's "What did not work" section.
