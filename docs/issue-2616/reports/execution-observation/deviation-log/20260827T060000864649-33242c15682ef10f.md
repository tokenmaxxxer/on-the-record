- 2026-08-27T15:05:00+09:00 | inline | acceptance check 1 ("put the clone one commit behind deliberately, spawn a session, and show the reported line") was demonstrated by invoking the exact `core_root() → core_clone_staleness_line()` call pair directly against a synthetic two-clone git fixture, not by launching a real `claude` process via `spawn.py` — a full session spawn was out of proportion for a 31-line report-only change and risked a nested role session against GitHub. Reasoning and the substitution itself are documented inline in `docs/issue-2616/reports/execution-observation.md` under "Acceptance check 1", including the literal `spawn.py`-driven command a reader could still run.
