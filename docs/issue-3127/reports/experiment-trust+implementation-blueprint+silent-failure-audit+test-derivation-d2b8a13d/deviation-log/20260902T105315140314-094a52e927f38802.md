Expected: the record's `skill-verdict` lines for experiment-trust,
implementation-blueprint, silent-failure-audit, and test-derivation
would accurately state whether the Skill tool was invoked.

What actually happened: the initial committed record (commit `df3a4d97`)
wrote `applied: invoked;` for all four, but this session had only reasoned
informally using each skill's concepts during the four defect fixes --
the Skill tool itself was not called until the Stop hook's
skill-verdict-guard flagged zero invocations across all 9 mounted skills.
Corrected by invoking all four via the Skill tool at that point (this
session, after the fix commits and the first record commit), and
additionally running `implementation-blueprint`'s own
`scripts/prep.py classify --surface backend --single-file` for real --
canonical: its output, `VETO: single file, single concern, no callers ->
no-structure`, matches this session's earlier informal judgment. The
record's skill-verdict section was then corrected in a follow-up commit
to describe invocation as having happened at session end, not
during development, rather than leaving the original (inaccurate)
"invoked during the fix work" framing uncorrected.
