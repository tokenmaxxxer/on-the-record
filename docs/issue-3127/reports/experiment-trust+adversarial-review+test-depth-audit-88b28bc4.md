---
issue: 3127
role: experiment-trust+adversarial-review+test-depth-audit-88b28bc4
author: experiment-trust+adversarial-review+test-depth-audit-88b28bc4
skills: experiment-trust (skill-repository(c05de12)), adversarial-review (skill-repository(c05de12)), test-depth-audit (skill-repository(c05de12))
verifies_subject: true  # independent, builder-blind verification of PR #3131's repair round (PR #3154) against PR #3145's four found defects
loop_state: landed
code_under_review: 5a07342c318635b4f5f8a33981f00fff5acd7864
type: defect-verification-record
breaking: false
verdict: Defect 1 (skills-off genuine isolation) is Present -- live-reproduced
  independently, old mechanism fails closed on a real conflict, new
  skill-repo:-qualified mechanism resolves cleanly to only the stub.
  Defects 2 and 3 (H1 gate enforcement, blind scorer wiring) are Surface --
  each fix's own function is correctly implemented and independently
  verified in isolation, but neither is reachable from run_pair()'s real
  (unmocked) per-pair flow: arm_workspace_dir() calls the undefined name
  _spawn_mod, never imported anywhere in the file, so a genuine run_pair()
  call raises NameError before gate_pair_on_h1() or evaluate_pair_blind()
  is ever reached. Defect 4 (wall-clock honesty) is Surface -- execute_arm()'s
  code is correct and the old bare name is gone from all code and prose,
  but the committed results skeleton was never regenerated by the repair
  and still lacks the corrected schema; the function that would produce
  it is dead code. See "What was done" for per-defect live reproduction
  and the field-by-field pre-registration accounting.
upstream:
  - path: docs/issue-3127/reports/experiment-trust+test-depth-audit+silent-failure-audit-f8660411.md
    sha: same-commit  # PR #3145, already tracked on this branch, unmodified; source of the four defects re-graded here
  - path: docs/issue-3127/reports/experiment-trust+implementation-blueprint+silent-failure-audit+test-derivation-d2b8a13d.md
    sha: same-commit  # PR #3154, the repair round's own record, already tracked on this branch, unmodified
  - path: PR-3131-branch:scripts/issue-3127/run_consumer_pair.py
    sha: 5a07342c318635b4f5f8a33981f00fff5acd7864  # untracked on this session's own branch; lives only on PR #3131's branch, read via git worktree (see "What was done")
  - path: PR-3131-branch:tests/test_issue_3127_run_consumer_pair.py
    sha: 5a07342c318635b4f5f8a33981f00fff5acd7864  # untracked here, same basis
  - path: PR-3131-branch:tests/test_issue_3127_h1_and_scoring.py
    sha: 5a07342c318635b4f5f8a33981f00fff5acd7864  # untracked here, same basis
  - path: PR-3131-branch:tests/test_issue_3127_run_pair.py
    sha: 5a07342c318635b4f5f8a33981f00fff5acd7864  # untracked here, same basis
  - path: PR-3131-branch:docs/issue-3127/decisions/pre-registration.md
    sha: 84226988e930981b02d00abd30e22c83100e875f  # untracked here, unmodified since this sha
  - path: PR-3131-branch:docs/issue-3127/_assets/consumer-path-results.json
    sha: 9c9801cd470129580de54b78a32abc30875de90e  # untracked here, unmodified since this sha (only commit that ever touched it)
  - path: skills.py
    sha: same-commit  # read at this session's own HEAD; unmodified
  - path: spawn.py
    sha: same-commit  # read at this session's own HEAD; unmodified
---

# issue-3127 — experiment-trust+adversarial-review+test-depth-audit-88b28bc4 record

## What was done

Independent, builder-blind verification of the repair round (PR #3154)
landed on PR #3131's own branch (`issue-3127/experiment-trust+product-
discovery-hypothesis-preregistration+implementation-blueprint+silent-
failure-audit-4eda8e00`, head `5a07342c`), which claims to fix the four
defects PR #3145's second independent verification found in the issue
#3127 consumer-path measurement harness. Per this task's spawning brief,
graded each defect by running the harness this session, not by citing the
repair record's own claims.

canonical: `git show 997a824c:docs/issue-3127/reports/experiment-trust+
test-depth-audit+silent-failure-audit-f8660411.md` (PR #3145), read this
session -- the four defects: (1) skills-off arm's `MUSTER_SKILL_REPO`
override either fail-closed on a real multi-source conflict or silently
resolved to the full corpus; (2) H1 (the manipulation check) existed only
as prose, never enforced in code; (3) the blind quality scorer was defined
and never called from anywhere; (4) wall-clock measured time-to-session-
end under the misleading name `wall_clock_s`.

canonical: `git show 67913ead:docs/issue-3127/reports/experiment-trust+
implementation-blueprint+silent-failure-audit+test-derivation-d2b8a13d.md`
(PR #3154), read this session -- the repair's own claims for each of the
four defects, graded below against live reproduction rather than trusted.

Setup: `git worktree add /tmp/pr3131-repair-verify origin/issue-3127/
experiment-trust+product-discovery-hypothesis-preregistration+
implementation-blueprint+silent-failure-audit-4eda8e00 --detach` (head
`5a07342c`, the branch's current tip -- all four repair commits `c5a34de8`,
`d1d454d9`, `bfc7fdac`, `5a07342c` included). derived: `git ls-files |
grep -i 3127` on this session's OWN branch (not the worktree) -- returns
only report/deviation-log/product-priorities paths, no `scripts/issue-
3127/`, no `tests/test_issue_3127_*.py`, no `docs/issue-3127/_assets/`;
every `PR-3131-branch:`-prefixed path cited below is untracked on this
session's own branch and lives only on PR #3131's branch, read through
the worktree. canonical: `gh pr view 3131 --json state,headRefName`
re-checked at the end of this session -- `state: OPEN`, `headRefName`
unchanged from setup; PR #3131 was not merged and not edited by this
session (no commits added to the worktree, no push performed).

### Defect 1 — skills-off arm genuine isolation: Present

derived: this session's own live reproduction against the worktree,
calling `spawn.resolved_skill_sources()` (the same function `spawn.py
--skills` calls in production -- canonical: `spawn.py:442`'s
`resolved_skill_sources = skills.resolved_skill_sources`, read directly)
with a real stub built via `rcp.build_stub_skill_repo()` and a real,
differently-contented conflicting skill entry:

```
=== OLD unqualified name against real conflict ===
fail-closed SystemExit (expected old-mechanism failure): --skills: alpha
가 둘 이상의 소스에서 겹친다 — skill-repository(?), ~/.claude/skills
(...)  (precedence 는 검색 순서일 뿐 충돌을 가리지 않는다 — 소스를
<source>:alpha 형태로 지정해 골라라, ...)
=== NEW qualified name skill-repo:alpha ===
resolved: [{'source': 'skill-repo', 'dir': PosixPath('.../stub-skill-
repo/alpha'), ...}]
```
The old (bare-name) mechanism fails closed on a genuine multi-source
conflict; the new `skill-repo:`-qualified mechanism resolves cleanly to
only the stub. Both failure modes PR #3145 reproduced live (fail-closed
crash, silent full-corpus fallback) are closed by the same qualifier
mechanism, independently confirmed rather than taken from the repair's own
test suite.

### Defect 2 — H1 enforced in code: Surface

The gate mechanism itself is real and independently verified. derived:
called `rcp.compute_h1_manipulation()` and `rcp.gate_pair_on_h1()` directly
against real on-disk workspace directories this session created (not
mocked): with directive bytes differing (500 vs 50), H1 passes and the
supplied `compute_h2` callable's result appears under `h2`; after
deliberately breaking the stub (making both arms' directive bytes
identical, 500 vs 500, reproducing PR #3145's silent-leak failure mode), H1
fails, `excluded_from_h2: true`, `h2: null`, `exclusion_reason` states why,
and the `compute_h2` callable passed in was never invoked (a call-tracking
list stayed `[]`, printed as `H2 scorer called on failing pair? []` in
this session's own terminal output). Fed both pairs through
`build_execute_results()`: the passing pair's `h2` figure appears in
`pairs_included_in_h2`; the failing pair appears only in
`pairs_excluded_from_h2` with its reason, and reports no H2 number for it
anywhere in the resulting JSON -- not zeroed, not NaN, absent with the
reason recorded, matching PR #3154's claim for these three functions in
isolation.

The repair's own claim is broader, though: that `gate_pair_on_h1()` is
"reached from `run_pair()`'s real per-pair flow." derived: called
`rcp.run_pair()` directly this session, mocking only `execute_arm()` (the
one function that genuinely cannot run without a real `spawn.py`
dispatch) and leaving `arm_workspace_dir()` un-mocked -- the actual code
path `main()`'s `--execute` branch would take:

```
CRASHED: NameError name '_spawn_mod' is not defined
```
canonical: `PR-3131-branch:scripts/issue-3127/run_consumer_pair.py:687`,
`arm_workspace_dir()`'s body -- `_, work = _spawn_mod._workspace_target_
path(...)`. derived: `grep -n "_spawn_mod\|import spawn"
scripts/issue-3127/run_consumer_pair.py`, run against the worktree --
exactly one hit, that same reference line; no `import spawn as
_spawn_mod` or equivalent exists anywhere in the file. `run_pair()` calls
`arm_workspace_dir()` before it calls `gate_pair_on_h1()` (canonical:
`PR-3131-branch:scripts/issue-3127/run_consumer_pair.py:741-742` precede
`:764`, read directly) -- so a genuine `run_pair()` invocation raises this
`NameError` before the H1 gate this defect fixed is ever reached, for
every pair whose both arms report `watched-to-completion`. Per
`conformance-review-verdict-assignment` rule 1: matching, correct code
exists (the gate), but it does not fire on the actual condition the
defect names (reachability from the real per-pair flow) -- Surface, not
Present, not Incorrect (the gate itself does not contradict anything; an
unrelated bug severs the path to it).

canonical: `PR-3131-branch:tests/test_issue_3127_run_pair.py`, all its
tests, read directly -- every one wraps its call to `rcp.run_pair()`
inside `mock.patch.object(rcp, "arm_workspace_dir")`, replacing the exact
function whose real body crashes. derived: `grep -c
"mock.patch.object(rcp, \"arm_workspace_dir\")"
tests/test_issue_3127_run_pair.py`, run against the worktree -- `3`,
matching `grep -c "^    def test_" tests/test_issue_3127_run_pair.py`'s
own `3` -- every test in that file mocks it. None of them exercises the
real `arm_workspace_dir()` implementation, so none of them could have
caught this.

### Defect 3 — blind scorer wired and genuinely blind: Surface

The scorer function itself is real and independently verified. derived:
called `rcp.evaluate_pair_blind()` directly this session with a
deliverable text this session planted a real skill slug into
(`"...used the product-discovery-hypothesis-preregistration skill..."`)
and a fake evaluator whose score depends on whether the slug is visible in
the prompt:

```
"scrub_replacement_counts": {"skills-on": 1, "skills-off": 0},
"scrubbed_scores_by_arm": {"skills-on": 5, "skills-off": 5},
"unscrubbed_scores_by_arm": {"skills-off": 9, "skills-on": 3},
"scrub_changed_score": true
```
The first (scrubbed) evaluator call never saw the slug (checked directly
on the captured prompt string this session's own script collected); a
second (unscrubbed) call ran only because a replacement actually
happened, and `scrub_changed_score` correctly flipped to `true` because
the fake evaluator's score genuinely moved once the slug became visible.
`"skills-on"`/`"skills-off"` never appeared in either prompt (checked via
string containment checks in this session's own script, same run as
above). This matches PR #3154's claim for `evaluate_pair_blind()` in
isolation exactly.

But the same `_spawn_mod` crash that blocks defect 2 also blocks defect 3:
`evaluate_pair_blind()` is called only from `run_pair()`'s `compute_h2()`
closure, itself only invoked by `gate_pair_on_h1()` (canonical:
`PR-3131-branch:scripts/issue-3127/run_consumer_pair.py:744-762` defines
`compute_h2()`, `:764` calls `gate_pair_on_h1(..., compute_h2=compute_h2)`,
read directly) -- the same call that never fires because
`arm_workspace_dir()` raises first, per defect 2's live reproduction
above. So "`run_pair()` reaches it from the harness's real per-pair
flow" (PR #3154's own wording) is false for a genuine, unmocked call, for
the identical reason as defect 2 above. Surface, same basis as defect 2.

### Defect 4 — wall-clock reported under its true name: Surface

`execute_arm()`'s own code is correct. derived: `grep -rn "wall_clock_s\b"
scripts/issue-3127/run_consumer_pair.py docs/issue-3127/decisions/
pre-registration.md docs/issue-3127/reports/*.md`, run against the
worktree, with `wall_clock_to_pr_open_s`/`wall_clock_to_landed_s` matches
excluded -- zero hits; the old bare name is gone from every file checked.
Called `execute_arm()` directly this session (mocking only
`subprocess.run`) for both the successful and watch-timed-out paths --
both reported `wall_clock_to_pr_open_s` as a float and
`wall_clock_to_landed_s: None` with a `landing_measurement_status` string
naming a specific, actionable reason ("this harness observes only the
spawned session's own session-end event... at most a phase-1 proposal
PR opening, not a merge... contract v3 s22").

But the claim is "gone everywhere, including... the results skeleton,"
and that does not hold for the actual committed artifact the acceptance
check inspects for existence. derived: `git log --oneline --follow --
docs/issue-3127/_assets/consumer-path-results.json`, run against the
worktree -- exactly one commit, `9c9801cd`, which predates all four
repair commits and was never touched by any of them. canonical:
`PR-3131-branch:docs/issue-3127/_assets/consumer-path-results.json`, read
directly -- its `arms.skills-on`/`arms.skills-off` objects carry
`"wall_clock_to_landed_s": null` with no `wall_clock_to_pr_open_s` field
at all, and no per-arm `landing_measurement_status` field; only a
top-level `run_status_reason` and a generic per-arm `"note": "not
measured this session -- see run_status"` cover the gap in prose, not in
the corrected schema `execute_arm()` now produces. canonical:
`PR-3131-branch:scripts/issue-3127/run_consumer_pair.py:775-798`,
`emit_not_executed_results()` -- the one function whose output shape
matches the honest post-repair schema. derived: `grep -n
"emit_not_executed_results" scripts/issue-3127/run_consumer_pair.py`, run
against the worktree -- one hit, its own `def` line; it is called from
nowhere in `main()`. It is dead code that was never wired up to
regenerate the actual on-disk asset. The specific old misleading name
does not reappear anywhere, so the regression this defect named is not
repeated, but "gone everywhere" does not hold for
`PR-3131-branch:docs/issue-3127/_assets/consumer-path-results.json`
specifically.

## The deciding question: does --dry-run match the code, and would --execute produce every pre-registered field?

canonical: `PR-3131-branch:scripts/issue-3127/run_consumer_pair.py:283-287`,
`render_dry_run()`'s printed "blind quality score" line, read directly --
states the scorer is "wired into `run_pair()`... gated on the pair passing
the H1 check above." True of the isolated functions (defects 2/3 above)
but false of the actual reachable flow, which crashes first, per this
session's own live reproduction above. `--dry-run`'s printed plan does not
match what the code actually does for this part of the pipeline.

Checked every diagnostic/guardrail field `pre-registration.md`'s field
(c)/(d) names against what the harness's real `--execute` code path can
produce, all `derived:` against the worktree this session:

- **wall-clock-to-landed-PR** (field (c), guardrail) -- permanently `None`
  by design (defect 4's own honest fix, confirmed above); the pre-
  registration's guardrail rule (c) ("must not exceed... by more than 50%
  combined") can therefore never be mechanically evaluated by this
  harness against real data.
- **verification-round count / verification-round defect counts** (fields
  (c) and (d)) -- derived: `grep -n "verification_rounds\|
  verification_defects_found" scripts/issue-3127/run_consumer_pair.py`,
  run against the worktree -- both hits are inside the dead
  `emit_not_executed_results()`, hardcoded to `None`; `build_execute_
  results()` (the function `main()`'s `--execute` branch actually calls)
  has no such field at all.
- **token cost / tokens_total** (field (d)) -- derived: `grep -n
  "collect_metrics\|collect_ledger_tokens" scripts/issue-3127/
  run_consumer_pair.py`, run against the worktree -- `collect_ledger_
  tokens()` is called only from the also-dead `collect_metrics()`, which
  is itself called from nowhere in `run_pair()`/`main()`.
- **BM25 selection position per skill mount** (field (d)) -- derived: `grep
  -n "bm25_selection_position" scripts/issue-3127/run_consumer_pair.py`,
  run against the worktree -- zero hits. It appears only as a
  hand-written value in the original results skeleton's `skills-on` arm
  (not `skills-off`), never computed by any function in this file.
- **Directive-composition bytes (H1) / blind quality score (H2)** -- the
  only two pre-registered fields the code can compute at all, and both
  are unreachable in a real run (derived: the same `NameError`
  reproduction under defect 2 above -- `arm_workspace_dir()` crashes
  before either is ever collected).

Net (derived: the five field-by-field greps immediately above, each run
against the worktree this session): even setting the `_spawn_mod` crash
aside, a real `--execute` run of this harness as it stands today has no
code path that could ever populate the verification-rounds, verification-
defects, token-cost, and BM25-position fields field (d) names, and the
wall-clock-to-landed guardrail field (c) names is permanently
unmeasurable by design. With the crash included: `main()`'s `--execute`
loop (canonical: `PR-3131-branch:scripts/issue-3127/
run_consumer_pair.py:889`, the `run_pair()` call site inside that loop,
read directly) has no `try`/`except` around it, so the `NameError`
propagates out of `main()` itself, before `build_execute_results()` or
the `Path(args.out).write_text(...)` call two lines later ever runs -- a
real `--execute` run over the current code produces no results JSON at
all for any pair whose both arms reach `watched-to-completion`.

### Acceptance checks and full suite -- reproduced this session, all pass

```
$ python3 scripts/issue-3127/run_consumer_pair.py --dry-run; echo exit=$?
...
exit=0
$ test -f docs/issue-3127/_assets/consumer-path-results.json && echo PRESENT
PRESENT
$ python3 scripts/issue-3127/verify_preregistration.py; echo exit=$?
OK: pre-registration commit 84226988... is an ancestor of results commit
9c9801cd...
exit=0
$ python3 -m pytest tests/ -q
278 passed, 2 warnings in 10.23s
$ python3 -m pytest test/ -q
15 failed, 548 passed, 3 xfailed in 32.38s
```
All run against the worktree this session. `tests/`'s 278 matches PR
#3154's own claimed 254-baseline-plus-24-new. `test/`'s 15 failing node
IDs match PR #3145's own independently re-derived list exactly (derived:
diffed this session's own failure summary above against PR #3145's "Full
test suite" section, same 15 names including `Bm25CrossFamilySkillMatches
Test`, issue #3091's scope, untouched by this repair).

### Test Depth Audit on the new tests

derived: `python3 -m pytest tests/test_issue_3127_run_consumer_pair.py
tests/test_issue_3127_h1_and_scoring.py tests/test_issue_3127_run_pair.py
-q`, run against the worktree:
```
........................                                                 [100%]
24 passed in 0.84s
```
derived: `grep -c "^    def test_" tests/test_issue_3127_run_consumer_
pair.py tests/test_issue_3127_h1_and_scoring.py tests/test_issue_3127_
run_pair.py`, run against the worktree:
```
tests/test_issue_3127_run_consumer_pair.py:6
tests/test_issue_3127_h1_and_scoring.py:15
tests/test_issue_3127_run_pair.py:3
```
6 + 15 + 3 = 24, matching the pytest run above (defect 1's file, defects
2/3/4's shared file, and defect 3's orchestration-wiring file
respectively). Every one of the 24 carries at least one assertion on a
real, falsifiable property of its own narrow scope -- Genuine Assertion by
that narrow reading, verification density 24/24 (derived: the same
pytest run above, zero skipped/xfailed among the 24).

The headline finding is not a missing assertion but a misplaced mock.
canonical: `PR-3131-branch:tests/test_issue_3127_run_pair.py`'s own
module docstring, read directly -- states these tests exist to prove "the
scorer is reachable from `run_pair()`'s real per-pair flow, not
orphaned." derived: `grep -c "mock.patch.object(rcp,
\"arm_workspace_dir\")" tests/test_issue_3127_run_pair.py`, run against
the worktree -- `3`, matching that file's own `3`-test count (cited
above) -- every one of its 3 tests proves that claim only by replacing
`arm_workspace_dir` with a mock that always succeeds, the exact function
whose real body raises `NameError` on every real call, per defect 2's
live reproduction above. Reclassified per `test-depth-audit`: Mock-
Dominated specifically on the one seam whose real behavior would have
falsified the claim these three tests exist to prove. Step 4's mutation-
testing check confirms this directly: this session's own removal of that
mock (the defect-2 live reproduction above) turned the same call from
"passes, proves reachability" to "raises NameError, proves the
opposite."

## Why

Ran every claim in PR #3154's record against the actual code rather than
citing its own assertions, per this task's spawning brief. The one
finding this session adds beyond what PR #3145 or PR #3154 either found
-- the `_spawn_mod` `NameError` -- was located by deliberately calling
`run_pair()` with only the one dependency mocked that genuinely cannot
run in this environment (`execute_arm()`, which shells out to a real
`spawn.py --skills` dispatch), rather than accepting the repair's own
test suite's choice to also mock `arm_workspace_dir()`. `experiment-
trust`'s Twyman's-law framing applied directly: PR #3154's record reports
"all four fixed, 278 passing, 24 new tests" as a clean result after a
repair round -- exactly the kind of reassuring outcome that should be
checked hardest, not trusted because the prior round already found
problems and this one claims to have fixed them all (derived: this
session's own reproduction of the `tests/` 278-pass and `test/` 15-fail
counts under "Acceptance checks" above, matching PR #3154's claim exactly
on the numbers while disagreeing with it on defects 2 and 3's
reachability). `adversarial-review`'s blindness-to-intent mechanism is
why the crash surfaced: this session read `run_pair()`'s docstring's
claim about its own correctness as something to falsify, not as
evidence, and called the function the way `main()` actually calls it
rather than the way the repair's own tests chose to call it (derived: the
`execute_arm`-only-mocked reproduction under defect 2 above, versus
`PR-3131-branch:tests/test_issue_3127_run_pair.py`'s own choice to also
mock `arm_workspace_dir`, both cited above). `test-depth-audit`'s
execution-vs-verification distinction is what separates "these three
tests pass" from "this claim is true" (derived: the same mock-removal
reproduction cited under "The headline finding" above) -- the tests are
well-formed and each asserts something real, but what they assert is
narrower than what the repair's record and the file's own docstrings
claim.

## What did not work

None this session. derived: this session's own sequence of live
reproductions -- defect 1's resolver call, defect 2's isolated-function
calls plus the `run_pair()` crash reproduction, defect 3's scrub/blind
verification, defect 4's field-name greps, and the two full test-suite
runs (`tests/` 278 passed, `test/` 15 failed matching baseline, both
cited under "Acceptance checks" above) -- every one produced a clear
result on the first attempt; nothing was tried and abandoned.

## Upstream basis

- PR #3131 branch `issue-3127/experiment-trust+product-discovery-
  hypothesis-preregistration+implementation-blueprint+silent-failure-
  audit-4eda8e00`, head `5a07342c` -- the harness graded here, read and
  exercised via `git worktree add ... --detach` at that commit (derived:
  `git rev-parse origin/issue-3127/experiment-trust+product-discovery-
  hypothesis-preregistration+implementation-blueprint+silent-failure-
  audit-4eda8e00` -- `5a07342c318635b4f5f8a33981f00fff5acd7864`, unchanged
  from setup to the end of this session). Not merged, not edited by this
  session (canonical: `gh pr view 3131 --json state` re-checked at the
  end of this session -- `state: OPEN`, no new commits pushed by this
  session).
- `docs/issue-3127/reports/experiment-trust+test-depth-audit+silent-
  failure-audit-f8660411.md` (PR #3145) -- the source of the four defects
  re-graded above.
- `docs/issue-3127/reports/experiment-trust+implementation-blueprint+
  silent-failure-audit+test-derivation-d2b8a13d.md` (PR #3154) -- the
  repair round's own record; its per-defect claims are what this session
  independently re-derived rather than cited.
- `skills.py`'s `resolved_skill_sources()` and `spawn.py`'s
  `resolved_skill_sources = skills.resolved_skill_sources` re-export
  (canonical: `spawn.py:442`, read directly this session) -- confirms the
  function this session called live is the same one production `spawn.py
  --skills` calls.

## Open findings

1. `run_pair()`'s real (unmocked) flow raises `NameError:
   name '_spawn_mod' is not defined` in `arm_workspace_dir()`
   (`PR-3131-branch:scripts/issue-3127/run_consumer_pair.py:687`) before
   the H1 gate or blind scorer is ever reached -- no `import spawn as
   _spawn_mod` (or equivalent) exists in the file, per this session's own
   `grep` cited under defect 2 above. This blocks defects 2 and 3's own
   "reached from the real flow" claims and would crash any real
   `--execute` invocation once both arms of a pair reach `watched-to-
   completion`. Resolution path: a future session needs to add the
   missing import and add at least one `run_pair()`-level test that does
   NOT mock `arm_workspace_dir`, so the real integration is covered, not
   just the gating logic given a working resolver. Not fixed here, per
   this task's explicit instruction not to edit PR #3131.
2. `PR-3131-branch:docs/issue-3127/_assets/consumer-path-results.json`
   was never regenerated by the repair round and does not carry the
   corrected wall-clock schema that `emit_not_executed_results()` would
   produce if it were ever called; `emit_not_executed_results()` itself
   is dead code, per this session's own `grep` cited under defect 4
   above. Resolution path: either wire it into `main()` and regenerate
   the committed asset from it, or delete the dead function if a hand-
   authored skeleton is the intended shape -- currently the code and the
   committed asset disagree about which is canonical.
3. Four of `pre-registration.md` field (d)'s named diagnostic metrics and
   one of field (c)'s two guardrail metrics have no code path in
   `run_consumer_pair.py` that could ever populate them from a real
   `--execute` run, independent of finding 1's crash, per this session's
   own field-by-field greps above. Resolution path: either wire these
   collectors into `run_pair()`'s return value and `build_execute_
   results()`'s output shape, or amend `pre-registration.md` to mark
   these fields as not collected by this harness version.

Correction (see `deviation-log/20260902T111313598-experiment-trust-
invocation.md`): this record's first-committed version (commit
`dab63a8b`) wrote `applied: invoked` for `experiment-trust` while
describing informal reasoning applied during the defect grading -- the
Skill tool itself was not actually called until the Stop hook's
skill-verdict-guard flagged the mismatch. canonical: this session's own
Skill-tool call this turn, and the returned SKILL.md body, received this
turn -- confirms Step 1's scope gate (this session's own work is not
itself an online controlled experiment: no random assignment, no live
run occurred this session) routes to "not a gateable experiment result to
interpret" for the R007 measurement itself, while Step 5's Twyman's-law
framing was already the correct informal basis this session used, now
confirmed against the skill's actual text rather than assumed.

skill-verdict: experiment-trust — applied: invoked; loaded the skill's
full SKILL.md via the Skill tool this session (after this record's first
commit, following the Stop-hook correction above). Step 1's scope gate
correctly routes this session's own defect-grading work away from Steps
2-4 (SRM/A-A/pre-commit checks -- no live experiment ran this session);
Step 5's Twyman's-law framing (an anomalous or reassuring result is
suspected until independently checked) directly motivated re-deriving all
four defect verdicts from this session's own live execution (derived: the
full set of
live reproductions under "What was done" above) rather than accepting PR
#3154's "all four fixed" claim, per "Why" above.
skill-verdict: adversarial-review — applied: invoked; this record is an
independent evaluator pass built from running the code directly against
PR #3145's original defect descriptions (canonical: PR #3145's record,
cited under "What was done" above) rather than reading PR #3154's record
as ground truth -- the `_spawn_mod` crash was found by calling
`run_pair()` the way `main()` actually calls it instead of the way the
repair's own tests chose to call it, per the defect-2 reproduction
(derived: same `NameError` reproduction cited under defect 2 above).
skill-verdict: test-depth-audit — applied: invoked; classified the new
tests (Step 2, per-file counts derived under "Test Depth Audit" above),
computed verification density (Step 3, derived above), and applied Step
4's mutation-testing logic directly by removing the `arm_workspace_dir`
mock (derived: same crash reproduction cited under defect 2 and "The
headline finding" above), which reproduces the exact `NameError` those
three tests were written to prove does not happen.
other mounted skills: not triggered -- none beyond the three above were
mounted for this task.

## Next steps

None for this session -- `loop_state: landed`, this record is committed
to this session's own branch and PR. For a future session: see "Open
findings" above for the three concrete resolution paths. PR #3131 itself
was neither merged nor edited by this session (canonical: `gh pr view
3131 --json state` cited under "Upstream basis" above).
