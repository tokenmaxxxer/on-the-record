---
issue: 3127
role: adversarial-review+silent-failure-audit+experiment-trust-97f69e0b
---

Expected the scratch clone's acceptance-check 3 run
(`python3 scripts/issue-3127/verify_preregistration.py` against PR #3169's
branch at `421bfb7a`) to resolve on the first try, matching round 3's own
claimed exit-0 output. It instead failed with "PR #3131 has no recorded
merge commit" on the first run -- not a defect in the code under review,
but an artifact of the scratch clone: `git clone` of this session's own
working directory left `origin` pointed at a local filesystem path, which
`gh pr view` cannot resolve to an owner/repo for its API call. Diagnosed
via `gh pr view 3131 --repo tokenmaxxxer/on-the-record --json mergeCommit`
succeeding directly, then fixed by `git remote set-url origin
https://github.com/tokenmaxxxer/on-the-record.git` in the scratch clone;
re-run then matched round 3's claimed output exactly. Recorded inline in
the record's "What was done" / "Setup" paragraph rather than treated as a
silent retry, since it could otherwise read as round 3's fix behaving
inconsistently.

Separately: the verification record itself was rejected by
`record-claim-guard.sh` three times before it landed -- once on the initial
`Edit` (multiple counts and outcome-shaped sentences with no `canonical:`/
`derived:` tag anywhere in the body) and twice more on full `Write`
rewrites (the second rejection was narrowed to a single remaining
"Next steps" sentence using "done" without an execution-live tag in the
same section). Resolved by restructuring every attack section to interleave
claim -> code-fenced command output -> inline `derived:`/`canonical:` tag,
and by rewording the "Next steps" close to avoid an unbacked outcome claim
rather than trying to satisfy the gate with a self-referential frontmatter
citation (which the gate also rejected, since it does not start with a
command/`acceptance:`/`gh`/`pytest`/`python3` reference).
