- 2026-09-02T12:20:00Z, inline, expected the code-repair commit's own `-m` message to satisfy `trailer-gate.sh` (it stages `docs/issue-3127/decisions/pre-registration.md`, under the issue tree) but the first `git commit` for the script/test/doc changes carried no `Subject: issue-3127` trailer and the hook flagged it after the commit already landed locally. Resolved by `git commit --amend` on that same, not-yet-pushed local commit to add the trailer -- safe since it had not been pushed or reviewed by anyone. Location: this session's own git history, commit now `1bd821e8b827f64b9827c0cf7a2db3a2c7148d08`.
- 2026-09-02T12:25:00Z, inline, expected a single `Edit` to land the record file's full content directly onto the pre-written skeleton, but `record-claim-guard.sh` refused it twice in a row: first for several bare status/defect/count claims lacking `canonical:`/`derived:` tags in-section and for citing PR #3166's record path (untracked on this branch, unmerged) as a backtick path/frontmatter `upstream:` entry when that path is not reachable from this branch's working tree; second, after adding citations, for one remaining status claim in the "Upstream basis" section lacking its own in-section `canonical:` tag. Both refusals left the record file completely unmodified (the whole `Edit` call is atomic and rejected as a unit), so nothing partial was left on disk. Resolved by adding `canonical:`/`derived:` tags next to every quantitative or status claim, dropping the unreachable PR #3166 record path from the frontmatter `upstream:` list (kept it as a plain-prose citation of the PR number + commit sha instead, with its own `canonical: gh pr view 3166 --json state` tag), and switching from `Edit` to a single `Write` of the full file once every claim carried its citation. Location: this session's own record file, `docs/issue-3127/reports/implementation-blueprint+experiment-trust+silent-failure-audit-9afe0675.md`.
