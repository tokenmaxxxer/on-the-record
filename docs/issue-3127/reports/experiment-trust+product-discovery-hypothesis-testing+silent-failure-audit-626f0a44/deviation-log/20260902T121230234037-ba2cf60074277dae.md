---
issue: 3127
role: experiment-trust+product-discovery-hypothesis-testing+silent-failure-audit-626f0a44
---

Task asked this session to run the real consumer-path `--execute` measurement (both arms through spawn.py, H1/H2/H3 collected, `consumer-path-results.json` populated with real per-arm numbers). That did not happen — two deviations from the requested path, both recorded in the session's record (`docs/issue-3127/reports/experiment-trust+product-discovery-hypothesis-testing+silent-failure-audit-626f0a44.md`, "What did not work" and "Open findings"):

- Creating the 4 seed GitHub issues `--issue-map` needs (attempted via `gh issue create --repo JiwonJung94/study-companion`) was refused live by this repo's own gh-guard pretooluse hook (two-account model, contract v3 s8/s9 — skill sessions cannot author requirement-backlog issues). No `spawn.py` dispatch ever ran; zero sessions were spawned, zero H1/H2/H3 data collected. Did not attempt to route around the gate (no alternate `gh` auth, no hook skip). canonical: this session's record, "What was done" bullet on the `gh issue create` refusal, which quotes the tool-result error text verbatim.

- While running acceptance check 3, found `scripts/issue-3127/verify_preregistration.py` fails (exit 1) and root-caused it to a structural defect independent of this session: PR #3131 landed as a single squash-merge commit (`fb0bb0d3`, already on `main`) that introduces both `docs/issue-3127/decisions/pre-registration.md` and `docs/issue-3127/_assets/consumer-path-results.json` in the same commit, collapsing the two-commit ordering the check's git-ancestry comparison requires. Did not attempt a fix — either a `main` history rewrite or a redesign of the check's ordering mechanism would be needed, both beyond this session's scope/authority. canonical: this session's record, "What was done" bullet with the four-command reproduction (`git show fb0bb0d3^:<path>` x2, `git log --diff-filter=A --follow` x2, `git cat-file -t 9c9801cd...`), and "Open findings" item 1.

Delivered instead — derived: `git log --oneline -1` on this branch this turn, result `733f26e3 issue-3127: record consumer-path execution attempt, gh-guard block, and squash-merge ordering defect`, and this turn's `gh pr create` result `https://github.com/tokenmaxxxer/on-the-record/pull/3166` — dry-run validated against the pre-registration, both blockers root-caused with reproducible commands, `run_consumer_pair.py`'s not-executed reason/next-steps updated to state them precisely, `consumer-path-results.json` regenerated via `--emit-not-executed` (harness-generated, not hand-edited), full test suite green (derived: `python3 -m pytest tests/ -q` this turn, result 352 passed), record written, PR #3166 opened with `Advances #3127` (intentional partial delivery, not closing the issue).
