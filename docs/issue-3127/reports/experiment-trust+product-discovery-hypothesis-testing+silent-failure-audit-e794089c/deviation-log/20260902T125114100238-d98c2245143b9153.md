canonical: docs/issue-3127/reports/experiment-trust+product-discovery-hypothesis-testing+silent-failure-audit-e794089c.md ("What was done" / "What did not work" / "Open findings" sections, this same commit) — full evidence chain for every entry below.

- 2026-09-02T21:22:00+09:00 inline: killed a detached `nohup ... & disown` first `--execute` attempt (lost harness completion tracking) 2s after launch and switched all subsequent real dispatches to the Bash tool's tracked `run_in_background` + `TaskOutput(block=true)`; verified no live process or stray GitHub side effect survived.
- 2026-09-02T21:35:00+09:00 inline: fixed `scripts/issue-3127/run_consumer_pair.py::build_stub_skill_repo()` to also stub `skills._STATIC_POLICY_SKILLS` (found live: the skills-off arm's dispatch failed on an unresolvable `work-in-english` policy skill before reaching any corpus-availability manipulation) — stays inside this issue's own harness file, mechanical extension of the existing stub-builder loop, does not change what the measurement claims to test; commit `1deb6198`.
- 2026-09-02T21:42:00+09:00 inline: found a second, distinct dispatch-blocking defect (cross-family skill-source tier conflict, `pipeline.py::_cross_family_candidate_corpus()`, issue #2055's own fail-closed safety check) and decided NOT to patch it — fixing it would mean weakening a shared safety check used by every session on this machine, out of this issue's scope; documented as Open finding 1 in the record instead, with a resolution path for whoever picks it up next.
- 2026-09-02T21:55:00+09:00 inline: hand-assembled the final `docs/issue-3127/_assets/consumer-path-results.json` via a one-off, uncommitted glue script (`/tmp/finalize_results.py`) instead of letting `run_consumer_pair.py --execute`'s `main()` produce it end-to-end, because no registered pair ever reached a state where both arms completed for `run_pair()`'s own H1/H2 gating logic to run over; every figure traces to a real, cited artifact.
