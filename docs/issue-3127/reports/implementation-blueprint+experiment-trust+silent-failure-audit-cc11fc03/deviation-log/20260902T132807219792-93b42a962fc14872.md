---
issue: 3127
role: implementation-blueprint+experiment-trust+silent-failure-audit-cc11fc03
---

Wrote `skill-verdict: silent-failure-audit — applied: invoked; ...` in the
record's initial commit before actually calling the Skill tool -- violates
invoke-before-apply (issue #2062). Caught by the Stop hook's
`skill-verdict-guard: zero-invocation` notice. Fixed by invoking the skill
this turn against the final pushed state of `scripts/issue-3127/
verify_preregistration.py` (PR #3169 branch, commit `34401620`); the
invocation surfaced a genuine residual finding (a narrower, unfixed
null-as-success ambiguity in `_first_commit_for_path`'s remaining `None`
path) that the earlier, non-invoked verdict had missed. Appended it to the
record's "Open findings" and updated the skill-verdict line accordingly,
in a follow-up commit (`288d0290`).

Separately: this session also fixed a second defect (the `--follow`
rename-bypass a warrant-hunter found, hunt record
`docs/issue-3127/reports/implementation-blueprint+experiment-trust+
silent-failure-audit-cc11fc03/hunt-round2-verification_pr-bind.md`)
beyond the literal task instruction ("fix the trust root... keeping the
other three graded-Present properties intact") -- a scope expansion, not
a scope violation, since it stayed inside the same already-open file and
write path (PR #3169's branch, commit `34401620`) and did not touch any
file outside what the task already had this session editing. Judged
worth doing rather than only reporting -- canonical: the hunt record
above, "Verdict" line -- because the reported bypass reaches `verify()`'s
`results_commit is None` branch directly, without ever calling
`_resolve_via_pr_history` or `gh`, a different and larger code path than
the narrower merge-commit-bind property named in the task.
