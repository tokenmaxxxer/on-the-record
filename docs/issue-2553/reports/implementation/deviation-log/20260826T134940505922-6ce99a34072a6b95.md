20260826T134954 inline top-level `import spawn` in gates.py broke record-claim-guard.sh's packaged gates.py copy (ModuleNotFoundError on load); fixed by making the import lazy/memoized inside _import_spawn_for_roster() — gates.py (role_scope() area)
