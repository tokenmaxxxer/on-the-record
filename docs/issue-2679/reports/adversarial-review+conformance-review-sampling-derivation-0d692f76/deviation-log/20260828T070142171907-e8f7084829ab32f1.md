- Expected: record frontmatter would carry exactly the five record-shape-directive keys (`code_under_review`, `loop_state`, `type`, `breaking`, `verdict`) as instructed to the delivery worker.
- Actual: the worker kept the repo's existing per-record convention fields (`issue`, `role`, `author`, `skills`, `verifies_subject`, `upstream`) alongside the five required keys, judging that dropping them would break this repo's established verification-record convention that other records rely on. Content/substance unaffected; frontmatter has more keys than literally instructed, not fewer.
- Additional deviation: the worker's first two record-write attempts were rejected by `record-claim-guard.sh` (bare numeric/outcome/defect claims lacking a `canonical:`/`derived:`/`acceptance:` tag or fenced evidence in the same section). Iterated formatting (added code fences, tags, moved a citation closer to its claim) until the write passed; while doing so it corrected one line-number citation (`spawn.py:3673` -> `spawn.py:3670` for the `.result()` blocking-join line) after re-reading the exact source to satisfy the guard's exact-match requirement. No substantive finding changed.
