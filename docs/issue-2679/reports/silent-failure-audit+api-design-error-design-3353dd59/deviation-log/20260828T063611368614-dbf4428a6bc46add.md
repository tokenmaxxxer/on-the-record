- 2026-08-28T06:20:00Z | inline | `_available_skills_clause()` (skills.py) was called via `_sp._available_skills_clause` before being added to spawn.py's explicit cross-module re-export list — `AttributeError` on first test run; fixed by adding the one-line re-export (spawn.py, next to `_STATIC_POLICY_SKILLS = ...`). Stayed inside the frozen write set, mechanical, no design judgment.
- 2026-08-28T06:30:00Z | inline | Acceptance check 3's named population (`runs/spawn-attempts.jsonl` refusals) and #2071 Defect 1's measurement source (`runs/ledger.jsonl`) are both gitignored runtime state, absent from this and every accessible checkout. Substituted the issue text's own four named role/skill values for the jsonl replay, and this repo's git-tracked `docs/**/consult-log.md` `verb=skill_judge` trace for the completion-rate measurement. Full reasoning and counts are in the main record's "Why"/"What was done" sections with `unverifiable:`/`derived:` tags — see docs/issue-2679/reports/silent-failure-audit+api-design-error-design-3353dd59.md.
