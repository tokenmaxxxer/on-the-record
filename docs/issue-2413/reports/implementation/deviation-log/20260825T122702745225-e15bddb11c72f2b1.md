- 2026-08-25T12:27:02Z — inline — the before-landing warrant-hunter dispatch (mandated by the warrant protocol, not by issue #2413's own acceptance criteria) found that `_pid_is_alive()`'s `isinstance(pid, int)` guard treated a numeric-string-encoded `pid` field as unconditionally dead with no OS probe, which could prune a genuinely live in-flight spawn attempt once its record aged past `SPAWN_ATTEMPTS_RETENTION_SEC` — violating the "never prune a live in-flight attempt" invariant the issue itself requires. Fixed inline within the same file/function/frozen scope (spawn.py `_pid_is_alive`, coerce digit-only string pids to `int` before the OS probe) and covered with a new test (`test_string_encoded_live_pid_survives_past_retention`, tests/test_watch_hardening.py). Full reproduction and reasoning: docs/issue-2413/reports/implementation/2026-08-25-hunt-issue-2413-prune-fix.md.
