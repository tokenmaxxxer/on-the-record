2026-08-24T16:10:00Z inline implementation(issue-2163): the issue's acceptance list named a literal reproduction step ("trigger `claude plugin marketplace update tokenmaxxxer` while a patrol poll tick is due and confirm no crash burst"). canonical: issue #2163 body, "## Acceptance" section (this session's `gh issue view 2163` read). Running that command live in this session would mutate this session's own plugin marketplace checkout — a shared, hard-to-reverse action outside the frozen write set (`on-the-record/monitors/poll-heartbeat.sh`, `on-the-record/monitors/test_poll_heartbeat.py`) and outside what a single code-fix session should do unprompted. Resolved inline, staying inside the frozen write set: added `t_patrol_tick_skips_when_checkout_vanishes_mid_sleep` to `on-the-record/monitors/test_poll_heartbeat.py:716-766`, which reproduces the same underlying condition (CHECKOUT physically absent mid-tick, from a background-subprocess race) under full test isolation instead of the live marketplace command — see `docs/issue-2163/reports/implementation.md`'s Open findings.
