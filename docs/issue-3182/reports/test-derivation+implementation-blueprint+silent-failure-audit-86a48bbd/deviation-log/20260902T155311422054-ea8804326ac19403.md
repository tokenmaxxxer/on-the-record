---
issue: 3182
role: test-derivation+implementation-blueprint+silent-failure-audit-86a48bbd
---

Invoked the `test-derivation` skill after `PR-3184-branch:tests/test_issue_3182_preflight.py`
and `PR-3184-branch:tests/test_issue_3182_install_sufficiency_doc.py`
(both untracked on this branch; pushed to PR #3184's own branch, not this
one) were already written, passing, and pushed -- not before, as the
invoke-before-apply obligation (issue #2062) requires. The test design had
already been routed informally (EP/BVA for the JSON-shape/exit-code/source
assertions, state/idempotence for the read-only property, a decision-table
single case for the `remote_push_access` negative) directly against the
issue's own itemized acceptance criteria before the Skill tool was called.
Invoking the skill afterward reproduced the same routing rather than
surfacing a different one, so no test was rewritten as a result. Logged in
the record's "What did not work" section and here rather than left silent.

Separately: the first version of the doc-cross-reference test
(`test_every_precondition_name_is_traceable_into_the_doc`) tokenized
`PR-3184-branch:docs/handbooks/install-sufficiency.md` (untracked on this
branch) into whole words and required each significant word of a
precondition's machine-cased `name` to appear as a standalone token. That
failed on `home_claude_skills_dir_present`'s `dir`, which the doc only ever
spells out in full as "directory" -- a false positive on drift, not real
drift. Replaced with a case-insensitive substring check against the full
doc text before the test file was pushed, so `dir` legitimately matches
inside `directory` while a genuinely absent word still fails the check
(verified by the regression drill in the record's "What was done").
