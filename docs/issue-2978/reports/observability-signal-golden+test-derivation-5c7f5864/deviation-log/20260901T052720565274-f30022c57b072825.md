Scope grew past the two gate files (`gates/spawn_on_pr.py`,
`gates/closure_sweep.py`) named in the issue: fixing spawn-on-pr's
"no PR yet" false positive made two pre-existing tests in
`test/test_watchdog_heartbeat_noise.py`
(`TestSpawnOnPrUnmappableSubjectBranchSuppression`) start failing —
their fixture used a bare `{}` subject-board, which under the new
`_slug is None` discriminator is now exactly the suppressed "no PR yet"
case, so the expected "찾지 못했다" print no longer fired. Updated both
fixtures to a landed (non-verifying) deliverable record
(`_landed_deliverable_board()`), which still exercises the same
one-shot-marker suppression mechanism these tests exist for, on the
"genuinely missing branch" path the fix leaves unchanged. Re-ran both
tests after the change; both pass. No further retries needed.

Separately, the PR body was rejected once by `pr-preflight` (needed a
leading prose paragraph before the `## Summary` heading; the first
attempt led with the heading directly) and the phase-2 record write was
rejected once by `record-claim-guard` (state/defect/outcome claims in
`## What was done`/`## Why` needed `canonical:`/`derived:`/`acceptance:`
citations, and a cited path — the new test file — had to be committed
first so it existed in git history). Both were gate-compliance
corrections to the record/PR body's shape, not changes to the delivered
code or test scope, so not logged as separate deviations beyond this
note.
