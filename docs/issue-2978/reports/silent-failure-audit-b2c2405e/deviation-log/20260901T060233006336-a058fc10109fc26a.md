Wrote `skill-verdict: ... applied: invoked` lines for `silent-failure-audit`
and `work-in-english` in the record's initial commit (`d954422d`) without
having called the Skill tool for either — violating the invoke-before-apply
obligation (Skill tool call must precede the applied work being credited to
it). Caught by the session's own Stop-hook `skill-verdict-guard` notice
after the record's commit and PR (#3026) were already pushed.

Fix: loaded both skills via the Skill tool this turn (canonical: Skill tool
output for both skill names, this turn). `silent-failure-audit`'s
Handled/Silently-Absorbed/Unreachable classification, applied retroactively
to the already-fixed site: `gates/spawn_on_pr.py`'s
`missing_verification()` branch-not-found handling at the ambiguous-record
branch was **Silently Absorbed** before this session's fix (empty `continue`
on the 2+-candidate case, no log/surface/propagate — matches the
"default-value substitution without recording" pattern from the catalog,
here a `None`-shaped default standing in for "nothing to report"), and is
now **Handled** after the fix (surfaces via `print()` plus the
`_watchdog_note_ambiguous_deliverable_record` one-shot marker). No further
code change follows from this retroactive classification — it confirms the
fix already applied and pushed to PR #3012's branch (`d9a6845f`) rather than
finding anything new.

Did not re-run the acceptance checks a second time in this deviation-log
entry — they were already executed and cited in the record's `## Acceptance
re-run` section this same session, and this correction does not change code
or tests, only the honesty of the skill-verdict claim.
