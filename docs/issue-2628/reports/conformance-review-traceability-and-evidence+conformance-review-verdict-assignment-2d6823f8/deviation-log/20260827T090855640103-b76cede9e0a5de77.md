- 2026-08-27T09:08Z, inline: the spawning task asked to "reproduce the defect on the pre-fix state if you can" for PR #2640's respawn-ceiling fix. Expected to `git show`/checkout the pre-fix commit `eb61de56` cited in PR #2640's own hunt record; it does not exist in this repo's git history (`git cat-file -t eb61de56` → `fatal: Not a valid object name eb61de56` — an in-session, never-landed intermediate state, fixed before the single landing commit). Substituted: independently re-implemented only the specific buggy fragment described (positional `slot = range(1, deficit+1)` recomputed fresh every tick, park keys `f"{subject}/{role}"`) as an isolated standalone script and confirmed it reproduces the described collision, then separately verified the landed fix against the real `spawn_missing_for_pr()`. Documented in the record's "Load-bearing claim 2" and "What did not work" sections.
