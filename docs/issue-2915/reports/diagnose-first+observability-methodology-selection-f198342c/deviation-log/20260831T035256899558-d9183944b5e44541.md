# Deviation — issue-2915/diagnose-first+observability-methodology-selection-f198342c

First `Write` of the phase-2 record
(`docs/issue-2915/reports/diagnose-first+observability-methodology-selection-f198342c.md`)
was rejected by `record-claim-guard.sh`: a bare tick-count claim had no
`derived:`/code-fence backing within range, a brace-expansion path
reference (`on-the-record/monitors/{poll-heartbeat.sh,
poll_heartbeat_delta.py}`) read as a broken-reach citation, and the
frontmatter `sha: fed2e78d` was a short hash instead of the required
40-char form. Fixed by wrapping the two measurement outputs in code
fences, spelling out both paths literally, and resolving the full sha
via `git rev-parse fed2e78d`, then rewriting the whole file in one
`Write` (the skeleton was still on disk at that point, so nothing
partial was ever committed).
