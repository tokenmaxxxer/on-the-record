# Deviation log — issue-2315 (conformance-review role)

- 2026-08-25T04:30:00Z | inline | the first `Write` on `docs/issue-2315/reports/conformance-review.md` (filling the skeleton with the full per-requirement record) was refused by `on-the-record/hooks/pretooluse-dispatcher.sh`'s record-claim-guard (issues #793/#870/#330/#1085): several status/outcome-marker sentences ("passed", "confirmed") with no `canonical:`/`derived:`/pinned citation in the same section as the claim, one bare (non-sha-pinned) mention of the PR-only, untracked-on-this-branch path `docs/issue-2315/reports/implementation.md`, and one self-citation of this record's own not-yet-committed path. canonical: the `PreToolUse:Write hook error` body returned by that tool call, this turn. Resolved by rewriting every R-item to route its outcome claim through this repo's `acceptance: <command> — result:` + fenced-block convention or a commit-pinned `<sha>:<path>:<line>` citation, sha-prefixing the one orphaned bare-path mention, and dropping the self-referential path citation from the warrant-hunter-skip note. Stayed inside this role's own frozen write set (its own record file only), mechanical (citation formatting, not a content or verdict change), one-off.
- 2026-08-25T04:45:00Z | inline | the `git commit` for the record used a heredoc-style `-m "$(cat <<'EOF' ... EOF)"` body and was refused by `on-the-record/hooks/pretooluse-dispatcher.sh`'s heredoc-command-refusal-gate (issue #1976). canonical: the `PreToolUse:Bash hook error` returned by that tool call, this turn. Resolved by re-running the same commit with one `-m` flag per paragraph instead, no change to the intended commit content. Mechanical (shell invocation shape only), one-off.
- 2026-08-25T04:45:00Z | inline | the same combined command's `gh pr create` used a heredoc-style `--body "$(cat <<'EOF' ... EOF)"` and was refused by the same heredoc-command-refusal-gate, plus a pr-preflight check that the body's first paragraph was heading/bullet-only rather than real prose. canonical: the `PreToolUse:Bash hook error` returned by that tool call, this turn. Resolved by writing the PR body to `/tmp/pr-2315-conformance-review-body.md` with an opening prose paragraph and a trailing `Closes #2315`, then passing `--body-file` instead. Mechanical (invocation shape only), one-off.
- 2026-08-25T05:00:00Z | inline | the Stop hook's `skill-verdict-guard` flagged three `skill-verdict:` lines (`conformance-review-requirement-extraction`, `conformance-review-verification-method-selection`, `conformance-review-traceability-and-evidence`) as missing the `invoked;` marker immediately after `applied:` (issue #2062) — the marker was present but landed on the next wrapped source line, not literally on the same line as `applied:`, which the guard's line-oriented scan did not credit. canonical: the `Stop hook additional context: skill-verdict-guard` message returned this turn. Resolved by editing those three lines so `applied: invoked;` stays on one unwrapped line, no change to the stated content/reasoning. Mechanical (line-wrap formatting only), one-off.

Not logged as a deviation (RECOGNIZE excludes it): R5's first live cache-valid-probe attempt classified `delta` instead of `no-change` because of genuine concurrent write traffic on the real repo between two calls a few hundred milliseconds apart — an expected retry of a live external system, not scope-exceeding work; already narrated in `docs/issue-2315/reports/conformance-review.md`'s "What did not work" section.
