issue-2289 CHANGES round on PR #2495: reviewer asked to remove the "Closes #2289" trailer from the PR body since this is a partial delivery. Attempted via `gh pr edit`; blocked by `on-the-record/hooks/pr-preflight.sh` (canonical: this session's `gh pr edit` attempt this turn, hook output quoted verbatim) — that hook forces `phase2 = True` unconditionally whenever `CORE_BUILD_NOW=1` (pr-preflight.sh:243-244, canonical: this session's Read of the file this turn) and then requires a Closes/Fixes/Resolves match in phase2 body (pr-preflight.sh:715-718), with no partial-delivery exception and no override env var (canonical: this session's grep for override hooks in the file found none). Resolution: kept the "Closes #2289" trailer to satisfy the hard gate, but rewrote the body to lead with the partial-delivery framing, link the deferred-remainder list, and add an explicit note that the trailer is present only to satisfy pr-preflight.sh and that a human merging the PR should decide whether to let the auto-close fire or file the follow-up issue first. This is a deviation from the reviewer's literal instruction, made because removing the trailer is blocked by a gate outside this session's write set (fixing pr-preflight.sh's phase2 model is a separate, larger change than issue-2289's scope). See also the sibling entry 20260826T030211879705-d80f87825dd04f0d.md, written before this session discovered its own canonical shard path via `spawn.py deviation-log-path --issue 2289`.
