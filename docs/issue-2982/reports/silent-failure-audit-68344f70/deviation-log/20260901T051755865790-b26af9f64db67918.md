---
issue: 2982
role: silent-failure-audit-68344f70
---

# Deviation: branch checkout reverted, merged instead

The spawning task said this was a follow-up "on the SAME branch
issue-2982/knowledge-management-taxonomy-tagging+test-derivation-d8949284
(PR #3003)." I initially `git checkout -b`'d that branch directly to
build the fix there. `on-the-record/hooks/pretooluse-dispatcher.sh`'s
approval-gate then refused the next Write with "sidecar role/issue
(issue-2982/silent-failure-audit-68344f70) disagrees with the
branch-parsed role/issue (issue-2982/knowledge-management-taxonomy-tagging+test-derivation-d8949284)"
— this session's `.on-the-record/role.json` sidecar is pinned to
`silent-failure-audit-68344f70`, and the gate enforces branch/sidecar
agreement.

Reverted: `git checkout issue-2982/silent-failure-audit-68344f70` back to
the spawned branch, then `git merge
origin/issue-2982/knowledge-management-taxonomy-tagging+test-derivation-d8949284`
(commit `e465079d`) to bring PR #3003's code onto the spawned branch
instead of building on the other branch directly. The fix, tests, and
this record all landed on `issue-2982/silent-failure-audit-68344f70` /
PR #3011, which functionally supersedes PR #3003 (same code plus the
re-derived floor) rather than extending PR #3003 in place.
