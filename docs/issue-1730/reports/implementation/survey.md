# Survey — issue #1730

Write surfaces:
- `spawn.py:5930-5934` — `_judge_trace_path(cwd)` returns `_consult_root(cwd) / "docs" / "reports" / "patrol-judge-log.md"`, a path inside the tracked `docs/` tree. Unlike `_consult_trace_path()` (spawn.py:5493-5501), whose writes are followed by an explicit `_commit_consult_trace()` call (spawn.py:5523-5544, invoked at spawn.py:5675 and 5789), nothing ever commits the judge trace after `_append_judge_trace()` (spawn.py:5937-5945) writes to it from `judge_cmd()` (spawn.py:6050 on, trace write path built at line 6061). Every `judge_cmd()` call therefore leaves the target repo's working tree dirty with an uncommitted, ever-growing tracked file. `_judge_roles_run_today()` (spawn.py:5948-5973) only ever reads whatever path `_judge_trace_path()` returns — it has no hardcoded path of its own — so relocating `_judge_trace_path()`'s return value is sufficient; the 3-role-per-merge cap keeps working unchanged.
  canonical: spawn.py:5930-5934, 5493-5501, 5523-5544, 5937-5945, 5948-5973, 6050-6061 (this session, read verbatim)
- `docs/reports/patrol-judge-log.md` — the tracked file itself, currently 2 lines of judge-trace output committed in a prior run. Issue #1730 asks for this file to be deleted from the repo once the write target moves.
  canonical: `git ls-files docs/reports/patrol-judge-log.md` (this session) — output `docs/reports/patrol-judge-log.md`, confirming the path is tracked; file content read verbatim (2 lines, both `verb=judge` trace entries)
- `docs/handbooks/spawn.md:96` and `:104` — both name the tracked path in prose (예산/budget section and 트레이스/trace section) documenting `_judge_trace_path()`'s current behavior; both need the `runs/` path once the function changes.
  canonical: docs/handbooks/spawn.md:92-106 (this session, read verbatim)
- `tests/test_spawn_judge.py` line 248 (`JudgeTraceAlwaysTest._trace_lines()`) and line 268 (inside `test_traces_on_cap_exceeded_without_dispatching_git`) — both hardcode `self.root / "docs" / "reports" / "patrol-judge-log.md"` as the path the test's temp-dir fixture expects `judge_cmd()` to write to; both need `self.root / "runs" / "patrol-judge-log.md"`. `TraceCapTest` (lines 140-227) builds its own `self.root / "patrol-judge-log.md"` fixture paths directly (never through `_judge_trace_path()`) and calls `_judge_roles_run_today()` on them directly — those lines are anchor-agnostic and need no change.
  canonical: tests/test_spawn_judge.py:140-280 (this session, read verbatim)
- `.gitignore` — `runs/` is already git-ignored repo-wide. No `.gitignore` change is needed; the new path inherits the existing ignore rule the same way the consult trace's sibling `runs/` writers already do.
  canonical: `grep -n "^runs" .gitignore` (this session) — output `1:runs/`
- `gates/patrol_wiring.py` — greped for `judge_trace_path`/`patrol-judge-log`/`docs/reports`, no hits. `patrol_wiring.py run` calls into `spawn.judge_cmd()` without knowing the trace path itself, so no change needed there; the issue's third Acceptance check (`git status --porcelain` clean after a `patrol_wiring.py run`) is a consequence of the `spawn.py` fix, not a separate write site.
  canonical: `grep -n "judge_trace_path\|patrol-judge-log\|docs/reports" gates/patrol_wiring.py` (this session) — empty output

Historical records under `docs/issue-1587/**` and `docs/issue-1605/**` also name `docs/reports/patrol-judge-log.md` in past-tense prose describing what was true when those issues landed — those are frozen records of prior state, not live documentation, and are out of this write set.

canonical: `gh issue view 1730` output (this session, read verbatim) — issue body carries `validity-consult-skip: trivial` and `design-research-skip: mechanical` as closed-vocabulary skip tags, and its Acceptance section fully specifies the fix's four checks (new path via `_judge_trace_path()`, tracked file removed, clean `git status` after a patrol run, `spawn.md` grep shows only the new path).

Skip condition: pure bugfix — per the canonical citation above, the fix is fully specified by the issue's own Acceptance section and confirmed by reading `_judge_trace_path()`/`_judge_roles_run_today()`/`judge_cmd()` above (no design decision is open: the anchor function, the cap-counting logic, and the trace-always line format are all unchanged, only the returned path moves). Scouting/full survey round skipped per scout-directive's bugfix skip condition.
