Subject: issue-2403

Deviation: the Stop-hook's skill-verdict-guard (issue #2039/#2153) expects
`skill-verdict:` lines for `conformance-review-requirement-extraction` and
`conformance-review-verification-method-selection` inside
`docs/issue-2403/reports/conformance-review.md` — the phase-2 record.
That file is execution-surface output under this repo's approval-gate
(contract v3 s19): no PR review Approve on PR #2462 and no issue comment
reading exactly `APPROVE issue-2403/conformance-review` exist yet
(checked live via `gh pr view 2462 --json reviews` and `gh issue view
2403 --json comments`, this session), so a Write to that path is denied
before it is even attempted. The two mandatory `skill-verdict:` lines
were instead placed in the phase-1 survey
(`docs/issue-2403/reports/conformance-review/survey.md`, "## Skill
verdicts" section) — the only record this session is authorized to
write that both skills' actual invocations. This is a genuine mismatch
between two hooks' expectations (skill-verdict-guard assumes a record
file this session is not always allowed to touch), not a skipped
obligation: the same two lines will carry over into the phase-2 record
verbatim once an Approve lands and that write is unblocked.
