Subject: issue-2403

Deviation: three PreToolUse gate refusals during this session's re-review
write to `docs/issue-2403/reports/conformance-review.md`, each requiring a
content/approach change rather than a straight retry.

1. `record-claim-guard` (issue #330) refused an `upstream:` frontmatter
   edit citing `docs/issue-2403/reports/implementation.md` — that path is
   untracked on this review branch (it lives only on
   `issue-2403/implementation`), and the guard requires a nearby literal
   "untracked"/"renamed from-to"/"moved from-to" marker before it will
   pass a reference to a path absent from the current tree. A prior
   sibling `#` comment on the same YAML line did not satisfy it; adding a
   `note: untracked on this review branch, read via \`git show\`` sibling
   key to the YAML list item did. Resolved by adding that key, not by
   dropping the citation.
2. `record-claim-guard` (issue #333, "레코드에 근거 없는 개수 주장") refused two
   bare-number claims (an 18.8x undercount ratio and a 5.6x total-inflation
   ratio) inside requirement 3b's evidence — both were already inside a
   parenthetical citing the ratio via `≈`, which the guard did not accept
   as a `derived:`-shaped calculation. Resolved by rewriting each as
   `derived: \`<dividend> / <divisor> = <result>\`, this session` with a
   literal `=` and plain integers, not by removing the ratio claim.
3. `heredoc-command-refusal-gate` (issue #1976) refused the landing
   commit's `git commit -m "$(cat <<'EOF' ... EOF)"` heredoc-shaped
   message. Resolved by resubmitting the identical commit content as one
   `-m` flag per paragraph, per the gate's own stated remedy.

None of these changed the substance of the finding being recorded (the
CHANGES round's #2383/PR #2389 token-cost row cites the wrong session
transcript); each was a shape/citation-format objection to how the finding
was phrased, resolved within the gates' own stated remedies before the
write was retried.
