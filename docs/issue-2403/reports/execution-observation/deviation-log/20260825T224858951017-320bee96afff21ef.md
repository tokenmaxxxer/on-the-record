# Deviation log — issue #2403 (execution-observation role, session shard 320bee96afff21ef)

canonical: docs/issue-2403/reports/execution-observation/2026-08-26-hunt-execution-observation.md
("### Reproduce" / "### Resolution" sections, this session's own
before-landing warrant-hunt output)

- 2026-08-26T07:45:00Z | filed | issue-2403/execution-observation |
  the before-landing warrant-hunter dispatched during this session's
  landing step found a systemic gap outside this task's frozen scope:
  `on-the-record/hooks/record-claim-guard.sh` never wires in
  `gates/record_lint.py`'s four issue-#2331 phantom-citation checks
  (`citation_line_bounds_check`, `citation_line_content_check`,
  `wc_l_recompute_check`, `pytest_count_recompute_check`), so the
  write-time guard silently allows a `file:line` citation that a full
  `python3 -m gates.record_lint` run refuses (reproduced live — see the
  canonical citation above: `HOOK EXIT: 0` vs `RECORD_LINT EXIT: 1` on
  the identical content). Fixing `on-the-record/hooks/record-claim-guard.sh`
  itself is outside this role's write set (this session's only
  authorized output is its own execution-observation record for
  issue-2403) and is a recognizable systemic pattern (a hook silently
  missing checks its own shared module already implements), not a
  one-off mechanical fix — so per the deviation loop's CLASSIFY this is
  FILE-AS-ISSUE, not INLINE-FIX. Per the "Role sessions" rule, a role
  session does not spawn a peer role or file a new issue on its own
  initiative mid-task — this is reported here (not spawned) and in the
  hunt record's own "### Resolution" section, plus this session's final
  reply. What was fixed instead, in scope: the two phantom citations
  this finding reproduced against, in this session's own record
  (`docs/issue-2403/reports/execution-observation.md`'s
  `gates/merge_gate.py:264`/`:276`), via a `derived-unverified:` note
  clarifying those citations resolve against the PR #2452 head commit,
  not this branch's own pre-merge working tree — `record_lint` now
  reports no violations on that file.
