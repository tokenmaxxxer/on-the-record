---
issue: 3245
role: experiment-trust+silent-failure-audit+implementation-blueprint-1f4e5af2
---

# Deviation: session halted before start, gh auth precondition unmet

Expected: proceed with issue #3245 (verify uncommitted prior-session work,
read the issue via `gh issue view 3245`, then build/commit/push/PR per
CORE_BUILD_NOW bypass).

Actual: SessionStart hook reported "PRECONDITIONS NOT MET (contract v3
s10): gh is not authenticated."

derived: `gh auth status` — result:
```
github.com
  X Failed to log in to github.com using token (GH_TOKEN)
  - Active account: true
  - The token in GH_TOKEN is invalid.

  X Failed to log in to github.com account JiwonJung94 (/home/jwjung/.config/gh/hosts.yml)
  - Active account: false
  - The token in /home/jwjung/.config/gh/hosts.yml is invalid.
```

Per the hook's explicit instruction, did not start work, did not create a
local substitute for issue/PR state, and did not touch any files beyond
this record. Reported the missing precondition and the fix (`gh auth
login` or `gh auth refresh -h github.com`) to the user and stopped.
