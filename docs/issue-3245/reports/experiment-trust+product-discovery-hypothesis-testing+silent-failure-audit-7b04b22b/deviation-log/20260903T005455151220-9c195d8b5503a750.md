---
issue: 3245
type: deviation
date_stamp: 2026-09-03
---

canonical: `docs/issue-3245/_assets/01-study-groups/result.json` (this session's own dispatch attempt)
acceptance: `python3 spawn.py lint --issue 20 -C /tmp/study-companion-clone-3245` -- result:
```
이슈 #20 lint: 위반 없음
```

Expected: the trust-rooted dispatch of pair `01-study-groups` (issues
#19/#20 on the target repo) to reach `watched-to-completion` on both
arms, since the preflight lint quoted above passed clean and
`prepare_arms.py`'s manifest for this pair verified clean
(`manipulation_held: true`).

What actually happened instead: both arms' dispatch failed
(`dispatch_returncode: 1`) before either session did any task work.
`result.json` records the cause via `dispatch_stderr`, which contains
the literal text "훅이 headless 에서 발화하지 않는다" (hooks do not
fire headless) -- `spawn.py`'s `require_doctor()` (`pipeline.py:524`)
ran a live hook-firing probe (`spawn.py:doctor()`, `spawn.py:2175-2219`)
against the installed CLI during this session's own dispatch attempt and
found `UserPromptSubmit`/`PreToolUse` hooks silent in headless
(`claude -p`) mode -- an environment-wide regression unrelated to the
on/off skills manipulation, blocking every `spawn.py --skills` dispatch
on this machine right now.

Consequence for this issue's deliverable: the planned "run one full
pair, stop if clean, continue to n>=5" sequence stopped after this first
real dispatch attempt, since a repeat against any other issue/skill
would fail identically for the same environment-level reason. 0 of the
registered n>=5 pairs were scored this run; full detail in
`docs/issue-3245/reports/experiment-trust+product-discovery-hypothesis-testing+silent-failure-audit-7b04b22b.md`.
