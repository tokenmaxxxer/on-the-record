---
issue: 3245
type: deviation
date_stamp: 2026-09-03
---

canonical: `gh api repos/tokenmaxxxer/on-the-record/issues/comments/5518683506` (read this session, after the first record write and PR had already landed)
acceptance: `HOME=<fresh empty tempdir with only .claude/.credentials.json copied in> python3 spawn.py doctor` (in `/tmp/verify-3245-pr3251-b`) -- result:
```
UserPromptSubmit: 발화 / PreToolUse: 발화  (CLI 2.1.259 (Claude Code))
doctor-ok 기록. 이 버전에서 스폰이 열린다.
```

Expected: verify PR #3251 by re-running its three literal acceptance
checks, cross-checking a handful of its citations against real sources,
and writing a `verdict: pass` record once everything matched -- which is
what this session did first, committing (`c6baaa53`), pushing, and
opening PR #3251-verifying work.

What actually happened instead: after that first commit/push, the
operator posted an addendum to issue #3245
(`issuecomment-5518683506`, timestamped after this session's start)
instructing both independent-verification sessions to reproduce PR
#3251's central dispatch-failure diagnosis directly rather than accept
it, and reporting that most of today's other headless sessions on this
machine show hooks firing -- contradicting PR #3251's "environment-wide
CLI/hook regression" claim. Reproducing `spawn.py doctor()` under three
`HOME` conditions (this session's real `HOME`: passes; an empty tempdir
`HOME`: fails with PR #3251's exact signature; the same empty tempdir
with only `.claude/.credentials.json` added: passes again) showed PR
#3251's stated root cause is false -- the real cause is
`prepare_arms.py`'s isolated per-arm `HOME` never provisioning CLI
credentials, not a CLI/hook regression.

Consequence: this session rewrote the record's "Why"/"What did not
work"/"Open findings" sections around this finding, changed
`verdict: pass` to `verdict: fail`, and pushed a second commit
(`e1632026`) before opening the PR (#3254) -- rather than the originally
planned single-commit, pass-verdict delivery. The mechanical acceptance
checks and the exclusion-bookkeeping claim in PR #3251 still hold and are
recorded as such; only the root-cause narrative is corrected.
