issue: 3245
role: experiment-trust+silent-failure-audit-726d85ba

The spawning prompt stated "the harness is fixed; nothing about it should
need rebuilding this round." This was false: dispatching pair 1 fresh
surfaced three real, previously-unexercised bugs in `run_pair()`'s
post-dispatch synthesis (class-body NameError, `gh -R` given a local path
instead of owner/repo, guessed workspace rooted in the orchestrator's own
HOME instead of the arm's isolated HOME), all fixed this round -- canonical:
docs/issue-3245/reports/experiment-trust+silent-failure-audit-726d85ba.md
"What was done" §1.

Second deviation: found the actual root cause of every pair's H1 failure --
canonical: docs/issue-3245/reports/experiment-trust+silent-failure-audit-726d85ba.md
"What was done" §2 (`skills.py::_skill_repo_root()` falls through to a
shared managed clone instead of failing closed when `MUSTER_SKILL_REPO` is
set but invalid) -- but deliberately did not patch it: it is shared core
infrastructure well outside this issue's own consumer-path scripts, with a
blast radius across every `--skills skill-repo:` dispatch on the machine.
Diagnosed with full reproduction evidence and filed as Open finding 1 in
the same record instead, rather than bundling an unreviewed core-infra
patch into a measurement-round PR.

Both deviations are inline, already reported in full in the record's own
"What was done"/"Why"/"Open findings" sections; this entry exists only to
satisfy the separate deviation-log file requirement, not to add new
information.
