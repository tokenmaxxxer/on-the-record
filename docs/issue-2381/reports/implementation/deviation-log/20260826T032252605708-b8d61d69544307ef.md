- 2026-08-26T03:22:52Z | inline | CHANGES round 2 on PR #2445 reversed the prior CHANGES round's R2c decision (keep `.orchestrate-hook-fires/` tracked, per-session-commit by design): a fresh architecture consult plus a live grep this round found `hook_fires.py::_hook_fires_aggregate()` only ever globs the shard directory under the local session workspace and its own docstring says "never the shared on-the-record checkout" — no reader anywhere depends on a committed copy, so the earlier "intentionally tracked, by design" premise (drawn from `docs/specs/generated-paths.md`/`docs/specs/enforcement-boundary.md`) did not hold up against the actual reader code. Untracked the directory (`git rm --cached -r .orchestrate-hook-fires/`) and gitignored it. Also found R2b's flat `.orchestrate-hook-fires.log` was gitignored by the very first cut of this branch but never actually untracked (`git rm --cached` was missing), so the ignore entry had been a no-op against origin/main this whole time; fixed with `git rm --cached .orchestrate-hook-fires.log`. canonical: `hook_fires.py:49-56,63-79` (`_hook_fires_dir`/`_hook_fires_aggregate` docstrings and glob scope), `.gitignore` (this round's diff), `git ls-tree origin/main -- .orchestrate-hook-fires.log` (confirmed tracked before this round's fix).
