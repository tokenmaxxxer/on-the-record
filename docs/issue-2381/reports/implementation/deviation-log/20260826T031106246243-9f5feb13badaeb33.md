- 2026-08-26T03:10:00Z | inline | before-landing warrant hunt (this CHANGES round) found the R1 fix's first cut (`merge_gate.py`'s `evaluate()` calling `check_runner.fetch_all_role_branches(repo)` unconditionally and unmocked) broke two `gates/test_merge_gate.py` tests' documented "no network" invariant by shelling out to a real `git fetch` against this repo's actual GitHub `origin`; fixed by stubbing `check_runner.fetch_all_role_branches` in both tests via `monkeypatch`, the same seam the rest of the suite already uses. canonical: `gates/test_merge_gate.py` (the two stubs, this round's commit), `docs/issue-2381/reports/implementation/2026-08-26-hunt-orchestrator-fetch-all-branches.md` (repro/fix).
- 2026-08-26T03:15:00Z | inline | the CHANGES round's dispatch text literally suggested `git rm --cached roles/implementation.json` (R2b) and a gitignore-or-fix-writer binary for `.orchestrate-hook-fires/` (R2c); investigation showed R2b's premise false (`git log --all -S "roles/implementation.json" -- .gitignore` returns no commit — no such ignore entry has ever existed) and both R2c options wrong-sized (gitignoring the shard directory contradicts `docs/specs/generated-paths.md`'s design classification; adding branch-awareness to `hook-fires.sh` violates its documented zero-overhead, operator-frozen constraint). Applied neither literal suggestion — documented the R2b finding as-is and, for R2c, applied the existing by-design per-session-commit convention to this session's own shard instead. canonical: `docs/issue-2381/reports/implementation.md` (R2b/R2c bullets under "CHANGES round" and the updated "Open findings" section).
