---
issue: 2904
skill: independent-verification-1
---

# Deviations, issue-2904

derived: `gh api repos/tokenmaxxxer/on-the-record/issues/comments/5472536977` and this session's own two record writes (`git log --oneline issue-2904/independent-verification-1`).

1. **Mid-session operator comment widened verification scope after the first record write; substantial portions of that first write were replaced, not incrementally extended.** The first pass through PR #2905 was code-read-plus-existing-unit-test-reproduction (re-ran the PR's own numeric claims, read the diff for correctness, checked the acceptance criteria against the subject's evidence). Before that record could be committed and a PR opened, `pr-preflight` flagged a new issue comment posted after this session started, directing both spawned verifications to attack the root-cause claim by live construction (not source-reading alone), including its converse (queue durability across a process boundary); construct the working/investigating/stalled tri-state live; measure per-tick cost across a small and a larger fleet size and confirm no re-scan-from-top as logs grow; verify delta-suppression survives multiple consecutive ticks; and enumerate every pre-existing watchdog signal for survival. Rather than append these as a new section, the "What was done"/"Why" sections were substantially rewritten so the record's structure matched the deeper evidence actually gathered:
```
$ git show --stat 8df98941 -- docs/issue-2904/reports/independent-verification-1.md
 docs/issue-2904/reports/independent-verification-1.md | 239 +++++++++++++++++++++++++++++--------------------------
 1 file changed, 165 insertions(+), 74 deletions(-)
```
and a new residual finding (the wrapper-process crash-window gap between `roster_remove()` and the completion-queue write) surfaced only during this second pass. The first, shallower record was pushed as a commit before this rewrite and superseded by a second commit rather than amended, per this session's git-safety instruction to create new commits rather than rewrite pushed history.
