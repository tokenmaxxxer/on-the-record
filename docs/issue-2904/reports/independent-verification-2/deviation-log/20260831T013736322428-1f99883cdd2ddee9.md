---
issue: 2904
skill: independent-verification-2
---

# Deviations, issue-2904

derived: `git log --oneline issue-2904/independent-verification-2` and `docs/issue-2904/reports/independent-verification-2.md` (this session's own record, committed on this branch) — the deviation below is cited from there.

1. **Scope widened mid-session by an operator comment posted after the review had already started, and the record's own risk assessment changed as a result.** This session's first pass re-derived PR #2905's own numeric claims (test counts, failing-test-set diff) and traced its code paths by line number — a first version of the record was assembled around that evidence, assessing an "acceptance provenance gap" against all four of the issue's `provenance: executed-live` acceptance tags (none demonstrated live). issuecomment-5472536977 landed mid-session (caught by `pr-preflight.sh` before the first PR could open) demanding live, non-mocked reproductions instead of trusting the PR's own test suite: attack the root-cause claim by exercising the real completion path and checking the crash-between-record-and-drain converse, construct all three tri-states (working/investigating/stalled) live, measure per-tick cost across a small fleet plus the bounded-read claim, verify absolute-timestamp delta-suppression survival live, and re-derive the standing invariants each with its own command and output. This session ran all five live reproductions in a scratch worktree (`/tmp/tristate`) against the actual shipped functions — derived: `python3 -m pytest .` output plus the five `derived:`-tagged live runs cited in the record's "Adversarial re-verification" section, this session — which closed most of the original acceptance-provenance gap with real evidence and surfaced one new, narrower finding not present in the first draft: a real but small residual crash-window between `roster_remove()` and the completion-queue write. The record's own "acceptance provenance gap" finding was re-scoped down to a smaller subset of checks, and the new crash-window finding was added — derived: `git show --stat be896e06` (this session), result showing the record file as the only path touched, with the second commit rewriting most of the first version's content — so the record was substantially rewritten rather than incrementally patched, since the risk assessment itself changed, not just its supporting evidence.
