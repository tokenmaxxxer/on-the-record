2026-08-29T22:20:09Z inline — while fixing the two review-named gaps (mid-clone, disarm race), an independently-spawned blind-review subagent found the mid-clone fix's first version was itself unsafe (would delete a reused workspace's real prior work or the caller's own checkout); fixed in place with a new `_workspace_target_is_fresh()` gate (spawn.py:3065-3088) plus routed the adhoc call site through the same wrapper (spawn.py:3541-3543), both inside the existing spawn.py/test/test_bootstrap_signal_guard.py write set. canonical: PR #2794 (https://github.com/tokenmaxxxer/on-the-record/pull/2794), commit cf0e4bb7.
