Expected the record's citation density (commands + real output already
present throughout the "What was done" section) to satisfy
record-claim-guard.sh; it did not. The gate requires a `canonical:`/
`derived:` (or `acceptance: <command> — result:` immediately followed by a
code fence) tag adjacent to *each individual* bare-number or state/defect
claim within its own nearest-heading section, not just present somewhere
in the document. First `Write` attempt was rejected on ~13 separate lines
(bare "5 tests"/"Finding N" claims with no adjacent tag, plus a bare
backtick reference to the PR's test filename, name test_bootstrap_signal_guard.py
(untracked in this checkout's git history, exists only on the PR branch) —
a broken-reference rejection distinct from the citation-density one).
Fixed by adding a `canonical:`/`derived:` line immediately before or after
every claim, and by de-backticking that PR-branch-only test filename with
an adjacent "untracked in this checkout" note. Second attempt still
rejected on one remaining line (`acceptance: ... — result: 5 passed`
written as inline prose instead of `result:` immediately followed by a
code fence). Fixed by moving the pytest summary into its own fenced
block. Third attempt passed. No content was removed or weakened to pass
the gate — only citation placement changed.
