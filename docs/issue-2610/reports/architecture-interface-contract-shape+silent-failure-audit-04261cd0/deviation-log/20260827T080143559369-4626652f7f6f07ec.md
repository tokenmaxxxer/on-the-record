---
issue: 2610
role: architecture-interface-contract-shape+silent-failure-audit-04261cd0
---

# Deviation: skipped the separate throwaway end-to-end spawn

The issue's acceptance asks to "spawn a session end-to-end and show it
reaching PR" as a check distinct from record-scaffold.sh's own
end-to-end demonstration. This session did not run an additional,
separate throwaway spawn for that specific check — turn budget ran out
late in the session (see the repeated approach-cap warnings this
session received late in its run, issue #2262's turn-budget mechanism).

derived: this session's own PR #2630 is offered as the substitute
evidence instead — it is itself a real spawn (`CORE_BUILD_NOW=1`
build-now delivery) that reached a PR through the unmodified
spawn/pipeline/PR machinery this session repointed away from
`spawn_roles.json`, per the spawn task's own note: "the previous attempt
satisfied that by spawning a real session, which produced PR #2624... that
is an acceptable way to satisfy it; just be aware the spawn is real and
costs a session." This session judged that its own spawn already pays
that cost rather than spending a second one under a shrinking budget.

Already recorded in the main record's "## Open findings" section at
commit time; this entry exists per the separate deviation-log obligation
(issue #803/#983/#2348) so the deviation is not traceless.
