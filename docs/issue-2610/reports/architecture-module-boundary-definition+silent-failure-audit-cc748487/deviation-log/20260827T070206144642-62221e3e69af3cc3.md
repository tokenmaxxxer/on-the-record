2026-08-27T07:02:06Z inline: wrote something and then found and fixed what it broke. The first edit to `gates/gates.py` renamed the module constant `_ROLE_DATA_PATH` to `_ROLE_DATA_DIR` and updated `_role_cfg()` to read `roles/<role>.json` instead of `spawn_roles.json`, but missed four downstream error-message f-strings (inside `record_enums`/`record_refusal_reasoned`/`record_checked_claims`'s except-branches) that still referenced the deleted name `_ROLE_DATA_PATH` — would have raised `NameError` the first time one of those functions hit an unreadable role, on the fail-closed path specifically. No test in the current suite exercises that except-branch with a real bad-role record, so this was not caught by `python3 -m pytest -q` (which passed both before and after). Caught by re-reading the full file for remaining `_ROLE_DATA_PATH` references before treating the file as done, fixed by replacing each with `_ROLE_DATA_DIR / f'{role}.json'`, re-synced to the `on-the-record/gates/gates.py` mirror (kept byte-identical to the root copy, per its existing convention), and re-verified live.
derived: `python3 -c "import sys; sys.path.insert(0,'gates'); import gates; gates._role_cfg('nonexistent-role-xyz')"` — result: `FileNotFoundError(2, 'No such file or directory')` raised cleanly (not `NameError`), confirming the fix.
