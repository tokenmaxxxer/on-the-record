# Deviation log — issue #2443 (conformance-review role)

- 2026-08-26T00:52:06Z | inline | issue-2443/conformance-review |
  landing hit the same two mechanical repo gates the issue-2463
  conformance-review session's own deviation log already recorded. (1)
  `record-claim-guard` refused the first `Write` of the review record:
  several sentences in the drafted "Open findings"/"What did not work"/
  "Next steps"/"Skill verdicts" sections restated a status/defect claim
  or a bare number (a stray digit-slash-digit token read as a fraction)
  without an adjacent `canonical:`/`derived:` tag in the same section —
  resolved by adding a `canonical:` tag near the top of each flagged
  section pointing back at the already-tagged transcripts/Findings
  entries those sentences summarize, and rewording the flagged token
  into plain words. (2) `heredoc-command-refusal-gate` (issue #1976)
  refused the first `git commit -m "$(cat <<'EOF' ... EOF)"` and the
  first `gh pr create --body "$(cat <<'EOF' ... EOF)"` — resolved by
  using one `-m` flag per paragraph for the commit, and `--body-file`
  (written via the `Write` tool to `/tmp/pr-2443-body.md`) for the PR,
  no content change either time.

  A third, non-mechanical deviation: this review deliberately did not
  re-execute the implementation record's own destructive live prune
  (`_prune_orphaned_sidecars()` against the real `~/.tokenmaxxxer/work`)
  a second time, even though issue #2443's Acceptance bullet 3 reads as
  a live demonstration. This review's own R2b finding (the liveness
  check protecting that prune only sees the calling checkout's own
  roster, not the other concurrently-running checkouts' rosters) meant
  a second real deletion pass could plausibly delete sidecar files for
  a genuinely active sibling-checkout session this review has no way to
  positively confirm is idle. Substituted a read-only recount of the
  same eligibility criteria against the real directory instead,
  corroborating the implementation record's reported before/after
  counts without adding a second destructive pass — the actual before/
  after numbers on both sides are recorded, with their own `canonical:`/
  `derived:` tags, in `docs/issue-2443/reports/conformance-review.md`'s
  "What was done" and "Findings" (R3) sections. Not filed as a new issue
  on its own: it is folded into R2b's own recorded resolution path
  (same record, "Open findings" — follow-up issue against
  `_live_workspaces()`/`ROSTER` scoping) rather than treated as a
  separate deviation needing its own follow-up.
  canonical: the two `PreToolUse:Write`/`PreToolUse:Bash` hook error
  transcripts (record-claim-guard, then heredoc-command-refusal-gate
  twice) are in this session's own transcript; every retried command
  succeeded after the fix (commit `00f67854`, PR
  https://github.com/tokenmaxxxer/on-the-record/pull/2482). The
  live-prune non-re-execution reasoning is recorded in full in
  `docs/issue-2443/reports/conformance-review.md`'s own "What did not
  work" section, written and committed in the same commit as this log
  entry.
