2026-08-22T00:00:00Z | filed | pr-preflight.sh's amendments-reconciled check (issue #1177) requires a citation line in docs/issue-1978/reports/implementation.md when an issue comment (issuecomment-5377916095) lands after session spawn, but approval-gate.sh unconditionally refuses any Write/Edit/MultiEdit to that exact path pre-approval (phase-2-shaped target, no content exception) — a phase-1 session with a post-spawn comment cannot satisfy pr-preflight without approval-gate refusing the write that would satisfy it. The pre-#1816 (2026-08-16) "gated placeholder" precedent used by issue-1174's role records (e.g. docs/issue-1174/reports/api-design.md) predates approval-gate.sh and is no longer reachable under current gates (verified live this session: approval-gate.sh refuses the identical placeholder shape). Not inline-fixable: outside this issue's frozen write set (spawn.py/tests/docs proposal), and is a systemic gate-composition gap affecting any phase-1 PR that receives a comment after spawn, not a one-off. reported, not spawned.
