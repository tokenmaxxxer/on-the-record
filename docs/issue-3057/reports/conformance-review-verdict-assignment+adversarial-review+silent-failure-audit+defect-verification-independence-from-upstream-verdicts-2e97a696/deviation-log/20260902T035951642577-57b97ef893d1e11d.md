2026-09-02T04:00Z inline — expected the first `Write` of the record to pass `record-claim-guard.sh` on the first attempt; it was blocked twice instead (once for an OUTCOME claim lacking a same-section canonical/derived tag plus several abbreviated/unpinned backtick paths that don't exist verbatim in this repo's tree, once more for a bare multi-digit-number sentence and one remaining unpinned path). Fixed by using full unabbreviated paths, pinning PR-#3058-branch-only paths as `<sha>:<path>`, and adding an explicit `derived:`/`canonical:` tag next to the flagged sentences; no content claim changed, only citation formatting. Third attempt passed.
