- 2026-08-27T08:26:35Z — filed (reported, not spawned — role session, contract v3 SCOPE-EXCEEDED RULE) — `python3 gates/spec_index.py --update` fails on `main` (reproduced pre-edit by stashing this session's changes and re-running against commit `49c4854b8d699130fe88e6f6db6e4287feb313c0`): `FileNotFoundError: roles/specs/brand-design.spec.json` — the generator still depends on the `roles/specs/*.spec.json` tree #2610 retired. Hit while editing `docs/specs/enforcement-boundary.md` in this session (a `docs/specs/*` edit normally requires regenerating `docs/specs/reconciled-index.md` in the same commit); could not regenerate it, so this commit ships without that regeneration. Out of #2631's scope (fixing the generator is its own design call, unrelated to removing model_routing/BAR_ROLES role-name lists) — reported in this session's reply rather than filed as a new issue, per role-session RESOLVE rules.
