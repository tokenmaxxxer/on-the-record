- 20260827T050518Z inline: `git add` with the exact record filename (containing `+`) was denied by board-gate.sh's R5 ownership check — the gate's docs-path extraction regex (`[\w./~$:-]`) excludes `+`, truncating the scanned path at `technical-writing-structure-comprehension` and misreading it as a foreign role's file. Worked around by staging `docs/issue-2607` (the directory) instead of the exact filename; verified only my own record file exists under that tree first. Gate bug is outside this issue's write set (lives in the core plugin's board-gate.sh, not this repo's target files) — not fixed here.
