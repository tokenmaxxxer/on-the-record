2026-08-26T12:28:50Z filed implementation — deliberately left the git branch naming on the old role axis (`checkout_issue_branch`, `issue-<n>/<role>`) instead of switching real spawns to `checkout_issue_branch_for_skill`, because `gates.role_scope()` resolves the acting role by regex-matching `spawn_roles.json` against the branch name and would break for every future PR under the new branch shape (not among the issue's 7 cited sites). Role session per the SCOPE-EXCEEDED RULE: finished the frozen write set, did not spawn or file a peer issue — reported in the reply and in `docs/issue-2545/reports/implementation.md`'s "Rationale for deviations" section instead.
