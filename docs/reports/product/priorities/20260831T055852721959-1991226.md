2026-08-31 — issue #2919 follow-up (spawning task body, this session): when adversarially reviewing a fix for a liveness/mutual-exclusion/staleness defect, re-confirming the fix's own headline scenarios (the exact case the original bug report demonstrated) is not sufficient — the review must construct and probe the precise boundary the fix claims to have moved (here: the alive-vs-abandoned classification line), because a fix that narrows a failure window without eliminating the underlying missing-invariant (here: no re-verification of lock ownership before a critical-section write) leaves the same failure shape reachable under a smaller but non-trivial window. A residual race documented in the fix's own code comments as "narrower, not zero" should be treated as a claim to reproduce live, not as a closed risk-acceptance note. canonical: this session's spawning task body ("Probe the boundary between 'slow' and 'crashed' — that boundary is the whole fix, so find where it sits and whether a holder can be misclassified on either side") and `docs/issue-2919/reports/adversarial-review-95d4569a.md` finding 1 (the live reproduction of exactly that misclassification), this session.
