2026-08-30 — issue #2876 (JiwonJung94, issue body): a check whose *negative* result can be produced without the check ever reaching the thing it claims to cover is indistinguishable, in its output, from a check that genuinely looked and found nothing — and the standing "must not increase" invariant framing makes an unreachable population *permanently* invisible rather than merely unmeasured. Two independent instances surfaced the same day (this issue's `\brole\b` plural blind spot; tokenmaxxxer-core#2873/#2139's `pytest test/` vs `pytest .` collection-scope blind spot). Priority: when reviewing or writing any invariant/regression check in this repo, verify the check's *reach* (what population it actually walks) before trusting its *result* (what it reports) — a 0 or "clean" verdict is not evidence of coverage on its own. canonical: `gh issue view 2876` body, "Why this matters more than the 129 sites" section.
