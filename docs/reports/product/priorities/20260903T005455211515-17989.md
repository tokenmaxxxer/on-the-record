---
category: priorities
source: issue-3245 session (gh issue view 3245)
---

canonical: `gh issue view 3245` body, read this session.

The operator states two standing priorities in issue #3245's own text,
directly relevant to how future measurement work on this repo should be
run:

1. **R007 (whether skills are worth having reachable) is named as "the
   one the operator's goal is actually about"** among the several R-numbered
   requirements this apparatus was built for -- prioritize actually running
   and scoring this measurement over further apparatus-hardening once the
   apparatus itself is sound.
2. **A plain, unflattering answer is explicitly preferred over a
   polished but overclaimed one**: "If the difference does not clear the
   pre-registered threshold, say so plainly; that is a real answer and
   this repo has spent two days building the apparatus to be able to say
   it honestly." This session's own result (0/5 pairs scored, blocked by
   an unrelated CLI/hook regression rather than a skills-value finding)
   was reported under this same standing preference -- stated as
   "not answerable this run," not stretched into an indistinguishable-
   result claim the data does not support.

Both are read as durable priorities for whoever next resumes this
measurement (after the CLI/hook regression this session found is
resolved), not one-off instructions scoped to this single run.
