- 2026-09-02: operator stated, in the task brief for this session's
  repair of `scripts/issue-3127/verify_preregistration.py`, that this
  repo lands every PR with `--squash`, and that a check whose
  correctness depends on multi-commit ancestry surviving to `main` "can
  never survive landing by design" -- explicitly the same self-defeating
  shape as issue #3083's `hooks.json` test, which asserted a count true
  only before its own merge. The must-not was equally explicit: do not
  restore the property by rewriting `main`'s history -- "not authorized
  and would not fix the next squash anyway." Future acceptance-check or
  gate designs anywhere in this repo that compare git commit order (not
  just this issue's pre-registration check) should treat "does this
  survive a squash-merge of the PR that introduces it" as a design
  question up front, not a defect discovered after the check starts
  failing on every subsequent branch. This session's fix (PR #3169)
  resolved the squash-collision case via the originating PR's own
  commit history fetched through `gh pr view <n> --json commits` (GitHub
  retains it independent of what the squash collapsed on `main`), kept
  as a narrow fallback rather than the check's primary path, so the
  common (non-collapsed) case stays network-free. Source: this session's
  task brief, issue-3127/implementation-blueprint+experiment-trust+
  silent-failure-audit-9afe0675 session, 2026-09-02 (see also
  `docs/issue-3127/reports/implementation-blueprint+experiment-trust+silent-failure-audit-9afe0675.md`).
