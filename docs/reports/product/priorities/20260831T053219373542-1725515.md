2026-08-31 — issue #2919 follow-up (spawning task body, this session): a fix for a silent-failure defect must not itself introduce a new silent (or merely time-inferred) failure of the same shape — the original poll-heartbeat.sh bug was silent (macOS exit 1 with no serialization), PR #2923's own fix for it introduced a second concurrency defect (a mkdir-mutex that evicted a live holder based on elapsed retry count rather than confirmed death), caught only by adversarial review, not by the fixing PR's own tests. Priority: when a fix touches liveness/mutual-exclusion/staleness logic, the correctness claim must be ESTABLISHED (a direct, checkable signal — e.g. a recorded PID + `kill -0`) rather than INFERRED from elapsed time or retry count, and any residual race window that remains after the fix must be stated precisely with its concrete cost, never claimed away as an unqualified guarantee that hasn't actually been demonstrated live. canonical: this session's spawning task body ("Resolve it so a live holder is never evicted: the holder's liveness must be ESTABLISHED, not inferred from elapsed retries... If a residual race window remains, state it precisely and say what it costs -- do not claim an unqualified guarantee you have not demonstrated.") and `docs/issue-2919/reports/adversarial-review-a4f05242.md` (the review that caught PR #2923's own new defect).
