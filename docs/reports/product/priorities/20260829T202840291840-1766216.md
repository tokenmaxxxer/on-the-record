- 2026-08-30: user stated a bounded-round-loop philosophy while sending
  PR #2763 back for a third (and explicitly final) round of a
  cwd-tracking fix in `gate-registration-guard.sh`: "if a fourth round
  still turns up fresh cwd-tracking bypasses, that is a signal about the
  approach rather than about the next shape... I would rather have an
  honest boundary than a parser that is always one shape behind."
  Applies beyond this one guard: when an iterative adversarial-review
  fix loop keeps finding a fresh gap in the same class one layer down
  after a bounded round explicitly asked to "finish" the model, the next
  step is to name the boundary explicitly (a different seam, or a stated
  scope the code does not cover) rather than open a further round of the
  same shape of patch. Source: PR #2763 CHANGES review comment, issue
  #2705, round 3 (this session, 2026-08-30).
</content>
