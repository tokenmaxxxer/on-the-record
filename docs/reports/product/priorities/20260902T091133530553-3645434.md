---
category: priorities
source: issue-3120 spawning prompt (this session, 2026-09-02)
---

canonical: this session's own spawning prompt, issue #3120 body (`gh issue view 3120`), read this session.

The spawner stated several standing principles while scoping the poll-heartbeat.sh watchdog-rc95 fix, in the same "recurring category of bug" spirit as prior priorities entries in this file:

1. **Refusing to run stale code is correct; the fix is what happens after the refusal, not the refusal itself.** "Do not remove or weaken the freshness check; running stale code is worse than restarting." A self-healing mechanism must never trade correctness (running known-stale code) for convenience (staying alive at any cost).
2. **Automated recovery only — a human/model noticing and re-arming by hand is not recovery.** "do not treat the orchestrator manually re-arming as recovery. That is what happened today and it is why the operator saw repeated deaths." Named explicitly as the same failure shape issue #3061 already documented: a rule sitting in context at every turn does not bind a model the way a mechanical gate does.
3. **Claims about platform/OS-level behavior must be established by running, not by reasoning about it.** Two specific claims (whether the platform Monitor wrapper tracks pid or pipe; what happens when `exec` targets a momentarily-absent file) were called out by name as things to measure directly rather than infer from documentation or intuition — and if a claim can't be fully established, the record must say so honestly rather than assert it as proven.
4. **A cross-platform probe that silently passes by finding nothing on one platform reproduces the exact defect class under test.** Stated specifically for macOS/Linux exec-semantics differences: a probe is only as good as its ability to actually fail when the underlying platform-specific mechanism is broken, not merely "not crash."
5. **Sequencing multiple sessions' work under one Acceptance section is the spawner's responsibility to state explicitly, not something each session should have to infer** — confirmed live mid-session via issuecomment-5507212053 on this same issue: splitting one issue's Acceptance list across file-disjoint sessions is fine, but each PR's own checks must be readable as "this deliverable's own scope," not misread as "the whole issue is 3/6 done."
