- 2026-08-30: user stated, in the task brief for this session's
  independent verification of PR #2774 (issue #2705, round 3 of
  `gate-registration-guard.sh`'s bundled-`git add` cwd model), a bound on
  how many single-shape patch rounds this parser gets: "the bound set on
  this work is that a fourth round finding a fresh cwd-tracking gap is a
  signal about the approach, not about the next shape." This session's
  own probing found four such fresh gaps (bare `pushd`, `pushd +N`/`-N`,
  env-prefixed `cd`, `$CDPATH`), making this the third consecutive round
  to find new gaps in code written to close the previous round's gaps —
  future work on this parser should weigh a scope decision (state
  explicitly what this parser does/does not cover) against continuing to
  patch newly-found shapes one at a time, per the user's own stated bound.
  Source: this session's task brief, issue-2705/adversarial-review-87659a67
  session, 2026-08-30 (see also `docs/issue-2705/reports/
  adversarial-review-87659a67.md`, "Open findings").
