2026-08-30 — issue #2876 round 3 (round-3 spawn note quoted in `docs/issue-2876/reports/silent-failure-audit-3c233115.md`): a fix built specifically to close a coverage-blind-spot check is itself unusually prone to carrying a fresh instance of that same blind-spot in its own plumbing — this recurred three times inside one issue's own remediation (the plural regex miss, the hand-typed `--include` list missing `.sh`, and round 2's `--list-files` mechanism being cwd-dependent), plus a fourth instance found this round by directly auditing the fixed file (`except OSError: continue` silently narrowing the scan's file coverage). Priority: when landing a fix for "this check silently under-covers X," treat the fix's own new code as a first-class audit target before closing the round, not just the original defect site — specifically, run the `silent-failure-audit` skill's classification pass over the new/changed error-handling paths in the fix itself, since a checker that exists to catch silent narrowing is exactly the kind of code most likely to reintroduce it. canonical: `docs/issue-2876/reports/silent-failure-audit-3c233115.md`, "Why" section ("The third fix (unreadable tracked files) was found by applying the `silent-failure-audit` skill's classification procedure... to the file after the first two fixes landed").
