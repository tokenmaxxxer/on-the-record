issue: 2924
session: silent-failure-audit+refactoring-legacy-seam-selection-140f0858
date: 2026-08-31

Priority stated in the spawning prompt for this issue: platform-parity
checks in this repo must be *proven*, not just asserted — a check that
claims to catch a class of bug is not credible until it is run against a
known-bad historical commit and shown to fail, then against the fix and
shown to pass. "Whatever else it does" does not substitute for that pair.

Four standing invariants restated for this issue (reusable beyond it):
no revival of the retired role axis in any reshaped form; no new bugs;
no overhead increase (explicitly: no added per-tick or per-spawn cost);
no monitor or watch-family signal breakage (must never become blockable,
droppable, or advisory-only).

Process note: "commit early and incrementally" was called out explicitly
because two prior sessions in the related #2919 chain died at the end of
a long verification pass with a fully-done analysis and no commit —
losing the work. This session checkpointed after each part (check
module, roster.py/watchdog.py visibility fix, tests) rather than batching
everything into one end-of-session commit.
