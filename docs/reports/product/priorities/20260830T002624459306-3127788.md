- 2026-08-30: user stated, in the task brief for this session's
  independent verification of PR #2808 (issue #2803, the "role"-to-
  "skill" prose rename in `test_spawn_attempt_staleness.py`), that a
  cited sweep command's population claim must be checked by actually
  re-running the command, not accepted from its prose: "On this same
  issue, a previous delivery claimed a 49-file population while its
  cited commands reached only 34 — that is twice now on this issue, so
  verify the commands actually cover the claimed population rather than
  accepting the sentence." This session's own re-run of PR #2808's own
  cited sweep command (`grep -rlnE '_skill_family|_attempt_superseded'
  test/`, at the PR's own base commit) returned 3 files where the PR's
  record cited 1 — a third instance of the same population/command
  mismatch shape on this one issue lineage. Future review sessions on
  any issue should treat a "sweep found N files" or "population is M
  files" claim as needing independent re-execution before being trusted,
  not just re-reading the cited command text. Source: this session's task
  brief, issue-2803/adversarial-review-b1ff090f session, 2026-08-30 (see
  also `docs/issue-2803/reports/adversarial-review-b1ff090f.md`,
  "Finding 1").
