---
proposal: issue-296 fix (no proposal file, pure-bugfix skip per contract v3 s19)
---

# Hunt record — issue-296

## before-landing — stance 4: assume the write set cannot carry this work — find the path the build will need that the proposal does not list

Verdict: NO FINDING
Seed: `git diff HEAD -- spawn.py test_spawn.py` — moves `_ttl_marker()` from `d / ".muster-last-pull"` (inside clone) to `ROOT / "runs" / "ttl-markers" / sha256(clone_path)[:16]`, `_mark_pulled` now does `marker.parent.mkdir(parents=True, exist_ok=True)`.
cap_seconds: 120
tier: default
diff_stat_lines: ~20 (2 files)
started_at: 2026-08-07T00:00:00Z
ended_at: 2026-08-07T00:08:00Z

Checked for a directory/path the fix implicitly needs but never creates or that
some other part of the build expects (e.g. `.gitignore` entry for `runs/ttl-markers`,
a cleanup/GC script that rm -rf's `runs/` and would race with marker creation,
`bench/run.py`'s dependency on `rulebook_version()`/`core_version()` staying
clean, and the real managed clones already checked out in this worktree).

- `runs/` is already gitignored as a whole (`.gitignore:1: runs/`), so
  `runs/ttl-markers/` needs no new gitignore entry — `git check-ignore -v
  runs/ttl-markers/test` confirms it matches the existing `runs/` rule.
- No script in the repo does `rmtree`/`rm -rf` on `runs/` or any subpath that
  would selectively wipe `runs/ttl-markers/` while leaving `runs/rulebooks/`
  intact (grepped `rmtree.*runs|runs.*rmtree|rm -rf.*runs` across `*.py`/`*.sh`
  — no hits outside test_spawn.py's own temp-dir cleanup).
  `_mark_pulled()` creates the parent dir itself
  (`marker.parent.mkdir(parents=True, exist_ok=True)`), so no external
  directory-creation step is missing.
- Ran the fix against the two real managed clones already checked out in this
  worktree (`runs/rulebooks/tokenmaxxxer-core`,
  `runs/rulebooks/tokenmaxxxer-execution-observation`):
  `python3 -c "import spawn; print(spawn.core_version())"` →
  `5e0afe3 (2026-08-07, on-the-record 클론)` — no `(커밋 안 된 변경 있음)` suffix,
  and `git -C runs/rulebooks/tokenmaxxxer-core status --porcelain` is empty.
  `runs/ttl-markers/` now holds the two marker files, confirming
  `_mark_pulled` writes to the new location and needs no extra setup.
- `bench/run.py`'s provenance gate (`if "커밋안됨" in version: sys.exit(...)`,
  bench/run.py:112-113) is named in the issue's acceptance criteria but not
  touched by the diff; confirmed it needs no separate fix by exercising
  `spawn.rulebook_version()`/`core_version()` directly — both are already
  clean on the existing checkouts, so the gate is satisfied without any
  change to bench/run.py.
- `test_spawn.py -k "TTL or Rulebook or ttl"` (4 tests, including the new
  `test_ttl_marker_does_not_dirty_clone`) passes.

Found no path the build additionally needs beyond what the diff already
creates/relies on (existing `runs/` gitignore rule, `marker.parent.mkdir`
inside `_mark_pulled`, no competing cleanup script). No reproduction of a
missing-path defect — stopping.
