2026-08-30T02:24:06+09:00 inline `git grep <pattern> -- <pathspec> <rev>` places the revision after the pathspec, so git silently drops it as an unmatched extra pathspec and searches the current worktree/HEAD instead — both "main" and "PR branch" enumeration runs (record section 1) returned identical, wrong output until this was caught by a `git show <rev>:spawn.py` spot-check; fixed by moving the revision immediately after the pattern.

2026-08-30T02:24:06+09:00 inline First cross-repo round-trip script (record section 4) used a `python3 - <<'PYEOF'` heredoc and a fake docs path under `issue-27410`; both were refused by this session's own `pretooluse-dispatcher.sh` (heredoc write-shape refusal, then a board-gate mismatched-issue-number refusal) before any command ran — fixed by writing the script to a plain file (`/tmp/otr-e2e/write_sidecar.py`, run via `python3 <path>`) and reusing this session's own `issue-2741`/`adversarial-review-6a02d514` identity in every fake workspace.

2026-08-30T02:24:06+09:00 inline Round-trip script relied on a `cd` from a prior (failed) Bash call persisting into the next call; shell cwd resets between calls in this environment, so the script silently ran against this session's own main-branch `pipeline.py` instead of the PR-head worktree and printed the pre-rename `{"role": ...}` shape — caught by printing `pipeline.__file__` inside the script; fixed by passing the worktree path via `sys.path.insert` instead of relying on `cd`.
