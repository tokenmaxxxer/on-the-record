2026-09-01T05:35Z filed (reported, not spawned — role session)

canonical: docs/issue-2979/reports/observability-signal-golden+test-derivation-547467ea/2026-09-01-hunt-delivery.md (before-landing warrant-hunter dispatch, stance 0)

Before-landing warrant-hunter (stance 0) found `closure_sweep._pr_index_all()`'s
branch-dedup can, in a narrow edge case (two PR numbers sharing one head
branch string), cause `_classify_narrowing_prs` to silently fold a real
subject-mapping-loss PR into the non-printed non-subject aggregate. Fixing
it needs edits to shared lookup infrastructure (gates/closure_sweep.py,
consumed by closure-sweep/spawn-on-pr/spawn-on-approve too) outside this
task's frozen scope, and role-shaped judgment on whether that's this
issue's concern — resolved by NOT widening scope: judged out of scope per
issue #2979's own must-not clause ("lookup-failure defects filed
separately") and its field-observed examples (which explicitly list
unresolved/`None` branches as aggregate-worthy). Recorded as an Open
finding with a resolution path (file a follow-up issue against
`_pr_index_all()`) in
docs/issue-2979/reports/observability-signal-golden+test-derivation-547467ea.md
and reported to the user in this session's final reply rather than
spawning a peer session. No issue filed yet — left as a suggested
follow-up, not spawned.
