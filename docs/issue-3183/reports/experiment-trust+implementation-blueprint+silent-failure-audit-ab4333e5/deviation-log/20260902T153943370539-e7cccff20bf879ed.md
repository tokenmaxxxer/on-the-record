- 20260902T153952Z inline: `resolve_skill_files()` in `scripts/consumer-path/prepare_arms.py` originally hashed every file under the "on" arm's real skills root, including `.pytest_cache/*` tooling artifacts (357 files vs. 273 real `SKILL.md` files); added a dot-directory exclusion. Location: `scripts/consumer-path/prepare_arms.py`, `resolve_skill_files()`.
- 20260902T153952Z inline: `_cleanup()` in `scripts/consumer-path/prepare_arms.py` used `shutil.rmtree(d, ignore_errors=True)`, silently absorbing any cleanup failure; replaced with a per-directory try/except that warns to stderr. Location: `scripts/consumer-path/prepare_arms.py`, `_cleanup()`.
- 20260902T153952Z inline: `build_manifest()` in `scripts/consumer-path/prepare_arms.py` leaked the "on" arm's just-created temp HOME when `make_on_arm()` raised `ArmPreparationError` before the HOME was added to the returned `created_dirs` list (caught live: `test_on_arm_rejects_empty_corpus` followed by `ls -la /tmp | grep consumer-path-` showed 4 leaked `consumer-path-on-home-*` dirs). Fixed by having `build_manifest()` create and track both HOMEs up front and clean them up before re-raising. Location: `scripts/consumer-path/prepare_arms.py`, `build_manifest()`.
- 20260902T153952Z inline: `verify_manipulation.py`'s sidecar read (`verify_manifest_integrity()`) had no try/except at all, and its transport-record read (`load_transport_record()`) caught `OSError`/`json.JSONDecodeError` but not `UnicodeDecodeError`, inconsistent with `load_manifest()`'s own handling -- an unreadable sidecar or non-UTF-8 transport file would have escaped as a raw traceback instead of the expected `pair_excluded` JSON shape. Hardened both, and added a last-resort `except Exception` in `main()`. Location: `scripts/consumer-path/verify_manipulation.py`, `verify_manifest_integrity()` / `load_transport_record()` / `main()`.
