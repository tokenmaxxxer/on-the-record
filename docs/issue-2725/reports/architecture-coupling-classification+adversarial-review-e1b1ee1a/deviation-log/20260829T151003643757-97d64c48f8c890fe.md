2026-08-29T15:10:03+00:00 inline — first tie-break signal used ISO commit-date strings (`_record_commit_date`, `%aI`) and sorted by string; failed against same-second back-to-back commits in the test, so replaced with ranking by position in `git log --reverse --format=%H` (helper renamed `_record_add_commit`, returns a SHA). board.py `_front_skill`/`_record_add_commit`.
2026-08-29T15:10:03+00:00 inline — `test_no_hardcoded_membership_test_on_a_name_list` first asserted the retired name strings were absent from the whole function source via `inspect.getsource`; failed against the docstring, which deliberately still narrates the retired names for history. Narrowed the assertion to the code after the closing `"""`. test/test_board_front_skill.py.
