# Deviation log

2026-08-30T02:50:32Z inline silent-failure-audit-3da5ceae(issue-2795):
implementing the remote-aware `_unrecovered_commit_count()` required
picking a branch name to pass to `git ls-remote` — derived: `pwd &&
basename "$(pwd)" && git branch --show-current` — result: dir basename
`on-the-record-issue-2795-silent-failure-audit-3da5ceae`, actual branch
`issue-2795/silent-failure-audit-3da5ceae` (this session's own
workspace). The existing pattern at both call sites
(`diagnose_health()`'s own `branch = Path(work).name if work else
None`, watchdog.py) resolves to the dir-basename form, not this branch
— confirmed by that same live check. Using that existing pattern for
the new `ls-remote` call would have queried a branch that never exists
and reintroduced this same issue's false positive through a new route,
so used the already-existing `board.py::_current_branch()` (`git
symbolic-ref --short HEAD`) at the two call sites instead — inside the
frozen write set (`board.py`/`watchdog.py`, the files this fix already
touches), mechanical (swap one expression for an existing helper),
doesn't change what the deliverable claims to do. Flagged the
pre-existing `Path(work).name` use inside `diagnose_health()` itself
(feeds the `branch=` display text and the PR-lookup fallback) as a
separate, unresolved Open finding rather than fixing it — see
docs/issue-2795/reports/silent-failure-audit-3da5ceae.md's Open
findings section.
