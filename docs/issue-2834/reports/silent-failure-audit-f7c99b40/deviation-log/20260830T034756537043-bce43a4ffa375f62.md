2026-08-30T03:47:56Z inline No dedicated overhead-measurement script exists in this repo — derived: `find . -iname CLAUDE.md` (no result) and `grep -rn "overhead" docs/specs/*.md README.md bench/*.py scripts/*.py` (zero matches). Substituted a direct hot-path argument (all four new `_current_branch()` calls sit inside already-`git`/`gh`-heavy dead-entry branches, not the healthy-tick path) in place of a before/after benchmark run for the "no overhead increase" invariant. See docs/issue-2834/reports/silent-failure-audit-f7c99b40.md, "Invariants" section.
