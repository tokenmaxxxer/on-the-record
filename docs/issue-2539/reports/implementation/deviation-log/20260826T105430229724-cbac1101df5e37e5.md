Renamed the migrated `roles/specs/<role>.spec.json` blob's key from the originally-planned `spec` to `record_spec` inside each `spawn_roles.json[role]` entry: caught via round-trip check (derived: `grep -l '"spec"' roles/*.json | wc -l` — result: 42 of 44) that most role JSONs already carry their own `"spec"` field (a doc-path string), which the first-draft key name would have silently overwritten. See docs/issue-2539/reports/implementation.md "What did not work" and canonical: that record's "What was done" section for the full round-trip check.

Two real-spawn verification attempts did not land a record file (board-gate branch-mismatch on an ad hoc/no-`--issue` spawn, unrelated to this migration); accepted the real bootstrap_timing/skills evidence obtained instead of continuing to force a clean landed-record outcome. canonical: docs/issue-2539/reports/implementation.md check 5 and "What did not work".
