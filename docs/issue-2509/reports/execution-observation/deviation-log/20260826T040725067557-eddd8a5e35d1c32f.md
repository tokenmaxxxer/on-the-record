# Deviation log — issue #2509 (execution-observation role)

- 2026-08-26T00:00:00Z | inline | issue-2509/execution-observation |
  `on-the-record/hooks/record-claim-guard.sh` refused several `Write`
  attempts on this record before it landed. Not anticipated in the
  original task brief. Root causes, each resolved inline in the same
  frozen write set, no content dropped: (1) an escaped `\`` inside a
  fenced shell one-liner was mis-parsed by `_PATH_REF` as part of a
  literal path with a trailing backslash — resolved by removing the
  unnecessary escaping (triple-backtick fences do not need inner
  backticks escaped) and, separately, by not backtick-wrapping
  deliberately-nonexistent fixture paths at all; (2) a bare digit count
  in the record's own frontmatter `test:` field tripped the bare-count
  check — resolved by spelling the count out in prose instead of citing
  a raw number there; (3) `loop_state: done` is not in this role's
  declared enum (`roles/execution-observation.json` — progress:
  running/collecting-evidence, terminal: handed-off, refusal:
  execution-not-possible, error: environment-setup-failed) — resolved
  by using `handed-off`, which in turn required adding an explicit
  `resolution path:` line under each open finding (contract §20's
  open-finding-resolution-path requirement); (4) a `file:line` citation
  paired with a nearby parenthesized function-name quote was checked
  against the wrong line (the quote-proximity matcher picked a
  different quoted fragment than the one actually intended) — resolved
  by separating the two so no ambiguous quote sat within the matcher's
  search radius of the wrong citation.
  canonical: this session's own `PreToolUse:Write` hook-error
  transcripts (four distinct `record-claim-guard`/`record_lint`
  refusal messages) and the record's own diff between the first
  refused `Write` and the version that passed
  `python3 -m gates.record_lint docs/issue-2509/reports/execution-observation.md`
  clean, both in this session's transcript.

- 2026-08-26T00:00:00Z | inline | issue-2509/execution-observation |
  Independent verification went one step past issue #2509's three
  literal Acceptance bullets: checked whether a foreign-owner
  possessive protects a command-shaped (`test`-classified) backtick
  token the same way it protects the `file-existence` fallback, since
  the issue's own title ("any backticked token with a slash") reads
  broader than the three worked bullets it enumerates. Not called for
  by the task's literal scope (none of the three bullets name this
  shape), and surfaced a real, reproducible gap: `parse_checks`
  consults `is_foreign_owned` only in the branch chain after
  `looks_like_command`, so a foreign-owned `.py`-shaped token still
  classifies `test` and mechanically fails. Not filed as a new GitHub
  issue and not patched: this role's mandate is independent execution
  observation, not implementation, and role-session `gh-guard`/
  `CLAUDE_ROLE` restrictions block issue creation from this session
  (same restriction independently checked in the issue-2479
  execution-observation precedent record). Resolved by recording it as
  open finding 1 in this record instead, with an explicit resolution
  path (left for a human or a future implementation-role session).
  canonical: `docs/issue-2509/reports/execution-observation.md`'s own
  "Beyond the three literal bullets" subsection and "Open findings"
  item 1, both written this session with their own `derived:`
  reproduction.
