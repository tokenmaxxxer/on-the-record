The record's final `skill-verdict: defect-verification-independence-from-upstream-verdicts —`
line wrapped the em-dash to the end of a line with the `applied:` content
starting on the next physical line, leaving the same-line capture group
empty under `skill_verdict_reason_check`'s per-line regex — the Stop hook
flagged it as a missing-reason violation for that name. Fixed by joining
the name/dash and `applied: invoked; ...` content onto one physical line,
matching the other three `skill-verdict:` lines in the same block, which
were already correctly single-line.
