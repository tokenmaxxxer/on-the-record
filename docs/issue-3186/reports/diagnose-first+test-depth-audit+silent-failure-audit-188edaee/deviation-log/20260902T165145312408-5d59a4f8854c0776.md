# Deviation — solo verification instead of freelunch-worker delegation

actor: session (diagnose-first+test-depth-audit+silent-failure-audit-188edaee)
issue: 3186
when: 2026-09-03T01:27Z (approximate, this session's turn)

## What was deviated from

The mounted `[freelunch-directive absolute]` instructs that any turn
needing a repo/environment tool call is DELEGATED, always: dispatch one
background `freelunch:freelunch-worker` owning the whole unit, and the
conversation session itself makes no repo tool calls of its own.

## What was done instead

Did every step inline in this session — checking out PR #3193 into a
throwaway worktree, reading `consult.py`/`pipeline.py`/`spawn.py` directly,
scanning `runs/ledger.jsonl` across 42 files, running the full test suite,
and building/running synthetic off-corpus fixtures against
`measure_cross_family.py` — with no delegation to any background worker.

## Why

The task that spawned this session explicitly instructed "Do the work
yourself, no background worker" — a direct, on-topic override for this
specific verification unit, matching the same override PR #3196 (the
first independent verification of this same PR #3193) recorded for
itself. Adversarial/independent verification's whole point — re-derive
the evidence yourself rather than trust a prior report — is defeated by
handing the check to a worker whose raw, unverified output the freelunch
protocol says to accept without a review pass; and headless/single-shot
contract v3 s22 (cited by `freelunch-protocol.md` itself) forbids ending
a turn having delegated work not consumed in that same turn, which this
single non-partitionable investigation (each finding informs the next
step) cannot satisfy.

A second, smaller deviation in the same session: an earlier draft of the
record added a `## Acceptance verification` heading (to satisfy
`gates.py`'s CI-time `record_checked_claims` check) but then removed it
after confirming neither PR #3193's nor PR #3196's own merged records
carry that heading, and that the live write-time hook
(`record-claim-guard.sh`) does not require it — kept the record's
structure consistent with both prior precedent in this same issue
lineage rather than diverging on an untested assumption about a gate
that was never actually invoked against either prior record.
canonical: `grep -n "^## " ` run against both PR #3193's and PR #3196's
own records (fetched via `git show`), executed live in this session —
neither contains a `## Acceptance verification` heading
