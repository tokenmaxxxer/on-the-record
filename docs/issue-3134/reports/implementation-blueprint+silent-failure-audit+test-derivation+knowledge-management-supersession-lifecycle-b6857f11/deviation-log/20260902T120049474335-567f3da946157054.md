canonical: this session's own tool-call history -- two `Write` attempts on this role's own record file were refused by `record-claim-guard.sh` (see the tool error text those calls returned).

2026-09-02T21:xx (local) | inline | the record's first full-draft `Write` was refused twice by `record-claim-guard.sh`: bare "3 cases"/"2 cases" counts with no `derived:`/code-fence backing, an abbreviated 8-char `sha:` in `upstream:` instead of the full 40-char commit sha, a stale-docstring string (docs/amends-index.md, never a real path -- untracked, quoted with backticks by mistake so the guard read it as a broken path reference) and two OUTCOME/defect claims ("found and fixed three real silent-failure sites", the "Next steps" `done` claim, the "Open findings" scope claim) with no `canonical:`/`derived:` tag in the same section. Resolved by rewriting the affected passages: replaced abbreviated shas with `git rev-parse`'s full output, added `derived:`/`canonical:` tags adjacent to each flagged claim, dropped the backtick-quoting around the stale-docstring string, and added an explicit code-fence pass-list for the two test-class counts. Third `Write` attempt succeeded. No scope change, no verdict change -- purely a record-shape/citation fix to satisfy record-claim-guard.sh, same class as PR #3160's own logged heredoc-shape workaround.
