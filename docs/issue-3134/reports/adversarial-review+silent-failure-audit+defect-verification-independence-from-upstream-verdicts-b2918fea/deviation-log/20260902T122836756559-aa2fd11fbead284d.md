Expected the `gh pr merge --help` attack fixture (scratch bare remote
`bare-origin.git`) to reproduce a push on the first attempt; it instead
surfaced only a caught clone error, because the scratch remote's
`origin/HEAD` symref was never set (a fixture-construction gap in this
session's own rig, not a change of approach or scope). Fixed with `git
remote set-head origin trunk` before re-running the identical payload,
which then produced the push documented in the record's Finding 3
subsection. Also logged in the record's own "What did not work" section.
