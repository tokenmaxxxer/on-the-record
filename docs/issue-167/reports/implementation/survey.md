# Survey — current state for issue-167

## Write surfaces this issue touches
- `roles/*.json` — 9 files exist today (the round-5 canon's "original 9"), generic-loaded by `spawn.py:role_settings()` (spawn.py:330) and `gates/gates.py` via filename match; no code hardcodes role names beyond `spawn.py`'s `ROLES` display tuple (spawn.py:646-648, currently 9 names, asserted `len==9` at test_gates.py:216).
- `test_vocab_coherence_roles.py` globs `roles/*.json` generically (no hardcoded names) — confirmed no change needed there.
- `.claude-plugin/marketplace.json` — not present at repo root for the target 8 (only referenced per-rulebook-repo, one marketplace per role); this repo's own root has no marketplace.json (confirmed: `find` returned none at repo root — the 9-role marketplace.json read earlier came from a rulebook repo checkout, not this repo).
- Rulebook skeleton exemplar: local checkout `~/tokenmaxxxer/rulebooks/implementation-rulebook` (round-5-renamed from `coding-agent-rulebook`) shows the concrete shape one role's rulebook repo takes: `<role>/.claude-plugin/plugin.json`, `<role>/hooks/{directive.sh,record-fields-gate.sh,trailer-gate.sh,handbook-trigger-gate.sh,<role>-progress-gate.sh,hooks.json,state.sh}`, `<role>/agents/warrant-hunter.md`, root `README.md`, `docs/specs/approvers.md`. This is the pattern the issue's item 2 says to adapt (role name substituted, `record-fields` mapped to the new role's own `produces` required fields) — not clone verbatim.
- `docs/issue-160/proposals/role-taxonomy.md` — the merged canon this issue executes; table rows for the 8 target roles (`user-discovery`, `requirements-engineering`, `refactoring-legacy`, `test-authoring`, `observability`, `incident-response`, `capacity-planning`, `knowledge-management`) already carry `decides`/`use_when`/`produces`(required fields, informally)/`write_scope`/hand-off — verbatim source for `roles/*.json` content, no re-derivation needed.
- `docs/issue-162/proposals/role-rename-execution-1.md` — issue-162's own execution proposal for the *original 9* rename; this issue's own text explicitly invokes "이행 ①의 교훈" (the lesson from that prior split), which per that proposal's §2/§7 was: no silent alias for unknown role names (spawn.py already fails closed generically), and GitHub repo creation/push is outside a role-session's authority — must be a human command list, not an executed action.

## Unknowns / gaps found
- No repo-root `marketplace.json` in *this* repo — the 8 new rulebook repos each carry their own marketplace.json (one per repo, as the exemplar shows), so this issue's scope does not touch a shared marketplace file the way issue-162's rename touched `.claude-plugin/marketplace.json` in a rulebook repo. Confirmed by absence of the file at this repo's root and by the exemplar rulebook's own marketplace.json living inside that rulebook's repo, not here.
- `spawn.py`'s `ROLES` tuple (spawn.py:646-648) is explicitly the **board display order** for the lifecycle chain of the original 9 roles (`status()`/`board()` iterate it to find `reports/<role>.md` per subject) — it is not "all known roles." Whether the 8 new roles belong in that tuple is a live design question the issue text raises but does not settle ("spawn.py ROLES 갱신과 테스트 갱신 포함") — addressed in the proposal below.
- Rulebook repos for the 8 new roles do not exist yet on GitHub (unverified by network call in this session, per gh scope below) and cannot be created by this role — `gh repo create` requires org-level authority outside a role session's write-scope, per the issue's own instruction and issue-162's precedent.

## Skip record — web scout
This pass skips the `scout-directive`'s web sweep. Reason: the issue is a **mechanical execution of an already-approved canon** (issue-160's merged proposal fixes every `decides`/`use_when`/`produces`/`write_scope`/hand-off value verbatim) plus an **adaptation of an existing internal exemplar** (the implementation-rulebook skeleton, read directly from the local checkout) — neither is a product-facing decision an external category sweep would inform. The one open design question found (`ROLES` tuple membership) is answered from this repo's own code semantics, not from external practice. Per the scout-directive's two skip conditions ("스펙이 설계 결정을 전혀 열어두지 않을 때"는 아니지만, 남은 결정이 내부 코드 시맨틱스로 결정 가능하다는 점에서 실질적으로 동일) — this is recorded explicitly per the mandatory skip-record rule rather than silently omitted.
