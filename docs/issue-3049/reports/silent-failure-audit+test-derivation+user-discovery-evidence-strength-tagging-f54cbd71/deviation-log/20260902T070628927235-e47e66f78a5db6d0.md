issue: 3049
role: silent-failure-audit+test-derivation+user-discovery-evidence-strength-tagging-f54cbd71

The first `git commit` attempt for `gates/probe_cwd_shapes.py` was refused
by the live `gate-registration-guard.sh` PreToolUse hook (the subject of
this very issue) — canonical: the hook's own stderr, "gates/probe_cwd_shapes.py:
no row in docs/specs/enforcement-boundary.md". Not anticipated in the
initial plan (the deliverable was expected to be two new files only).
Added a row for `probe_cwd_shapes.py` to `docs/specs/enforcement-
boundary.md` in the same commit, per the guard's own required remedy.

Second, smaller deviation: this repo's own landing convention calls for
`python3 gates/spec_index.py --update` after any `docs/specs/*` edit.
Running it failed with `FileNotFoundError:
.../roles/specs/brand-design.spec.json` — canonical: `git stash -u &&
python3 gates/spec_index.py --update` reproduces the identical traceback
against `origin/main` before this session's own files were staged,
confirming a pre-existing stale-generator bug (`roles/specs/` was deleted
repo-wide by commit 480d1a78, #2542, while `docs/specs/reconciled-
index.md` still lists a path under it) unrelated to issue #3049. Did not
attempt to fix it — out of this issue's scope. `docs/specs/reconciled-
index.md` is left unregenerated for this delivery as a result.

Both deviations are also recorded in this session's own record under
"What did not work"
(docs/issue-3049/reports/silent-failure-audit+test-derivation+user-discovery-evidence-strength-tagging-f54cbd71.md).
