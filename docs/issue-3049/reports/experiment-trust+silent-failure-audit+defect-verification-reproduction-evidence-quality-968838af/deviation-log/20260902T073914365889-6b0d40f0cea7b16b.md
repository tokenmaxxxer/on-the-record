issue: 3049
role: experiment-trust+silent-failure-audit+defect-verification-reproduction-evidence-quality-968838af

Four gate-driven deviations from the initial write-once-then-land plan,
none anticipated going in:

1. `record-claim-guard.sh` refused the first `Write` of this session's own
   record twice — canonical: the hook's own stderr, listing bare-count
   claims without `derived:`/`canonical:` tags immediately adjacent, an
   OUTCOME-pattern match on the bare word "pass" in ordinary prose (e.g.
   "a confirming pass", "a second clean pass"), and backtick paths
   (`2bf34f46:gates/probe_cwd_shapes.py`,
   `2bf34f46:tests/test_cwd_shape_coverage.py` — untracked on this
   session's own branch, only present at that PR sha) that only exist on
   PR #3088's branch, not this session's own. Reworded the affected
   passages to carry inline `derived:`/`canonical:` tags, avoided the bare
   word "pass" outside quoted command output, and sha-pinned every
   PR-only path.
2. The first commit landed without the required `Subject: issue-3049`
   trailer — canonical: `trailer-gate.sh`'s post-commit stderr. Amended
   the still-unpushed commit to add the trailer rather than creating a
   throwaway follow-up commit, since nothing had been pushed yet.
3. `pr-preflight.sh` refused the first `gh pr create` attempt — canonical:
   its own stderr, "PR body's first paragraph is not real prose
   (trailer-only)". Added a genuine opening prose paragraph before the
   `## Summary` heading; the `Advances #3049` trailer itself was already
   present but not recognized until the leading-paragraph requirement was
   also satisfied.
4. The Stop hook's `skill-verdict-guard` flagged two `skill-verdict:` lines
   as missing content after the dash — canonical: the hook's own stderr —
   because this session had wrapped `applied:`/`not-applicable:` onto the
   line following the em dash instead of keeping it on the same line.
   Edited both lines so `applied:` (or `not-applicable:`) starts on the
   same line as the em dash.

All four are also visible in this session's own record under "What was
done" (the Write-rejection history is not separately narrated there) and
the commit history of this branch (one amend, one landing commit) —
recorded here per the deviation-loop obligation rather than folded
silently into the final artifacts.
