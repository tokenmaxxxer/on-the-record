# issue-2293 deviation log (implementation)

2026-08-25T10:50:00Z filed reported-not-spawned implementation — pre-existing unrelated test failure discovered while running the acceptance test suites for this issue: `tests/test_spawn_gate_wiring.py` test `Ledger.test_toolchain_cache_env_redirected_into_workspace` fails on a clean, unmodified `HEAD` too (reproduced via `git stash` + rerun, same `CARGO_HOME` path-mismatch assertion). Outside this issue's frozen scope (admission-time degenerate-task refusal + watchdog adhoc-visibility) — not fixed here. Per the role-session deviation-loop variant (docs/handbooks/deviation-loop.md, "Role sessions" section), reported in the implementation record's Provenance section rather than filed as a new issue or spawned as a peer role mid-task.
