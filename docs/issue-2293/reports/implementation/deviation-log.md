# issue-2293 deviation log (implementation)

2026-08-25T10:50:00Z filed reported-not-spawned implementation — pre-existing unrelated test failure discovered while running the acceptance test suites for this issue: `tests/test_spawn_gate_wiring.py` test `Ledger.test_toolchain_cache_env_redirected_into_workspace` fails on a clean, unmodified `HEAD` too (reproduced via `git stash` + rerun, same `CARGO_HOME` path-mismatch assertion). Outside this issue's frozen scope (admission-time degenerate-task refusal + watchdog adhoc-visibility) — not fixed here. Per the role-session deviation-loop variant (docs/handbooks/deviation-loop.md, "Role sessions" section), reported in the implementation record's Provenance section rather than filed as a new issue or spawned as a peer role mid-task.
2026-08-25T12:41:00Z filed reported-not-spawned implementation — CHANGES-round task (PR #2306) was framed as "your change broke role-model flag composition in spawn_cmd"; reproduction showed the failing tests actually exercise a pre-existing pytest-xdist race + test-isolation gap on the shared `role_model.txt` file, present identically on `main` (verified via a scratch `git worktree`), with zero touch of `spawn_cmd`/`ROLE_MODEL_CONFIG`/`resolved_role_model` in this issue's own diff (`git diff main...HEAD -- spawn.py`). Fixed the actual root cause (test isolation, `tests/test_spawn_pipeline.py` + `pytest.ini`) rather than the diagnosis given in the task, since the stated diagnosis did not match the reproduced evidence. Documented in the implementation record's new "CHANGES round" section rather than filed as a separate issue, per the same role-session deviation-loop variant as the entry above.
