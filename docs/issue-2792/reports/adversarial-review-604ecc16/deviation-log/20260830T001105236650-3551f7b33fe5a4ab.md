Wrote `/tmp/verify-2805/repro.py`'s first version mocking the whole
`spawn_on_pr.state_paths` module object via
`mock.patch.object(spawn_on_pr, "state_paths")` to redirect state to a
tmp dir; this broke `load_merged_seen()`'s separate
`state_paths.orchestrator_state_path()` call
(`TypeError: the JSON object must be str, bytes or bytearray, not
MagicMock`). Replaced with `mock.patch.object(spawn_on_pr.state_paths,
"STATE_ROOT", tmp_path / "state")` — patching only the attribute the
PR's own test (`gates/test_spawn_on_pr.py`) patches — which resolved
it. No further retries needed; also logged in the main record's "What
did not work" section.
