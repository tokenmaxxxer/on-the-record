# Deviation log — issue-2285 (implementation role)

- 2026-08-25T04:43:31Z | inline | the authoritative proposal's own line citations (`consult.py:470-471,484`, `skills.py:333-355`) do not resolve against the current tree — canonical: `awk 'NR>=465 && NR<=490' consult.py` and `awk 'NR>=328 && NR<=358' skills.py`, this session, show those ranges landing inside an unrelated function signature/docstring, not `resolve_role_source()` or its call sites. Ordinary line drift from unrelated intervening commits since the survey was written, not a reverted rulebook path (confirmed no forbidden identifier reappeared). Stays inside the frozen write set (`docs/specs/consult-guidance-source.md` is a listed path), mechanical (citation correction only, no design/architecture/security/product judgment), and does not change what the deliverable claims (unconditional skill-repo resolution, no allowlist branch) — classified INLINE-FIX per the deviation loop's own criteria. Resolved by citing the actual current locations (`skills.py:354-375` for `resolve_role_source()`, `consult.py:636,910,1303` for its three call sites) in the spec doc instead of copying the proposal's stale numbers verbatim; recorded in `docs/issue-2285/reports/implementation.md`'s Deviations section. Diff location: `docs/specs/consult-guidance-source.md` (commit `0baac601`).
- 2026-08-25T05:19:00Z | inline | CHANGES-loop task (PR #2344 review R5) instructed validating the corrected spec doc against "the landed #2331 citation checks (gates/record_lint.py citation_line_*)", but `git merge-base --is-ancestor d62e865e origin/main`/`HEAD` this session showed issue #2331's citation-check commit is not actually merged into `main` or this branch — it exists only on `origin/issue-2331/implementation`. Stays inside the frozen scope (validation only, no write-set change), mechanical (fetching a read-only reference copy of one file to run a check, not adopting the branch), and does not change what the fix claims — classified INLINE-FIX. Resolved by fetching `gates/record_lint.py`/`gates/gates.py` from `origin/issue-2331/implementation` into `/tmp` (never merged or written into this branch's tree) and running `citation_line_bounds_check`/`citation_line_content_check` from there against `docs/specs/consult-guidance-source.md`; also reworded three spec-doc bullets (quoted code moved immediately after each citation) so the checker's nearest-backtick-quote heuristic matches the cited line instead of a nearby function-name mention — same eight line numbers, no change to what is claimed. Recorded in `docs/issue-2285/reports/implementation.md`'s Deviations section and Acceptance evidence. Diff location: `docs/specs/consult-guidance-source.md`, `docs/issue-2285/reports/implementation.md` (commit `fbf16357`).
