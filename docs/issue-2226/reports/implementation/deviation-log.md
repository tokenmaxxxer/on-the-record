# Deviation log — issue-2226 / implementation

- 2026-08-25T00:00:00Z | inline | first fix attempt (evict `sys.modules["gates"]` before the bare `import gates` in each of `gates/record_lint.py`/`claims.py`/`risk_report.py`/`ci.py`) repaired each file's own `-m gates.<X>` invocation but left `sys.modules["gates"]` pointed at a flat module with no `__path__`, breaking a later `-m gates.<other>` resolution in the same interpreter — surfaced by a before-landing warrant-hunter dispatch, not part of the issue's own reproduction steps. Replaced with an `importlib`-by-path load under a private cache key in the same four files, same commit, before opening the PR; see docs/issue-2226/reports/implementation.md, "What did not work".
- 2026-08-25T01:00:00Z | CHANGES-loop | a conformance review (relayed via the dispatching prompt for this round, no docs/issue-2226/reports/conformance-review.md file on this branch — see canonical note in docs/issue-2226/reports/implementation.md, "CHANGES round") named three more `gates/*.py` sites with the same collision the original `^import gates$` exact-line-anchored audit grep could not match: `gates/ui_evidence_gate.py:82` (indented, function-local), `gates/roles_due.py:32` (aliased `as _gates`), `gates/skip_eligibility.py:28` (trailing `# noqa: E402` comment). Applied the identical fix shape to all three, same private cache key. A second before-landing warrant-hunter dispatch on this round's diff surfaced a pre-existing (not newly introduced) mutable-global-state smell in `gates/roles_due.py:204`'s `_gates.BASE = base`, judged out of this issue's scope and left open — see docs/issue-2226/reports/implementation.md, "Open findings".
