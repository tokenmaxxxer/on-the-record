# Deviation log — issue-2226 / conformance-review

- 2026-08-25T00:29:56Z | inline | mid-review, ran `git checkout pr-2243-review -- .` intending to read a few PR-head files, but with no path scope and no preceding `git status` check — it silently overwrote this session's own working tree with PR #2243's entire file set (`events.py`, `watchdog.py`, `spawn.py`, `tests/test_watchdog_local_signals.py`, `gates/*.py`, etc.), outside the conformance-review record's own frozen write set. Caught immediately via `git status --short`; reverted with `git reset --hard HEAD` before anything was inspected or acted on from that state (this session's own untracked files, `docs/issue-2226/` and `.on-the-record/directive/`, survived the reset since untracked paths are unaffected by `--hard`). All subsequent PR-head reads used `git show <sha>:<path>` / `git diff main..pr-2243-review` (read-only) or a disposable `git worktree`, never another in-place checkout of this session's own tree; see `docs/issue-2226/reports/conformance-review.md`, "What did not work", for the full account.
