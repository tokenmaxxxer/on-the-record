Expected: threading `wrapper_pid` into `session_end_verdict()` alone would be sufficient to fix `diagnose_health()`'s dead-branch completion check. Actual: `test_diagnose_health_completion_without_a_pr_via_wrapper_pid` failed (`'DEAD-ERRORED' is not None`) because that function's completion condition only recognized `verdict == "normal"`, not the newly-possible `verdict == "in-progress"`. Fixed by widening the condition to `verdict in ("normal", "in-progress")` — documented in the delivery record's "Why" section with the failing-test evidence.
