# Deviation log — issue-2204 / implementation

- 2026-08-24T00:00:00Z | filed | tokenmaxxxer-core's directive.sh SessionStart hook (a separate git repository) still tells every spawned session to Read session-protocol.md before its first task action — the larger of the two Read-round-trip sources issue #2204 measured, and outside this repo's frozen write set (no commit access to that repository; same boundary docs/handbooks/spawn-directive-assembly.md already drew for the sibling CORE_BUILD_NOW mechanism). Not inline-fixable: an edit outside the frozen write set, and a systemic gap (affects every spawn, not a one-off). Needs a companion issue against tokenmaxxxer-core to move directive.sh's output from a Read pointer to the SessionStart-hook additionalContext channel issue #2204 itself names; already surfaced in docs/issue-2204/reports/implementation.md's Open findings; reported, not spawned.
