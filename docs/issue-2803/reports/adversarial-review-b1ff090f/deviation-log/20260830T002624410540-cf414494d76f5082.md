- Expected: the record file's first `Write` to `docs/issue-2803/reports/adversarial-review-b1ff090f.md` would pass `record-claim-guard.sh` on the first attempt. Actual: rejected across two drafts — status/defect-shaped prose sentences ("Confirmed.", "the mechanism is exercised only...") with no `canonical:`/`derived:` tag in the same section, OUTCOME-shaped sentences ("41 passed", "15 failed, 425 passed") stated as prose without an execution-live tag in the same section, a bare path reference (`docs/issue-2803/reports/technical-writing-style-guide-compliance-632a1d33.md`, untracked on this branch — it only exists on PR #2808's branch, not this one's tracked history) that the gate's orphaned-path check rejected, and an un-derived count claim ("watchdog.py, roster.py, lifecycle.py ... absent"). Fixed by adding `acceptance: <command> — result:` code fences immediately after every outcome claim, adding `derived:`/`canonical:` tags near every remaining status/count sentence, and switching every reference to the PR's own record file to the commit-pinned form `b5dae50d948e2afa9a4a80c5b0d4f44afaf10da9:docs/issue-2803/reports/technical-writing-style-guide-compliance-632a1d33.md` (untracked on this branch) — no claims were weakened, only citation placement/formatting changed.
