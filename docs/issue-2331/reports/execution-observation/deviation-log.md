# Deviation log — issue-2331 (execution-observation role)

- 2026-08-25T00:00:00Z | filed | `git worktree add /tmp/pr2351-review origin/issue-2331/implementation` (used to independently re-execute PR #2351's tests against its head commit, outside this role's frozen write set of `docs/issue-2331/reports/execution-observation.md`) registered corrupted metadata: the new worktree's `.git` file resolved to `gitdir: /home/jwjung/.tokenmaxxxer/work/on-the-record-issue-2331-conformance-review/.git/worktrees/pr2351-review` — a *different* sibling session's checkout, not this session's own `on-the-record-issue-2331-execution-observation` repo — even though `commondir`/`gitdir` under this repo's own `.git/worktrees/pr2351-review/` pointed correctly back at `/tmp/pr2351-review/.git`. canonical: `cat /tmp/pr2351-review/.git`, this session — result: `gitdir: /home/jwjung/.tokenmaxxxer/work/on-the-record-issue-2331-conformance-review/.git/worktrees/pr2351-review`. `git worktree remove --force /tmp/pr2351-review` then refused with `fatal: 유효성 검사에 실패하여, ... '/tmp/pr2351-review'은(는) '.git/worktrees/pr2351-review'을(를) 가리키지 않습니다` (validate_worktree() mismatch) — a risk that could recur for any other role session in this shared-filesystem environment that runs `git worktree add` from a directory name resembling a sibling session's own working directory. Resolved this turn by `rm -rf /tmp/pr2351-review` plus `rm -rf .git/worktrees/pr2351-review` (this repo's own metadata only) and `git worktree prune`, confirmed clean via `git worktree list`; no sibling session's files were touched (only this repo's own now-orphaned metadata pointer was removed). Self-diagnosing/filing an environment-level issue about cross-session worktree-metadata corruption is outside this role session's own scope; reported here and in this session's reply, not spawned.
