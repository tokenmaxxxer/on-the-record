Expected the record draft to pass `record-claim-guard.sh` on first Write. It did not: needed 4 rewrite attempts before the write succeeded, because bare count claims ("249 passed", "254 passed", "27 tests, 39 asserts") lacked adjacent `derived:`/`canonical:` tags or code fences, two cross-references to files that only exist on PR #3126's branch (not this session's own branch) were flagged as dead path references, and two prose paragraphs describing skill-methodology outcomes were flagged as unsupported OUTCOME claims needing an execution-live citation in the same section. Fixed by wrapping actual command output in code fences, sha-pinning or adding "untracked on this session's own branch" notes next to cross-branch paths, and adding a `derived: <command> → <result>` line directly inside each flagged paragraph. No content claim changed as a result — only citation shape.
