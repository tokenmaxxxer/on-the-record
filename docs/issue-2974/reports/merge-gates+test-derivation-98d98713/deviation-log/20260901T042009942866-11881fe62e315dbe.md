- 2026-09-01T04:20:09Z, inline, acceptance item A's `population:` note named 6 issues (#2774/#2864/#2872/#2883/#2890/#2956) as context for the empty-canon problem; canonical: `gh issue view <n> --json body` output for #2705/#2139/#2941 (their 3 common parents, read in full this session) — all three legitimately carry `infrastructure/no-direct-requirement`, so minting R-IDs from them would have been fabrication against `requirements.md`'s own "operator's own words, unedited" constraint — substituted two different, non-infra-tagged, already-enforced-check issues (#1664, #511) as R005/R006 sources instead. docs/specs/requirements.md, docs/issue-2974/reports/merge-gates+test-derivation-98d98713.md ("Why" section).
- 2026-09-01T04:20:09Z, inline, acceptance item A's literal check `grep -c "^R[0-9]" docs/specs/requirement-digest.md` returns 0 regardless of R-ID count because every digest line is bullet-prefixed (`- R001: ...`), a format `watchdog.py`'s `_DIGEST_LIVE_ENTRY_RE` depends on for real advisory parsing — derived: `grep -c "^R[0-9]" docs/specs/requirement-digest.md` result `0`, `grep -c "R[0-9]" docs/specs/requirement-digest.md` result `6`. Did not change the render format to satisfy the bare anchor (would have broken that consumer for a cosmetic match); documented as `unverifiable:` in the record instead. docs/issue-2974/reports/merge-gates+test-derivation-98d98713.md (Verification section).
