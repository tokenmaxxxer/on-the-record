# issue-2213 — performance-engineering deviation log

- 2026-08-25T00:10:56Z | inline | Instrumenting `_skill_judge_consult()` added a second `ledger_write()` call per spawn, which broke `tests/test_spawn_gate_wiring.py`'s `Ledger::test_entry_carries_the_live_log_path` test (it asserted exactly one call); fixed by filtering to the spawn-summary entry before the assertion. Diff: `tests/test_spawn_gate_wiring.py` (this branch's commit `01e01501`).
- 2026-08-25T00:10:56Z | inline | Running the live measurement harness called `_skill_judge_consult()` against this repo's real checkout (not an isolated clone), and its existing `_commit_consult_trace()` auto-commit behavior committed 18 small `docs/issue-2213/reports/consult-log.md` trace-append commits onto this branch as a side effect. Judgment call: left them in branch history rather than squashing/rewriting — they are legitimate, in-scope (`docs/issue-2213/reports/` is this role's own write area), and squashing unpushed-but-already-relied-on history risked more than the noise it would remove. Diff: none (informational; see commits between `01e01501`'s parent and `2f888a24` on `issue-2213/performance-engineering`).
