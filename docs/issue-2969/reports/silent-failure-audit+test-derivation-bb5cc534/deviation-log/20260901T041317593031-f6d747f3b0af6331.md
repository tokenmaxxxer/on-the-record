2026-09-01T04:13Z inline — first implementation of `watchdog._record_verdict_and_check_flapping()`'s "short window" check measured the gap from the *first* observation of a state to its return (`t3 - t1`, where the 3-entry history is `[(s1,t1), (s2,t2), (s3,t3)]`). Expected this to satisfy "a verdict that reverses within a short window is reported as its own signal" directly. Instead derived: `python3 -m pytest tests/test_flapping_verdict.py -q`, run this session against that version — result: `1 failed, 6 passed` (`test_flapping_verdict_reversal_within_window_flags` failed: a reversal timed to land just inside `FLAPPING_WINDOW_SEC` of the state's *return* still measured as outside the window when timed from its *first sighting*, because `t1` could be arbitrarily far in the past for a state the session had been in for a while before it flapped). Corrected the window check to `t3 - t2` (from when the verdict left the first state to when it returned to it) — this is the round-trip duration of the reversal itself, independent of how long the session had been healthy before flapping — derived: same `python3 -m pytest tests/test_flapping_verdict.py -q` command, re-run this session after the fix — result: `7 passed`. Also documented in `docs/issue-2969/reports/silent-failure-audit+test-derivation-bb5cc534.md`, "What did not work" section.
