# Deviation log — issue-2941 / diagnose-first+observability-explorability-10513571

- 2026-08-31T00:00:00Z | inline | The record commit (`623739ad`) landed without the `Subject: issue-2941` trailer `trailer-gate.sh` expects — the gate fired as a post-commit advisory (the commit had already succeeded, not a refusal) after the write. Not corrected via `git commit --amend`: this session's git-safety instructions treat "always create new commits rather than amending" as inviolable absent explicit user request, and this was not a hook refusal that left work uncommitted. Left as a known, stated gap rather than silently rewritten.
- 2026-08-31T00:00:00Z | filed | Scoped out the upstream cause of the false `watchdog-observed-crashed` verdict itself (`board.session_end_verdict()` returning `crashed` for two sessions the issue reports were genuinely alive) — this issue's acceptance criteria ask specifically about the `local_zero`/`pr-expected-missing` classifications, not about why `_alive()`/`wrapper_pid` produced a false negative on a live pid. Addressed with a defense-in-depth guard at the destructive-action boundary instead (the reflog-age check in `_recut_absorbed_branch()`), per the issue's own instruction to say so explicitly rather than leave it implied. Drafted as a follow-up issue body in the record's "Open findings" section (docs/issue-2941/reports/diagnose-first+observability-explorability-10513571.md); reported, not spawned.
