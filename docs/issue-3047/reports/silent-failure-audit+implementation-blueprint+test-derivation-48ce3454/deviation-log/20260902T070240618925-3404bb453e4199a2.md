- 2026-09-02T07:02:41Z inline: first draft of `_classify_narrowing_prs` handled a not-supplied `pr_index` with `pr_index = pr_index or {}`, letting the default reach `_classify_mapping_loss_cause` and come back a confident `no-record-yet` — collapsing "never checked" into "checked and found nothing," the same defect shape issue #3047 itself reports, reproduced at the width of one optional parameter. Caught during the silent-failure-audit skill pass, before any commit — replaced with an explicit `pr_index is not None` branch that routes the not-supplied case to `unclassified` instead. canonical: `1ab1b0804af47d6ea5a6cb5a62a1fd5414134d0d:watchdog.py:947-949` (the corrected form; the flawed draft was never committed separately, so there is no earlier sha). Documented in the record's `## Why` section. Location: `docs/issue-3047/reports/silent-failure-audit+implementation-blueprint+test-derivation-48ce3454.md`.
