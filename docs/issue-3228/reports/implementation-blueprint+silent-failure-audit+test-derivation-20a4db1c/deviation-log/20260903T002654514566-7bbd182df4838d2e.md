Deviation: merged an existing branch's work instead of authoring fresh
commits on this session's own branch.

canonical: `git log --oneline pr3233-work ^main` (run this session) —
result showed a local branch `pr3233-work` at head `cba54d1b`, with
commits already addressing PR #3237's review of PR #3233 (round 1):
wiring guard, advisory gate, null-byte/permission-denied reliability
fixes, site7 fixture correction.

canonical: `gh pr view 3233 --json headRefOid` (run this session) —
result: `cba54d1b1de232369b619f381575047cce226bd0`, confirming that
same commit is PR #3233's live head on origin, not orphaned local
work.

Why: on starting, `docs/issue-3228/reports/implementation-blueprint+silent-failure-audit+test-derivation-20a4db1c.md`
already existed on disk fully written (not a blank skeleton), matching
the content of the commits on `pr3233-work`. derived: `git diff --stat main...pr3233-work`
(run this session) — result: 27 files changed, all additions, no
deletions or modifications to any file also touched on `main`, so no
merge conflict was possible. Re-deriving the same fixes from scratch
would have duplicated already-existing, real work for no benefit;
instead I ran `git merge pr3233-work` onto this session's branch and
independently re-ran every acceptance check and adversarial probe
myself before delivering (canonical: this session's own record under
"What was done" and the PR #3246 body, both listing the independently
re-run commands and their results), rather than trusting the prior
commits' claims unverified.

Effect: the delivered commits on PR #3246 (canonical: `gh pr view 3246
--json url` this session — https://github.com/tokenmaxxxer/on-the-record/pull/3246)
are a merge, not freshly-authored history in this session — `git
blame` on `scripts/lint/silent_failure.py` attributes the code to the
earlier `pr3233-work` commits, not to a new commit made in this
session. The record's own "What was done" section already states this
plainly ("Round 2 on PR #3233's branch"), matching the actual history
this deviation entry documents.
