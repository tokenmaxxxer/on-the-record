20260902T232356Z inline SF003 false-positive on `_repo_owner_repo`/`_pr_merge_commit`-shaped helpers in `scripts/issue-3127/verify_preregistration.py` (real fail-closed pattern, not the site-3 bug) -- added the same `# silent-failure: allow <reason>` escape hatch SF001/SF002 already had to SF003 (`scripts/lint/silent_failure.py:325-328`) and scoped the regression-guard test to the two named functions instead of the whole file (`tests/test_issue_3228_silent_failure_lint.py:145-168`). Stayed inside the frozen write set (scripts/lint/, tests/); one-off, not a systemic pattern change.

checked: `python3 -m pytest tests/test_issue_3228_silent_failure_lint.py -q` — result:
```
11 passed in 0.84s
```
