# Deviation — issue-2525/conformance-review

canonical: `gh pr create --repo tokenmaxxxer/on-the-record --base main
--head issue-2525/conformance-review ...` (this session) — result:
refused by `pr-preflight.sh`'s amendments-reconciled check.

**What deviated:** the phase-1 PR could not be opened as planned.
`pr-preflight.sh` requires `docs/issue-2525/reports/conformance-review.md`
(this role's record file) to carry an `amendments-reconciled:
issuecomment-5421173580` line before `gh pr create` is allowed, because
issue #5421173580 (posted by `JiwonJung94`) landed on issue #2525 after
this session started. That exact file is phase-2 execution surface per
`approval-gate.sh` (contract v3 s19) — confirmed by two denied Edit
attempts this session, both citing "neither the PR ... nor issue #2525
carries an approval from a listed human approver." No Approve exists for
`issue-2525/conformance-review`; no `CORE_BUILD_NOW`/`CORE_CHECKPOINT`
stamp is set (`env | grep CORE_`, no hits, checked this session). The
two gates' requirements are mutually exclusive for this exact sequence
(a post-spawn human comment arriving before any phase-2 content exists),
and neither is bypassable from within this role's authority.

**Resolution attempted:** none available within this session's
authorization. The reconciliation content itself is not blocked — it is
already written, twice: as an `amendments-reconciled` line drafted for
(and rejected from) the record file, and substantively in this survey's
own findings, which independently corroborate the same comment's claims
(same sha, same 232-file/56,524-line count, same three undeleted
guards).

**What was NOT deviated:** the investigation itself. All of R1a-R1f/R2/R3
in `docs/issue-2525/reports/conformance-review/survey.md` were checked
in full against sha `9f0239d1`, per the original phase-1 plan.

**State left behind:** `issue-2525/conformance-review` branch committed
(`531acfde`, "issue-2525: conformance-review phase-1 survey +
proposal") and pushed to `origin`. No PR open. `docs/issue-2525/reports/conformance-review.md`
remains the untouched pre-written skeleton — no content added, per
`approval-gate.sh`'s refusal.

**Resolution path for the next session/human:** either (a) a human
posts `APPROVE issue-2525/conformance-review` on the issue before any
phase-2 record content exists (unusual but not disallowed by
`approval-gate.sh`'s own logic — the Approve check does not require the
record to already exist), which would let a next session write both the
amendments-reconciled line and the phase-2 verdict in the same pass; or
(b) a human opens the phase-1 PR manually outside this session's gated
tool path, since the branch and commit are already pushed and ready.
