2026-08-26T02:30:00Z filed (reported, not spawned — role session) implementation — before-landing warrant-hunter found the `hooks/`-subdirectory guard shared by `resolved_skill_sources()`/`resolve_role_source()`/`resolve_skill_source()` (skills.py) is bypassable via a plugin.json `"hooks"` redirect, pre-existing and out of #2488's frozen scope; role session cannot file an issue (contract v3 s9, `gh issue create` refused this turn by gh-guard), so reported in docs/issue-2488/reports/implementation.md ("Open findings") and the PR #2497 description for the user to file.
