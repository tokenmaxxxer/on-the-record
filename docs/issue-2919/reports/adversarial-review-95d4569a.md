---
issue: 2919
role: adversarial-review-95d4569a
author: adversarial-review-95d4569a
skills: adversarial-review (skill-repository(c05de12))
verifies_subject: true
loop_state: landed
upstream:
  - path: PR #2935 (github.com/tokenmaxxxer/on-the-record/pull/2935), commits e2441a9b (fix) + 2585022d (deviation-log + product-priorities capture)
    sha: 2585022dc07e810730dcdaa8eeeaba3017c17950
  - path: docs/issue-2919/reports/adversarial-review-a4f05242.md (the prior review that found the defect this PR fixes)
    sha: 00143ce89d484521002993d0d08fe0428b3be607
---

# issue-2919 — adversarial-review-95d4569a record

skill-verdict: adversarial-review — applied: invoked; loaded via Skill tool before starting verification work. canonical: this session's own tool-call transcript (Skill invocation of `adversarial-review`, this session). Used as the standing frame (re-derive from primary sources, do not read the PR's own record as ground truth, evaluate rather than fix) for every attack point below.
skill-verdict: defect-verification-independence-from-upstream-verdicts — applied: invoked; loaded via Skill tool before devising the fan-out attack points. canonical: this session's own tool-call transcript (Skill invocation, this session). Used for rule 4 (a clean scenario 1/2 result did not stop the search for other candidate attempts on the same mutex — see point 1's forming-boundary construction below) and rule 1 (the PR's own "byte-identical" and "~3.4s recovery" claims were re-derived, not cited).
skill-verdict: test-depth-audit — applied: invoked; loaded via Skill tool before classifying the 3 new mutex tests. canonical: this session's own tool-call transcript (Skill invocation, this session). Used to classify `t_alive_stamp_mutex_recovers_crashed_holder_issue_2919` via the audit's Step-4-style confirmation experiment (remove the harness's reaping thread, re-run, see whether the pass survives — point 2 below).
other mounted skills: not triggered (work-in-english's guidance was followed without a separate Skill invocation since it's a standing session-wide language policy, not a discrete procedure to apply at one point; verify-finding-record doesn't fit — this is an adversarial-review verification record, not a defect-verification outcome recording).

## What was done

canonical: this session's own tool-call transcript (worktree setup, `gh pr view 2935`/`gh pr diff 2935`, 4 background-worker Agent calls, Docker container runs) plus the 4 workers' own reported commands and raw output, this session. Independently re-derived every claim in PR #2935 against primary sources: `git worktree add /tmp/wt-prhead origin/issue-2919/silent-failure-audit+refactoring-legacy-seam-selection-9f67d71e` (resolved sha `2585022dc07e810730dcdaa8eeeaba3017c17950`), `git worktree add /tmp/wt-main origin/main` (resolved sha `46779718d2435a8749d7c736e86d3f54a5dc30d5`), and a real `bash:3.2` Docker container with `flock` excluded from a restricted PATH. Per the freelunch protocol's research-fan-out exception, 4 independent background workers ran the tool-heavy attack points against this frozen shared contract (the two worktree paths + container recipe); the 5th attack point (who reaps a crashed Monitor child) was resolved inline via repo/platform-doc research before dispatching them, since it needed no live execution. This record is my own synthesis connecting all 5 to each other, written after reading each worker's raw output — not a pass-through of any one worker's text, and not sourced from the PR's own record (`silent-failure-audit+refactoring-legacy-seam-selection-9f67d71e.md`, read only for cross-check after independently reaching the findings below).

**1. Core mutual-exclusion property — holds for the reported defect, but does not close the underlying bug class (highest-severity finding).** A worker extracted `_alive_stamp_lock_owner_status` and `_alive_stamp_write` verbatim from `/tmp/wt-prhead/on-the-record/monitors/poll-heartbeat.sh` and ran them as real forked OS processes inside the bash-3.2/no-flock container. derived: `command -v flock; echo $?` inside the container's restricted PATH → rc=1 (not found), and `flock` invoked directly → `command not found`, rc=127 — flock's absence confirmed live before trusting any other result, this session (worker output, reproduced by the worker verbatim in its report).

- Slow-but-alive 25s holder vs. a challenger starting 0.3s later: derived: worker's timestamped log, this session — A holds `8299013.69`→`8299038.69` (25.0s), B's ENTER `8299039.05` is strictly after A's EXIT `8299038.69` — zero overlap. **Holds.**
- Genuinely crashed holder (`kill -9 $$` mid-hold): derived: worker's timestamped log, this session — A self-kills at `8299045.36`, B re-enters at `8299046.67` → recovery ≈1.31s (not the PR body's claimed "~3.4s"; that figure applies to the code's `forming`-branch grace constant, not the `dead`-branch path, which reclaims on a challenger's very first status check per the same log). **Holds**, with a documentation-precision note, not a defect.
- **The boundary itself: a live holder that pauses >~3s between its successful `mkdir` and its own `owner.pid` write IS misclassified as abandoned, evicted, and never notices.** derived: worker constructed a holder that sleeps 5s between acquiring the lockdir and writing `owner.pid`; timestamped log (this session, worker output) shows the challenger reclaims (`rmdir`) at the 3s mark (`8299182.56`, "no owner pid recorded after 3s wait, reclaimed") while the original holder is still alive and resumes only at `8299185.25`, when its own `owner.pid` write then fails silently (`.../owner.pid: No such file or directory`, captured in the worker's raw stderr) and it proceeds directly into the stamp-file write with no lock held. Reading `on-the-record/monitors/poll-heartbeat.sh` lines 213-253 (`_alive_stamp_write`, no-flock branch, this session, Read tool) confirms the structural cause: the `while ! mkdir "${_lockdir}" 2>/dev/null; do ... done` loop is only entered on contention; a holder whose own first `mkdir` succeeds immediately skips the loop body entirely, and nothing after `done` re-verifies lockdir ownership before the `printf '%s' "$$" >"${_owner_pid_file}"` / stamp-write / `rmdir` sequence. This reproduces the identical unprotected-write failure shape the prior adversarial review found in PR #2923 (canonical: `docs/issue-2919/reports/adversarial-review-a4f05242.md`, point 10, "B entered and exited its critical section entirely inside A's still-open window"), just under a narrower trigger window. This is not confined to a contrived pathological host: derived: the same worker's unmodified 8-concurrent-normal-speed-writer regression run (no injected delay), 6 repeated runs, this session — the identical false-positive "no owner pid recorded after 3s wait, reclaimed" mechanism fired in 5 of the 6 runs (worker's own count, quoted verbatim in its report), without tipping into an observed double-write only because the loop's fixed 1s `sleep` happened to give the true holder enough headroom each time. **Misclassified.** derived: `sed -n '200,211p' on-the-record/monitors/poll-heartbeat.sh` (this session, Read tool) — the PR's own code comment already discloses "a residual race remains ... not zero" for this window, but frames it only as narrower, not as reproducing the identical full unprotected-write failure mode under ordinary (not just pathological) contention.
- PID-reuse window: derived: `_alive_stamp_lock_owner_status` body, `on-the-record/monitors/poll-heartbeat.sh` lines 170-183 (this session, Read tool) — one `[ -z ]` test plus a bash builtin `kill -0` call between reading `owner.pid` and using it, no fork/exec in between. **Not practically exploitable.**
- Pre-fix baseline: derived: worker's timestamped log against `/tmp/wt-main` (sha `46779718...`), this session — B's interval `[236.18, 236.18]` nested entirely inside A's still-open `[235.88, 260.88]`, unserialized, `flock: command not found` in stderr with no error check. Confirms `origin/main`'s pre-#2923 code genuinely races when flock is absent, matching the PR's stated rationale for existing.

**2. Zombie/crash-recovery liveness check — the fix's own regression test is fixture-dependent, and this repo cannot establish whether production shares the fixture's assumption (second finding).** canonical: `git show 2585022d:docs/issue-2919/reports/silent-failure-audit+refactoring-legacy-seam-selection-9f67d71e/deviation-log/20260831T053219317192-92b4e85a04f87337.md` (untracked on this branch — lives only on the PR branch, same commit as the fix), read this session — discloses that `t_alive_stamp_mutex_recovers_crashed_holder_issue_2919`'s first version hung because its self-killed worker A became an unreaped zombie `kill -0` kept reporting as alive, and states this is "a real, generic limitation of PID-based liveness checks the fixed production code also relies on." Per defect-verification-independence rule 1, I took that as a claim to test, not a fact to accept. derived: a worker's throwaway copy of the test with the harness's `threading.Thread(target=proc_a.wait, daemon=True).start()` reaping line removed changed the result from `RESULT: PASS in 2.43s` to `RESULT: ERROR (TimeoutExpired) in 15.32s` (worker's own experiment output, this session) — the test's pass is contingent on its own harness reaping worker A promptly, not solely on the production `kill -0` check identifying a *fully reaped* dead PID.

The record's own reasoning ("in production, poll-heartbeat.sh instances are peers ... a crashed peer's zombie lifetime is governed by whatever process actually spawned it, typically reaped promptly") is an assertion about an external process, not a derived fact — checked directly this session: canonical: `$ON_THE_RECORD/docs/specs/platform-capabilities.md` lines 26-53 and `$ON_THE_RECORD/docs/handbooks/monitor-liveness.md`, read via Read/Bash tools this session — `poll-heartbeat.sh` runs as a Claude Code plugin Monitor, documented as "session-bound," auto-started, with "no plugin API for OS-level scheduling," and explicitly: "this repo cannot arm the initial auto-started plugin Monitor with a `persistent` flag ... the auto-start itself is a platform capability documented as unmechanizable from repo code." Who reaps a crashed `poll-heartbeat.sh` instance, and how quickly, is not established anywhere in this repo — genuinely unknowable from primary sources available to this review, the same category of boundary `platform-capabilities.md` already documents for other Monitor properties. If a real crashed writer's parent process does not promptly reap it — plausible but unverified, since the PR's own regression test had to manufacture prompt reaping to pass within its 15s bound — `kill -0` would report the zombie as alive indefinitely, `_alive_stamp_lock_owner_status` would never return `dead`, and a contending writer would wait forever, contradicting the PR's claim that "a genuinely crashed holder's lock still recovers without deadlocking the tick."

**3. Test counts and depth — count claim confirmed; two of three new tests are Genuine Assertion, one is Mock-Dominated.** derived: `git diff acd74b3b HEAD -- on-the-record/monitors/test_poll_heartbeat.py` (worker, this session) → 234 `+` lines, only the diff header itself starts with `-` — pure addition. derived: `git show acd74b3b:.../test_poll_heartbeat.py | grep -c '^def t_'` → 38; `git show HEAD:.../test_poll_heartbeat.py | grep -c '^def t_'` → 41; name-diff of the two lists shows exactly 3 new `def t_` lines, 0 removed (worker output, this session). derived: `python3 test_poll_heartbeat.py` run via `git worktree add /tmp/wt-prbase acd74b3b`, this session — base worktree: `38/38 passed`; head worktree (`/tmp/wt-prhead`): `41/41 passed` (worker's actual execution, not the PR's own reported number). **Count claim confirmed exactly: 38 = 38, 41 = 38+3, 0 modified.**

Classification, per Test Depth Audit Step 2: `t_alive_stamp_lock_owner_status_establishes_liveness_issue_2919` (`test_poll_heartbeat.py:1391`, worker's line citation) drives the real extracted function against a lockdir naming a genuinely-running PID (asserts `stdout == "alive"`) and a PID confirmed fully reaped via active `os.kill(pid,0)` polling before use (asserts `stdout == "dead"`) — **Genuine Assertion**, both branches independently falsifiable against real process state. `t_alive_stamp_mutex_never_evicts_slow_live_holder_issue_2919` (`test_poll_heartbeat.py:1440`) runs a 22s live holder, both processes `communicate()`'d directly with no reaping-thread shortcut, and asserts `events["B"]["ENTER"] >= events["A"]["EXIT"]` — **Genuine Assertion**, would fail against the pre-fix (main) code per point 1's baseline run above. `t_alive_stamp_mutex_recovers_crashed_holder_issue_2919` (`test_poll_heartbeat.py:1472`) — **Mock-Dominated**: per point 2's experiment above, its pass is contingent on the test's own background-thread reaping of the simulated-crashed worker, an assumption about parent-process behavior this repo cannot verify for production. Verification density (Test Depth Audit Step 3) for the 3 new tests: derived: 2 of 3 classified Genuine Assertion above → 2/3 = 67%.

**4. Overhead and flock-path identity — clean.** derived: `git diff acd74b3b HEAD -- on-the-record/monitors/poll-heartbeat.sh` (worker, this session) — the `if [ "${_alive_stamp_has_flock}" -eq 1 ]` branch body (the `flock -x 200` subshell) shows zero `+`/`-` lines inside it, only re-indented one level — confirms the PR's "still byte-identical" claim for the flock-present path itself. derived: worker's benchmark, this session — 10 runs of 20 ticks each (`POLL_HEARTBEAT_MAX_TICKS=20 POLL_HEARTBEAT_SLEEP_SECONDS=0`) against `/tmp/wt-prhead` and `/tmp/wt-main` on a real flock-present host: median 51.68ms/tick (PR head) vs 51.72ms/tick (main) = 0.04ms/tick difference, an order of magnitude below each version's own ~2ms/tick run-to-run stdev (worker-reported stdev 0.0341-0.0438s over 20 ticks). **No measurable per-tick regression**, `command -v flock` detection confirmed to run once at startup (before `while true`, `on-the-record/monitors/poll-heartbeat.sh` line ~163, this session Read tool), not per tick.

**5. Invariant/scope sweep — four of five checks pass; one process (not content) hazard.** derived: `git merge-base --is-ancestor acd74b3b 2585022d` (worker, this session) → fails (non-zero exit) — PR #2935's nominal base branch (`.../4495e32f`, PR #2923's branch) is NOT an ancestor of the PR head; PR #2935's two commits are built directly on `origin/main`, silently re-carrying PR #2923's entire unmerged content into `gh pr diff 2935`'s view. All checks below re-diff against PR #2923's actual code commit `acd74b3b` to isolate PR #2935's own authored changes, per this finding.

- derived: `git diff acd74b3b HEAD -- on-the-record/monitors/poll-heartbeat.sh | grep -i patrol` (worker, this session) → empty output — both Monitor-visible lines PR #2919 added (`[patrol-poll] skills query failed at startup, patrol skipped this tick` and the `_poll_watchdog_log_append` call) are present, unmodified, verbatim in the PR head. derived: `grep -n "patrol-poll\|patrol-skills query failed" on-the-record/monitors/poll-heartbeat.sh`, this session → 2 matches, one the printf line, one the log-append call. **Pass.**
- derived: same `git diff acd74b3b HEAD -- on-the-record/monitors/poll-heartbeat.sh | grep -i patrol` → empty — patrol code untouched by this PR's own authored changes. **Pass.**
- derived: `git log --oneline --all | grep -i "role axis"` (worker, this session) → issue #2241/PR #2252 ("staged proposal for retiring the role axis"); `grep -n -i "\brole\b" /tmp/pr2935.diff` (this session) → only the standard `role:` doc-record frontmatter field (used identically across all `docs/issue-*/reports/*.md`) and the pre-existing, unchanged `spawn.role_data()` call (already covered in the patrol check above). **Pass.**
- derived: `git diff acd74b3b HEAD -- on-the-record/monitors/monitors.json` (worker, this session) → empty output, file untouched; no other Monitor-registration file appears in the diff stat below. **Pass.**
- derived: `git diff 46779718 2585022d --stat` (worker, this session), 5 files / 668 insertions / 6 deletions, matching `gh pr diff 2935 --stat` exactly — every file falls into an expected category: the record and its deviation-log entry under `docs/issue-2919/reports/`, the product-priorities capture file, `on-the-record/monitors/poll-heartbeat.sh`, `on-the-record/monitors/test_poll_heartbeat.py`. **Pass.**
- **Process CONCERN, not a content defect**: because PR #2935's commits sit on `main` rather than on PR #2923's own open branch tip, `gh pr diff 2935` bundles PR #2923's entire unmerged fix inside a commit titled "fix live-holder eviction," confirmed byte-identical to `acd74b3b` by the checks above — not new/edited content, but a landing-order/attribution hazard for anyone merging or reviewing without cross-referencing commit shas the way this review did.

## Why

Per the adversarial-review skill's blindness-to-self-defense mechanism and the defect-verification-independence skill's re-derive-don't-cite rule, every claim above was reproduced from primary sources rather than read from the PR's own record and accepted. The task named the slow/crashed boundary as "the whole fix," so the heaviest attack (point 1's forming-window construction) targeted exactly that boundary rather than re-confirming the two headline scenarios (slow-alive, crashed) the PR's own verification already covered. The task also explicitly asked to take the deviation log's zombie disclosure seriously rather than accept it — point 2's removed-reaping-thread experiment and the platform-capabilities research (establishing "who reaps" is genuinely unknowable from this repo, not merely undocumented) follow directly from that instruction. Fan-out (4 background workers against a frozen worktree/container contract, per the freelunch protocol's research exception) was used for the four tool-execution-heavy angles; this record is my own synthesis connecting their raw findings to each other and to my own inline platform-capabilities research on point 2, not a concatenation of their outputs.

## What did not work

- One fan-out worker (test-count/depth audit) initially computed a diff against a moving `FETCH_HEAD` ref that changed mid-session due to concurrent git activity in the shared environment, producing a misleadingly large diff that appeared to show this PR introducing the entire flock-detection/mkdir-mutex scaffold from scratch. canonical: worker's own self-correction note, this session — caught by cross-checking against the literal pinned sha `4a2f3c17b82716dfaa0a5929eb2a1a146bd4b40a` (later superseded in my own synthesis by pinning to `acd74b3b`, PR #2923's actual code commit, once finding 5 established the correct baseline); the count/depth figures cited in point 3 above use the corrected baseline. No repository file was affected — this was a read-only diff-base mixup in a worker's own scratch investigation.
- A second fan-out worker (invariant sweep) initially diffed against the literal base branch the task text named (`.../4495e32f`) and found it is not an ancestor of the PR head (see finding 5's `git merge-base --is-ancestor` result above), making a naive `git diff <base>` misattribute PR #2923's whole unmerged patch to PR #2935. Re-ran against `acd74b3b` to isolate PR #2935's own authored changes; the discrepancy itself became finding 5's process-hazard note rather than being discarded.
- This review's own freelunch STEP 1 tally (the mandated "before any other action" paragraph) was written after several exploratory `gh`/`git`/`grep` calls had already run, not before them — a process miss on my part, corrected by writing the tally explicitly once noticed rather than skipping it. It did not change the fan-out decision itself (the same 4-unit width/effort assessment held once written).

## Upstream basis

- PR #2935 (github.com/tokenmaxxxer/on-the-record/pull/2935), commits `e2441a9b` (fix) + `2585022d` (deviation-log + product-priorities capture) — the subject of this verification. canonical: `git worktree add /tmp/wt-prhead origin/issue-2919/silent-failure-audit+refactoring-legacy-seam-selection-9f67d71e`, this session, resolved sha `2585022dc07e810730dcdaa8eeeaba3017c17950`.
- PR #2923's actual code commit `acd74b3b63d8f97886c027d0d9c59a65784ee98d` — used as the correct diff baseline to isolate PR #2935's own authored changes, since the PR's nominal base branch (`.../4495e32f`) is not an ancestor of the PR head. canonical: finding 5 above, `git merge-base --is-ancestor acd74b3b 2585022d` fails, this session.
- `origin/main` at `46779718d2435a8749d7c736e86d3f54a5dc30d5`, pre-#2923 baseline for point 1's old-code comparison and point 4's overhead benchmark. canonical: `git worktree add /tmp/wt-main origin/main`, this session.
- `docs/issue-2919/reports/adversarial-review-a4f05242.md` (commit `00143ce89d484521002993d0d08fe0428b3be607`) — the prior review that found the defect PR #2935 fixes. canonical: full text read via the Read tool, this session, cited in point 1 above for the matching failure-shape comparison; read for context and cross-check only, not as a source for this review's own live-reproduced findings.
- `docs/specs/platform-capabilities.md` and `docs/handbooks/monitor-liveness.md`, the basis for finding 2's determination that the Monitor's spawn/reap mechanics are a closed-source platform capability, not repo-derivable. canonical: read from `$ON_THE_RECORD` (the plugin marketplace checkout at `/home/jwjung/.claude/plugins/marketplaces/tokenmaxxxer/`) via Read/Bash tools, this session.
- `docker run --rm bash:3.2` with a restricted PATH (symlinks to only the binaries needed, `flock` excluded) — the real bash-3.2/no-flock execution environment for point 1's concurrency attacks. canonical: worker's own transcript, `command -v flock`/direct-invocation confirmation reproduced in point 1 above, run this session.
- `on-the-record/monitors/poll-heartbeat.sh`, `on-the-record/monitors/test_poll_heartbeat.py` — read directly from both worktrees. canonical: Read tool invocations, this session, cited at specific line ranges throughout points 1-5 above.

## Open findings

- **A live holder whose gap between its successful `mkdir` and its own `owner.pid` write exceeds the ~3s forming-grace window is misclassified as abandoned, evicted, and — because nothing after the retry loop re-verifies lock ownership — proceeds to write the shared stamp file with no lock held, reproducing the unprotected-concurrent-write failure shape the prior adversarial review found in PR #2923.** canonical: point 1 above, live-timestamped reproduction inside the bash-3.2/no-flock container (this session's fan-out worker), reinforced by the false-positive-reclaim mechanism observed firing in 5/6 runs of the unmodified 8-concurrent-normal-writer regression scenario. Resolution path: the code path that exits the retry loop on a holder's own immediate `mkdir` success needs the same re-verification the `dead`/`forming` reclaim branches already give a *challenger* re-acquiring — e.g. re-check that `${_lockdir}/owner.pid` still contains `$$` immediately before the critical-section write, and treat a mismatch as "I was evicted, re-acquire via a fresh `mkdir`" rather than proceeding unprotected. Not a revert candidate (the design correctly fixes the reported 20s-eviction defect for any holder whose PID is already recorded, confirmed in point 1's first two scenarios) — the current code narrows the bug class this task asked it to close but does not yet close it.
- **The production identity and promptness of whatever reaps a crashed `poll-heartbeat.sh` process is not established anywhere in this repo, and the PR's crash-recovery regression test only passes because its own test harness manufactures prompt reaping.** canonical: point 2 above — the reaping-thread-removal experiment (`PASS in 2.43s` → `TimeoutExpired after 15.32s`, this session's fan-out worker) and the `platform-capabilities.md`/`monitor-liveness.md` research establishing the Monitor as a closed-source, session-bound platform capability with no repo-visible spawn/reap code. Resolution path: not fixable from this repo (the same boundary `platform-capabilities.md` already documents for other Monitor properties) — but the PR's own record should state this as an unverified assumption rather than as an established fact, and a follow-up could add a repo-side safety valve independent of reap timing (an absolute staleness ceiling on top of the PID check, so a zombie-shadowed lock cannot stall a tick forever even if the liveness check itself is fooled).
- **Landing-order/attribution hazard**: PR #2935's commits are built directly on `origin/main` rather than on PR #2923's own open branch tip, so `gh pr diff 2935` bundles PR #2923's entire unmerged content inside a commit titled as a live-holder-eviction fix. canonical: point 5 above — `git merge-base --is-ancestor acd74b3b 2585022d` fails (this session), content confirmed byte-identical to `acd74b3b` by the patrol/role/monitors.json diffs in point 5. Resolution path: not a code defect — no action needed if PR #2923 merges before PR #2935, as the branch names and PR number ordering suggest is intended; worth a note to whoever merges so #2935 is not merged first expecting it to be a small, isolated diff.

## Next steps

None from this session — `loop_state: landed`. derived: this session's tool-call transcript (worktree setup, `gh pr diff 2935`, 4 fan-out worker Agent calls with their commands/output reproduced above, inline platform-capabilities research) — every finding above traces to a live command run this session against primary sources (worktrees, a real bash-3.2 container, pinned-sha diffs), not to the PR's own claims accepted at face value. The three Open findings above are handed off (the mutex re-verification gap and the reaper-identity gap to follow-up fix work under issue #2919 or a new issue, the landing-order note to whoever merges #2923/#2935) rather than fixed here, per the adversarial-review skill's evaluate-don't-fix contract — PR #2935 itself was not modified by this review.
