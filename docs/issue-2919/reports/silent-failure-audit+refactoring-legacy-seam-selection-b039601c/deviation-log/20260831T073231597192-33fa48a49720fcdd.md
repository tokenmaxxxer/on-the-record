# Deviation log — issue-2919 (silent-failure-audit+refactoring-legacy-seam-selection-b039601c role)

- 2026-08-31T00:00:00Z | inline | freelunch's mechanical executor test says any turn needing repo/environment tool calls must delegate to a background `freelunch:freelunch-worker`, always. This session elected the contract-v3-s22 escape hatch instead (do not delegate the unit at all) rather than dispatch-and-block-wait, because the core deliverable was the harness verification itself (proving no two workers are ever concurrently in the critical section) — exactly what a raw, no-verification-pass worker is instructed to skip — and a headless single-shot session cannot leave delegated work unconsumed across a turn boundary. Stated explicitly in the STEP 1 tally at session start, not discovered mid-task. canonical: this session's own STEP 1 tally paragraph (first assistant turn), and the freelunch-protocol directive's own s22 override clause.
- 2026-08-31T00:00:00Z | inline | the warrant-protocol's after-proposal/before-landing warrant-hunter dispatch was skipped entirely for the same reason: this is a `CORE_BUILD_NOW=1` build-now single-phase delivery in a headless session, and dispatching a background hunter without being able to wait on and consume its result within the same turn would violate contract v3 s22. No hunter was dispatched at either transition.
- 2026-08-31T00:00:00Z | inline | first draft of the scratch bash-3.2 verification harness (`/tmp/otr-2919-verify4/`, not committed) applied `sed -i 's/date +%s/date +%s.%N/g'` to both the harness's own logging calls and, by accident, the sourced copy of the real production code (`instrumented_extract.sh`) — this would have broken the real script's `$(( $(date +%s) - _wait_started ))` integer arithmetic had it reached production code. Caught before any repository file was touched (this was scratch-only tooling); fixed by regenerating the instrumented copy fresh from the untouched extraction and re-applying only the pure-instrumentation lines, confirmed via `diff`. No repository file was affected. canonical: `docs/issue-2919/reports/silent-failure-audit+refactoring-legacy-seam-selection-b039601c.md` "What did not work", written this session.
- 2026-08-31T00:00:00Z | inline | the same scratch harness's overlap-checking script (`check_overlap.py`, not committed) initially crashed (`ValueError` on `int()`) because the `bash:3.2` Docker image's `date` binary does not support `%N` and returns a literal trailing `.` with no digits. Fixed by parsing as float with the trailing dot stripped; sub-second log-timestamp ordering was not actually recoverable from this container, so the final B-vs-D non-overlap claim in the record relies on the filesystem's own `O_EXCL` happens-before guarantee instead, stated explicitly rather than papered over. canonical: `docs/issue-2919/reports/silent-failure-audit+refactoring-legacy-seam-selection-b039601c.md` "What did not work" and "What was done", written this session.
