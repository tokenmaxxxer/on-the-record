# Deviation log — issue-2919 (adversarial-review-95d4569a role)

- 2026-08-31T05:50:00Z | inline | fan-out worker for the test-count/depth audit initially diffed against a moving `FETCH_HEAD` ref that changed mid-session (concurrent git activity in the shared environment), making PR #2935 appear to introduce the entire flock-detection/mkdir-mutex scaffold from scratch. Caught via cross-check, re-pinned to a literal sha before reporting. No repository file affected — read-only scratch investigation. canonical: `docs/issue-2919/reports/adversarial-review-95d4569a.md` point 3 / "What did not work", read this session.
- 2026-08-31T05:50:00Z | inline | fan-out worker for the invariant/scope sweep initially diffed against the literal base branch the task text named (`.../4495e32f`), then found via `git merge-base --is-ancestor` that it is not an ancestor of the PR head — PR #2935's commits sit directly on `origin/main`, so a naive diff against the named base misattributed PR #2923's whole unmerged patch to PR #2935. Re-ran against PR #2923's actual code commit `acd74b3b` to isolate PR #2935's own changes; the discrepancy itself became finding 5 (landing-order hazard) in the record rather than being discarded. canonical: `docs/issue-2919/reports/adversarial-review-95d4569a.md` finding 5 / "What did not work", read this session.
- 2026-08-31T05:50:00Z | inline | this session's own freelunch STEP 1 fan-out tally (mandated "before any other action") was written after several exploratory `gh`/`git`/`grep` calls had already run, not before them. Corrected by writing the tally explicitly once noticed rather than skipping it; did not change the fan-out decision itself (same 4-unit width/effort assessment held once written). canonical: `docs/issue-2919/reports/adversarial-review-95d4569a.md` "What did not work", read this session.
