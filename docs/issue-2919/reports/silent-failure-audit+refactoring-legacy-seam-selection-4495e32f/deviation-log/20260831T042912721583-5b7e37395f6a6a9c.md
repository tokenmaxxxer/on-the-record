issue: 2919
skill: silent-failure-audit+refactoring-legacy-seam-selection-4495e32f

Expected `t_patrol_skills_query_failure_is_visible_issue_2919`'s first draft (using the pre-existing `FAKE_SPAWN_PY` fixture) to reproduce a failed `role_data()` query via `AttributeError`. It didn't: `FAKE_SPAWN_PY` has no `if __name__ == "__main__":` guard, so `import spawn` in the patrol-skills query's `python3 -c` runs its module-level body directly, which falls through to an unconditional `sys.exit(0)` before ever reaching a `role_data()` call — a clean rc-0/empty-output exit, not a failure. Diagnosed via `bash -x` on the exact env; fixed by adding a new `FAKE_SPAWN_PY_NO_ROLE_DATA` fixture (same `__main__`-guarded shape as the existing `FAKE_SPAWN_PY_WITH_SKILLS`, minus the `role_data()` def), which lets `import spawn` succeed and only fails at the `role_data()` attribute lookup — the actual failure shape needed. Full detail in the record's "What did not work" section.
