---
issue: 3061
role: implementation-blueprint+decision-brief+silent-failure-audit+test-derivation-7559ea9e
---

Expected: this repair round's task explicitly instructed working on the
existing branch `issue-3061/implementation-blueprint+silent-failure-audit+test-derivation+decision-brief-f458808c`
(a different session's branch, spawned under a different `CLAUDE_SKILL`
than this session's own `...-7559ea9e`), committing both code and this
session's own record there, landing as one PR extending PR #3087.

What actually happened: code commits landed fine on that branch after
syncing `.on-the-record/role.json` to match it (required by
`approval-gate.sh`). But writing this session's own record into
`docs/issue-3061/reports/` from that same checkout was refused by
`board-gate.sh`'s R4 write check, which ties any `docs/issue-3061/` write
to the session's fixed `CLAUDE_SKILL` env var (or a sidecar matching it),
independent of branch content — and `CLAUDE_SKILL` cannot be changed
mid-session (spawner-set). `approval-gate.sh` wants the sidecar to match
the checked-out branch; `board-gate.sh` wants the sidecar to match
`CLAUDE_SKILL`. With the checked-out branch's suffix (`f458808c`)
permanently different from `CLAUDE_SKILL`'s suffix (`7559ea9e`), no single
`role.json` value satisfies both gates at once. Resolved by keeping code
commits on PR #3087's branch (satisfies approval-gate there) and writing
this session's own record from this session's own branch instead
(satisfies board-gate there) — two branches, two PRs (PR #3087 extended
in place, PR #3116 opened for the record), not the single-PR shape the
task described.

Second, smaller deviation, already logged inline in the record's own
"What did not work" section per the record-order directive: the R3
beacon-wiring commit (`c2c38c64`)'s own targeted tests were clean in
isolation, but a full-suite run afterward surfaced one pre-existing
pinned-stdout test in `on-the-record/monitors/test_poll_heartbeat.py`
whose exact-equality assertion no longer held against the new output
line — not caught until that full-suite run, fixed in a follow-up commit
(`3f1bb626`) in the same session.
