## Deviation

Expected: `CORE_BUILD_NOW=1`'s "skip the proposal round and deliver
directly" instruction to also exempt this architecture-role record from
the survey-order/scout-directive phase-1 artifacts
(`docs/issue-2548/reports/architecture/survey.md`,
`docs/issue-2548/reports/architecture/scout-brief.md`) before setting
`loop_state: landed`.

What actually happened: `arch-sequence-gate` (a `PreToolUse` hook on
`docs/issue-2548/reports/architecture.md`) refused the write —
build-now bypasses the phase-1/phase-2 *approval* round, not this
role's own phase-1-artifact-before-decision-bearing-record sequencing.
Wrote both files (real current-state survey from the session's own
verification work; a genuine two-angle web scout on
identity/authorization separation patterns) before the record write was
accepted.
