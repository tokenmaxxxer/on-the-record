- 20260903T005857Z inline: fixing the SessionStart skill-repository clone's missing credential-prompt guard (round 3's declared single-file scope: `skills.py`'s `_skill_repo_managed_root()`) shifted `spawn.py`'s re-export block line numbers, which broke `tests/test_issue_3182_citation_line_accuracy.py`'s hardcoded line anchors. Fixed the shifted anchors in `scripts/preflight/consumer_preconditions.py` (`posix_fork_support`, `claude_cli_on_path`, `skill_repository_resolvable`, `home_claude_skills_dir_present`, `workspace_disk_headroom`) and the matching prose line numbers in `docs/handbooks/install-sufficiency.md` (lines 56, 57, 183), outside the originally-described single-file guard-addition scope. canonical: `git show FETCH_HEAD:docs/issue-3231/reports/implementation-blueprint+silent-failure-audit-43f2f6d1.md` (FETCH_HEAD = `d1573e71`, PR #3235 branch `issue-3231/implementation-blueprint+silent-failure-audit+technical-writing-structure-comprehension-c01699d6`, read this session), "Collateral: citation-line-accuracy repair" section — that record's own `derived:` tag there quotes `python3 -m pytest tests/test_issue_3182_citation_line_accuracy.py -q` going from `2 failed, 1251 passed, 3 xfailed` before the anchor fix to `10 passed` after it. Location: `scripts/preflight/consumer_preconditions.py`, `docs/handbooks/install-sufficiency.md` (both on PR #3235 branch, untracked in this checkout).
- 20260903T005857Z inline: the first repro attempt for that record's Demonstration 1 (credential-prompt hang) used a `script(1)`-wrapped `git clone` under `env -u GIT_ASKPASS -u GIT_TERMINAL_PROMPT`, matching PR #3247's own described repro shape; it returned instantly with exit 128 (a false pass) instead of blocking, because that sandbox's own stdin is not a tty and `script(1)` forwards keystrokes from its own stdin into the pty it creates, so EOF propagated immediately regardless of whether the fix's env was present. Replaced with a `pty.openpty()`-based repro that opens its own kernel pty pair, forks a child, and makes the pty slave the child's controlling terminal via `TIOCSCTTY` directly, independent of the outer terminal. canonical: `git show FETCH_HEAD:docs/issue-3231/reports/implementation-blueprint+silent-failure-audit-43f2f6d1.md` (FETCH_HEAD = `d1573e71`, PR #3235 branch, read this session), "What did not work" section, and "Demonstration 1" section's `derived:` tag quoting `python3 docs/issue-3231/_assets/round3-repro/repro_1_credential_prompt.py` — result showed the child "still blocked (alive) after probe window: True" (5.045s, before the fix) versus "exited within probe window: True" at 0.05s (after the fix). Location: `docs/issue-3231/_assets/round3-repro/repro_1_credential_prompt.py` (renamed from an abandoned, never-committed `script(1)`-based `.sh` variant; new this round, untracked in this checkout — lives on PR #3235 branch).
