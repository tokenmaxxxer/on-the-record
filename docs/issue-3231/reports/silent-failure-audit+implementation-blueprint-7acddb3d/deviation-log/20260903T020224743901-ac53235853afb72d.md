- 2026-09-03T02:02Z, inline, before-landing warrant-hunter found `docs/issue-3231/_assets/round4-repro/repro_round4_three_sites.py` printed `VERDICT: FAIL` for one site but always exited 0 (silent-failure in this round's own verification tool); fixed by replacing the boolean verdict with `PASS`/`INCONCLUSIVE`/`REGRESSION` and calling `sys.exit(1)` only on `REGRESSION` -- `docs/issue-3231/_assets/round4-repro/repro_round4_three_sites.py`, `report()`/`main()`.
  canonical: `docs/issue-3231/reports/silent-failure-audit+implementation-blueprint-7acddb3d.md`, "What was done" item 6 and "What did not work" first bullet (this session, commit f5f129b3) -- same finding, same fix, with the hunter's Reproduce/Observed transcript.
