2026-09-02T07:40 inline — planned to reuse PR #3084's `_drift_cache_key`
mechanism exactly, re-keying every `gates/spawn_on_pr.py` park-state entry
as `f"{repo}:{subject}"` (matching drift's cache key change, so both the
read and write paths become repo-scoped, not just read). Before
implementing it, checked: `grep -c "KEY = SUBJECT" gates/test_spawn_on_pr.py`
— result: 1 (the line `KEY = SUBJECT # issue #2628: park state is keyed by
subject alone, not "subject/role"`), and `grep -c "^KEY:" gates/test_spawn_on_pr.py`
— result: 0 further confirming fixtures reference `KEY` (bare subject, no
`repo` field) throughout that file's ~30 seeded/read state dicts. Re-keying
would have required rewriting most of that file's fixtures. Swapped to
keeping the bare-subject key and adding a `repo` field to each entry's
value instead, filtering at read time (`parked_report`) and evicting a
foreign-repo `prior` at lookup time (`spawn_missing_for_pr`) rather than
relying on key non-membership. This closes the two properties the issue's
acceptance actually tests (report-time leak, retention/eviction split);
derived: `python3 -m pytest tests/test_spawn_on_pr_repo_scope.py -q` —
result: 6 passed, covering both. It leaves a narrower, explicitly
documented boundary open (a literal same-numbered-issue collision across
two swept repos can still overwrite on write, not just misread) that the
abandoned re-keying approach would have closed too — this residual gap is
not covered by any test in this session's own suite, by inspection of the
same file's case list.

Alternative swap from the originally-intended #3084-identical mechanism,
made before any re-keying code was written — no code was written and then
reverted. Documented in the phase-2 record's "Why" section with the full
reasoning and the accepted boundary. Location: gates/spawn_on_pr.py
(commit ab0d81e4), docs/issue-3095/reports/implementation-blueprint+
silent-failure-audit+test-derivation-0cae2f1d.md (commit 4395bdf4).
