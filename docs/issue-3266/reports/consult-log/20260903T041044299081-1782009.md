- 2026-09-03T04:10:44.299113+00:00 | skill=silent-failure-audit+test-derivation+implementation-blueprint-0ba690d0 | verb=skill_judge | issue=3266 | question="Task:\nIssue #3266. Read the issue and the orchestrator comment carrying the revised runnable Acceptance section -- the original acceptance wording was not runnable and that comment replaces it.\n\nThe problem in one line: `spawn.py clean` kept 279 of 298 workspaces because any non-ignored untracked file counts as unpreserved work, and every crashed session leaves an empty report stub. So the workspaces most worth reclaiming are exactly the ones it refuses to touch, and disk grows without bound. This directory reached 34G on one machine and 79G on another, where it killed sessions.\n\nThe seam is `lifecycle.py:_workspace_untracked_not_ignored()` (line 831) and the classification at `lifecycle.py:901-930`.\n\nWhat to decide and implement: what 'unpreserved work' means, precisely enough that a report stub with nothing past its front matter and the harness's own `.on-the-record/` scaffolding are reclaimable, while a report with real body content is never touched. Do not swing to the opposite failure -- deleting a real uncommitted report is worse than keeping a stub.\n\nTwo things you must not break, both already in the code and both there for a reason:\n- the `git fetch --all`-then-recheck at `lifecycle.py:903-921`, which exists because a stale workspace reads as 'ahead' forever when its branch already merged;\n- the fact that this repository squash-merges, so a landed branch's original commits are absent from `main` by SHA. Commit-SHA absence is not evidence of loss.\n\nReport the measured effect plainly: run the new classification over this machine's remaining workspaces and say how many change verdict, in both directions.\n\nScope constraint, absolute: sessions and PRs only against tokenmaxxxer/on-the-record. Do not touch, clone, or spawn against any other repository, including as a test target. Use a local scratch git repo if you need a throwaway.\n\nPortability: must run on macOS and Linux -- no GNU-only flags, no /proc, no readlink -f.\n\nWork in the background and open a PR on your own branch. Do not merge.\n\nCandidates:\n- adversarial-review — Use when you need an honest assessment of any AI-made artifact (code, design doc, report, config) and can't trust the making agent to critique its own work.\n- implementation-audit — Use when applying Implementation Audit — a two-session audit protocol: the builder session extracts falsifiable claims from the specification, then a structurally independent evaluator session (via adversarial-review) classifies each claim against the implementation as Present, Surface, Absent, Incorrect, or Unverifiable, with no access to the builder's intent.\n- overengineering-audit — Use when a completed implementation may exceed its specification and you need an audit for unnecessary abstraction layers, interfaces with one implementation, features not in the requirements, unused dependencies, and scope that grew beyond what was asked — typically after AI wrote the code.\n- model-routing — Use this skill on EVERY non-trivial task in any domain — development, design, architecture, data, docs, or multi-step orchestration — to decide what you do yourself, what goes to a high- capability reasoner, and what goes to a low-cost executor.\n- prose-modes — Use when writing or revising prose a human will read — explanation, report, book/article draft, teaching material, design writeup — in Korean or English, or when a draft reads badly and you need to find which rule it is breaking.\n- product-discovery-guardrail-metrics — Use this skill while the product skill is in `hypothesis-registered`, before the transition to `measuring`, to name guardrail metrics that must not move adversarially, and read it again once `measuring` starts so breaches are checked against what was named.\n- premortem — Use whenever the user wants to pressure-test a written plan BEFORE committing to it: assume the plan has already failed, gather reasons independently, then convert every reason into a mitigation, a detection signal, or an explicit accepted risk.\n- p" | outcome="ok: picked=[model-routing=Non-trivial development task requiring decision on classification logic, impleme; prose-modes=Explicitly asks 'report the measured effect plainly' on workspace verdict change] rejected=[adversarial-review=No explicit request for AI-artifact assessment; task asks for implementation, no; implementation-audit=Two-session audit protocol doesn't fit single-session implementation task; overengineering-audit=Audits post-completion scope creep; this is a focused maintenance fix, not over-; product-discovery-guardrail-metrics=Task has no connection to hypothesis testing or metrics guardrails; premortem=User requests implementation and measurement, not plan pressure-testing before c; prior-art-scan=Not checking existing solutions; task is to implement a specific design decision]"
