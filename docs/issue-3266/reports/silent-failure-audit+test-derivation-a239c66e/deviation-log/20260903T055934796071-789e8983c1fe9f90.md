- 2026-09-03T05:59:34Z | inline | first draft of the FIFO regression test exercised `spawn._workspace_clean_state()` end-to-end (mkfifo at the report path, expect `reason == "dirty"`) and failed: `git ls-files -z --others` never lists a FIFO at all (verified: `git ls-files -z --others` on a scratch repo with a `mkfifo`'d file printed nothing), so `_workspace_untracked_not_ignored()` never calls `_report_stub_has_no_content()` on it and the workspace read back as fully clean instead of dirty — switched the test to call `spawn._report_stub_has_no_content()` directly (matching PR #3271's own reproduction method), which does exercise the stat-gate fix. Location: `tests/test_issue_3266_reclaimable_stub.py`, method `test_fifo_report_path_is_never_deleted_and_does_not_hang`.
