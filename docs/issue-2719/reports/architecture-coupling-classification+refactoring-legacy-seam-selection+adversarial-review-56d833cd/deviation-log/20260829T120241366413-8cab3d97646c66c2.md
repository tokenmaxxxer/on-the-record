2026-08-29T12:02Z inline: a `merge-allow-gate.sh` test-fixture command's `git rm -rq --cached docs` ran against this checkout's real working directory instead of the intended `/tmp` throwaway fixture repo (cwd mismatch), staging deletion of all tracked `docs/` paths in the index. Recovered with `git reset -q -- docs/` (unstage only; working-tree content was already verified byte-identical to HEAD, so no write was needed). No commit was affected. See docs/issue-2719/reports/architecture-coupling-classification+refactoring-legacy-seam-selection+adversarial-review-56d833cd.md, "What did not work".
