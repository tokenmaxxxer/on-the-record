2026-08-30T00:00:00Z inline: acceptance check 3 names a cross-repo tally
this session cannot reproduce derived: `gh issue view 2798` (Acceptance
section, third bullet: "empty state: 1263, matching the value before PR
#2794"; measured "across both repos" per the issue's Ask section) — only
one repo checkout was available, and the operator's exact historical
command is unknown. Substituted a same-repo before/after delta
(`grep -rIc --exclude-dir=.git --exclude-dir=docs --exclude-dir=runs -inE
'\brole\b' .` summed via awk: 1120 → 1108 = -12) as the closest
reproducible equivalent, and recorded the gap as `unverifiable:` rather
than claiming the issue's absolute figures as verified. Location:
docs/issue-2798/reports/adversarial-review-99b10ef0.md, "What did not
work" / "Open findings" sections.
