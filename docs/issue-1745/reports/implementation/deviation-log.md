2026-08-22T00:00:00Z filed: test_sweep_call_budget in tests/test_gh_quota_guard.py still fails after applying the approved proposal's fixture fixes (repo-slug + bulk `/issues` and `/pulls` REST stubs). A correctly-stubbed cold-start sweep now issues 10 gh calls (rate_limit x2, repo view x1, delta-probe issues-index x1, closure-sweep issues-index x1, closure-sweep pulls-index x2, git ls-files x2, issue list x1), exceeding the test's asserted ceiling of 8. This was not visible to the phase-1 survey/proposal because the fixture gap it diagnosed (unresolved repo slug) was itself masking this second, independent issue: the sweep's three gh-calling signals (spawn-on-pr, closure-sweep, spawn-coverage) plus the issue #1688 delta-probe each cost their own gh call with no cross-signal call sharing, and closure-sweep's PR-index fetch runs twice per tick with no in-tick caching. Deciding whether the ceiling should simply move to reflect this (still-bulk, still-O(1)-in-subject-count) behavior, or whether the duplicate rate_limit/pulls calls are themselves a fixable inefficiency, is a production-behavior judgment outside this issue's frozen write set (test_gh_quota_guard.py and test_spawn.py fixture edits only) and outside the approved proposal's scope (which explicitly ruled out production-code changes). Reported, not spawned.
2026-08-22T00:20:00Z filed: production-code fix for the same gap (dedup shared `_pr_index_all` call and dedup `gh api rate_limit` snapshot fetch across gates/closure_sweep.py, gates/spawn_on_pr.py, gates/gh_budget.py, spawn.py) is outside this proposal's frozen write set (tests/test_gh_quota_guard.py, tests/test_spawn.py only) and outside the approved proposal's scope, which explicitly rejected production-code batching. Applied under this turn's explicit task-level authorization naming these exact production files as the continuation's scope (see docs/issue-1745/reports/implementation.md "Continuation" section for the fix and rationale). Reported, not spawned as a new peer role.
