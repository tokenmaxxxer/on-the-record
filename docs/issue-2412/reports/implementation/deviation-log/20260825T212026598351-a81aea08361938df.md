- 2026-08-25T21:20:26Z — filed — the issue-2241 stage-3 and stage-4
  proposals name migration-doc destinations under the parent program
  issue's own tree; `board-gate.sh` R4 (branch/tree scope, no matching
  `maintenance-targets:` entry on this issue's body) refused a live
  `Edit`-tool probe against the stage-3 proposal from this branch, and a
  role session cannot self-grant that exception (`gh issue edit` denied)
  or spawn a peer role on its own initiative — reported rather than
  spawned: wrote the exact patch under this issue's own tree instead
  (`docs/issue-2412/reports/implementation/stage-proposal-path-corrections.md`)
  and filed `gh issue comment 2241` naming both unblock paths (a session
  on the parent issue's own implementation branch, or a human adding a
  `maintenance-targets:` line to a future delivering issue's body). Full
  reasoning and the verbatim gate refusal:
  `docs/issue-2412/reports/implementation.md`'s "Rationale for
  deviations" section.
