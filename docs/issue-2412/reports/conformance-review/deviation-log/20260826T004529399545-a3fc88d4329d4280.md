- 2026-08-26T00:45:29Z — filed — this review's own verdict on PR #2449.
  canonical: `grep -n "^verdict:" docs/issue-2412/reports/conformance-review.md`
  (this session) —
  ```
  verdict: fail
  ```
  (AC2 and AC4 render Absent; see that record's "Findings" section) — but
  this session's `gh pr create` was refused twice by the `pr-preflight`
  hook until the PR body carried a `Closes #2412` trailer — the hook
  enforces that trailer mechanically for any `CORE_BUILD_NOW=1`
  phase-2-equivalent PR, without regard to the review's own verdict.
  Complied rather than leave the PR blocked: added `Closes #2412` to PR
  #2476's body, matching this repo's own precedent (PR #2461's
  conformance review of issue #2402 also carried a `Closes` trailer).
  The PR body itself states that AC2/AC4 remain unmet and are not
  resolved by this PR — only the review record is delivered here.
  Flagging this because a `Closes` trailer on a `verdict: fail` review PR
  could otherwise read as asserting the reviewed work is finished; the
  record's own "Next steps" section says the opposite.
