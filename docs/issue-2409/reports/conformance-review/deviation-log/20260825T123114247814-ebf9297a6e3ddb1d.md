# Deviation log — issue-2409 (conformance-review role)

- inline | The Stop-hook `skill-verdict-guard` flags `docs/issue-2409/reports/conformance-review.md`
  as missing `skill-verdict:` lines for the three skills this session
  invoked (`conformance-review-requirement-extraction`,
  `conformance-review-sampling-derivation`,
  `conformance-review-verification-method-selection`). Did not write
  those lines into that file: it is this role's phase-2-only
  `write_scope` (`roles/specs/conformance-review.spec.json`), and this
  session's own live `approval-gate` PreToolUse denial (quoted in
  `docs/issue-2409/reports/conformance-review/survey.md`'s "Board /
  approval state" section) refuses any write there before a human
  Approve exists. Recorded the three `skill-verdict:` lines in the
  phase-1 survey instead (same file/section as above), matching the
  issue-2211 precedent's own phase-1 skill-verdict placement. Reason:
  `skill-verdict-guard` is explicitly non-blocking
  (`hookSpecificOutput.additionalContext`, never `decision:"block"`,
  per the hook's own header comment) and its hardcoded
  `docs/issue-<n>/reports/<role>.md` target path does not account for
  two-phase roles mid-phase-1 — satisfying it by force would mean
  violating the stronger, hard-enforced phase-1/phase-2 write-scope
  boundary to silence an advisory reminder. Location: session-wide,
  noticed at Stop, after the phase-1 PR (#2420) was already opened.
