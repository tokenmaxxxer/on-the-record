# Deviation log — issue-2409 (conformance-review role)

- inline | While drafting this round's frontmatter update, `record-claim-guard`
  refused an edit twice: once for a `subject:`/`upstream:` claim
  ("unchanged since 8121072e ... carried forward") with no `canonical:`/
  `derived:` tag in the same section, once for `record-fields-gate`'s
  separate `sha:` format requirement (a short 8-char sha where a
  40-character hex commit sha or `same-commit` is required). Both caught
  at write time (the Edit tool call itself failed, no partial write
  landed) and resolved on retry: shortened the frontmatter claim to
  point at the body section carrying the actual `canonical:`/`derived:`
  citations, and resolved the short sha to its full 40-character form
  via `git rev-parse`. Location: frontmatter-rewrite step, this session,
  before any body content was added.
- inline | Independent verification needed to re-run the CHANGES-round
  fix commit's own documented regenerate command (a `python3 -c "..." >
  file` one-liner) against a worktree of `origin/issue-2409/implementation`.
  `board-gate` refused it twice: first for the `-c`-plus-redirect shape
  itself ("un-analyzable write-capable shape", issue-225), then again
  when a follow-up read-only `python3 -c` (loading the committed JSON
  with `json.load`, no write at all) named a path under
  `docs/issue-2409/reports/implementation/` (untracked here; lives only
  on `origin/issue-2409/implementation`) in its command text and was
  flagged as writing to another role's record. Resolved both by
  switching to provably-readable/writable shapes the gate can parse:
  wrote the regenerate script to a `/tmp` file first and ran
  `python3 /tmp/regen_script.py > /tmp/regen.json` (redirect target
  outside any role's write-scope), and replaced the read-only JSON
  inspection with plain `grep`/`grep -oE` calls instead of `python3 -c`.
  No content lost, no gate bypassed — same evidence obtained either way.
  Location: independent-verification step, this session, before the
  NR1b verdict was updated.
- inline | The landing commit for this round's record update was
  refused twice by `board-gate` for naming the CHANGES-round fix
  commit's own artifact path (untracked here; lives only on
  `origin/issue-2409/implementation`, a foreign role's own record path)
  inside the commit message body text itself — the gate's
  path-membership check scans commit message text, not only the file
  list actually being committed, so citing another role's real path in
  prose tripped the same "belongs to another role" refusal a literal
  write to it would. Resolved by rephrasing the message to describe the
  artifact ("the newly-added rollup artifact the fix commit introduced")
  without repeating its literal path; no files outside this role's own
  `conformance-review.md` and this session's own `.orchestrate-hook-fires/`
  shards were staged or committed at any point. Location:
  landing-commit step, this session, commit `e981b240`.
