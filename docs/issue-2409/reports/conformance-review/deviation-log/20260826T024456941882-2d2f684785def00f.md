# Deviation log — issue-2409 (conformance-review role)

- inline | First attempt to commit this session's housekeeping
  (hook-fires shard landing) used a heredoc-shaped commit message body
  (`git commit -m "$(cat <<'EOF' ... EOF)"`), which
  `heredoc-command-refusal-gate` refused as an un-analyzable shape (per
  issue #1976). Retried immediately with the required two-`-m`-flags
  form (one `-m` per paragraph) and the commit succeeded on the second
  attempt — no content lost, no gate bypassed. Notable only because
  this issue (#2409) is itself about hook-refusal turn-cost: this is a
  live instance of exactly the "arrives one at a time, after the fact"
  refusal shape the issue's `## Ask` describes, hit by this same
  session despite this role having written the record that cites the
  hook-contract mechanism addressing it (the contract lives on
  `origin/issue-2409/implementation`, not on this
  `issue-2409/conformance-review` branch's own directive). Location:
  landing-commit step, this session, immediately before the housekeeping
  commit (`1703b9b1`).
