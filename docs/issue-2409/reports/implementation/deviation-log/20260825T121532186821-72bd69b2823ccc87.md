# deviation log — issue-2409/implementation

- 2026-08-25T21:00Z | inline | skipped the full `-m "not slow"` regression
  suite the #2211/#2262 precedent both ran, after this host's root
  filesystem hit 100% used (19M then 570M free, shared across many
  concurrent sessions' `/tmp` usage) twice while attempting it — repeating
  a ~500s+ subprocess/git-clone-heavy run risked tipping a shared,
  already-saturated disk for other sessions. Killed my own runaway
  background test process instead of retrying. Substituted the targeted
  new/updated-test suite plus an isolated worktree check confirming the
  one failure an earlier combined run surfaced
  (`test_delegation_phrasing_signal`) pre-exists on the parent commit,
  unrelated to this delivery.
  canonical: acceptance: pytest targeted suite (see docs/issue-2409/reports/implementation.md, Acceptance evidence section) — result: pass, run live that session
  Documented in `docs/issue-2409/reports/implementation.md`'s
  "Full-suite comparison" section, not silently dropped.
- 2026-08-25T21:00Z | inline | acceptance item 5 (corpus-scale after
  re-measurement, median wall-clock/turns across a comparable batch)
  substituted with per-mechanism live-fire proof (one real nested `claude
  -p` spawn in a throwaway scratch repo, plus live `related_files.py`
  runs against the 5 real before-sample issue numbers) rather than
  spawning 5+ new full `implementation`-role sessions against real
  GitHub issues — that would itself open real duplicate PRs against a
  shared repo, judged outside this delivery session's safe blast radius
  without separate operator authorization.
  canonical: acceptance: live-fire hook-contract commit-shape check + related_files.py live runs (see docs/issue-2409/reports/implementation.md, "After — what was and was not measured") — result: pass, run live that session
  Recorded as an explicit "NOT measured" item and an Open finding with a
  resolution path in `docs/issue-2409/reports/implementation.md`, not
  silently narrowed.
