# issue-2409 — execution-observation deviation log (phase 2)

- 2026-08-25T23:20:00+09:00 inline: the first draft of this round's
  phase-2 record was refused by `record-claim-guard.sh` on the Write
  tool call — one clause carried a bare digit pair with no
  `derived:`/`canonical:` tag nearby (derived: diffed against the
  pasted gate-total breakdown in `docs/issue-2409/reports/
  implementation.md`, untracked in this tree — lives only on branch
  issue-2409/implementation/PR #2416), plus two other references to
  that same untracked path with no nearby marker at all. No content
  reached disk. Fixed by attaching an inline `derived:` tag next to the
  count and adding an "(untracked in this tree — lives only on branch
  issue-2409/implementation/PR #2416)" marker next to every reference
  to that path — same self-caught pattern this round's own phase-1
  entries already recorded for the survey draft.
- 2026-08-25T23:35:00+09:00 inline: a `gh pr edit --body-file` attempt
  for PR #2419 was preceded by a `cat > /tmp/pr2419-body.md << 'EOF'`
  Bash call to stage the body text, refused outright by `board-gate`
  (tokenmaxxxer-core) as an un-analyzable write-capable heredoc shape
  outside this session's provable write-set (issue #225 precedent). No
  file was written by the refused call. Fixed by writing the same body
  text through the Write tool instead (a provably-scoped write) and
  passing that path to `gh pr edit --body-file`, then removing the
  scratch file after the edit succeeded.
