# issue-2409 — execution-observation deviation log (CHANGES round)

- 2026-08-26T11:15:00+09:00 inline: the first draft of the
  "CHANGES round — NR1b fix re-verification" section (added to
  `docs/issue-2409/reports/execution-observation.md`) was refused by
  `record-claim-guard.sh` on the Write tool call — one clause carried
  a bare "1/16 Absent" digit pair with no nearby `derived:`/`canonical:`
  tag, plus two references to paths untracked in this tree
  (`docs/issue-2409/reports/implementation.md` and the new artifact
  path, both live only on branch `issue-2409/implementation`/PR #2416)
  with no marker attached. No content reached disk. Fixed by dropping
  the bare digit pair in favor of a `derived:` tag citing the issue
  thread's own CHANGES-requested comment, and adding the
  untracked-in-this-tree marker next to each new reference — same
  self-caught pattern this branch's phase-1/phase-2 entries already
  recorded.
- 2026-08-26T11:16:00+09:00 inline: an edit to the "Upstream basis"
  section stating which commit each upstream file was read at was
  refused by `record-claim-guard.sh` as an unsupported OUTCOME claim
  (issue #870) — the clause named two commit shas without an
  execution-live `canonical:`/`derived:` tag in the same section. No
  content reached disk. Fixed by rewording to a plain commit-fixed
  citation ("read at commit X and again at commit Y") instead of an
  outcome phrase, which the guard's own commit-fixed-citation escape
  accepts without a live tag.
- 2026-08-26T11:17:00+09:00 inline: an Open-findings entry stating the
  NR1b blind spot in this record's own first pass is "Resolved, not
  open" was refused by `record-claim-guard.sh` on the same OUTCOME-claim
  ground — a resolved/closed claim with no nearby execution-live tag.
  No content reached disk. Fixed by attaching a `derived:` tag pointing
  at the CHANGES-round section's own live `git ls-files` and `diff`
  results (both run this session) as the basis for the resolved claim.
