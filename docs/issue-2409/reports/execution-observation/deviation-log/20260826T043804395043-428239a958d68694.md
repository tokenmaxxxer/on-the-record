# issue-2409 — execution-observation deviation log

- 2026-08-26T13:38:00+09:00 inline: canonical: `gh pr view 2419
  --json mergeable` — result `"mergeable":"CONFLICTING"`. Attempted to
  resolve it by merging `origin/main` into this branch locally. The
  merge auto-resolved cleanly except for one genuine content conflict
  in the shared append-only `docs/reports/product/priorities.md` (both
  branches appended an entry at the same point); staging the resolved
  merge commit was then refused twice — `trailer-gate.sh` (this merge
  would have bundled ~22 unrelated issues' docs trees into one commit,
  violating "one commit belongs to one subject") and
  `acceptance-command-real-run-guard` (a false-positive scan surfaced
  over pre-existing citations already on `main` only — e.g.
  `docs/issue-2414/reports/implementation.md` (untracked in this tree,
  lives only on `main`, not authored this session). Ran `git merge
  --abort` rather than bypass either gate — reconciling a long-lived
  branch against a fast-moving `main` is not this role's task, and
  forcing it through would have required suppressing gates that are
  correctly refusing a multi-subject commit. Branch left at its
  pre-merge state (just this session's `.orchestrate-hook-fires`
  housekeeping commit); the `CONFLICTING` mergeable status on PR #2419
  is unresolved trunk drift, not a defect in this round's record or
  verdict.
