# Deviation log — issue-2414 / execution-observation

- 2026-08-25T12:52:10Z | inline | `gh pr create --body "$(cat <<PRBODY ...)"` was refused by
  `heredoc-command-refusal-gate.sh` (issue #1976) mid-session, requiring a switch to
  `--body-file <path>`; the same gate also refused an earlier `git commit -m "$(cat <<EOF ...)"`
  for this record. Both retried successfully with the non-heredoc shape (two `-m` flags for the
  commit, `--body-file` for the PR) — no scope change, just a mechanical retry.
- 2026-08-25T12:52:10Z | inline | This PR's body includes `Closes #2414`. canonical: `gh issue view
  2414 --json state -q .state` (this session) -- result: `CLOSED`, already superseded by #2415
  before this session started. Included the trailer anyway, diverging from PR #2422's own explicit
  precedent (`gh pr view 2422 --json body -q .body`, this session -- states "This PR does **not**
  carry a `Closes #2414` trailer... closing #2414 is the operator's call") of omitting it on the
  reasoning that closing a superseded issue is the operator's call, not a delivering session's.
  `pr-preflight`'s phase-2 gate mechanically requires a Closes/Fixes/Resolves trailer on every
  phase-2 delivery PR body and refused to open the PR without one, with no carve-out for an
  already-closed target issue. Included the trailer as a no-op (GitHub does not re-close an
  already-closed issue) to satisfy the gate rather than leave the PR unopenable. Not a scope
  change — this record itself, in its own PR description, states the trailer is a no-op included
  only for the gate.
- 2026-08-25T12:52:10Z | inline | canonical: `git branch -a` / `git status` (this session, mid-task)
  -- `gh pr checkout 2422` (used to inspect PR #2422's diff) switched this session's working
  directory to a newly created local branch `issue-2414/implementation` instead of leaving
  `issue-2414/execution-observation` checked out. Caught immediately and corrected with
  `git checkout issue-2414/execution-observation` before any file was written or staged on the
  wrong branch — no commit, edit, or push happened while on the wrong branch.
