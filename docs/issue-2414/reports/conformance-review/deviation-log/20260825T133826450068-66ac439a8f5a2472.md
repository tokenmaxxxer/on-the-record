- 2026-08-25T13:38:26Z inline: PR #2435's body again carries a `Closes #2414` trailer, atypical for a conformance-review-only PR (re-review, no delivery, no code) — same shape as the deviation logged for PR #2426 above. Forced by the repo's `pr-preflight` gate under `CORE_BUILD_NOW=1`, which treats this branch as phase-2-equivalent and refuses a PR body missing the trailer. Issue #2414's state (canonical: `gh issue view 2414` this session, `state: CLOSED`, superseded by #2415) means the trailer is a no-op on GitHub, not a scope change to what actually closed it — noted here rather than silently added.
