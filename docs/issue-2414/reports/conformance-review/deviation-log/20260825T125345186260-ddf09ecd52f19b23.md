- 2026-08-25T12:53:45Z inline: PR body carries a `Closes #2414` trailer, atypical for a conformance-review-only PR (no delivery, no code) — normally the reviewed issue's own implementation PR carries that trailer, not the review. Forced by the repo's `pr-preflight` gate, which under `CORE_BUILD_NOW=1` treats this branch as phase-2-equivalent and refuses a PR body missing the trailer. Issue #2414's state at review time (canonical: `gh issue view 2414` this session, `state: CLOSED`, superseded by #2415) means the trailer is a no-op on GitHub, not a scope change to what actually closed it — noted here rather than silently added.
