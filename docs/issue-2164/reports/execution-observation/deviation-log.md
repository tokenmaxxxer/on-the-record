# Deviation log — issue #2164, role execution-observation

2026-08-24 | filed (reported, not spawned — role session, contract v3 SCOPE-EXCEEDED rule) — the spawning task framed this session's job as directly filling this role's terminal record (its write-scope path, phase 2, not created yet on this branch's git history — no forward reference), but `on-the-record/hooks/approval-gate.sh` denies that Write without a posted `APPROVE issue-2164/execution-observation` comment (none exists) — role-shaped judgment: delivered phase-1 (survey + proposal) instead and stopped, per contract v3 s19's two-session default, rather than bypassing or waiting inline for an approval this headless single-shot turn cannot observe; reported in this session's own reply and in PR #2169's body/commit message.
