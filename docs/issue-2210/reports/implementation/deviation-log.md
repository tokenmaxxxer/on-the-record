# Deviation log — issue-2210 / implementation

- 2026-08-24T00:00:00Z | filed | `heredoc-command-refusal-gate.sh` scans the whole raw Bash command (including heredoc BODY text) with `_COMMIT_RE`/`_GH_WRITE_RE`, sharing the same false-positive-activation risk this issue's fix removed from the four `git commit`-detection gates — a heredoc-appended record that merely mentions "git commit"/"gh issue create" in prose could trigger its refusal message on an innocuous append. Not inline-fixable within #2210's frozen write set: this issue is scoped to latency (that gate's cost is not the same order of magnitude — benchmarked separately, ~140ms even at 40,000 synthetic dash-tokens), and fixing it is a denial-message-correctness question, not a performance one, so it needs its own scoped issue rather than riding in on this one. Already surfaced in docs/issue-2210/reports/implementation.md's Open findings #2; reported, not spawned.
