- 2026-08-27T04:16Z | role=implementation-audit-4d2b6d61 | issue=2579

Deviation from "take the stamp from PR #2584 unchanged": PR #2584 added
`skill_sources` as `write_record_skeleton()`'s 4th positional parameter.
Current `main` had since grown its own 4th parameter (`task_text`,
issue #2575's is-coding detector) that #2584's base predates — reusing
position 4 as instructed would have silently swapped in a skill-sources
list wherever a task-text string was expected at the one real call
site. Added `skill_sources` as a 5th, keyword-only parameter instead
(`spawn.py`'s call site passes `skill_sources=skill_sources`
explicitly), and same for `_stamp_additive_record_fields()`'s 3rd
param, keeping `task_text`'s existing position untouched. Test/code
logic taken from #2584 otherwise verbatim (derived: `gh pr diff 2584`
diffed against the applied change — only the parameter slot differs).
