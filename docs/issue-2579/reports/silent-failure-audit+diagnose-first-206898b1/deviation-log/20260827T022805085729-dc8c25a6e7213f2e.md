2026-08-27T02:28:05Z inline: expected the `<source>:<name>` qualifier syntax to be purely additive inside `resolved_skill_sources()`. Running a real dispatch (`spawn.py --skills skill-repo:diagnose-first ... --issue 2579 --no-wait`, required by the issue's own acceptance text — "must not: substitute --dry-run") surfaced that `spawn.py`'s `if a.skills:` CLI block joined the raw, still-qualified token (colon included) into the git branch name, which git rejects: `브랜치 issue-2579/skill-repo:diagnose-first-5db414e4 로 못 갈아탔다: ... 올바른 브랜치 이름이 아닙니다`. Fixed by extracting `skill_branch_slug()` (skills.py:220-227, stripping the qualifier before joining) and switching `spawn.py`'s slug construction to call it; re-ran the identical dispatch clean afterward. Left an unreachable workspace directory with a colon in its path from the failed attempt, removed via `rm -rf` after confirming no process referenced it. Full quotes in docs/issue-2579/reports/silent-failure-audit+diagnose-first-206898b1.md's "What did not work" section.
