# Deviation log — issue-2180 / conformance-review

- 2026-08-24T19:52:00+09:00, inline. canonical: docs/issue-2180/reports/conformance-review/2026-08-24-hunt-2026-08-24-conformance-review-issue-2180.md (the after-proposal warrant-hunter dispatch, stance 0 — "assume the gate/verification-discipline this transition just exercised is bypassable"). The first-drafted proposal Constraints wording said the survey's evidence was "verified against gates/record_lint.py's own check functions" — true of the individual functions this session called directly, but phrased ambiguously enough that a reader could take it as "record_lint.py's CLI/lint_record() path accepted the file," which is false: that CLI path's RECORD_PATH regex does not recognize a nested reports/<role>/survey.md path at all. Reworded in the same commit, before landing, to name the exact functions called and to state the CLI-wrapper gap explicitly. docs/issue-2180/proposals/2026-08-24-conformance-review-issue-2180.md (Constraints section).

- 2026-08-24T19:58:00+09:00, inline, unresolved at session end. canonical: this session's own repeated `gh pr create` denials (pasted verbatim in docs/issue-2180/reports/conformance-review/survey.md Sec9). Expected: committing and pushing the phase-1 survey and proposal, per the role-handoff contract's own default flow, would be immediately followed by opening the phase-1 PR. Actual: `pr-preflight.sh`'s amendments-reconciled check hardcodes its target to this role's phase-2 record file regardless of the phase1/phase2 distinction it itself computes, and that same file is what `approval-gate.sh` refuses to let this session write before a human Approve — a post-spawn issue comment (here, one entirely about a different role's own PR) makes phase-1 PR creation unsatisfiable through any legitimate write this session can make. Reconciled the actual issue-thread content in the survey (Sec9) as the closest legitimate substitute; logged as an open finding (survey Sec8's second bullet) rather than routed around via a different `gh` invocation or a write to the gated path. PR creation itself remains blocked at session end — reported to the user directly.

- 2026-08-24T20:31:57+09:00, inline (phase-2 session). canonical: this session's own diff of `gates/record_lint.py` against `on-the-record/gates/record_lint.py` (executed-unit, not re-pasted here). Expected: the record-claim-guard checks this phase-2 session validated its draft against ahead of writing (the same repo-root `gates/record_lint.py` functions the phase-1 proposal's Constraints section names) would match what `on-the-record/hooks/record-claim-guard.sh`'s PreToolUse hook actually runs at write time. Actual: the hook imports a materially older, stricter copy vendored at `on-the-record/gates/record_lint.py` — missing the newer version's word-sense exemptions for a literal section-title mention and for the noun sense of a certain outcome-marker word, and using an evidence-adjacency window that only looks *above* a claim line (`lines[i-3:i+1]`), not the newer symmetric above-and-below window. Three `outcome_claim_citation_check` refusals resulted from two false-positive-triggering phrases in the record's first draft:

  > "... in the fenced run above (\"What was done\")."
  > "None — both independent re-verification passes this review performed"

  Reworded the affected record prose (dropped the quoted section-title references, replaced the flagged noun-sense word with "runs") and re-validated locally against the actual vendored copy at `on-the-record/gates/record_lint.py` before the write succeeded, rather than continuing to validate only against the newer repo-root copy the proposal's own Constraints section cited. `docs/issue-2180/reports/conformance-review.md` (the record itself, final wording).

- 2026-08-24T20:31:57+09:00, inline (phase-2 session). canonical: `git merge-base --is-ancestor 3271d8f8 origin/main` (this session, returned false) and `git log origin/main -1 --oneline -- on-the-record/monitors/poll-heartbeat.sh` (this session, returned `abdb5ac0 issue-2180: distinct new-returned-pr signal, stop repeating already-surfaced returned-pr lines (#2181)`), both executed-unit and not re-pasted here. The approved phase-1 proposal's "What will be done" step 1 said evidence citations would carry the `3271d8f8`/`f33a7a62` commit shas from the pre-merge `issue-2180/implementation` branch. Between phase-1 approval and phase-2 execution, PR #2181 was squash-merged to `main` as a new sha, `abdb5ac0` — the pre-merge shas never landed in `main`'s own history under that name. Re-pinned every evidence citation in `docs/issue-2180/reports/conformance-review.md` to `abdb5ac0` instead (the sha actually reachable from `origin/main`), and re-ran every independent-verification command a second time against it, rather than citing shas a reader could not `git show` from `main`. Stated in the record's own "Why" section and in the `conformance-review-traceability-and-evidence` skill-verdict line as superseding the proposal's stated shas.
