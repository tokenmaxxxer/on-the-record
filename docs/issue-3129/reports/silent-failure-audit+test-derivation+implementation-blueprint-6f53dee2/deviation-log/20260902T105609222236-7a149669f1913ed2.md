- 2026-09-02T10:56:09Z, inline, spawn instructions said to "reuse their slug resolution" (PR #3084/#3106's `plumbing._repo_slug()`) but that helper is network-based (`gh repo view`) and this module runs fresh on every `PostToolUse` call with no cross-call cache — reusing it verbatim would reopen the exact `gh`-polling-from-`PostToolUse` must-not issue #3129 itself forbids. Read the instruction as "reuse the owner/repo identity scheme," not the specific resolver, and implemented a local-only `repo_slug_for_cwd()` (`git remote get-url origin`) instead. Location: on-the-record/hooks/amendment_channel.py (commit 7d951975 on branch issue-3129/implementation-blueprint+silent-failure-audit+test-derivation-a641f019); rationale also recorded in this session's own record's "Why" section.
- 2026-09-02T10:56:09Z, inline, board-gate refused writing this session's own record file from branch a641f019 (where the code repair landed) — writes under docs/issue-3129/ require this session's own assigned branch (issue-3129/silent-failure-audit+test-derivation+implementation-blueprint-6f53dee2) unless the issue declares a `maintenance-targets:` entry, which issue #3129 does not. Switched to this session's own branch to write and land the record (as a separate PR, #3155), keeping every code commit on a641f019 where the spawn instructions explicitly directed them, and citing all code facts in the record by commit sha so they stay reproducible regardless of which branch's working tree the record itself lives on. Location: docs/issue-3129/reports/silent-failure-audit+test-derivation+implementation-blueprint-6f53dee2.md.
