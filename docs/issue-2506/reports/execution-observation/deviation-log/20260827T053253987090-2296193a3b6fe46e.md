- 2026-08-27T05:33Z inline: accidental `git push origin HEAD:main` against the real remote during independent scratch-repo verification of acceptance bullet 1 (caused by a `cd` failure in a non-`&&`-chained Bash block leaving cwd on this repo); git rejected it as non-fast-forward, confirmed no-op via `git ls-remote origin main`, no file/write-set change needed — full incident already disclosed in docs/issue-2506/reports/execution-observation.md ## What did not work.
