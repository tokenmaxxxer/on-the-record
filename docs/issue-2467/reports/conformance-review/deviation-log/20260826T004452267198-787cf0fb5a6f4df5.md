# Deviation log — issue #2467 (conformance-review role)

- 2026-08-26T00:44:52Z | inline | issue-2467/conformance-review |
  the record's `Write` was refused twice by this session's own
  `record-claim-guard` (issue #333/#793/#870) before it landed. First
  attempt used generic frontmatter (no `type:`/`code_under_review:`/
  `breaking:`/`verdict:` fields) and free-floating prose claims with no
  `canonical:`/`derived:` tags near several outcome/count assertions, and
  cited two paths — `docs/issue-2467/reports/implementation.md` and its
  hunt record — that are untracked in this worktree (they live only on
  branch `issue-2467/implementation`, not on `issue-2467/conformance-review`),
  which the guard's issue #330 rule refused as unreachable. Resolved by:
  (1) matching this repo's own prior working template
  (`docs/issue-2463/reports/conformance-review.md`) for the frontmatter
  shape; (2) running `git worktree add --detach /tmp/review-2467
  origin/issue-2467/implementation` so every cross-branch citation pins to
  a real, currently-checked-out path (`2c86c1e0:<path>`) instead of a
  path untracked in this worktree; (3) adding an explicit
  `canonical:`/`derived:` tag next to every remaining outcome or count
  claim the guard flagged, rather than relying on a "see above"
  cross-reference. No content of the review's findings changed between
  attempts — only how the evidence was tagged and pinned.
  canonical: this session's own transcript — two `PreToolUse:Write`
  hook-error blocks quoting `record-claim-guard`'s exact flagged lines,
  immediately followed by the successful third `Write` call.
- 2026-08-26T00:44:52Z | inline | issue-2467/conformance-review |
  the first `git commit ... && git push ... && gh pr create --body
  "$(cat <<EOF ... EOF)"` compound command was refused pre-execution by
  this session's `heredoc-command-refusal-gate` (issue #1976) — the same
  refusal class the PR #2469 deviation log recorded for the prior
  session. Resolved the same way: wrote the PR body to a plain file
  (`.pr-body-2467.md` via the `Write` tool) and passed it with `gh pr
  create --body-file`, then deleted the scratch file after the PR opened
  successfully. No commit ran before the fix — the whole compound command
  was refused as one unit, so nothing needed to be undone.
  canonical: this session's own transcript — the `PreToolUse:Bash`
  hook-error block quoting `heredoc-command-refusal-gate`, followed by
  `git log --oneline -3` confirming no partial commit landed, followed by
  the successful retry (`git log` showing commit `b42fef96`, PR
  https://github.com/tokenmaxxxer/on-the-record/pull/2475).
