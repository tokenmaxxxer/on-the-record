# Deviation log — issue #2467 (execution-observation role)

- 2026-08-26T00:42:00Z | inline | issue-2467/execution-observation |
  landing-time `heredoc-command-refusal-gate` (issue #1976) refused the
  first `gh pr create --body "$(cat <<'EOF' ... EOF)"` compound command
  (push half included) as an un-analyzable heredoc-shaped `--body`.
  Resolved by writing the PR body to a scratch file
  (`.pr-body-2469-eo.md`, removed after use) and re-running with
  `--body-file` instead — no content change to the body text itself, and
  `git push` (which had not yet run inside the refused compound command)
  was re-run together with the corrected `gh pr create` call.
  canonical: the `PreToolUse:Bash` hook-error transcript naming
  `heredoc-command-refusal-gate` is in this session's own transcript; the
  retried command succeeded, landing PR
  https://github.com/tokenmaxxxer/on-the-record/pull/2473.
