## Deviation: filed a phase-1 proposal instead of filling the pre-written record skeleton directly

The spawning prompt stated the record skeleton at
`docs/issue-2507/reports/execution-observation.md` was pre-written and
instructed filling in its sections directly, framing this as the
session's deliverable. derived: `gh pr list --search "execution-observation"
--state all` — result: every precedent execution-observation PR found
(#2521, #2518, #2499, #2249, #2524) delivered as a single PR with no
separate phase-1 proposal, reinforcing that framing.

Checked live instead of assuming it: `printenv CORE_BUILD_NOW` and
`printenv CORE_CHECKPOINT` both returned nothing (exit 1) in this
session's own environment — neither the build-now bypass (contract v3
s19a) nor checkpoint mode (contract v3 s19, on-the-record #2129) was
spawner-authorized here, unlike PR #2524's session — canonical: `gh pr
view 2524 --json commits` — result: its own commit trailer reads
"Proposal: build-now bypass (CORE_BUILD_NOW=1, contract v3 s19a)". A live
`Edit` probe on the record skeleton's "What was done" section was then
mechanically denied by `approval-gate.sh` — canonical: the tool-error
text returned to this session verbatim: "neither the PR for
issue-2507/execution-observation nor issue #2507 carries an approval
from a listed human approver (jiwonjung94, jjongkwann)... phase 2 waits
for the human. (contract v3 s19)".

Given both signals agreed, filling the record this session would have
been an unauthorized phase-2 write the gate does not permit, and would
have required self-granting a bypass the contract explicitly reserves to
the spawner. Scope narrowed instead to: write a phase-1 proposal
(`docs/issue-2507/proposals/2026-08-26-execution-observation-verification-plan.md`)
stating the phase-2 verification plan, commit, open PR #2533 referencing
#2507 as a plain reference (no Closes trailer), and stop per the default
two-session flow — leaving the record skeleton itself untouched and
uncommitted for the later, human-approved phase-2 session to fill.
