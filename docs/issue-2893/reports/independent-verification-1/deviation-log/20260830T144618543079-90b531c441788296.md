2026-08-30T14:46 inline: ran `git checkout issue-2893/diagnose-first-6a58e6a9 -- .` while inspecting the subject PR, which staged that PR's own code files (directive_assembly.py, gates/record_lint.py, on-the-record/hooks/skill-verdict-guard.sh, etc.) into this verification branch's working tree/index. Recognized before commit via `git status --short`; restored the 6 non-record files with `git restore --source=HEAD --staged --worktree`, and committed only docs/issue-2893/reports/independent-verification-1.md via a pathspec-limited `git commit -- <path>` (the 7th file, another author's record, was left staged/untouched since board-gate refuses any git command referencing a foreign-authored record path).
