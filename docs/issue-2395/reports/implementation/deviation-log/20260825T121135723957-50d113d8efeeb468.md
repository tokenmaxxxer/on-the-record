# Deviation log — issue #2395 (implementation role)

- 2026-08-25T21:06:00Z | inline | issue-2395/implementation |
  mid-rebase (`git rebase origin/main`), a subsequent `grep`/`du` call
  failed with the shared host's `/` filesystem at 100% (37M free of
  916G) — `git`, `du`, and even plain tool-output writes started
  erroring with ENOSPC. Not called for by this task's scope (a
  gate-ordering fix + rebase), and required a judgment call outside the
  frozen write set: identify and remove disposable scratch data without
  touching other sessions' active work on this shared machine. Resolved
  by deleting only my own `/tmp` scratch clones/workdirs from this
  session's own live-demo evidence-gathering (`/tmp/issue2395-workdir`,
  `/tmp/issue2395-v3-workdir`, `/tmp/issue2395-reuse-workdir`,
  `/tmp/eo2395-workdir`, `/tmp/eo2395-workdir-before`,
  `/tmp/eo2395-workdir2`) — enough free space returned (`git`/`grep`
  succeeded again immediately after) to finish the rebase; no other
  path under `/tmp` or elsewhere was touched. Not filed as a new issue:
  a one-off unblock action, not a design/architecture/security/product
  judgment call, and this session did not investigate root cause (which
  other sessions/processes are filling the shared disk) — that remains
  open for whoever next hits it, worth flagging if it recurs.
  canonical: `df -h /tmp` (this session, before cleanup) — `/dev/nvme0n1p2  916G  869G   37M  100% /`; `rm -rf` of the six paths listed above (this session); `git rebase --continue` and subsequent `grep`/`gh`/`pytest` calls succeeded immediately after with no further ENOSPC errors.
