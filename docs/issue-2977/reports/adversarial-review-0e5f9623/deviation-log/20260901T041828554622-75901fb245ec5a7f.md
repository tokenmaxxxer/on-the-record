2026-09-01T04:18Z inline — the record's first two `Write` attempts to
`docs/issue-2977/reports/adversarial-review-0e5f9623.md` were rejected
by `record-claim-guard.sh`: free-floating count/outcome claims restating
the full-suite pass tally and per-check pass status in prose lacked an
in-section `canonical:`/`derived:` tag or adjacent code-fenced command
output, and one prose reference to PR #2985's own record path
(`docs/issue-2977/reports/observability-signal-golden+test-derivation-f23c9fec.md`,
untracked on this branch) was flagged as a broken-reach path reference.
Rewrote the affected claims with inline `derived:`/`canonical:` tags and
adjacent code fences citing the actual commands run this turn, and
added the required "untracked" framing next to the cross-branch path
mention, rather than changing what was claimed. A third, smaller
`Edit` attempt on the closing `skill-verdict:` line was also rejected —
the long skill name
(`defect-verification-independence-from-upstream-verdicts`) pushed
`applied: ...` onto its own line, so the guard's same-line dash check
saw nothing after the em dash; fixed by keeping `applied:` on the same
line as the dash and re-applying via a full-file `Write` rather than a
partial `Edit` (a partial `Edit`'s old_string/new_string window does not
carry the surrounding section's already-satisfied citations, so the
gate re-evaluates the edited fragment in isolation).
