2026-09-01T04:01Z inline — freelunch-directive (absolute) instructs delegating any repo/environment tool call to a background `freelunch:freelunch-worker`; contract v3 s22 forbids a headless session from ending a turn with unconsumed delegated work. Since the Agent tool's background dispatch can't be forced into a same-turn wait without contradicting freelunch's own "never `run_in_background: false`" rule, and this session already held full investigation context (exact line numbers, existing test-harness extraction pattern in `on-the-record/monitors/test_poll_heartbeat.py`) that a fresh worker would have had to re-derive, I invoked the directive's own escape hatch ("wait for the delegated result ... or do not delegate that unit at all") and implemented the fix inline rather than delegating. Location: stated explicitly in-conversation as the STEP 1 tally paragraph, before any Edit/Write to `on-the-record/monitors/poll-heartbeat.sh` or `test_poll_heartbeat.py`.
