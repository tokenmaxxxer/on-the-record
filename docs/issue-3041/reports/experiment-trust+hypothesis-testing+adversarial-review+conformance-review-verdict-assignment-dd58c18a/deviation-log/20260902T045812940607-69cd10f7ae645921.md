- 2026-09-02T04:58:12Z, inline, `git archive pr-3052 -- ...` was used for the first read of `scripts/issue-3041/run_pair.sh`'s file permissions and did not preserve the executable bit, producing a result that looked like it might be a checkout artifact rather than a real defect — replaced with `git ls-tree pr-3052 scripts/issue-3041/run_pair.sh` (reads the tree object's mode directly) plus a `git worktree add --detach` checkout re-run of the literal `test -x` acceptance check, both confirming mode `100644` independently of checkout method, before reporting the non-executable finding in the record.
