- 2026-09-02T01:00:00Z, inline, evaluate_pair.py's greedy `re.search(r"\{.*\}", ..., re.DOTALL)` JSON extraction crashed on pair 04's evaluator response — replaced with a balanced-brace scan (`_extract_json_object`) in `scripts/issue-3041/evaluate_pair.py`, re-ran pair 04's evaluation.
- 2026-09-02T01:05:00Z, inline, a `claude -p` smoke test outside this repo surfaced this repo's own operator hooks (Stop-hook text) leaking into the subprocess via user-level settings — added `--setting-sources project,local` to every harness/evaluator invocation in `scripts/issue-3041/run_pair.sh` and `evaluate_pair.py` before running the real pairs.
- 2026-09-02T01:55:00Z, inline, the `product-discovery-hypothesis-preregistration` skill-verdict line's `applied: invoked;` marker was split across a markdown line-wrap, tripping skill-verdict-guard's same-line check — rejoined onto one line in the record.
