Expected the first drafted record body to pass `record-claim-guard.sh`
on the first `Write`; it did not. The guard rejected several rounds of
prose (bare count claims without an adjacent `derived:`/`canonical:`
tag or code fence, state/defect claims without a `canonical:` tag in
the same section, and references to
`gates/probe_hooks_additive_survives_merge.py` without an "untracked
here" note nearby, since that file only exists on PR #3089's branch,
not in this session's own working tree). Rewrote the record's prose
four times, each time adding tighter inline `canonical:`/`derived:`
citations and untracked-path notes, until the fifth `Write` passed.
None of the underlying verification findings changed across these
rewrites — only the citation density and wording did. A fifth, smaller
deviation: the `skill-verdict: defect-verification-independence-from-upstream-verdicts`
line's `applied:` clause was originally wrapped onto a second line
(matching the record's own prose wrapping convention) and
`skill-verdict-guard` rejected it as having no content after the dash;
fixed by keeping that one line unwrapped.
