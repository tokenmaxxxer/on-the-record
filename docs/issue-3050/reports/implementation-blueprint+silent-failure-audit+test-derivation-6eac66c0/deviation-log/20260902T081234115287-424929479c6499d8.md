---
issue: 3050
role: implementation-blueprint+silent-failure-audit+test-derivation-6eac66c0
---

# Deviation: repair-round code + record could not land on the same PR/branch as instructed

The spawning task instructed working on the existing branch
`issue-3050/implementation-blueprint+silent-failure-audit+test-derivation-150a8ac4`
(PR #3086) and committing both code and this session's own record there.
`board-gate.sh` refused every write under `docs/issue-3050/` while
checked out on that branch, naming this session's own branch
(`...-6eac66c0`, from `CLAUDE_SKILL`) as the only one it would accept —
independent of which git branch was actually checked out. Not called for
by the task's literal instruction (which assumed both writes would land
in the same place), and required a judgment call: this is precisely the
structural ownership boundary issue #3050 itself is about, so working
around it (e.g. forcing the sidecar/branch to disagree, or retrying) was
not appropriate — the correct move was to respect the boundary the same
way PR #3054's session did on the structurally identical block against
PR #2872.

Resolved: the code fix (spawn.py, tests/test_failed_no_commit_reconcile.py,
docs/handbooks/record-contract.md, docs/handbooks/record-authoring.md —
none of them under `docs/issue-3050/`) landed as commit `80ff89f8`,
pushed directly onto PR #3086's own branch, continuing that PR as
instructed. This session's own record could not follow it there, so it
was written at this session's own canonical path and delivered as a
separate PR, #3117 (`Advances #3050`), which references the code commit
by sha rather than duplicating it. Not spawned or filed as a new issue —
a role session reports a scope-exceeded-shaped block rather than
spawning a peer (role-handoff contract v3's SCOPE-EXCEEDED RULE); this
is that report. Fully detailed with the exact refusal transcript in the
delivered record's own "Why" section
(`docs/issue-3050/reports/implementation-blueprint+silent-failure-audit+test-derivation-6eac66c0.md`,
"Deviation -- this record could not land on PR #3086's own branch").

canonical: the `board-gate.sh` PreToolUse hook error from this session's
own transcript (attempted `Edit` on PR #3086's own record while checked
out on its branch) — quoted verbatim in the delivered record's "Why"
section; `gh pr view 3086 --json headRefOid` / `gh pr view 3117
--json headRefOid` after delivery confirm the split (`80ff89f8` on PR
#3086, `50356045` — this record's own commit — on PR #3117).
