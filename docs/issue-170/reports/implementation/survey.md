# Survey — current state for issue-170

## Write surfaces this issue touches
- `roles/*.json` — 17 files exist today (original 9 + the 8 round-4 roles landed by issue-167). Generic-loaded by `spawn.py:role_settings()` (spawn.py:330) and `gates/gates.py` via filename match; no code hardcodes role names beyond `spawn.py`'s `ROLES` display tuple (spawn.py:646-651, currently 17 names, asserted `len==17` at test_gates.py:216). Schema confirmed from `roles/capacity-planning.json` (issue-167 output): `marketplace`/`repo`/`path`/`sandbox`/`decides`/`use_when`/`produces`/`write_scope`/`record_fields.loop_state`.
- `spawn.py`'s `ROLES` tuple — issue-167 explicitly decided **not** to extend it for the 8 round-4 roles, reasoning it is board *display order* for the original 9-role sequential lifecycle chain, not "all known roles" (see issue-167's proposal §2). The current tuple (spawn.py:646-651) still lists only those original 17 names — confirming that decision was carried through, not reverted.
- `docs/issue-160/proposals/role-taxonomy.md` — the merged canon this issue executes. The "26 promoted roles (round 3)" table (role-taxonomy.md:51-80) carries `decides`/`use_when`/`produces`(required fields)/`write_scope`/hand-off for all 26 target roles verbatim — no re-derivation needed. None of the 26 has non-empty `write_scope` except `ux-engineering`/`api-design`/`architecture`/`data-modeling`/`technical-writing`/`brand-design`/`devrel`, each carrying a path placeholder marked "(TBD at execution)" or a fixed `docs/**`/`src/**` scope in the canon table.
- Rulebook skeleton exemplar: this repo's own `docs/issue-167/_assets/rulebook-skeleton/<role>/**` (8 roles, 80 files, landed in issue-167's phase 2) is now the closer, in-repo exemplar to adapt from — supersedes issue-167's own exemplar source (`implementation-rulebook` local checkout) as the pattern reference, since it already shows the adapted (not cloned) shape: `<role>/.claude-plugin/{marketplace.json,plugin.json}`, `<role>/agents/warrant-hunter.md`, `<role>/hooks/{directive.sh,handbook-trigger-gate.sh,hooks.json,record-fields-gate.sh,trailer-gate.sh}`, `<role>/README.md`, `<role>/docs/specs/approvers.md`.
- `docs/issue-162/proposals/role-rename-execution-1.md` and issue-167's own precedent — both confirm GitHub repo creation/push is outside a role-session's write authority; deliverable stays a human command list, never executed by this role.

## Unknowns / gaps found
- No repo-root `marketplace.json` in this repo (confirmed absent by directory listing) — unchanged from issue-167's finding; each of the 26 new rulebook repos carries its own marketplace.json inside its own future repo, out of this repo's write scope.
- `ROLES` tuple membership for the 26 new roles: same live design question issue-167 already resolved one way (do not extend) for the 8 round-4 roles. This issue's 26 roles are domain roles even more clearly orthogonal to the original 9-role sequential chain (market-analysis, pricing, sales, brand-design, etc. have no place in a lifecycle-stage board render) — the issue-167 precedent applies with more force here, not less. Proposal below carries that decision forward rather than re-opening it.
- Several of the 26 roles' `write_scope` in canon carries a placeholder ("TBD at execution", e.g. `ux-engineering`, `api-design`) rather than a concrete path list — canon defers that decision to the executing issue. This issue is that executing issue; the proposal below resolves each placeholder to a concrete scope or explicitly keeps it `[]` pending a later issue, rather than copying the placeholder string into a `roles/*.json` file (which would break `board-gate.sh`'s path-matching contract).
- Rulebook repos for the 26 new roles do not exist yet on GitHub (unverified by network call in this session — no `gh api` call was made to check; not needed since repo creation is out of scope regardless of current existence) and cannot be created by this role.
- Issue #170's own text allows splitting phase-2 delivery into 2-3 PRs given output scale (26 × ~10 files = ~260 skeleton files + 26 roles/*.json + `ROLES`/test edits) — a scale decision the proposal below must make explicit, per the issue's own instruction to state each PR's scope in the proposal if split.

## Skip record — web scout
This pass skips the `scout-directive`'s web sweep. Reason: the issue is a mechanical execution of an already-approved canon (issue-160's merged proposal fixes every `decides`/`use_when`/`produces`/`write_scope`/hand-off value verbatim for these exact 26 roles) plus an adaptation of an existing **in-repo** exemplar (issue-167's own landed rulebook skeleton, one step closer to this issue's needed shape than an external product). No product-facing decision here would be informed by an external category sweep — the one open design question found (`ROLES` tuple membership) is answered by a precedent already set inside this same codebase (issue-167), not by external practice. Recorded explicitly per the mandatory skip-record rule.
