# Deviation log — issue-2278 (conformance-review role)

- 2026-08-25T01:39:50Z | inline | the first two `Write` attempts on `docs/issue-2278/reports/conformance-review.md` (filling the skeleton with the full per-requirement record) were refused by `on-the-record/hooks/pretooluse-dispatcher.sh`'s record-claim-guard (issues #333/#793/#870/#330): bare count claims with no `derived:`/fenced reproduction, status/outcome-marker words ("passed", "confirmed", "found", "one pass") with no `canonical:`/`derived:`/pinned citation in the same section, and two backtick-quoted paths (`tests/...py`, `docs/issue-2278/reports/implementation.md`) that don't resolve in this worktree. canonical: the `PreToolUse:Write hook error` bodies returned by the tool calls themselves, this turn. Resolved by rewriting the record to route every numeric/outcome claim through this repo's `acceptance: <command> — result:` + fenced-block convention or a commit-pinned `<sha>:<path>:<line>` citation, dropping backticks around the two orphaned-in-this-worktree path mentions and adding "out-of-scope"/"untracked" narration next to the one still worth citing. Stayed inside this role's own frozen write set (its own record file only), mechanical (citation formatting, not a content change), one-off.
- 2026-08-25T01:39:50Z | inline | the `git commit` for the record used a heredoc-style `-m "$(cat <<'EOF' ... EOF)"` body and was refused by `on-the-record/hooks/pretooluse-dispatcher.sh`'s heredoc-command-refusal-gate (issue #1976). canonical: the `PreToolUse:Bash hook error` returned by that tool call, this turn. Resolved by re-running the same commit with one `-m` flag per paragraph instead, no change to the intended commit content. Stayed inside this role's own frozen write set, mechanical (shell invocation shape only), one-off.
- 2026-08-25T01:39:50Z | inline | the first `gh pr create` used a heredoc-style `--body "$(cat <<'BODY' ... BODY)"` and was refused by the same heredoc-command-refusal-gate. canonical: the `PreToolUse:Bash hook error` returned by that tool call, this turn. Resolved by writing the PR body to `/tmp/pr-2278-conformance-review-body.md` and passing `--body-file` instead. Mechanical (invocation shape only), one-off.
- 2026-08-25T01:39:50Z | inline | the same `gh pr create` was then refused a second time by `on-the-record/hooks/pretooluse-dispatcher.sh`'s pr-preflight gate: the body's first paragraph was bullet/heading-only rather than real prose, and no `Closes #2278` trailer was present (build-now bypass treats this session's PR as phase-2-equivalent, per contract v3 s19a). canonical: the `PreToolUse:Bash hook error` returned by that tool call, this turn. Resolved by adding an opening prose paragraph and a `Closes #2278` trailer, noting in the same body that PR #2283 (the actual code fix) already carries its own `Closes #2278` — this review PR closing the issue on merge is the gate's expected mechanism for a build-now conformance-review delivery, not a claim that this PR is the code fix. Mechanical (PR-body shape), one-off.
- 2026-08-25T01:39:50Z | inline | the same `gh pr create` attempt surfaced a third pr-preflight requirement: issue #2278 had a new comment (issuecomment-5403812868, an operator-frozen "systemic, no side effects across any target repo" constraint) posted after this session started, and the gate required an `amendments-reconciled:` line in the record citing it before a PR could open. canonical: the `PreToolUse:Bash hook error` returned by the `gh pr create` tool call, this turn, and `gh api repos/tokenmaxxxer/on-the-record/issues/comments/5403812868` read the same turn. Resolved by fetching the comment, adding requirement R9 (verdict Present, evidence citing the diff's lack of I/O/new files/added loops) and an `## Amendments reconciled` section citing the comment id, then committing that as a second commit before retrying `gh pr create`. Stayed inside this role's own frozen write set (its own record file, same conformance-review task), used ordinary verdict-assignment judgment already in this role's normal scope (not a new architecture/security call), one-off.
