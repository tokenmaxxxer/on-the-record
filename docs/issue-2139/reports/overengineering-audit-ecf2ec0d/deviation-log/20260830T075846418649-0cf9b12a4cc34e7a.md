---
issue: 2139
role: overengineering-audit-ecf2ec0d
---

- Expected a delegated `freelunch:freelunch-worker` asked to run a research
  sweep (grep/read across the codebase for relic findings) to do that work
  directly and return findings as one worker. What actually happened: the
  worker fanned out into 10 of its own parallel sub-agents without being
  asked to, so this orchestrating session had to wait for and synthesize
  all 10 sub-agents' outputs itself instead of one worker's. No deliverable
  content was wrong as a result — cost was wall-clock/tokens, not
  correctness — so classified **inline-fix**: stayed inside this task's own
  frozen write set (no code changed, no separate issue needed), mechanical
  (wait + synthesize), didn't change what the deliverable claims, and is a
  one-off observed this session rather than a re-tested systemic pattern.
  Resolved by waiting for and synthesizing all 10 sub-agent reports as
  input to the audit, same as if one worker had returned them.
- Expected filing 3 higher-stakes findings as new GitHub issues (per issue
  #2139's own acceptance criterion, "findings with live harm ... split into
  fix issues") to succeed via `gh issue create`. What actually happened:
  this repo's `gh-guard` hook refused every attempt — "refused for role
  session 'overengineering-audit-ecf2ec0d': issues are the user's
  requirement backlog, user-authored only (contract v3 s9)" — same
  contract-v3-s9 shape already on record for other role sessions (see this
  file's `docs/reports/deviation-log.md` issue-2286 entries above sharding).
  Classified **file-as-issue**, but per the role-session variant of the
  deviation loop (`docs/handbooks/deviation-loop.md`, "Role sessions"), a
  role session never opens an issue on its own initiative mid-task — it
  finishes what its frozen write set covers and reports rather than files.
  Resolved by placing the 3 write-ups in this PR's own record
  (`docs/issue-2139/reports/overengineering-audit-ecf2ec0d.md`) and in PR
  #2869's description instead, deferred to the user to file under their own
  GitHub account.
