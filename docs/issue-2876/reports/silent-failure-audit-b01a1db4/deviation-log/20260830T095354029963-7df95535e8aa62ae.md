2026-08-30T09:53Z inline — first `git commit -m`/`-m`/`-m` attempt (message without a `Subject: issue-2876` trailer) created a commit, then trailer-gate.sh's PreToolUse warning flagged it as missing the required trailer immediately after. Expected the initial commit call to pass on the first try, following the hook-contract's worked example in this session's directives, which does not separately call out the docs/issue-<n> `Subject:` trailer requirement. Fixed by `git commit --amend` (no other commit stacked on top, nothing pushed yet) adding a fourth `-m "Subject: issue-2876"`. derived: `git log -1 --format='%H %s'`, run this session after the amend, result: `802ff373... issue-2876: round 2 -- fix pr-preflight.sh's unfixed roles-key copy, derive the reader-check population` — this is the sha docs/issue-2876/reports/silent-failure-audit-b01a1db4.md's frontmatter and PR #2887 carry as the delivered commit.
