---
issue: 2755
role: test-authoring-isolation-and-fixture-strategy+adversarial-review-40c15437
---

# Deviation: delegated freelunch-worker did not deliver the requested report; work done inline instead

Per the freelunch-directive's mandatory delegation rule, dispatched one
foreground `freelunch:freelunch-worker` (run_in_background: false) to own
the entire technical unit: apply the fix, run the crash-hook/restore
proof, sweep the repo, and check the four invariants — with instructions
to return a structured raw report.

canonical: the worker's own final Agent-tool result, verbatim: "Good, no
stray background processes. Now waiting for the Explore sweep agent's
completion notification." — a two-sentence stub, not the requested
structured report (diff plus command output plus sweep output plus
invariant checks).

derived: `git diff -- test/test_upstream_defect_scope_guard_cross_repo_cwd.py`
run immediately after the worker returned showed the fix already applied
correctly across the named call sites (one `_assert_denied_for_documented_reason(self, r)`
call added per site) — so step A was done, but the worker's own report
text never said so or showed it.

canonical: a separate `<task-notification>` (task-id `a3cdfc0ad2a5555af`,
summary "Agent 'Sweep test files for bare returncode==2 checks'
finished") arrived to this session directly, not folded into the
worker's own final answer — this is the worker's internally-spawned
Explore subagent for the sweep step, whose finding of additional
same-shape instances elsewhere turned out accurate on independent
re-check, but which the outer worker's own return did not report,
consume, or acknowledge before exiting.

Rather than re-dispatch a second worker for the same unit (forbidden by
the freelunch-directive's NEVER clause: "two workers on one unit"), the
remaining steps were done directly in this session via Bash instead of
through the worker: the crash-hook swap/restore proof
(`derived: python3 -m pytest test/test_upstream_defect_scope_guard_cross_repo_cwd.py -v`,
full output quoted in the main record under the Acceptance-check
sections), the sweep re-verification
(`derived: grep -rn "returncode, 2" test/ | wc -l`, cross-checked against
the Explore subagent's per-file breakdown by reading each flagged file
directly), and the four invariant checks (each with its own
`derived:`/`canonical:` tag in the main record).

No assertion was weakened and no scope was cut as a result — the
acceptance-criteria proof and the sweep were still produced in full in
`docs/issue-2755/reports/test-authoring-isolation-and-fixture-strategy+adversarial-review-40c15437.md`,
just via direct execution instead of the intended single-delegate
hand-off.
