20260827T010229Z | inline | Background-fork consult dispatch (spawn.py, `consult` CLI): first version had the child `print(json.dumps(verdict, ...))` to stdout redirected into a log file, then call `os._exit(0)` right after — expected the judgment JSON to land in the log alongside the timing line. It did not: `os._exit()` skips interpreter cleanup, and stdout is block-buffered once redirected to a regular file, so the buffered print never reached the fd. Found via a real live background run (`runs/consult-logs/20260827T004923881354-2559709.log` held only the timing line). Fixed same-session before delivery by adding explicit `sys.stdout.flush()`/`sys.stderr.flush()` before `os._exit(0)`; a repeat run then carried both lines (quoted in full in docs/issue-2569/reports/implementation.md, "What did not work"). No peer role spawned (build-now single-session). Location: spawn.py (`consult` CLI dispatch, background-fork branch).
