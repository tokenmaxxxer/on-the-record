2026-08-30T16:31:00+09:00 | inline | expected the per-item SIGALRM timeout (otr_lint_test_timeout_plugin.py) alone to bound the traced episode's sleeping test file end-to-end; it did not.
derived: 3 consecutive real `bash on-the-record/hooks/lint-test-on-edit.sh post` runs against a real `spawn.py` edit, pre-fix, all ~15.03-15.04s, `additionalContext` = "budget exceeded", none of the three target tests present.
Root cause: the per-item timeout interrupts the parent test's blocking call but does not kill the forked grandchild the test leaves behind, which keeps the `subprocess.run(capture_output=True)` pipe open. Resolved inline: rewrote `_run()` in `lint-test-on-edit.sh` to capture output via a temp file (not a pipe) and kill the whole process group (`start_new_session=True` + `os.killpg`) on completion/timeout.
derived: re-measured post-fix, 8 consecutive real end-to-end runs at 9.15-13.21s each, all three target tests present every run (same 8-run table as docs/issue-2326/reports/diagnose-first-71f82584.md's "What was done" item 1).
No scope change to the task's actual deliverable (the per-file-timeout ship decision) -- this was a necessary component of making that decision hold, not a detour from it.

2026-08-30T16:31:00+09:00 | filed | a background warrant-hunter dispatch (before-landing, stance 0) found a real bypass not anticipated going in: a `file_path` shaped as `docs/../<real-code-path>` matches the bash fast path's docs-only glob on the raw un-normalized string, silently skipping all lint/test checks for a real code file.
canonical: hunt record `docs/issue-2326/reports/diagnose-first-71f82584/2026-08-30-hunt-per-file-timeout-ship.md`, sha cabaae7044972f59ad0beab935d8f4b215bc8c67.
Resolved inline before landing: added a `*..*)` arm to the bash `case` statement that falls through to python's authoritative `posixpath.normpath` check instead of fast-exiting, plus a regression test added to `tests/test_spawn_gate_wiring.py` (new method in the `DocsOnlyEmptyState` class). No scope change -- fixed in the same commit as the hook it was found in, before the PR was opened.
