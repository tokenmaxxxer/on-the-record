---
issue: 2326
role: diagnose-first-4658f30a
---

- Expected the first `scripts/rework_fraction.py --batch` run (33 sessions,
  unfiltered) to give a usable materiality signal directly. What actually
  happened: it was inflated by `adversarial-review`-family sessions that run
  `pytest` twice as a main-vs-PR diff-comparison methodology, not a
  build-fix loop, scoring every comparison re-run as a false "rework
  episode" (42.9%/10.8% headline vs. the corrected 26.9%/7.9% after
  filtering that family out). Caught before use in the materiality call by
  filtering by filename and re-running against the 17 remaining sessions;
  the corrected figures are what the hook's docstring and the record cite.
- Expected to build the fast-model delegation half of the issue's Ask (route
  record/PR-body authoring to a Haiku subagent) alongside the hook, since
  the timing precondition (record+pr-body+landing at 47.6%-51.9% of session,
  from existing `docs/issue-2837` data) was already material. What actually
  happened: implementing it would mean editing spawn/directive assembly,
  which this task's own instructions explicitly place out of scope
  ("your work belongs in the hook") and which issues #2324/#2325 own —
  confirmed as a live collision, not hypothetical —
  canonical: this session's own ListAgents tool call, this turn
  — showing `on-the-record-issue-2324-diagnose-first-7a` as a
  concurrently-running peer session on that exact surface. Deferred to a
  follow-up issue instead of building it here.
