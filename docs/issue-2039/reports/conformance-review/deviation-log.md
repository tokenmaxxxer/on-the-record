- 2026-08-23T00:00:00Z filed: skill-verdict-guard.sh's Stop-hook name extraction (on-the-record/hooks/skill-verdict-guard.sh, `extract_names`) mis-splits this session's own spawn-directive mounted-skill line — it treats each clause of a skill's own one-line description (split on internal commas, e.g. "checkable requirement list", "before any verdict is rendered.") as a separate mounted-skill name, instead of stopping at the actual skill-name boundary. It then demands a `skill-verdict:` line under docs/issue-2039/reports/conformance-review.md for each mis-split fragment. That path is also this role's phase-2 record, which contract v3 s19 gates on human Approve — writing it now to satisfy the guard would violate the phase gate. The guard is advisory only (`additionalContext`, never `decision:"block"`), so this turn's work is unaffected; reported, not spawned, per the SCOPE-EXCEEDED rule (a role session does not open an issue mid-task on its own initiative).
