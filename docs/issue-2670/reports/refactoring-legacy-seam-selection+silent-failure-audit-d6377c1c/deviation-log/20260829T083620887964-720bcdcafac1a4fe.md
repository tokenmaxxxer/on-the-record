- 2026-08-29T08:36:03Z | inline | no live nested Claude Code spawn-to-PR performed for CLAUDE_ROLE->CLAUDE_SKILL rename verification (issue #2670 acceptance bullet) -- security/authorization judgment call, not a mechanical fix; substituted pipeline.spawn_cmd source inspection + existing gate-level pass/refuse test fixtures instead.

acceptance: `python3 -c "import inspect, pipeline; print(inspect.getsource(pipeline.spawn_cmd))"` — result:
```
env = {"CLAUDE_SKILL": role, "TOKENMAXXXER_SPAWNED": "1",
```
Full pass/refuse evidence (`tests/run-orchestrate-tests.sh`, `tests/test_stop_gate.sh`) in the main record.

- 2026-08-29T08:36:03Z | inline | accepted a real, unavoidable merge-order window between the two repos' PRs (on-the-record#2710, tokenmaxxxer-core#348) rather than closing it with a dual-read/alias -- issue #2670's own must-not forbids a compatibility alias.

acceptance: `grep -rn 'CLAUDE_ROLE' --exclude-dir=.git --exclude-dir=docs .` in both repos — result:
```
0
```
Full reasoning in the main record's `## Why` (last paragraph) and `## Open findings` #1.
