---
name: enforcement-boundary
description: >
  Which enforcement mechanisms are part of run.md's contract (and must
  reach consumer projects) and which are repo-local, per mechanism,
  derived and enforced by gates/test_boundary.py (issue #441).
---

# Contract enforcement boundary

Every `gates/*.py` module, every `on-the-record/hooks/*.sh` script, and
every `.github/workflows/*.yml` workflow in this repository must have a
row below with a recorded verdict. `gates/test_boundary.py` fails the
build if one is added or renamed with no row here — the boundary is
derived from this file's completeness against the filesystem, not
maintained by hand elsewhere (#333, #376).

Verdict values:
- `contract` — enforces an obligation `run.md` states on a consumer's role
  session; must be reachable by a consumer.
- `contract, CI-supplement` — contract-bound, but its violating act is only
  reachable via the CI supplement (not built in this delivery; see the
  proposal), because the zero-install baseline (plugin hooks + `spawn.py`)
  cannot see it.
- `out of scope — operator decision, 2026-08-07` — detecting already-drifted
  state was explicitly ruled out of scope for issue #441 (operator's
  approval-comment follow-up on that issue); this is a drawn boundary, not
  an unmet obligation. Narrowed by issue #464's ADR
  (`docs/issue-464/decisions/2026-08-08-board-state-into-orchestrator-loop.md`):
  the `closure_sweep.py`/`spawn_coverage.py` board-wide cases are reversed
  (see the `contract, orchestrator-loop` rows below) — #312/#388/#407 stay
  under this original ruling, untouched.
- `contract, orchestrator-loop` — board-wide (multi-subject) case enforced
  zero-install via `spawn.py:roster_watchdog()`'s per-tick sweep call,
  reachable by any consumer running the orchestrator loop; observe-only,
  per issue #464.
- `repo-local` — checks this repository's own source/process, not a
  consumer's; no obligation to reach consumers.
- `n/a (infrastructure)` — router, dispatcher, or read-only feed with no
  standalone clause of its own.

## `gates/*.py`

| mechanism | verdict | reason |
|---|---|---|
| `ci.py` | contract | enforced zero-install via `contract-guard.sh` (phase-2 Closes requirement) and `spawn.py`'s `acceptance_gate` preflight; write-scope/phase-1-must-not-close portions remain CI-supplement (`plan-aware-closes-gate.yml`, not shipped to consumers in this delivery) |
| `pr_reference.py` | contract | phase-2 Closes/Fixes/Resolves requirement enforced zero-install by `contract-guard.sh` |
| `delegation_metrics.py` | contract | new (issue #707): pure functions computing the pre-registered `operator_approvals_per_landed_pr` (H1 primary) and `self_approval_violation_count` (guardrail) metrics from `gh` comment data and hook deny-event records the caller supplies; no network calls, no persisted instrumentation added — the delegation-citation grammar already makes the two APPROVE shapes distinguishable by construction, so this module only enables the computation, not a new emission path |
| `closure_sweep.py` | contract (single-PR case) / **contract, orchestrator-loop** (board-wide case) | single-PR closing-keyword act folds into `contract-guard.sh`; board-wide drift detection is now called each `spawn.py:roster_watchdog()` tick via `find_violations()`, reversing the 2026-08-07 out-of-scope ruling for this row per issue #464's ADR. Issue #512 requirement 4: `accumulation_trend()` also runs each tick (via the same `spawn.py:_board_wide_sweep()` call site), an advisory (non-blocking) count report of shape-1/shape-5 instance totals in the merged tree vs. the previous tick — compensates for the authoring-time hooks' local-diff-only visibility with a board-wide, cross-session view |
| `acceptance_gate.py` | contract | enforced zero-install via `spawn.py` preflight (`require_acceptance_gate`), before a phase-2 session starts |
| `precision_measure.py` | not a hook itself, CLI-invoked | new (issue #1614): scripted stratified-sample + one-sided-90%-Wilson-LB precision report over `patrol_queue.py`'s sweep-lane enabled-rule population — `python3 gates/precision_measure.py sample|report`; no network calls, no LLM judging in this module (judgments are supplied via an external file, same convention `patrol_queue.py` already holds) |
| `requirement_linkage.py` | contract | new (issue #1017, northpole req#6): enforced zero-install via `spawn.py` preflight (`require_requirement_linkage`), before a phase-1 (pre-approval) session starts for an issue with no prior `issue-<n>/*` branch — grandfathers already-open issues, no retroactive blocking |
| `gh_rest.py` | repo-local | new (issue #1569, extended issue #1681): pure helper wrapping `gh api repos/{owner}/{repo}/{issues,pulls}/{n}` (owner/repo via local `git remote get-url origin`, no gh call of its own); no `check()`/gate entrypoint and no independent enforcement boundary — it is imported by `requirement_linkage.py`, `acceptance_gate.py`, `requirement_intake_consult.py`, `pr_reference.py`, `issue_bundling.py`, and `check_runner.py`, whose own rows above/below carry the enforcement verdict for the read this helper performs on their behalf. Issue #1681 adds `fetch_open_prs`, a REST/ETag-conditional PR-poll helper, same repo-local/no-gate-entrypoint shape |
| `gh_budget.py` | repo-local | new (issue #1681): pure `GhBudget` per-consumer-class token-bucket tracker over one cached `closure_sweep.rate_limit_remaining` snapshot, plus the `budget_message` degradation-line helper; no `check()`/gate entrypoint and no independent enforcement boundary of its own — it is a library imported by callers (`requirement_linkage.py` so far) that decide when to charge a class and what to do with a budget-exhausted result. spawn.py wiring the actual watchdog loop through it is an out-of-scope, sequenced follow-up |
| `state_paths.py` | repo-local | new (issue #2240): single accessor `orchestrator_state_path(filename)` anchoring orchestrator cross-tick state (gh_delta cursors, watchdog drift/noise caches, spawn_on_pr/spawn_on_approve/closure_sweep sweep-state) to `MUSTER_STATE_ROOT` when set else this install's own `<repo-root>/runs`, never to a caller's `root` (target-repo) argument; no `check()`/gate entrypoint and no independent enforcement boundary of its own — it is a pure path-resolution library imported by `gh_delta.py`, `board_read.py`, `watchdog.py`, `spawn_on_pr.py`, `spawn_on_approve.py`, and `closure_sweep.py` |
| `requirement_intake_consult.py` | repo-local | new (issue #1024): `check_issue_body`/`check` verify a drafted issue body carries a `validity-consult:` trace reference or the closed `validity-consult-skip: trivial` tag; the proposal's write set builds the check function itself and the directive-text default step, not a `spawn.py`/`ci.py` preflight wiring point — no zero-install enforcement path exists yet, so this stays repo-local pending a future wiring proposal |
| `landing_readiness.py` | contract, CI-supplement | advisory scope-overlap/checks judgment; not folded into `contract-guard.sh` in this delivery, remains CI-only where installed |
| `design_research_consult.py` | repo-local | new (issue #1653, northpole req#6): `check_issue_body`/`check` verify a design-bearing issue body carries a `design-research:` trace reference or the closed `design-research-skip: mechanical` tag; module + tests only in this delivery — no `spawn.py`/hooks wiring yet (deferred to a follow-up issue to avoid colliding with #1652's spawn.py change), so this stays repo-local pending that future wiring proposal |
| `design_bearing_classifier.py` | repo-local | new (issue #2012, artifact-gate phase 1): `check_issue_body`/`check` classify an issue body as design-bearing or not, reusing `spawn.py`'s `_tokenize` cross-family keyword-overlap shape (copied, not imported, to keep `gates/` a leaf relative to `spawn.py`) against a fixed design-signal keyword vocabulary; returns verdict + cited evidence (matched keywords or an explicit `design-bearing-override: yes\|no` tag); precision-first threshold calibrated against `docs/issue-2012/reports/implementation/corpus.md`. Module + tests only in this delivery — no `spawn.py`/core-gate wiring yet (that consumption is issue #2013, a separate follow-up), so this stays repo-local pending that future wiring proposal |
| `design_artifacts_gate.py` | repo-local | new (issue #2013, artifact-gate phase 2): `parse_declaration`/`missing_artifacts`/`check` read a `design-artifacts:` tag (bulleted list or fenced block of repo-relative paths) from an issue body and check those paths' existence in the working tree; fails CLOSED with an actionable message when the issue body itself cannot be fetched (approval amendment, replacing the merged proposal's original fail-open constraint). The module itself stays repo-local (source of truth + test bed, `gates/gh_rest.py` dependency not guaranteed on a consumer repo's `sys.path`); the actual zero-install enforcement is `parse_declaration`/the existence check ported inline into `on-the-record/hooks/pr-preflight.sh` (row below), same split as `check_body`/`_plan_from_body` in that same file |
| `artifact_smoke_rule.py` | repo-local | new (issue #2073, artifact-smoke acceptance): `check_issue_body`/`check` (gates/artifact_smoke_rule.py) refuse a drafted issue that declares `runtime-artifacts:` (same tag+bullet/fence contract shape as `design-artifacts:`, parsed by reusing `design_artifacts_gate.parse_declaration` with a tag argument) when no `check:`/`gate:` line in its `## Acceptance` section names a declared path under the closed parse/execute verb allowlist; byte-inert when the tag is absent, fail-CLOSED when the issue body cannot be fetched, suppressed by an `artifact-smoke-override: yes` line. `advisory_line` is a non-refusing drafting hint only. Standalone CLI (`python3 gates/artifact_smoke_rule.py <issue>`) invoked manually — not called from `gates/ci.py`'s check graph and not ported into any `on-the-record/hooks/*.sh` preflight, same not-yet-reachable class as `acceptance_authoring_rule.py` below; the contract it enforces is `docs/specs/artifact-smoke-contract.md` |
| `model_routing.py` | repo-local | new (issue #2070): `load_policy`/`route_model` pick a `--model` tier (fable/opus/sonnet) from spawn-time signals (single-phase, design-bearing verdict) once the existing `--model`/`MUSTER_ROLE_MODEL`/`role_model.txt` chain is empty, reading `.on-the-record/model-routing.json` as repo-overridable data and falling back to an in-module default policy on any read/parse error — never raises (fail-open), never refuses a spawn. Wired directly into `spawn.py`'s `resolved_role_model()`/`spawn_cmd()` in this same delivery (not deferred) since it is a routing choice, not a pass/fail gate; stays repo-local because `gates/` is this repo's home for the pure decision logic `spawn.py` calls into via `sys.path.insert` (same convention `design_bearing_classifier.py`, row above, already uses). Issue #2631: `route_model()` lost its `role` parameter and the fixed-name-list tier-membership test it drove — role identity is no longer a routing signal at all |
| `scope_adherence.py` | repo-local | new (issue #1658, northpole req#6): pure `classify(declared_scope, pr_files, issue)` + gh-wrapped `check(root, issue, pr)` — blocks a PR touching files outside every declared `scope:` prefix (always allowing the issue's own `docs/issue-<n>/` tree); `declared_scope=None` is advisory (`scope-undeclared`) and never blocks; same `{scope, pr_files}` shape as `landing_readiness.py` (row above). Module + tests only in this delivery — no `spawn.py`/hooks wiring yet (deferred to a follow-up issue to avoid colliding with #1652's spawn.py change), so this stays repo-local pending that future wiring proposal |
| `quality_bar.py` | contract | new (issue #1156): pure `classify`/`bar_scoped_roles` functions, `landing_readiness.classify`-shaped; called zero-install by `quality-bar-gate.sh` (row below) at `gh pr merge` time — no CI-only gap |
| `auto_approval_class.py` | repo-local | new (issue #1739): pure `classify(diff_paths, ...)` + `shadow_verdict(...)` — labels a PR diff's risk class and composes it with `scope_adherence.py`/`stale_revert_guard.py`/`requirement_met.py`'s results plus quota/circuit-breaker state read from `.on-the-record/auto-approval-state.json`, appending one line to `docs/reports/auto-approval-audit-log.md`. Shadow-mode only in this delivery: not wired into `on-the-record/hooks/approval-gate.sh`, no `check()`/gate entrypoint, and no independent enforcement boundary — `approval-gate.sh`'s human-APPROVE requirement is unaffected by any value this module returns. Wiring a real bypass path is a separate, future, human-gated decision (see `docs/specs/contract-v3-amendment-auto-approval.md`), out of scope here |
| `claim_scan.py` | contract, zero-install-hooked, CI-supplement | issue #476 H1: scans a record/PR body for claim-language ("reproduced"/"verified"/…) with no adjacent runnable evidence or no traceable target — CLI wrapper reads `gh pr diff`, still CI-only for target-traceability; issue #476 round 2 (H1b): `CLAIM_RE`/`EVIDENCE_MARKER_RE`/fence-adjacency (evidence presence only, no target traceability) ported inline into `on-the-record/hooks/claim-scan-preflight.sh`, a `PreToolUse`+`Bash` hook on `gh pr create`/`gh pr edit` joining `pr-preflight.sh`'s matcher group — warn-only (`additionalContext`, never blocks) pending the H1b flip-to-deny measurement window, zero-install, ships with the plugin |
| `reexecution_gate.py` | contract, CI-supplement | new (issue #476 H1): SHA-pinned worktree re-execution of a claim-adjacent command, gate-owned verdict written to `.reexecution/<issue>-<role>.json`, never role-writable; feeds `landing_readiness.reexecution_blocking_cause()`, folded into `landing_readiness.py`'s existing CI-supplement path — same boundary, no new install surface |
| `spawn_coverage.py` | **contract, orchestrator-loop** | "an issue was filed but no session ever started" is now checked each `spawn.py:roster_watchdog()` tick via `find_uncovered()`, including on an empty live roster, reversing the 2026-08-07 out-of-scope ruling for this row per issue #464's ADR |
| `requirement_digest.py` | contract, zero-install-hooked | new (issue #930, northpole req#6): `parse`/`render`/`update`/`check` condense `docs/specs/requirements.md` into `docs/specs/requirement-digest.md`, one line per live (non-`stale`) requirement — commit-time enforced zero-install via `on-the-record/hooks/requirement-digest-preflight.sh` (`PreToolUse`/`Bash`, joins `spec-index-preflight.sh`'s matcher group), `gates/ci.py` runs `check()` as a CI-timing backstop; `spawn.py:requirement_drift()`, called each `spawn.py:roster_watchdog()` tick next to `accumulation_trend()`, is advisory/board-wide/non-blocking — same shape as that row, never folded into `anomaly_count` |
| `repo_scope.py` | repo-local | issue #415: flags an unscoped capability/contract absence claim in issue/record prose; ships as a standalone script/pytest module, not wired into any zero-install preflight or `contract-guard.sh` |
| `accumulation.py` | repo-local | issue #424: flags a proposal touching a named accumulation-prone shape (inline subprocess/gh call-site growth, `roles/*.json`-style repeated one-line edits) with no `## Accumulation` line; checks this repo's own proposal-authoring practice, not wired into any consumer-reachable preflight. Issue #512: strengthened from heading-existence to heading-plus-non-empty-body (still presence-only, contract §14); the same check is also now reachable on consumer target repos via `accumulation-claim-guard.sh` (row above) |
| `issue_bundling.py` | repo-local | this org's own filing hygiene; `run.md` states no such obligation on a consumer's role sessions |
| `skip_gate.py` | repo-local | wraps this repo's own CI invocation of its own test suite |
| `trivial_lane_gate.py` | repo-local | new (issue #1492): `classify()` classifies a diff's numstat rows/paths into a trivial-lane shape class (`rename-only`/`docs-only`/`test-name-only`) or `None` (fail-closed); its CLI entry point checks a PR's diff against `EFFECTIVE_AFTER` (#362 retroactivity). Not wired into `spawn.py`'s bootstrap sequence or any CI trigger — that wiring is explicit out-of-scope in docs/issue-1492/proposals/2026-08-15-trivial-lane-machine-gate.md, a follow-on issue |
| `spec_index.py` | repo-local | checks this repo's own `docs/specs/` set, not a consumer's |
| `operator_experience.py` | repo-local | issue #1006: mechanical checks for the operator-experience layer (`directive.sh` carries blocks A-D's marker strings; a seeded utterance has a testable `## Acceptance` shape) — consumed only by `harness/fixture-operator-experience/scenario.py`, not wired into any zero-install preflight or `contract-guard.sh` |
| `risk_report.py` | contract | issue #511: `batch_blocked()`'s four-axis dominant-axis rule is enforced zero-install via `on-the-record/hooks/impact-guard.sh`, which denies a batch-merge Bash command; `classify()`/`report()` remain the non-blocking review-surface feed they always were |
| `gates.py` | n/a (infrastructure) | router/dispatcher to the modules above |
| `flows.py` | repo-local | feeds this repo's own status-board UI |
| `retirement_count.py` | repo-local | new (issue #2876): `line_hits`/`main` (gates/retirement_count.py) count py/sh occurrences of the retired `role`/`roles` axis via identifier-aware tokenization (snake_case/camelCase-aware, plural/case-variant closed match), `docs/` and the module's own two files excluded; standalone CLI (`bash gates/retirement_count.sh`, exit 0 iff zero occurrences, exit 1 iff occurrences found, exit 2 iff the py/sh population itself could not be determined — round 3: `tracked_sources()` resolves the repo root via `git rev-parse --show-toplevel` rather than the caller's raw cwd, so both the CLI and its `--list-files` mode return the same population regardless of the invoking cwd, and refuse rather than silently reporting a partial result if that resolution ever yields zero files), invoked manually — not wired into `gates/ci.py`'s check graph or any `on-the-record/hooks/*.sh` preflight in this delivery, since the invariant is "must not increase from a non-zero baseline" (this repo is mid-way through the `docs/decisions/2026-08-25-retire-role-axis-staging.md` staged retirement), not "must be zero," and wiring a ratchet-baseline comparison into a blocking gate is an explicitly separate, unscoped follow-up (see this issue's implementation record, "Open findings") |
| `claims.py` | repo-local | new (#377): opt-in `# CLAIM-CHECK:` marker checker over this repo's own source (role-JSON enum drift, `spec.md`-producer claim); registered in `gates.ALL`, not wired into `gates/ci.py`'s required path in this delivery — promotion to required-check status is a separate follow-up decision per the proposal's own Rationale |
| `approval_request_shape.py` | contract | `missing_approval_clauses` is a testable extraction of the clause logic already enforced zero-install by `stop-gate.sh` (#411/#318); `has_generator_section` (#363) is presence-only and not wired into a blocking hook — `run.md` instructs its use, not a hook |
| `open_work.py` | contract, CI-supplement | `build_open_work_query` (#379) builds the lookup query only; the actual open-issue/open-PR check runs manually per `run.md`'s instruction, not via a blocking hook in this delivery |
| `record_lint.py` | contract | new (issue #517): `lint_record(path)` aggregates every `gates.py` record check plus the four checks lifted out of `record-claim-guard.sh`'s inline mirror against one record's full text; `record-claim-guard.sh` and `gates/ci.py` call into this module instead of duplicating the logic — zero-install (CLI + importable), ships with the plugin |
| `patrol_queue.py` | repo-local | new (issue #1582, tier-1 pilot): pure functions (`fingerprint`/`enqueue`/`absence_close`/`apply_budget`/`verify`/`record_dismissal`) plus a `record_lint.py` scanner adapter, maintaining `.on-the-record/findings/queue.jsonl`; standalone CLI (`scan <repo-root>`), invoked manually or via `patrol_trigger.py` below — not wired into `gates/ci.py`, any `PreToolUse` hook, or `spawn.py`'s watchdog, so it checks a target repo's existing state on manual/triggered invocation only, not a consumer's role-session obligation in this delivery (issue design req 9: queue-to-issue promotion, the step that would make a finding visible to a consumer, stays out of scope for this pilot) |
| `patrol_trigger.py` | repo-local | new (issue #1582, tier-1 pilot): `should_fire`/`run_if_eligible` gate `patrol_queue.py`'s scan behind a post-merge-event shape and a #1360-class origin check (refuses when the triggering event's changed files are entirely patrol-produced artifacts); exposed as a callable meant to be chained onto the merge-command seam, not a git-native `.git/hooks/post-merge` file — not yet wired into that seam in this delivery (proposal's stated Out of scope), so it has no zero-install reachability path yet, same not-yet-reachable class as `merge_gate.py`'s row above |
| `patrol_board.py` | repo-local | new (issue #1588, patrol board C1): pure rendering functions (`select_board_entries`/`render_board_body`/`parse_board_body`/`dedup_fingerprints`/`diff_board`/`build_next_body`) over `patrol_queue.py`'s queue plus a thin `gh`-calling shell (`find_board_issue` ETag-conditional read, `write_budget_ok`/`record_write`/`record_drop` daily write budget, `run_patrol_board` orchestration); standalone CLI (`run <repo-root> <role> [--dry-run]`), invoked manually — not wired into `gates/ci.py`, any `PreToolUse` hook, or `spawn.py`'s watchdog in this delivery; no checkbox-tick interpretation or per-finding issue creation (issue #1589/C2, out of scope here) |
| `patrol_wiring.py` | repo-local | new (issue #1597, E1): wires `patrol_trigger.should_fire`, `spawn.judge_cmd`, and `patrol_board.run_patrol_board` together behind a file-based kill-switch (`.on-the-record/patrol-disabled`, checked first via the shared `kill_switch_active` helper) and a 3-role-per-merge cap counted from `judge_cmd` HIT return values only (binding review correction, PR #1601 — not a raw trace-line count, which would also count prefilter MISSES); reachable via the merge-command seam documented in `on-the-record/commands/run.md`'s merge step (`python3 gates/patrol_wiring.py run <repo-root> <merge-sha>`, run immediately after `gh pr merge`), so — unlike `patrol_trigger.py`'s and `patrol_board.py`'s own rows above — this delivery closes the "no zero-install reachability path yet" gap those rows note, for this one entry point (E1); a future E2 entry point (out of scope here) is expected to import the same `kill_switch_active` helper |
| `patrol_promote.py` | repo-local | new (issue #1589, patrol board C2): pure functions (`detect_ticks`/`build_finding_issue_body`/`rate_cap_ok`/`move_ticked_line`) plus a thin `gh`-calling shell (`find_existing_promotion` fingerprint-marker idempotence search, `promote_tick`, `run_patrol_promote` orchestration reusing `patrol_board.py`'s `find_board_issue`/`write_budget_ok`/`record_write`); standalone CLI (`run <repo-root> <role> [--dry-run]`), invoked manually — not wired into `gates/ci.py`, any `PreToolUse` hook, or `spawn.py`'s watchdog in this delivery; scope ends at creating the promoted issue and moving the board line, no spawn/classification (issue #1589's own stated scope) |
| `role_spec_shape.py` | contract | new (issue #521): hand-rolled shape checker (no `jsonschema` dependency) for `roles/specs/<name>.spec.json` against `docs/specs/role-spec-template.schema.json`'s shape; also exposes `record_path_role`/`reference_resolution_check`, called by `role-spec-reference-guard.sh`; `gates/test_role_spec_shape.py` (pytest, `-k "spec"`) loads and validates the 6 batch-1 files — zero-install (CLI + importable), ships with the plugin |
| `spec_schema_five_activities_test.py` | repo-local | new (issue #1130): pytest-only, asserts the 14 cause-d `roles/specs/*.spec.json` in this repo carry the five-activity methodology fields (`judgment_methodology`/`planning_methodology`/`deliverable_form`/`feedback_methodology`/`review_methodology`, each with a non-empty `source`) plus `degree_level_knowledge`; checks this repo's own spec files, not a consumer's — same class as `spec_index.py` above |
| `playbook_depth_gate.py` | repo-local | **RETIRED 2026-08-24 (#2141)** — rulebook-era 43-role playbook program tooling (#1955 retired the rulebooks; single-skill-axis freeze 2026-08-21), zero live callers (grep-proven in the #2139 table), files removed; was: new (issue #1174 (c)): CLI + importable shape checker for a role's operational-playbook text — counts condition+choice+source rule blocks, rejects glossary-shaped (definition-only) blocks, enforces the role's recorded `rule_count_floor`, and enforces amendment 4 (a playbook with zero removal-classified rules on a declared axis fails even when count clears the floor); not wired into any preflight in this delivery — invoked per-role by the future per-role fan-out units' own batch PR, same manual-invocation pattern as `spec_index.py --update` |
| `playbook_tracker.py` | repo-local | **RETIRED 2026-08-24 (#2141)** — rulebook-era 43-role playbook program tooling (#1955 retired the rulebooks; single-skill-axis freeze 2026-08-21), zero live callers (grep-proven in the #2139 table), files removed; was: new (issue #1174): renders the issue's 43/44-item playbook completion checklist from `roles/*.json` against `roles/specs/*.spec.json`'s `playbook_refs` (non-empty = landed); rendering only, not a gate — exit 0 always, checks this repo's own role/spec set, not a consumer's, same class as `spec_index.py` above |
| `ui_evidence_gate.py` | repo-local | new (issue #685): `check_record`/`is_ui_facing` are pure functions wired only as `ui_evidence_gate_gate` in `gates.ALL`, via `gates.py`; not called from `gates/ci.py`'s actual `check()` graph (confirmed by `ci_reachable_gates`'s own reachability scan) and not ported into any `on-the-record/hooks/*.sh` preflight — registered but not yet reachable by any consumer-facing enforcement path, same class as `claims.py` below; promotion to a reachable check is a separate follow-up decision |
| `roles_due.py` | repo-local | new (issue #896 step 2): `spawn.py roles-due` board_condition evaluator for JUDGMENT residue only (test-authoring's own bar is a standing invariant, enforced by `test-authoring-invariant-guard.sh` below, not this module) — reads a role spec's `use_when.trigger` (path/content patterns + `record_absent_for`), matches against the diff, checks the board for an existing record, and reports "due" roles; surfaced-only, never blocks, same read-only reporting class as `flows.py` above; not wired into any `PreToolUse`/`Stop` hook or `gates/ci.py` |
| `need_detector.py` | repo-local | new (issue #1160 step 3 machinery): `spawn.py needs-due` evaluator — reads a pilot role spec's `use_when.need_detector.present_patterns`/`absent_patterns` glob lists and evaluates them against an arbitrary `target_root` file tree (not this repo's own diff — mirrors `roles_due.py`'s `root` parameter but the input is a target project's tree, not a diff); a role is "due" iff a `present_patterns` glob matches and no `absent_patterns` glob matches; advisory-only, surfaced via `[needs-due]`-prefixed lines alongside `roles-due`'s own `[roles-due]` stream, never spawns; same read-only reporting class as `roles_due.py` above; not wired into any `PreToolUse`/`Stop` hook or `gates/ci.py` |
| `finding_shape.py` | repo-local | new (issue #1202 requirement 2/3): `check_finding(path) -> list[str]` — hand-rolled frontmatter/section shape checker for a role's domain-finding file (`docs/reports/findings/<role>/<date>-<slug>.md` or the per-issue variant), rejecting a finding missing/empty `domain_rule` (playbook citation), `## Evidence`, `## Impact`, or `## Proposed direction`; also exposes `check_rate_bound(findings_root, role, session_id, bound=3)`, a pure per-session N=3 counter over an optional `session:` frontmatter field; zero-install (CLI + importable), not wired into any `PreToolUse`/`Stop` hook or `gates/ci.py` in this delivery — the frozen phase-1 write set names only the gate module and its tests, no hook wiring |
| `findings_due.py` | repo-local | new (issue #1202 requirement 4): `spawn.py findings-due` evaluator — mirrors `need_detector.py`'s two-function shape (`findings_due(target_root) -> list[dict]` / `format_report(due) -> list[str]`) over `docs/reports/findings/<role>/*.md` and every `docs/issue-*/reports/findings/<role>/*.md`, skipping session-summary files and any finding already carrying `relayed_to_issue:`; advisory-only, never spawns, never files a `gh issue`; same read-only reporting class as `roles_due.py`/`need_detector.py` above; not wired into any `PreToolUse`/`Stop` hook or `gates/ci.py` |
| `remediation_spawn.py` | contract | new (issue #587); reachability path retired (issue #2695): `pending_remediation_tasks`'s sole producer field (`routed_to`, `delegated-judgment-gate.sh`) has been hardcoded `None` since #2559, so the function could never return a task; `run.md`'s instructed call to it was removed in #2695 rather than left dangling on an empty mechanism, so this module now has no automated or documented call site at all — invoked manually only, same not-yet-reachable class as `check_runner.py`/`merge_gate.py` below. Whether the module itself should be retired is a separate, unresolved judgment (#2695 non-goals) |
| `acceptance_authoring_rule.py` | repo-local | new (issue #1323 req 1): `check_issue_body`/`check` (gates/acceptance_authoring_rule.py:53-77) flag an issue's `## Acceptance` section when it offloads full-suite/no-regression checking to the builder with no exemption phrase assigning that check to a verification role/check-runner; standalone CLI (`gates/acceptance_authoring_rule.py:100-121`) invoked manually with an issue number, not called from `gates/ci.py`'s check graph and not ported into any `on-the-record/hooks/*.sh` preflight or `spawn.py` preflight — same not-yet-reachable class as `requirement_intake_consult.py` above |
| `forbidden_action_rule.py` | repo-local | new (issue #2503, #2479 R3): `check_issue_body`/`check` (gates/forbidden_action_rule.py) flag an issue's `## Acceptance` section when a file/open/create/raise verb is paired with issue/ticket, unless the same window reassigns the action to the orchestrator/operator/a non-role account (the sanctioned follow-up wording's own shape); fails closed when the issue body cannot be fetched. Standalone CLI (`python3 gates/forbidden_action_rule.py <issue>`) invoked manually, not called from `gates/ci.py`'s check graph and not ported into any `on-the-record/hooks/*.sh` preflight or `spawn.py` preflight — same not-yet-reachable class as `acceptance_authoring_rule.py` and `artifact_smoke_rule.py` above |
| `tool_learnings_tracker.py` | repo-local | **RETIRED 2026-08-24 (#2141)** — rulebook-era 43-role playbook program tooling (#1955 retired the rulebooks; single-skill-axis freeze 2026-08-21), zero live callers (grep-proven in the #2139 table), files removed; was: new (issue #1199): renders this issue's 43-item tool-learnings completion checklist from `roles/*.json` against `roles/specs/*.spec.json`'s `tool_learnings_refs` field (non-empty = landed) — `render`/`is_landed` (gates/tool_learnings_tracker.py:29-45), `main` prints to stdout and always returns 0 (`gates/tool_learnings_tracker.py:48-58`: "rendering, not a gate — nothing here blocks a commit"); reads this repo's own role/spec set, not a consumer's, same class as `playbook_tracker.py` above |
| `check_run_artifact.py` | repo-local | new (issue #1493 req 1): `build_artifact`/`validate`/`write_artifact`/`read_artifact`/`sample_eligible`/`select_sample` (gates/check_run_artifact.py:34-172) — standalone JSON schema module for the check-run artifact (Alternative B per `docs/issue-1493/proposals/check-run-artifact-design.md`); pure data-shape/serialization, no `subprocess`/`gh` calls of its own beyond `tree_hash`'s local `git rev-parse`; imported by `check_runner.py` (write side) and `merge_gate.py` (read side, `verify_artifact`), not called from `gates/ci.py`'s check graph or any `on-the-record/hooks/*.sh` preflight — same not-yet-reachable class as `check_runner.py`/`merge_gate.py` below |
| `check_runner.py` | repo-local | new (issue #1323 req 2): `parse_checks`/`run_checks`/`format_comment` (gates/check_runner.py:37-123) execute the executable `check:`/`gate:` lines out of an issue's `## Acceptance` section against a PR branch and post the result as one PR comment; `main` (gates/check_runner.py:141-) is a standalone CLI taking `<pr-number> <issue-number>`, invoked manually per its own docstring — not called from `gates/ci.py`'s check graph, not ported into any `on-the-record/hooks/*.sh` preflight, and not called from `spawn.py`'s watchdog sweep |
| `merge_gate.py` | repo-local | new (issue #1323 req 4); revised (issue #2609): `evaluate` (gates/merge_gate.py) judges a PR mergeable only once `check_runner.py`'s posted result is all-pass, `required_verification_missing()` sees `spawn_on_pr.REQUIRED_INDEPENDENT_VERIFICATIONS` (2) qualifying `verifies_subject: true` records with an `author:` different from the subject's own deliverable author (issue #2609 Option 2 — a self-declared, counted check; no `kind:` value, filename, or skill name decides this anymore, replacing the old closed two-name tuple entirely), and (issue #1664 req#6) `stale_revert_guard.check_pr` reports no reverted paths; standalone CLI (`gates/merge_gate.py:83-`) taking `<pr> <subject>`, invoked manually — its own docstring states "`.github/workflows/` 파일이 아니다... role 세션은 그걸 추가하는 게 거절된다" (this repo has no CI surface for it); depends on `check_runner.py`'s comment, `spawn_on_pr.py`'s import, and `stale_revert_guard.py`'s import but is not itself called from `gates/ci.py`, any hook, or `spawn.py`'s watchdog — same not-yet-reachable class as `check_runner.py` above |
| `stale_revert_guard.py` | repo-local | new (issue #1664, northpole req#6): `classify` (gates/stale_revert_guard.py) is a pure function over three file-content snapshots (base HEAD, PR merge-base, PR head) that simulates a git 3-way merge via local `git merge-file` and REFUSEs only when the simulated merge result still lacks content base HEAD gained after the PR's merge-base; `check_pr`/`changed_paths` wrap it with local `git show`/`git diff` reads, no network of their own; imported by `merge_gate.py`'s `evaluate`/`stale_revert_reasons` (the only `gh`-touching caller) — not itself called from `gates/ci.py`, any hook, or `spawn.py`'s watchdog, same not-yet-reachable class as `merge_gate.py`/`check_runner.py` above |
| `recovery_policy.py` | repo-local | new (issue #1670, northpole req#6): `classify` (gates/recovery_policy.py) is a pure function over already-observed failure signals (`has_commit`, `respawn_count` vs `cap`, `failure_signature`/`last_failure_signature`) returning `RESPAWN_IDENTICAL`/`RESPAWN_WITH_HANDOFF`/`ESCALATE`, no network/git calls of its own; `classify_from_state` wraps it with a local per-(issue,role) JSON counter file under `.on-the-record/recovery-state/`; conceptually reuses `spawn.py:reconcile()`'s `pr-expected-missing` death-signal shape but is not imported by `reconcile()`/`spawn.py` and not called from `gates/ci.py`, any hook, or `spawn.py`'s watchdog — spawn.py wiring is an explicitly out-of-scope sequenced follow-up, same not-yet-reachable class as `stale_revert_guard.py`/`merge_gate.py` above |
| `spawn_on_pr.py` | **contract, orchestrator-loop** | new (issue #1323 req 3, hotfixed issue #1360); revised (issue #2609): `spawn_missing_for_pr`/`missing_verification` (gates/spawn_on_pr.py:60-) auto-spawn two fixed producer roles (`AUTO_SPAWN_ROLES`) for OPEN-issue subjects — issue #2609 decoupled this role-selection list from the merge-gating obligation entirely (`merge_gate.py::required_verification_missing` no longer reads it, or any kind/name, at all — see that row below); called each `spawn.py:roster_watchdog()` tick via `_board_wide_sweep()` (spawn.py:2880-2884, alongside `closure_sweep`/`spawn_coverage`), same board-wide zero-install reachability class as those two rows below |
| `skip_eligibility.py` | **contract, orchestrator-loop** | new (issue #745 Item 3); narrowed (issue #2609): `classify_for_subject()` and its private helpers are deleted — its sole production caller, `spawn_on_pr.py`'s per-subject execution-observation skip-eligibility filter, is deleted too (operator ruling on #2609's Open finding 1: "every subject takes the same count requirement," no kind-specific exemption survives under any name). `classify_rows`/`non_docs_lines_changed`/`hard_to_revert_hit`/`claim_vocabulary_hit` (gates/skip_eligibility.py:1-) remain — `trivial_lane_gate.py` imports them directly, unrelated to the deleted per-subject wrapper — but are no longer reachable from `spawn_on_pr.py` at all |
| `spawn_on_approve.py` | **contract, orchestrator-loop** | new (issue #2173): `ready_for_phase2`/`spawn_phase2` (gates/spawn_on_approve.py) find `(subject, role)` pairs whose role is approved (`APPROVE issue-<n>/<role>`) but has no phase-2 record yet and whose phase-1 PR is already open, and auto-spawn phase-2 for them, one-shot per pair (`runs/spawn_on_approve_attempted.json`); called each `spawn.py:roster_watchdog()` tick via `_board_wide_sweep()` as a fourth `closure_sweep.BOARD_SWEEP_CATEGORIES` entry alongside `spawn_on_pr`/`closure_sweep`/`spawn_coverage`, same board-wide zero-install reachability class as `spawn_on_pr.py` above |
| `tool_learnings_gate.py` | repo-local | **RETIRED 2026-08-24 (#2141)** — rulebook-era 43-role playbook program tooling (#1955 retired the rulebooks; single-skill-axis freeze 2026-08-21), zero live callers (grep-proven in the #2139 table), files removed; was: new (issue #1199 step 1): `evaluate`/`classify_entry` (gates/tool_learnings_gate.py:88-114) parse a role's "## Tool learnings" section into per-entry facet checks (tool/adoption-evidence/problem/how/learning/source) plus a per-role entry-count cap; standalone CLI (`gates/tool_learnings_gate.py:125-`) taking `<file-or-dir> --role <name> --cap <N>`, invoked manually per its own docstring — not called from `gates/ci.py`'s check graph and not ported into any `on-the-record/hooks/*.sh` preflight or `spawn.py` |
| `probe_supersession_marker.py` | repo-local | new (issue #3050, acceptance amendment check 2): standalone acceptance probe, no CLI arguments — imports `supersession.py` and asserts `resolve_authoritative()` resolves a synthetic two-artifact correction tree (an original record plus a correction carrying a `supersedes:` frontmatter field) to exactly one authoritative path, from file contents alone. `python3 gates/probe_supersession_marker.py`, invoked manually per the issue's own check line — not called from `gates/ci.py`'s check graph and not ported into any `on-the-record/hooks/*.sh` preflight; fails against current main (`supersession.py` does not exist there) |

## `on-the-record/hooks/*.sh` (plugin-shipped)

| mechanism | verdict | reason |
|---|---|---|
| `absorbed-branch-recut-guard.sh` | contract | new (#784): `PreToolUse`+`Bash`, intercepts `git commit`/`gh pr create` and, immediately before allowing either through, shells out to `spawn.py recut-if-absorbed` (calling the shared `_recut_absorbed_branch` helper factored out of `checkout_issue_branch()`) against the session's own cwd — recuts a mid-run session's own branch when a concurrent orchestrator merge absorbed it out from under the session, closing the gap #732's spawn-time-only recut left. Runs synchronously inside the session's own process, unlike the parent's post-hoc transcript scan the after-proposal hunt found couldn't gate anything (docs/issue-784/reports/implementation/2026-08-11-hunt-absorbed-branch-mid-run-recut.md). No roster/cross-process lookup — pure local git state. Fail-open when `spawn.py` is absent from the checkout (non-self-hosted consumer repos) or the recut attempt itself fails; never denies, only ever adds a recut before the matched command runs. Ships with the plugin |
| `contract-guard.sh` | contract | new (#441): `PreToolUse`+`Bash`, intercepts `gh pr merge` before it executes; zero-install, ships with the plugin |
| `pr-preflight.sh` | contract | new (#459): `PreToolUse`+`Bash`, intercepts `gh pr create`/`gh pr edit` before the PR body is set, denying a wrong Closes/plain-`#n` trailer for the phase; ports `pr_reference.check_body`/`flows._plan_from_body` inline (zero-install), ships with the plugin. Extended (issue #707): phase2 detection also recognizes a live, in-scope `APPROVE issue-<n>/<role> VIA DELEGATION <scope>` citation, same acceptance rule as `approval-gate.sh`. Extended (issue #2013, artifact-gate phase 2): also ports `design_artifacts_gate.py`'s `parse_declaration`/existence check inline, denying `gh pr create`/`gh pr edit` when the issue body declares `design-artifacts:` paths that are missing from the working tree; a body-fetch failure for this one check fails CLOSED (approval amendment), the sole fail-closed exception in this file's otherwise fail-open policy besides `pr-base-guard.sh`'s row. Extended (issue #2073): on a phase-2 PR whose issue declares a storyboard among its `design-artifacts:`, also requires a `screen-verified: <path> — <verdict>` line in a record under `docs/issue-<n>/reports/`, the cited screenshot to exist in the working tree, and the line to carry a verdict — presence and existence only, never the verdict's content (no pixel diff, no perceptual hash, no LLM verdict inside a gate). Fail-open on a missing record file and on phase-1 PRs |
| `claim-scan-preflight.sh` | contract | new (#476 round 2, H1b): `PreToolUse`+`Bash`, joins `pr-preflight.sh`'s matcher group on `gh pr create`/`gh pr edit`, scans the extracted `--body`/`--body-file` text for claim language with no adjacent runnable evidence; ports `claim_scan.py`'s `CLAIM_RE`/`EVIDENCE_MARKER_RE`/fence-adjacency inline (zero-install, evidence presence only, no target traceability); warn-only (`additionalContext` + mirrored stderr, exit 0) pending the H1b flip-to-deny window (two weeks, >=60% correction rate to flip to `exit 2`), ships with the plugin |
| `pr-base-guard.sh` | contract | new (#1461): `PreToolUse`+`Bash`, joins `pr-preflight.sh`'s matcher group on `gh pr create`/`gh api .../pulls`, extracts `--base`/`base=` and denies a role-workspace PR create whose base is not the repo's resolved default branch, unless the subject issue's body explicitly names that base; a `gh repo view --json defaultBranchRef` lookup failure fails CLOSED (denies) rather than passing a non-default base through, the one deliberate fail-closed exception in this matcher group (every other lookup failure here fails open); zero-install, ships with the plugin |
| `spec-index-preflight.sh` | contract | new (#459): `PreToolUse`+`Bash`, intercepts `git commit` before it lands, denying a staged spec-index-tracked file whose content changed without a matching index regen in the same staged set; ports `spec_index.parse_index` inline (zero-install), ships with the plugin |
| `requirement-digest-preflight.sh` | contract, zero-install-hooked | new (issue #930, northpole req#6): `PreToolUse`+`Bash`, joins `spec-index-preflight.sh`'s matcher group, intercepts `git commit` before it lands, denying a commit that changes `docs/specs/requirements.md` (staged, or via a working-tree `-a`/`-am` diff-against-HEAD check, since `-a` never appears in a pre-run `git diff --cached` snapshot) without a matching `docs/specs/requirement-digest.md` regeneration landing in the same effective change set; ports `requirement_digest.parse`/`render` inline (zero-install, no `gates/` checkout required in the consumer repo), ships with the plugin |
| `deliverable-guard.sh` | contract | already shipped; blocks orchestrator-authored deliverables |
| `directive.sh` | contract | already shipped; `UserPromptSubmit` role directive injection; now also sources `poll-rearm.sh` for its poll-due/watchdog arm step (#801) instead of carrying its own copy |
| `poll-rearm.sh` | contract | new (#801): not a hook itself — a shared function library (`poll_rearm_resolve_checkout()`, `poll_rearm_arm_if_due()`) sourced by `directive.sh` (`UserPromptSubmit`) and `stop-poll-rearm.sh` (`Stop`) so both turn-boundary trip points call the exact same `spawn.py poll-due`/`watchdog --auto-respawn` arm logic, not two copies; zero-install, ships with the plugin |
| `stop-poll-rearm.sh` | contract | new (#801): `Stop`, orchestrator-only (no-ops when `CLAUDE_ROLE` is set), re-arms the same poll-due/watchdog check `directive.sh` arms on `UserPromptSubmit`, now also at turn-end — widens the turn-driven best-effort self-poll loop (phase-1 proposal `docs/issue-801/proposals/technical-feasibility.md` candidate 4) to both turn boundaries; does not survive the session's own death (unchanged external hard boundary); zero-install, ships with the plugin |
| `record-claim-shape-directive.sh` | contract | new (#730): `UserPromptSubmit`, role-session-only (no-ops unless `CLAUDE_ROLE` is set), states `record-claim-guard.sh`'s citation shape proactively before the `PreToolUse` gate ever fires; text generated at hook-run time from `gates/record_lint.py`'s own check function docstrings, not a hand-typed second copy; zero-install, ships with the plugin |
| `stop-gate.sh` | contract | already shipped; `Stop` hook |
| `record-claim-guard.sh` | contract | new (#457): `PreToolUse`+`Write|Edit|MultiEdit`, session-side write-time mirror of `gates.py`'s record-claim-integrity checks (#310/#330/#331/#332/#333); zero-install, ships with the plugin |
| `credential-record-guard.sh` | contract | new (#858): `PreToolUse`+`Write|Edit|MultiEdit`, denies a write under `docs/**` whose new content (or, for MultiEdit, whose edit-fragment concatenation) contains a full-length credential pattern (GitHub `gho_`/`ghp_`/`ghs_`/`ghr_`/`github_pat_`, OpenAI-style `sk-`, AWS `AKIA`), allowing `[REDACTED]` markers and short truncated prefixes by construction; closes the PR #855 near-miss; self-contained (no shared gates module — issue #858 asked only for this write-time gate, no CI-diff-scan counterpart), zero-install, ships with the plugin |
| `upstream-defect-scope-guard.sh` | contract | new (#1131): `PreToolUse`+`Bash`, denies fail-closed any call shape that would create a pull request against the upstream on-the-record repo — `gh pr create` (incl. `GH_REPO`/`GH_HOST`-env-var-prefixed), `gh api` POST against a `/pulls`-shaped endpoint, GraphQL `createPullRequest` via `gh api graphql`, `hub pull-request`, and direct `curl`/`wget` against the GitHub REST pulls endpoint or GraphQL endpoint with a `createPullRequest`-shaped body; universal (fires on every Bash call, no per-session scoping), same posture as `credential-network-guard.sh`; coverage surface widened by the phase-1 warrant hunt beyond the single `gh pr create` literal shape (issue #1131 requirement 4: "Consumers file ISSUES ONLY — never PRs" must be structurally enforced, not advisory); self-contained, zero-install, ships with the plugin |
| `credential-network-guard.sh` | contract | new (#903): `PreToolUse`+`Bash\|WebFetch`, denies fail-closed when a known credential/secret pattern (GitHub `gho_`/`ghp_`/`ghs_`/`ghr_`/`github_pat_`, OpenAI-style `sk-`, AWS `AKIA`) appears in a `Bash` command that reaches the network (`curl`/`wget`/`nc`/`ssh`/`scp`/etc., directly or via a pipe/chain segment) or in a `WebFetch` `url`/`headers`/`body` input; universal pattern-based match, not a per-command allow/deny list; closes the #894/#902 residual-risk gap (credential-record-guard.sh only covers `docs/**` writes, not the Bash/network surface); honest limit: base64/hex/other obfuscation of the credential text evades a plaintext-pattern match, so this raises the bar for the one irreversible-harm class rather than closing it completely — network egress allow-listing at the hosting environment is the complementary control for obfuscated exfiltration; self-contained (no shared gates module, same precedent as credential-record-guard.sh), zero-install, ships with the plugin |
| `record-tiering-directive.sh` | contract | new (#760): `UserPromptSubmit`, role-session-only (no-ops unless `CLAUDE_ROLE` is set), states `record-tiering-guard.sh`'s self-declared-empty shape for `docs/issue-<n>/reports/implementation.md`'s `## What did not work` section proactively, before the `PreToolUse` gate ever fires; static text (no separate gates module to generate from), authored and kept in sync with the guard in the same commit; zero-install, ships with the plugin |
| `role-deviation-directive.sh` | contract | new (#983): `UserPromptSubmit`, role-session-only (no-ops unless `CLAUDE_ROLE` is set), states the #958/#803 deviation loop's RECOGNIZE/CLASSIFY/RESOLVE steering for a role session — mirrors `record-tiering-directive.sh`'s gate skeleton, opposite of `directive.sh`'s orchestrator-only gate. Its file-as-issue resolution differs from the orchestrator variant: a role session finishes its frozen write set, stops, and reports (role-handoff contract v3's SCOPE-EXCEEDED RULE) instead of calling `spawn.py spawn`. Closes audit E Finding 1 (`docs/issue-754/reports/defect-verification.md`) together with `deviation-log-guard.sh`'s widened gate below; zero-install, ships with the plugin |
| `deviation-log-guard.sh` | contract | widened (#983): `Stop`, now binds in BOTH orchestrator and role-session contexts (previously `CLAUDE_ROLE`-unset orchestrator-only, the gap audit E Finding 1 named) — its existing `issue-<n>/<role>` branch-to-path regex already resolves a role session's own branch correctly, so only the early-exit line was removed; zero-install, ships with the plugin |
| `record-tiering-guard.sh` | contract | new (#760): `PreToolUse`+`Write|Edit|MultiEdit`, scoped to `docs/issue-<n>/reports/implementation.md` only — the one section `docs/issue-745/reports/product-discovery/current-state.md` measured at zero cross-issue citation; denies a `## What did not work` body that starts with "none" (case-insensitive) but is not the bare marker (`None.`/`None`); a body not starting with "none" is never inspected, so this is a content-shape rule on the self-declared-empty branch only, never a length threshold on content in general; zero-install, ships with the plugin |
| `role-test-claim-guard.sh` | contract | new (#457): `Stop`, role-session mirror of `gates/skip_gate.py` (#334) and the stub/full-suite integrity lesson behind #435, applied to the reply's own pasted test output; zero-install, ships with the plugin |
| `test-tier-directive.sh` | contract | new (#1518): `UserPromptSubmit`, unconditional (not role-scoped), states the test-tier contract convention (`.on-the-record/test-tiers.json` in a target repo: fast command + `budget_seconds` default 300, optional slow command + `trigger_change_classes`) and the no-silent-full-run rule for the no-contract path; observe-only — never denies a tool call; gating is explicitly deferred until the convention sees real target-repo adoption; consumer-side parsing lives in `gates/test_tier_contract.py`; zero-install, ships with the plugin |
| `self-update.sh` | contract | already shipped; `SessionStart`, `git fetch`-only since #2749 (refs/objects only, never the working tree) -- records staleness (`pull=ok`/`deferred:<n>-behind-origin`/`unknown:<reason>`/`failed:fetch:<reason>`) to `.pull-check` but no longer merges; the working-tree advance moved to `spawn.py self-update`, which refuses while any session is live |
| `decision-queue-stopgate.sh` | contract | new (#466): `Stop` hook, surfaces aged `decision_queue` items (>=1h nudge, >=4h block); zero-install, ships with the plugin |
| `impact-guard.sh` | contract | new (#511): `PreToolUse`+`Bash`, denies a Bash command batching 2+ `gh pr merge` calls when the target repo's own open proposals include one requiring individual approval (`docs/specs/impact-classification.md`'s dominant-axis rule); zero-install, ships with the plugin |
| `spawn-allow-gate.sh` | contract | new (issue #810 SCOPE EXTENSION 2): `PreToolUse`+`Bash`, follow-up to `merge-allow-gate.sh` (row below) — grants `permissionDecision: "allow"` for a `python3 <path-ending-in-spawn.py> ...` invocation, scoped to (a) the orchestrator session (`CLAUDE_ROLE` unset, same `session-role-bind.sh` snapshot primitive) and (b) the resolved path normalizing to the checkout's own `spawn.py`, with no unquoted shell chaining or command/process substitution (`&&`, `;`, `\|`, `$(...)`, `` ` ``, `<(...)`, `>(...)`) reachable outside single-quoted spans; only ever adds a permission signal, never denies; zero-install, ships with the plugin |
| `merge-allow-gate.sh` | contract | new (#810, candidate 4): `PreToolUse`+`Bash`, grants `permissionDecision: "allow"` for an explicit-PR-number `gh pr merge` call when the orchestrator session (`CLAUDE_ROLE` unset) invokes it and `gates/landing_readiness.py` reports that PR `READY`; deny (exit-code-2) from any existing gate still wins over this hook's JSON allow on the same event (empirically verified, see `docs/issue-810/reports/implementation.md`); zero-install, ships with the plugin |
| `quality-bar-gate.sh` | contract | new (issue #1156): `PreToolUse`+`Bash`, `merge-allow-gate.sh`-shaped, denies (JSON `permissionDecision: "deny"` + exit-code-2) an explicit-PR-number `gh pr merge` call when `gates/quality_bar.py`'s `classify` returns `BAR_NOT_MET`/`ESCALATE` for any of the 7 bar-scoped roles (`quality_bar`-carrying specs) touched by the PR's changed files; account-resolved anti-circularity (record-author vs. producer git-author accounts, never a bare `CLAUDE_ROLE` compare); bounded rejection escalates at 3 consecutive `bar-not-met` verdicts on the same (PR, role); zero-install, ships with the plugin |
| `heredoc-command-refusal-gate.sh` | contract | new (issue #1976): `PreToolUse`+`Bash`, denies (JSON `permissionDecision: "deny"` + exit-code-2) a role session's (`CLAUDE_ROLE` resolves non-empty, same `session-role-bind.sh` snapshot primitive) `git commit`/`gh issue create`/`gh issue comment`/`gh pr create`/`gh pr comment` command containing any `<<` heredoc redirection, with a refusal message naming the sanctioned alternative literally (two `-m` flags for `git commit`, `--body-file <path>` for the `gh` verbs) — the host's default write-capable-command classifier already refuses this shape as un-analyzable but with no actionable next step (dogfooding note: every role session's first attempt burns 1-3 retries discovering the workaround independently); orchestrator sessions are untouched (`gh-write-allow-gate.sh` already owns the orchestrator's benign quoted-heredoc allow path for the `gh` verbs it recognizes); zero-install, ships with the plugin |
| `gh-write-allow-gate.sh` | contract | new (issue #856, unbuilt half of #810 SCOPE EXTENSION 2): `PreToolUse`+`Bash`, grants `permissionDecision: "allow"` for one of five recognized gh issue/pr write verb shapes (`gh issue create`, `gh issue comment`, `gh pr comment`, `gh issue close`, `gh pr close`), keyed on command SHAPE only via `shlex.shlex(posix=True, punctuation_chars=True)` strict tokenization (issue #824/#834 design) — no argument text past the verb is ever inspected, so a `--body`/comment literal cannot flip the decision — scoped to the orchestrator session (`CLAUDE_ROLE` unset, same `session-role-bind.sh` snapshot primitive) with no unquoted shell chaining or command/process substitution reachable outside single-quoted spans; only ever adds a permission signal, never denies, so an existing deny gate on the same event still wins; zero-install, ships with the plugin |
| `git-push-guard.sh` | contract | new (issue #2617): `PreToolUse`+`Bash`, denies (JSON `permissionDecision: "deny"` + exit-code-2) a role session's (`TOKENMAXXXER_SPAWNED` resolves non-empty, mirror of `heredoc-command-refusal-gate.sh`'s scope) `git push` whose destination refspec(s) resolve to the remote's own default branch — resolved live per push via `git ls-remote --symref <remote> HEAD`, never a hardcoded branch-name literal and never the remote's branch-protection API; a destination shaped like this system's own role branch (`issue-<n>/<slug>`) is fast-path allowed with no network call; a destination not shaped that way that cannot be resolved against the remote (offline, unreachable) fails CLOSED (denies), same posture as `pr-base-guard.sh`'s `defaultBranchRef` lookup failure; command-shape detection tokenizes every `&&`/`;`/`\|`-delimited segment (shlex, same primitive as `gh-write-allow-gate.sh`) rather than using an anchored substring regex, closing the `git -c <key>=<val> push ...` global-option-insertion bypass class issue #876/#866 already found and fixed for `git commit`; orchestrator sessions (`TOKENMAXXXER_SPAWNED` unset — including `spawn.py init --push`'s own default-branch push in `board.py`) are untouched entirely; not wired into `hooks.json` directly — dispatched via `pretooluse_dispatcher.py`'s `GATES` list (issue #2146 consolidation), same as `gh-write-allow-gate.sh`/`heredoc-command-refusal-gate.sh`; zero-install, ships with the plugin |
| `report-framing-check.sh` | contract | new (#320): `Stop`, checks a PR/board report turn's `last_assistant_message` for the four semantic-effect framing elements (resolved problem, prior cost, newly possible, still broken); zero-install, ships with the plugin, appended to the existing `Stop` array (declared first by `stop-gate.sh`) |
| `post-landing-obligation-gate.sh` | contract | new (#1098, northpole req#3/req#5): `PostToolUse`+`Bash`, opens a `.landing-obligations/<issue>-<role>-<pr>.json` verification obligation after a successful `gh pr merge` (command-shape check reused from `merge-allow-gate.sh`; issue/role resolved via the merged PR's own `headRefName`, not the caller's branch); pure side-effect, `PostToolUse` cannot deny; zero-install, ships with the plugin |
| `landing_obligation.py` | contract | new (#1098): obligation lifecycle (`open_obligation`/`resolve_with_reexecution_verdict`/`list_open_obligations`), same shape as `reexecution_gate.py` (row above); composes with `reexecution_gate.py`'s existing verdict rather than re-implementing execution; feeds `landing_readiness.obligation_blocking_cause()` |
| `retry-loop-bound.sh` | contract | new (#507): `PreToolUse`/`PostToolUse` on `Write|Edit|MultiEdit|Bash`, bounds identical-refusal retry loops at K/2K denials per `sha256(tool_name, target)` signature (allow-with-context in `[K, 2K)`, deny outright at `>= 2K`); zero-install, ships with the plugin |
| `approach-cap-warning.sh` | contract | new (#2262): `PreToolUse`/`PostToolUse`, no tool matcher (counts every tool call as a turn-approximation) — `post` bumps a per-session counter, `pre` reads it against `MUSTER_SESSION_MAX_TURNS_RESOLVED`/`MUSTER_APPROACH_WARNING_TURNS` (both set by `pipeline.py:spawn_cmd` only for a role spawn with a resolved, capped turn budget) and, while remaining turns sit in `(0, warn_turns]`, emits `hookSpecificOutput.additionalContext` telling the session to converge (commit/PR/record) instead of exploring further; no-op when the cap env is unset/<=0 or no role is bound; zero-install, ships with the plugin |
| `lint-test-on-edit.sh` | contract | new (issue #2326 Ask #2, round 3): `PostToolUse`+`Write|Edit|MultiEdit`, on a non-docs `.py`/`.sh` edit runs `python3 -m py_compile`/`bash -n` lint plus import-graph-selected impacted tests (greps `test/`+`tests/` for a leading `import <stem>`/`from <stem> import` line naming the edited module's stem, not a `test_<stem>.py` filename match); the impacted-test pytest invocation loads `otr_lint_test_timeout_plugin.py` (`-p otr_lint_test_timeout_plugin -o addopts=`, disabling this repo's default `-n auto` xdist) which bounds any single matched test item to `OTR_LINT_TEST_PER_FILE_TIMEOUT_S` seconds (default 3, `SIGALRM`-based) so one slow/hanging matched file cannot blow the hook's own combined budget; every subprocess call runs via `start_new_session=True` + temp-file output capture + a process-group kill on completion/timeout (not `capture_output=True`/pipes), reclaiming any grandchild the invoked test itself forked and left behind rather than blocking on its held-open pipe fds; combined wall-clock bounded by `OTR_LINT_TEST_BUDGET_S` (default 15) via `subprocess.run(..., timeout=<remaining>)`, same primitive as `retry-loop-bound.sh`; fails open on any missing tool, malformed JSON payload, or path-resolution failure (`trap 'exit 0' EXIT`); docs-only edits fast-path skip before any subprocess (bash pattern match, re-checked authoritatively in the python body via `posixpath.normpath` — the bash fast path itself falls through to that authoritative check rather than skip-exiting whenever the raw guessed path contains a `..` segment, so a `docs/../real.py`-shaped path cannot fast-path past the real check); zero-install, ships with the plugin |
| `call-shape-guard.sh` | contract | new (#512): `PreToolUse`+`Write|Edit|MultiEdit` on `.py` writes, session-side mirror of `gates.py`'s `subprocess_call_shape_divergence` (repo-wide, `git ls-files`-scoped) and `sibling_mention_check` (diff-scoped to the write, checked against the local working-tree branch record) — issue #419 checks, unreachable since `gates/ci.py`'s runner was retired (#460); zero-install, ships with the plugin, root discovered by walking up from `cwd` |
| `role-spec-reference-guard.sh` | contract | new (issue #521): `PreToolUse`+`Write|Edit|MultiEdit`, denies a write to one of the 6 batch-1 verification-family roles' own record file (`docs/issue-<n>/reports/<role>.md`) whose new content carries a backtick-quoted relative path that does not resolve in the working tree — enforces each of those roles' `roles/specs/<name>.spec.json` `reference_resolution.rule`; delegates to `gates/role_spec_shape.py`'s `record_path_role`/`reference_resolution_check`, same zero-install pattern as `record-claim-guard.sh`, ships with the plugin |
| `accumulation-claim-guard.sh` | contract | new (#512): `PreToolUse`+`Write|Edit|MultiEdit` on `.py` writes, session-side mirror of `accumulation.py`'s `check_accumulation_claim` (issue #424), strengthened to field-presence (non-empty `## Accumulation` body, not just heading-existence); zero-install, ships with the plugin, root discovered by walking up from `cwd`; if no proposal file exists yet on disk this hook does not block |
| `pretooluse-dispatcher.sh` | contract | new (issue #2146): the single `PreToolUse` registration (`Write|Edit|MultiEdit|NotebookEdit|Bash|WebFetch`) — a thin bash shim over `pretooluse_dispatcher.py`, which runs all 20 PreToolUse gate checks (the gate `.sh` files above remain on disk as the source of truth; the dispatcher extracts each script's PY heredoc body and executes it in one python process, replicating each bash preamble). Behavior-identical verdicts and refusal text per gate (asserted gate-by-gate by `on-the-record/hooks/test_dispatcher_equivalence.py`), per-gate fail-open with `hook_ledger` recording moved in-process, chain exit 2 with all deny messages on stderr when any gate denies; 20 processes -> 1 (1,021ms -> well under the 150ms target per tool call); zero-install, ships with the plugin |
| `record-scaffold.sh` | repo-local | new (issue #517): CLI-invoked (not a `PreToolUse` hook — no natural lifecycle event to hang it off, per a warrant-hunter finding on the phase-1 proposal), generates a `docs/issue-<n>/reports/<role>.md` skeleton with `PLACEHOLDER:` tokens; not wired into `hooks.json` |
| `delegated-judgment-gate.sh` | contract | new (issue #573): `PreToolUse`+`Bash`, auto-approves/auto-rejects a candidate decision only when both the depth axis (derives from an operator judgment recorded under `docs/product/*.md`) and the mechanical impact axis clear AND the multi-role panel reaches quorum and synthesizes under the fixed `panel-unanimous-support-v1` rule; any missing precondition escalates (no OR fallback, no special-case branch for an empty `docs/product` corpus); zero-install, ships with the plugin |
| `product-capture-stopgate.sh` | contract | new (issue #566): `Stop` hook, nudges the orchestrator session to record requirements/priorities/philosophy/goals stated during the conversation into `docs/product/<category>.md` when a category was flagged but the corresponding doc file gained no new line; advisory only (`additionalContext`, never `decision:"block"`); zero-install, ships with the plugin |
| `design-rationale-guard.sh` | contract | new (issue #960): `PreToolUse`+`Write|Edit|MultiEdit` on `on-the-record/commands/*.md` (this plugin's own user-facing command surface), denies a write whose resulting frontmatter lacks a non-empty `design-rationale:` field — the first landed gate off `docs/specs/role-invariant-coverage.md`'s row 18 (interaction-design)/row 43 (ux-engineering); reconstructs the full post-edit file the same way `record-tiering-guard.sh` does; zero-install, ships with the plugin |
| `accessibility-guard.sh` | contract | new (issue #1130, `docs/specs/role-invariant-coverage.md` row 1): `PreToolUse`+`Write|Edit|MultiEdit` on `**/*.{html,jsx,tsx,vue,svelte}`, denies an added `<img>` with no `alt` or an added interactive element with no discoverable accessible name — presence-check only, target-root-anchored; zero-install, ships with the plugin |
| `api-version-guard.sh` | contract | new (issue #1130, row 2): `PreToolUse`+`Write|Edit|MultiEdit` on `**/openapi.{yaml,json}`/`**/swagger.{yaml,json}`, denies a removed OpenAPI path/method when `info.version` is unchanged — structural diff only, target-root-anchored; zero-install, ships with the plugin |
| `perf-measurement-guard.sh` | contract | new (issue #1130, row 28): `PreToolUse`+`Bash`, denies a `git commit` whose staged diff touches a hot-path file (`*perf*`, `*/hot/*`, `*/hotpath/*`, plus a project-declared `PERF_HOT_PATH_GLOB`) with no `perf:` trailer in the commit message; target-root-anchored; zero-install, ships with the plugin |
| `test-authoring-spawn-check.sh` | contract | new (issue #1130 routing-fix, cause-b test-authoring): `PreToolUse`+`Bash`, denies a `gh pr merge` when `roles/specs/test-authoring.spec.json`'s `use_when.trigger.path_patterns` matched the branch's diff against `origin/main` and `docs/issue-<n>/reports/test-authoring.md` is absent; target-root-anchored; zero-install, ships with the plugin |
| `issue-retrospective-spawn-check.sh` | contract | new (issue #1130 routing-fix, cause-b issue-retrospective): `PreToolUse`+`Bash`, denies a `gh issue close <n>` when `docs/issue-<n>/reports/issue-retrospective.md` is absent; target-root-anchored; zero-install, ships with the plugin |
| `interaction-design-spawn-check.sh` | contract | new (issue #1130 routing-fix, cause-b interaction-design): `PreToolUse`+`Bash`, denies a `gh pr merge` when `roles/specs/interaction-design.spec.json`'s `use_when.trigger.path_patterns` matched the branch's diff against `origin/main` and `docs/issue-<n>/reports/interaction-design.md` is absent; target-root-anchored; zero-install, ships with the plugin |
| `ux-engineering-spawn-check.sh` | contract | new (issue #1130 routing-fix, cause-b ux-engineering): `PreToolUse`+`Bash`, denies a `gh pr merge` when `roles/specs/ux-engineering.spec.json`'s `use_when.trigger.path_patterns` matched the branch's diff against `origin/main` and `docs/issue-<n>/reports/ux-engineering.md` is absent; target-root-anchored; zero-install, ships with the plugin |
| `role-axis-completeness-guard.sh` | contract | new (issue #650, hunt #628 finding): `PreToolUse`+`Bash`, denies a `git commit` when the staged+working-tree `roles/*.json` set has an axis owned by zero or by more than one role, or a role's own `judgment_axes` shape is invalid; wires a real caller onto `gates/role_spec_shape.py`'s `check_axis_ownership`/`check_role_judgment_axes` (issue-573), which had been unit-tested with zero operational caller; fails open on environment gaps (missing `python3`/`git`, no candidate module exposing both functions, not a `git commit`, no staged `roles/*.json`), fails closed only on a positively-determined axis-completeness failure; zero-install, ships with the plugin |
| `plan-order-guard.sh` | contract | new (issue #659): `PreToolUse`+`Bash`, denies a spawn/merge for a plan step before its declared prerequisite step is done, per the issue body's `## 실행 계획` (`gates/flows.py:plan_order_blocked()`/`_plan_from_body()`); zero-install, ships with the plugin |
| `deviation-log-guard.sh` | contract | new (issue #803), sharded + role-scoped (issue #2348): `Stop` hook, no-traceless-deviation invariant for the #803 deviation loop — reads `transcript_path` off the raw Stop event JSON, scans it for a recognized-deviation marker (`inline-fix`/`file-as-issue` phrasing), and cross-checks via `git diff`/`git log -p`/`git status --porcelain` whether a matching append landed under `docs/issue-<n>/reports/<role>/deviation-log/` (role from `$CLAUDE_ROLE`; no role or no issue drop the matching path segment — same issue-keyed-vs-not split `consult-log.md` uses) — `git status --porcelain` was added in #2348 because sharding makes a session's first shard a brand-new untracked file, which `git diff`/`git log -p` alone never report; same `transcript_path`+git-diff mechanism as `product-capture-stopgate.sh` (warrant hunt finding, docs/issue-803/reports/implementation/2026-08-12-hunt-implementation-deviation-loop.md, stance 3), not `stop-gate.sh`'s text-only shape; advisory only (`additionalContext`, never `decision:"block"`); zero-install, ships with the plugin, appended to the `Stop` array immediately after `stop-gate.sh` |
| `hook-fires.sh` | contract | new (issue #2348): not a hook itself — a shared function library (`hook_fires_record()`) sourced by `directive.sh` (`UserPromptSubmit`), `stop-gate.sh` and `stop-poll-rearm.sh` (`Stop`) so all three always-on hooks shard the issue #2028 fire counter (`.orchestrate-hook-fires/<sha256(session_id)[:24]>.log`) into the same per-session file the same way, not three copies — same "shared library, not a live hook" shape as `poll-rearm.sh`; zero-install, ships with the plugin |
| `skill-verdict-guard.sh` | contract | new (issue #2039), scope narrowed to invoked-only (issue #2153): `Stop` hook, per-invoked-skill verdict obligation — reads `transcript_path` off the raw Stop event JSON, scans the first user message for the two spawn.py mounted-skill line prefixes (`마운트된 스킬(--skills`, `이 역할은 skill-repository(`) to get the mounted-name set, then scans the FULL transcript's assistant `tool_use` blocks named `Skill` for the subset actually invoked (`input.skill`, intersected against mounted), and checks the current branch's role record for a `skill-verdict: <name> — applied: ... | not-applicable: ...` line per invoked name only — a mounted-but-never-invoked skill owes no line (delegating the shape check to `gates/record_lint.py`'s `skill_verdict_reason_check`) — same `transcript_path`+git-diff mechanism as `deviation-log-guard.sh`; zero mounted or zero invoked skills is a no-op (no output); advisory only (`additionalContext`, never `decision:"block"`); zero-install, ships with the plugin, appended to the `Stop` array immediately after `deviation-log-guard.sh` |
| `gate-registration-guard.sh` | contract | new (issue #759): `PreToolUse`+`Bash`, denies a `git commit` staging a newly-added `gates/*.py`/`on-the-record/hooks/*.sh`/`.github/workflows/*.yml` mechanism file with no matching row in `docs/specs/enforcement-boundary.md` (and, for a newly-added hook script, also `docs/specs/generated-paths.md`); ports the same derive-and-compare presence check `gates/test_boundary.py`/`gates/test_generated_paths.py` already implement inline (zero-install). Extended (issue #909): a newly-added hook script whose `docs/specs/enforcement-boundary.md` row does not explicitly say it is not a live hook ("not a hook itself", "not wired into `hooks.json`", "CLI-invoked") must also have a matching command entry in `on-the-record/hooks/hooks.json` in the same staged tree — doc row and hooks.json wiring must land together, closing the gap that let `absorbed-branch-recut-guard.sh` carry a "live hook" doc row while never firing (docs/issue-909/reports/conformance-review/survey.md). Fails open on environment gaps (including no `hooks.json` present at all to cross-check against), fails closed only on a positively-determined missing registration; zero-install, ships with the plugin. Blind spot named, not fixed here (issue #2705): this `PreToolUse` read of `git diff --cached` fires BEFORE the intercepted Bash command's text runs, so a bundled `git add gates/new_gate.py && git commit` in ONE Bash call has nothing staged yet at hook-fire time and passes silently — the unbundled shape (stage in one call, commit in the next) is unaffected and still refused exactly as above. Four adversarial rounds established that predicting the eventual staged set from command text (cd/subshell resolution, pushd/popd stacks, symlinks) is not closable by parsing harder; this file's own detection is intentionally left unchanged rather than grown into a fifth parsing round. `gate-registration-post-guard.sh` is the named, weaker-promise companion that catches the bundled shape after the fact. |
| `gate-registration-post-guard.sh` | contract | new (issue #2705): `PreToolUse`(broad matcher, `pre` mode)+`PostToolUse`(`Bash`, `post` mode) pair, the deliberately weaker-promise companion to `gate-registration-guard.sh` for the one shape that guard's `PreToolUse`/`--cached` read structurally cannot see — a bundled `git add <mechanism file> && git commit` in one Bash call. Does not parse command text to predict the staged set (the seam consult's own conclusion after #2705's four rounds: that prediction is undecidable against bash's subshells/aliases/functions/`CDPATH`). `post` mode instead reads git's own reported outcome — the `[<branch> <sha>] <subject>` line `git commit` prints on success — out of the `PostToolUse` payload's `tool_response` text (no exit-code field is available there, same gap `post-landing-obligation-gate.sh` documents), then inspects that exact commit's own tree via `git show --name-status` for a newly-added `gates/*.py`/`on-the-record/hooks/*.sh`/`.github/workflows/*.yml` file missing its `docs/specs/enforcement-boundary.md`/`docs/specs/generated-paths.md`/`hooks.json` row (the identical classification `gate-registration-guard.sh` uses, intentionally duplicated inline rather than imported, per this file's own zero-install convention), and records any miss to a session-keyed state file. **Cannot deny — the commit already exists by the time this fires.** `pre` mode (any tool, next call) reads that state, re-checks the CURRENT working tree in case a follow-up commit already added the row, and — only while still open — emits `hookSpecificOutput.additionalContext` naming the commit, the missing row, and explicitly that this is a report after the write happened, not a refusal before it; repeats every call until the row lands (mirrors `approach-cap-warning.sh`'s own "cannot scroll out of context" rationale), then clears. Fails open on every environment/parse gap, same posture as every other gate in this file; zero-install, ships with the plugin |
| `session-role-bind.sh` | contract | new (issue #698): `SessionStart`, snapshots `CLAUDE_ROLE` into a session_id-keyed state file before any session-controlled code runs, so `approval-gate.sh` (and future consumers) can trust a pre-session read instead of a later, model-influenced env-var read; first-observation wins, fail-open on missing `CLAUDE_ROLE`/`session_id`; zero-install, ships with the plugin |
| `delegation-post-gate.sh` | contract | new (issue #707): `PreToolUse`+`Bash`, denies a `gh issue comment`/`gh api` call posting a body matching `APPROVE issue-<n>/<role> VIA DELEGATION <scope>` when the posting session has any bound role (positively checked via #698's session-role-bind snapshot, falling back to the live `CLAUDE_ROLE` env var — absent both is this repo's own existing "orchestrator" signal); the after-proposal hunt (`docs/issue-707/reports/product-discovery/hunt-after-proposal.md`) found the narrower "role differs from the citation's own branch role" check insufficient, so this refuses ANY bound role, not only a matching one; zero-install, ships with the plugin |
| `test-authoring-invariant-guard.sh` | contract | new (issue #896 step 2, REFRAME: invariant-first): `PreToolUse`+`Bash`, intercepts `git commit` (same interception point as `pr-preflight.sh`/`spec-index-preflight.sh`), classifies the staged (or, if nothing staged, working-tree-vs-HEAD) diff's paths as code/test/doc/config by extension and path segment, and denies when at least one code path changed with no test path changed in the same set, unless the commit message carries a reasoned `Test-N/A: <reason>` trailer; the standing, always-on invariant half of test-authoring's expertise — no spawn, no cost decision; zero-install, ships with the plugin |
| `live-fire-test-guard.sh` | contract | new (issue #914 step 2, mechanism b, highest RICE): `PreToolUse`+`Bash`, sibling to `gate-registration-guard.sh` on the same `git commit` interception point — one layer stricter. For a newly-staged (`A`/`R`/`C`) `on-the-record/hooks/*.sh` or `gates/*.py` module that already carries (or is gaining, in the same commit) a `docs/specs/enforcement-boundary.md` row, requires the same commit also stage a live-fire test (`on-the-record/hooks/test_<slug>.py` for a hook script; `gates/test_<stem>.py` for a gate module) that actually invokes the module as a real lifecycle event — a hook script's test must pipe a crafted payload via `subprocess.run(..., input=...)` into the script by name and assert >= 2 distinct exit-code outcomes; a gate module's test must reference and call the module from >= 2 distinct test functions — not merely that the module imports or a test file exists. Directly closes the #909 orphan class: `absorbed-branch-recut-guard.sh` had a test file present and a "live hook" doc row while carrying no `hooks.json` entry to fire against; a live-fire test asserting a real allow/deny outcome would have had nothing to invoke and failed this guard at commit time. Escape hatch: commit-message line `Live-fire-N/A: <reason>` for a module with no lifecycle-event surface to live-fire. Defers the missing-registration-row condition to `gate-registration-guard.sh` (fails open, no double-deny). Fails open on environment gaps (no `python3`/`git`, not a `git commit`, unparseable payload, no newly-staged mechanism file); zero-install, ships with the plugin |
| `acceptance-command-real-run-guard.sh` | contract | new (issue #914 step 2, mechanism a, generalizes #870 candidate-b): `PreToolUse`+`Bash`, sibling to `gate-registration-guard.sh`/`live-fire-test-guard.sh` on the same `git commit` interception point. For a staged file whose content carries an `acceptance: <command> — result: PASS|FAIL` citation (the exact shape #892's `outcome_claim_citation_check` already accepts as executed-live), requires the cited command to have a row in `docs/specs/acceptance-commands.md` (the one-time-confirmed acceptance command per target, mirroring #831's remote-preflight setup pattern — adding the row IS the confirmation event, a durable git-tracked substitute for spawn.py's orchestrator-side `ledger_write`, which a stateless PreToolUse hook cannot reach), then actually re-runs that command (bounded 180s timeout, `shlex.split` argv, no shell interpolation) against the real current target and denies the commit if the actual exit status does not match the claimed `PASS`/`FAIL`. `result: UNMEASURED` (the degrade path — no acceptance command on record) is never re-run and always allowed, so an unmeasurable target degrades honestly instead of blocking. Closes the gap #892 leaves open: a citation can look like a real run without one having happened this turn; this guard makes the citation true by making the run actually happen. Escape hatch: commit-message trailer `Acceptance-recheck-N/A: <reason>`. Fails open on environment gaps (no `python3`/`git`, not a `git commit`, no staged `acceptance:` citation, unreadable registry); zero-install, ships with the plugin |
| `live-fire-claim-real-run-guard.sh` | contract | new (issue #914 step 2, mechanism c): `PreToolUse`+`Bash`, general done-claim gate composing mechanisms (a)/(b) rather than duplicating either — sibling to `gate-registration-guard.sh`/`live-fire-test-guard.sh`/`acceptance-command-real-run-guard.sh` on the same `git commit` interception point. For a staged file whose content carries a `live-fire: <on-the-record/hooks/*.sh or gates/*.py path> — result: allow|deny|log` citation (the new shape `gates/record_lint.py`'s `outcome_claim_citation_check`/`_EXECUTED_LIVE_CANONICAL` now also accepts as executed-live, same additive widening `acceptance:` already got for mechanism a), derives the cited module's live-fire test path with the exact same slug rule `live-fire-test-guard.sh` uses, then actually EXECUTES that test via `python3 -m pytest -q` (bounded 180s timeout) against the real current tree and denies the commit if it does not exist or exits nonzero — turning mechanism (b)'s "a live-fire test exists in the required shape" into "that live-fire test actually passes, right now, for this claim", the same real-execution strengthening mechanism (a) already supplies for `acceptance:` citations. Where neither an `acceptance:` nor a `live-fire:` citation backs a done-claim, this guard does not fire — presence/shape enforcement for that case stays owned by `record-claim-guard.sh`/#892 (`UNMEASURED-with-reason` degrade path), never re-implemented here. Escape hatch: commit-message trailer `Live-fire-recheck-N/A: <reason>`. Fails open on environment gaps (no `python3`/`git`, not a `git commit`, no staged `live-fire:` citation); zero-install, ships with the plugin |
| `approval-gate.sh` | contract | new (issue #608): `PreToolUse`+`Write|Edit|MultiEdit`, role-session-only (no-ops unless `CLAUDE_ROLE` is set), denies a phase-2-shaped write (the acting role's own `docs/issue-<n>/reports/<role>.md`, or a `src/`/`test(s)/` path) unless `docs/specs/approvers.md` is present AND a matching `APPROVE issue-<n>/<role>` issue comment from a listed account exists; absent `approvers.md` denies with a refuse-and-instruct (bootstrap-offer) message rather than a silent allow. Closes the coverage hole step 1's fixture measurement confirmed (`docs/issue-608/reports/execution-observation/fixture-measurement.md`, Findings 1-2): the only prior approval checks (`contract-guard.sh`, `pr-preflight.sh`) are both `Bash`-matcher, gated on `gh pr` verbs only, never reached by a plain file write. Branch-name parse failure (detached HEAD, non-issue branch) fails open, matching `pr-preflight.sh`/`contract-guard.sh`'s existing policy; a `gh` lookup failure also fails open (infrastructure failure, not an approval-state failure). Extended (issue #707): also accepts a distinct `APPROVE issue-<n>/<role> VIA DELEGATION <scope>` citation shape, when backed by a live (unexpired, unrevoked, in-scope) `DELEGATE <scope> UNTIL <date>` grant comment from a listed account; no `DELEGATE` comment present degrades byte-identically to the pre-#707 behavior. Zero-install, ships with the plugin |

## `.github/workflows/*.yml` (retired, issue #460)

The operator ruled (2026-08-08, issue #460) that this repo's own CI red-X
checks — including its own — are retired: all enforcement lives in the
shipped hook surface plus locally runnable gate commands. `.github/workflows/`
is deleted; the table below is the migration record `gates/test_boundary.py`
(via `test_boundary_workflow_migration.py`) checks against, not a listing of
files that still exist.

| mechanism | verdict | replacement |
|---|---|---|
| `on-the-record-tests.yml` | repo-local, deleted | locally runnable `python3 -m pytest` (or `pytest -q`), run by hand or by the orchestrator loop before landing, per the no-mock "build it, run it" phase-2 bar; no shipped hook can run the suite (hooks fire on tool-use events inside a session, not on a schedule or PR event) |
| `plan-aware-closes-gate.yml` | repo-local, deleted | `--closes-only` step: zero-install `on-the-record/hooks/contract-guard.sh` + `spawn.py`'s `acceptance_gate` preflight (see `ci.py` row above). Full-bundle step (write_scope/protected-path/deps/`record_checked_claims`): no zero-install replacement; existing `contract, CI-supplement` drop, runnable locally as `python3 gates/ci.py . --pr <n> --autodetect` |
| `closure-sweep.yml` | repo-local, deleted | single-PR case: zero-install `contract-guard.sh` (see `closure_sweep.py` row above). Board-wide case: now `contract, orchestrator-loop` via `spawn.py:roster_watchdog()` (issue #464 ADR), also runnable locally as `python3 gates/closure_sweep.py` |
| `issue-bundling-gate.yml` | repo-local, deleted | no replacement possible — issue-creation is a GitHub webhook event, unreachable by any Claude Code session hook; runnable locally as `python3 gates/issue_bundling.py <issue#>` |

## `spawn.py`

| mechanism | verdict | reason |
|---|---|---|
| `spawn.py` | contract | not marketplace-shipped, but every caller (including consumers) runs this exact file directly — no per-consumer copy to go stale; carries `require_board`/`require_no_repo_config`/`require_acceptance_gate` preflights |

## Consumer-readable extract

`on-the-record/UNENFORCED-CLAUSES.md` (issue #452) is the derived,
gate-checked extract of this file's `contract, CI-supplement` and
`out of scope — operator decision` rows, shipped inside the deployed
`on-the-record/` tree so a consumer session can read it zero-install.
`gates/test_boundary.py` fails the build if it drifts from this file.

## Reachable vs. unreached by the zero-install baseline

See `docs/issue-441/proposals/2026-08-07-contract-enforcement-boundary.md`
("Reachable vs. unreached" table) for the full act-by-act breakdown. In
summary: a Claude Code session's `gh pr merge`/`git push` and opening a
phase-2 session are reached with zero installation. A human merging or
closing via the GitHub web UI, or a person running `gh`/`git` from a plain
terminal outside any Claude Code session, are not reached by anything in
this delivery — genuinely unreached, not solved, per #310. Board-wide
drift detection is out of scope per the operator decision above, so it is
not "unreached" in that sense — there is no obligation for it here.
