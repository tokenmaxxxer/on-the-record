2026-08-24T21:24:30+09:00 inline implementation(issue-2195): the before-landing warrant-hunt (docs/issue-2195/reports/implementation/2026-08-24-hunt-auto-sweep-background-dispatch.md) surfaced that the first cut of the auto_sweep background dispatch (threading.Thread(daemon=True), no completion logging) left bootstrap_timing's auto_sweep= field reading ~0.000s unconditionally, silently erasing the only place a future 148s-class regression could ever be observed -- the same hidden-time failure mode issue #2186 was filed to eliminate for the other eight phases. Resolved inline, staying inside the frozen write set (spawn.py's _spawn_one, the same block already being edited): added an elapsed-time/outcome print(..., file=sys.stderr) to the _run_auto_sweep background closure, and extended tests/test_auto_sweep_nonblocking.py to assert the completion line appears with the right removed-count -- see docs/issue-2195/reports/implementation.md's item 4 above and its Open findings.
