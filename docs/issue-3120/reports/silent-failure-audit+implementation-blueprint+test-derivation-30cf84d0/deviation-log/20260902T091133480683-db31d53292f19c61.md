2026-09-02T09:11Z inline — the record's first two `Write` attempts to `docs/issue-3120/reports/silent-failure-audit+implementation-blueprint+test-derivation-30cf84d0.md` were refused by `record-claim-guard.sh` (bare count claims without `derived:`, outcome claims — "passed"/"complete" — without a `canonical:`/`derived:` tag in the same section, and two backticked paths for not-yet-created sibling probes owned by another session). Expected the first pass to satisfy the gate given the worked example already in this session's directives; fixed by restructuring every `acceptance:`/`checked:` line into `canonical: acceptance: <command> — result: ...` form and dropping backticks from the two nonexistent probe paths. derived: `git log -1 --format='%H %s'` after the eventual successful write, result: `8a3d8bb9 issue-3120: implementation record for layers 1/2 delivery`.

2026-09-02T09:12Z inline — the same record commit's first `git commit -m/-m/-m` (no `Subject: issue-3120` trailer) landed, then `trailer-gate.sh`'s PostToolUse context flagged it as missing the required trailer immediately after (same shape as docs/issue-2876/reports/silent-failure-audit-b01a1db4/deviation-log's prior entry). Fixed via `git commit --amend` (unpushed, nothing stacked on top) adding a `Subject: issue-3120` trailer. derived: `git log -1 --format='%H %s'`, result: `8a3d8bb9 issue-3120: implementation record for layers 1/2 delivery` — the amended sha, later cited as `code_under_review: same-commit` in the record's own frontmatter.

2026-09-02T09:26Z inline — `gh pr create` was refused once by `pr-preflight.sh`: issue #3120 had a new comment (`issuecomment-5507212053`, posted by the other session working `directive.sh`) after this session's start, and the record lacked an `amendments-reconciled` line citing it. Expected no new issue activity mid-session; fixed by reading the comment (`gh api repos/tokenmaxxxer/on-the-record/issues/comments/5507212053`), confirming it independently corroborates this delivery's own already-recorded scope split (layers 1/2 here, wake-notice in PR #3132, layer 3 in issue #3125), and adding the `amendments-reconciled:` line to the record's "Scope note" section before retrying. derived: `gh pr create --title ... --body-file /tmp/pr-body-3120.md`, result: `https://github.com/tokenmaxxxer/on-the-record/pull/3133` (succeeded on the retry).
