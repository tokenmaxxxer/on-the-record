# Deviation log — issue #2417 (execution-observation role)

- 2026-08-26T00:00:00Z | inline | issue-2417/execution-observation |
  mid-session, this host's own `/` filesystem hit real inode exhaustion
  (`df -i /` at 100%, free-inode count fluctuating through 0 and 9-10
  before recovering) while the delegated freelunch worker was setting up
  its isolated test worktree — its own tool calls and this session's own
  `Bash` calls started failing with live `ENOSPC` writing their own
  output files. Not called for by this task's scope (observing the fix,
  not living through the exact failure mode it fixes), and required a
  judgment call: wait it out rather than force through, since forcing
  file writes under 0 free inodes is exactly the illegible-failure
  condition issue #2417 is about. Resolved by starting a Monitor poll
  loop for the host's own inode recovery, then resuming the worker twice
  via SendMessage as headroom returned (first to ~10 free, then to
  ~11,000 free). Not filed as a new issue: a one-off unblock action, and
  directly relevant corroborating evidence for the issue itself — it is
  documented in the delivered record's own "What was done" section
  (final paragraph) rather than spun out separately.
  canonical: `df -i /` (this session, mid-incident) — free-inode column
  read 0, then later 9-10, then recovered past 500 (Monitor task
  `bo055dg5v` captured `11031` free at recovery); the worker's own
  independent `df -i /` readings (12699, then 1368677 free) are in the
  delivered record.

- 2026-08-26T00:00:00Z | inline | issue-2417/execution-observation |
  the delegated worker's first attempt to `Write` the completed record
  file was refused by this repo's `record-claim-guard` pretooluse hook
  (issues #793/#870/#330): several status/outcome claims in the "Open
  findings" section lacked an adjacent `canonical:`/`derived:` citation
  in the same section, and a reference to the implementer's record path
  needed an explicit "untracked in this checkout" annotation near every
  occurrence, not just the first. Not anticipated in the original task
  brief (which described the record's content requirements but not this
  repo's mechanical citation-adjacency gate). Resolved by relaying the
  hook's exact flagged phrases back to the worker and having it add the
  missing tags/annotations in place, then retry the `Write` — no content
  was removed or weakened, only citations added. Not filed as a new
  issue: a one-off mechanical fix to satisfy an existing repo gate, no
  design/architecture/product judgment involved.
  canonical: the `PreToolUse:Write` hook error transcript from the
  worker's first `Write` attempt (quoted in full in this session's own
  transcript) named the three exact flagged phrases; the retried `Write`
  succeeded with no further hook error.

- 2026-08-26T00:00:00Z | inline | issue-2417/execution-observation |
  landing hit two mechanical repo gates not anticipated in the original
  plan: (1) `heredoc-command-refusal-gate` (issue #1976) refused the
  first `git commit` attempt because its message body used
  `-m "$(cat <<'EOF' ... EOF)"` — resolved by switching to one `-m` flag
  per paragraph, no content change. (2) `pr-preflight` refused the first
  `gh pr create` attempt because the PR body lacked a `Closes #2417` (or
  Fixes/Resolves) trailer — under this session's `CORE_BUILD_NOW=1`
  build-now bypass, the gate treats this branch as a phase-2-equivalent
  delivery PR, which this repo's convention requires to carry that
  trailer regardless of role. Resolved by adding `Closes #2417` to the
  PR body (issue #2453, the actual code-fix PR, also carries this same
  trailer — both referencing the same issue is expected under this
  convention, not a conflict). Not filed as new issues: both are
  one-off, mechanical corrections to match existing, already-documented
  repo conventions, no design/architecture/product judgment.
  canonical: the two `PreToolUse:Bash` hook error transcripts from this
  session (heredoc refusal, then pr-preflight's exact
  `expected: 'Closes #2417'` message) are in this session's own
  transcript; both retried commands succeeded after the fix (commit
  `760bdb8b`, PR https://github.com/tokenmaxxxer/on-the-record/pull/2459).
