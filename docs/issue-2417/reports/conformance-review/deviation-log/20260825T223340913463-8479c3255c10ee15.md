# Deviation log — issue #2417 (conformance-review role)

- 2026-08-26T07:09:00Z | inline | issue-2417/conformance-review |
  mid-review, this host's own `/` filesystem hit real, live inode
  exhaustion (`df -i /` observed at 100%, free-inode count as low as 8,300
  of 61,022,208) — the harness's own Bash/Write/Agent tool calls started
  failing with live `ENOSPC` for a stretch of roughly 30 consecutive tool
  calls across this session, recovering and re-failing intermittently
  before staying up. Not called for by this task's scope (reviewing the
  disk-exhaustion fix, not living through a fresh instance of it), and
  required a judgment call outside the frozen review scope: keep retrying
  the blocked tool calls vs. abandon the confirmatory re-run and rely on
  code inspection alone. Resolved by retrying until tools recovered
  (space freed on its own, consistent with the issue's own "bursts"
  framing) and completing the interrupted verification
  (`test_roster_watchdog_survives_checkpoint_workspace_oserror`) once
  Bash access returned. Not filed as a new issue: the same live "bursts"
  phenomenon issue #2417 and the implementation record (PR #2453) already
  both name, not a new failure mode — directly relevant, live
  corroborating evidence for the issue under review, documented in detail
  in `docs/issue-2417/reports/conformance-review.md`'s "What did not work"
  section rather than spun out separately.
  canonical: `df -i / /tmp` (this session, mid-episode) —
  `/dev/nvme0n1p2 61022208 61013908 8300 100% /`; the record's own "What
  did not work" section carries the full transcript and the eventual
  clean pass once tools recovered.

- 2026-08-26T07:40:00Z | inline | issue-2417/conformance-review |
  a first attempt to write the review record was rejected in full by this
  repo's `record-claim-guard`/path-existence hooks (bare numeric claims
  without `derived:`/`canonical:` tags, cross-branch file:line citations
  that don't resolve on this reviewer's own branch, and an outcome
  sentence that happened to contain the literal substring "pass" inside
  an unrelated clause about Python's `pass` statement, tripping the
  outcome-claim pattern match). Not a scope deviation, but consumed
  significant session effort: rewrote every finding's evidence with
  `<sha>:<path>` pinning to the reviewed commit and inline code-fenced
  transcripts of this session's own command output, re-ran several checks
  live specifically to have fresh, citable transcripts rather than relying
  on memory of earlier-session output. Resolved inline, same session, no
  scope change.
  canonical: the two `PreToolUse:Edit hook error` tool results in this
  session's own transcript naming `record-claim-guard`/issue #333, #793,
  #870, #330 by number; the final accepted record is
  `docs/issue-2417/reports/conformance-review.md` (commit `990a583e`).
