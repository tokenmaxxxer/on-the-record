# Deviation log — issue #2417 (implementation role)

- 2026-08-26T06:20:00Z | inline | issue-2417/implementation |
  mid-session, this host's own `/` filesystem hit real inode exhaustion
  (`df -i /` at 100%, free-inode count as low as 85) — the harness's own
  Bash/Write tools started failing with live `ENOSPC` for roughly ten
  consecutive tool calls (`/tmp/claude-1000/.../tasks/*.output`, and
  separately `.git/warrant` in this repo). Not called for by this task's
  scope (the disk-exhaustion fix itself, not disk-exhaustion recovery),
  and required a judgment call outside the frozen write set: wait it out
  vs. act, and if acting, identify disposable scratch data without
  touching other sessions' active work on this shared host. Resolved by
  retrying the tool calls (space freed on its own after several retries,
  consistent with this issue's own "bursts" framing) and then deleting
  only this session's own `/tmp/demo*`/`/tmp/fstest*` live-repro fixture
  files (~950MB combined, large-but-few files — not the inode
  contributor, byte space only). Not filed as a new issue: a one-off
  unblock action, not a design/architecture/security/product judgment
  call — this is also directly relevant, live corroborating evidence for
  the issue this session was already fixing, so it is documented in
  detail in this record's own "What did not work" section rather than
  spun out separately.
  canonical: `df -i /` (this session, mid-incident) —
  `/dev/nvme0n1p2 61022208 61022123 85 100% /`; `git status --short`
  (this session, immediately after cleanup) showed no repo-tree residue
  from the `/tmp` cleanup, only this delivery's own tracked changes.

- 2026-08-26T06:36:00Z | inline | issue-2417/implementation |
  the mandatory before-landing warrant-hunter (stance 0: "assume the gate
  just touched is bypassable") found that `_spawn_capacity_check()` was
  only wired into `issue_workspace()`'s fresh-clone branch, not the two
  reuse branches (`src == work.resolve()` and existing-`.git`
  workspace-reuse) that call `_fetch_or_halt` and can equally ENOSPC —
  live-reproduced by the hunter with an impossible `MUSTER_MIN_FREE_BYTES`
  threshold. Not called for by this task's original write set framing
  (the fresh-clone call site had already been treated as "the" clone
  path), but mechanical to resolve — move one existing function call
  earlier in the same function, in the same two already-being-edited
  files, no design/architecture/security/product judgment, one-off.
  Resolved inline: moved `_spawn_capacity_check(work)` to run once before
  all three branches (spawn.py, `issue_workspace()`), added
  `SpawnCapacityCheck::test_reuse_branch_is_also_refused_not_just_fresh_clone`
  as a regression test, and appended a "Resolution" section to the
  hunter's own hunt-record file. Not filed as a new issue: same commit,
  same delivery, before landing — filing it separately would have split
  one fix across two issues for no reason.
  canonical: `docs/issue-2417/reports/implementation/2026-08-26-hunt-disk-inode-exhaustion-fix.md`
  (this delivery's own hunt record, landed in the same commit) —
  reproduction, before/after, and the regression test name are recorded
  there in full.
