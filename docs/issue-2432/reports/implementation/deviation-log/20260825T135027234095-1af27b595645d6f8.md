- 2026-08-25T13:50:27Z — filed — the stage-4 proposal's `files:` list places the in-flight-branch-handling gate doc under the parent program issue's own tree (issue #2241's own architecture-reports tree, untracked); `board-gate.sh` R4 (cross-subject write, no `maintenance-targets: issue-2241` line on this issue) and then R11 (cross-role write, `architecture/` vs this role's own `implementation/** ` scope) both refused it live from this branch, and a role session cannot self-grant the `maintenance-targets` exception (`gh issue edit` denied) or spawn a peer role on its own initiative — reported rather than spawned: landed the same content under this issue's own `implementation/` subtree instead (`docs/issue-2432/reports/implementation/in-flight-branch-migration.md`) and filed `gh issue comment 2432` naming both unblock paths (a session on `issue-2241/implementation`, or a human adding the `maintenance-targets` line). Full reasoning and verbatim gate refusals: docs/issue-2432/reports/implementation.md's "Rationale for deviations" section.
