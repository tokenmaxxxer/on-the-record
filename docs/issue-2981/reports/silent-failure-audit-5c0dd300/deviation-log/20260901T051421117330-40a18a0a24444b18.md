2026-09-01T05:14:21Z inline — PR #3002's branch (`issue-2981/merge-gates+test-derivation-2f452df8`) was cut before issue #2974 (which added `check_runner.pr_diff_paths()`/`touches_implementation_paths()`, the functions this fix needed to reuse) landed on `main`; the first test run hit `AttributeError: module 'check_runner' has no attribute 'pr_diff_paths'`. Merged `origin/main` into the branch (`git merge origin/main --no-edit`, clean, no conflicts, commit `4fd0dc65`) before writing the fix, rather than reimplementing issue #2974's diff-classification logic locally.

derived: `git log --oneline -1 4fd0dc65` — result:
```
4fd0dc65 Merge remote-tracking branch 'origin/main' into local-3002-work
```

canonical: `docs/issue-2981/reports/silent-failure-audit-5c0dd300.md` § "What did not work" (this session's own record, same commit) — full account of this deviation.
