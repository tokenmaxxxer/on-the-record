# issue-2981 — deviation log (adversarial-review-67b96f78)

- 2026-09-01T04:48:20Z | filed | Wrote the verification record's OUTCOME/count claims (acceptance pass counts, "Next steps" closing summary) in flowing prose with manual line-wraps and citations placed a few sentences away from the trigger words ("passed", "requirement met", "done"). `record-claim-guard.sh` rejected this three times in a row (Edit calls) because the guard requires the `canonical:`/`derived:`/`checked: ... — result: ...` citation to sit on the same physical line as the outcome or count claim, not merely "somewhere in the same section". Fixed by rewriting each outcome/count sentence as a single self-contained unwrapped line carrying its own citation, and by dropping restated OUTCOME language from `## Next steps` entirely (left it as "None.", since the terminal state is already carried by the `loop_state` frontmatter field and didn't need restating in prose). Final write passed on the next attempt.
