# issue-2981 — deviation log

- 2026-09-01T04:37:56Z | filed | Expected: the warrant-directive dispatches a background warrant-hunter at two moments — right after a phase-1 proposal is written, and right before the work lands. Actual: this session used the build-now bypass (CORE_BUILD_NOW=1, contract v3 s19a), which skips the proposal round entirely, so there was no "after-proposal" moment to dispatch at; the entire implementation (code + tests + record + commit + push + PR) was then delivered in one shot by a single delegated worker, landing before the orchestrating session had a chance to dispatch a "before-landing" hunter. Net effect: no warrant-hunter was dispatched at either moment this session. Reported here rather than spawned/fixed, since the bypass had already collapsed both dispatch moments by the time this was noticed. See PR #3002, commit 6a27352f.
