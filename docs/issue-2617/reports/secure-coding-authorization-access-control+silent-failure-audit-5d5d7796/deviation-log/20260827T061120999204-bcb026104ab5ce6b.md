derived: `git show 9883a0b4 --stat` and `docs/issue-2617/reports/secure-coding-authorization-access-control+silent-failure-audit-5d5d7796.md`'s "What did not work" section (both entries below cite the same commit and section).

- 2026-08-27T06:15:00Z — inline — before-landing warrant-hunter dispatch found a live bypass (unquoted newline between bash statements not recognized as a segment boundary); fixed inside `on-the-record/hooks/git-push-guard.sh`'s tokenizer (whitespace/punctuation_chars adjustment), same commit `9883a0b4`. Detail: record's "What did not work", entry 1.
- 2026-08-27T06:20:00Z — inline — follow-up self-testing found resolution subprocess calls used this hook's own inherited cwd instead of the pending command's real target directory; fixed by threading the PreToolUse payload's `cwd`/an in-command `-C <dir>` through every subprocess call in `on-the-record/hooks/git-push-guard.sh`, same commit `9883a0b4`. Detail: record's "What did not work", entry 2.
