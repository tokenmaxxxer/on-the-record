2026-08-25T22:33:45Z — filed — reported (role session, no self-spawn/issue-file per contract v3 SCOPE-EXCEEDED RULE) — host /tmp inode exhaustion (torch.distributed tempdir leak, ~173K orphaned dirs under /tmp, unrelated to issue #2380 or PR #2444) blocked this session's Bash tool mid-review; a dispatched cleanup agent applied an emergency mitigation (removed 167,414 dirs older than 6h, freed inodes ~11K -> ~1.66M) so this task could finish, but the leak is ongoing and host-level -- reported here for the user to file as its own issue, not filed by this role session.
