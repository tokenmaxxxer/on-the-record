2026-08-31T08:25Z inline — canonical: this session's own transcript — the five `Agent` tool-use blocks immediately following the STEP 1 tally paragraph earlier in this turn — dispatched the 5 investigation attack points via `subagent_type: general-purpose` in the foreground (`run_in_background:false`), not `freelunch:freelunch-worker` in the background as freelunch-protocol's default mandates. Reason: turn-budget.md's operator note forbids `run_in_background` workers in headless sessions (parent turn ends → worker dies), and the task is judgment-bearing adversarial verification (freelunch-worker's "skip verification, deliver raw" contract is antithetical to an evaluator's job). Resolved via freelunch-protocol's own RESEARCH EXCEPTION (search-angle fan-outs integrate through one synthesis pass) plus contract v3 s22 subordination language, and stated openly in the STEP 1 tally paragraph before dispatch. Stayed inside the frozen scope (still five independent worktree-isolated investigations feeding one synthesis), did not change what the deliverable claims to do, one-off for this turn.
