2026-09-02T06:58 inline — issue #3081's 5th comment (`issuecomment-5505557426`) landed mid-session, after the code fix and probe/tests were already committed, establishing the leak is bidirectional (not just repo-B-reads-repo-A, but the reverse too) and that both boards print the byte-identical union when unfiltered. Extended `gates/probe_drift_repo_leak.py` and `tests/test_requirement_drift_repo_scope.py` with a same-tick, both-directions check (assert the two repos' outputs are not identical) per the comment's own suggested tightening. Stayed inside the frozen write set (same two files already owned by this task), mechanical (added assertions, no new code path), did not change what the deliverable claims to do (the already-committed composite-key fix needed no code change, being symmetric by construction), one-off reaction to new information rather than a systemic pattern — classified INLINE-FIX per docs/handbooks/deviation-loop.md. Location: gates/probe_drift_repo_leak.py, tests/test_requirement_drift_repo_scope.py (commit db1e9e5e).
