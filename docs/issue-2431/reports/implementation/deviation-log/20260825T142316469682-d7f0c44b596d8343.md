canonical: `docs/issue-2431/reports/implementation/2026-08-25-hunt-spawn-attempt-dead-pid-grace-gate.md` (before-landing warrant-hunter finding, this session) and this record's "Open findings" section (independent reproduction run this session).

- 2026-08-25T14:23:16Z — filed — the before-landing warrant-hunter dispatch (mandated by the warrant protocol, not by issue #2431's own acceptance criteria) found that the new `SPAWN_ATTEMPT_GRACE_SEC` dead-pid age gate has no visibility into `roster.py`'s pre-existing per-tick `reported_subjects` dedup (#2413): when two dead-pid attempt_ids share the same `(issue, role)` subject and both cross the grace threshold in the same sweep tick, only one gets its own report line before both are pruned. Verified this is pre-existing behavior unchanged by this round (the prior commit's fully-unconditional dead-pid prune had the identical interaction), not a regression introduced here — a role-shaped scope judgment, since fixing it would need `_prune_spawn_attempts()` to track which specific attempt_id the report loop actually printed this tick, a larger design change than this round's mandate (guarantee a report before pruning a record that dies fast). Per role-session scope-exceeded rule, did not spawn a peer role or file a new issue; reported it instead — documented with full reproduction in this record's "Open findings" section and in `docs/issue-2431/reports/implementation/2026-08-25-hunt-spawn-attempt-dead-pid-grace-gate.md`, and flagged as a candidate follow-up issue in a PR #2434 comment.
