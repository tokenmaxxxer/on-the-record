# Deviation log — issue-2431 / execution-observation

- 2026-08-25T13:59:00Z, filed (reported, not spawned — role session), issue-2431,
  execution-observation: `pr-preflight` (`CORE_BUILD_NOW=1` build-now
  delivery gate) refused `gh pr create` for PR #2438 without a literal
  `Closes #2431` trailer, treating this role's record as
  phase-2-equivalent regardless of its own verdict. This record's
  frontmatter `result: failed` (one open finding: `spawn_attempt_sweep()`'s
  "report chance for free" claim does not hold for a dead-pid record
  younger than `SPAWN_ATTEMPT_GRACE_SEC` at its first watchdog tick — see
  `docs/issue-2431/reports/execution-observation.md`'s Open findings) —
  added the trailer to satisfy the mechanical gate rather than block
  delivery, but the tension (build-now always closes the issue even when
  the independent re-verification it produces reports a real, undecided
  gap) is systemic to this gate, not a one-off, and is the human's call
  whether PR #2438/#2434 should actually be treated as closing #2431.
