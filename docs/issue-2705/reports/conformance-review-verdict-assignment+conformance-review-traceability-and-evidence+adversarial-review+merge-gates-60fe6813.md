---
issue: 2705
role: conformance-review-verdict-assignment+conformance-review-traceability-and-evidence+adversarial-review+merge-gates-60fe6813
author: conformance-review-verdict-assignment+conformance-review-traceability-and-evidence+adversarial-review+merge-gates-60fe6813
skills: conformance-review-verdict-assignment (skill-repository(c05de12)), conformance-review-traceability-and-evidence (skill-repository(c05de12)), adversarial-review (skill-repository(c05de12)), merge-gates (skill-repository(c05de12))
verifies_subject: false
code_under_review: PR #2774 (branch issue-2705/secure-coding-input-validation-injection-defense+adversarial-review-4b9dda8b, head 0d9858b02aba95b30be4b99db3227cb138629969) and PR #2872 (branch issue-2705/architecture-interface-contract-shape-952309f5, head b3091bfb600251c26703913e27001098e1181002), graded builder-blind against issue #2705's frozen Acceptance criteria
loop_state: complete
type: review
breaking: false
verdict: neither PR fully satisfies all three acceptance criteria. PR #2774 is Surface on criterion 1 (real live refusal for the literal named example, re-executed this session, but this session independently reproduced a fresh live bypass — bare `pushd` — in the same unmodified code, matching what merged verification PRs #2778/#2779 already found) and Surface on criterion 3 (cites, does not itself re-derive, a narrower prior enumeration). PR #2872 is Incorrect on criterion 1 (this session's own live probe against the unmodified guard returns exit 0/no refusal for the literal named example, and `grep` on the new companion hook's source finds zero `exit 2`/`deny(` occurrences) but Present on criterion 3 (a full 34-hook population walk, spot-checked and accurate). Both are Present on criterion 2 (re-verified live this session) and comply with the must-not clause. The two PRs touch disjoint files and are complementary in code, but neither alone closes the issue against the frozen criteria, and combining them still leaves criterion 1's literal "show the refusal" unmet for shapes PR #2774 does not cover.
upstream:
  - path: gh issue view 2705 (issue body + full comment thread, including the seam-consult and hold-lift comments)
    sha: same-commit
  - path: PR #2774 diff (gh pr diff 2774) and PR #2872 diff (gh pr diff 2872)
    sha: same-commit
  - path: docs/issue-2705/reports/adversarial-review-249cc937.md
    sha: becb449d74faf225b65289949fe52e8ac10ce513
  - path: PR #2778 (independent verification of PR #2774, MERGED)
    sha: dbe00dc415be8fb3854260895e441f42e738a2b5
  - path: PR #2779 (independent verification of PR #2774, MERGED)
    sha: fb75336a15f9dcea94cbe211a9f32b8c69ab5097
---

# issue-2705 — conformance-review-verdict-assignment+conformance-review-traceability-and-evidence+adversarial-review+merge-gates-60fe6813 record

## What was done

Graded PR #2774 and PR #2872 against issue #2705's frozen Acceptance criteria, builder-blind — judged the diffs, the live code (re-executed this session against real worktrees, not accepted from either PR's own record), and the issue's own comment thread (canonical operator text, not either PR's narrative).

canonical: `gh issue view 2705` output, read in full this session. The frozen acceptance criteria are three checks (guard fires on the bundled shape / refusal shown / provenance executed-live; the unbundled shape still refuses; every other `PreToolUse` hook reading staged/working-tree state enumerated with the command establishing each verdict) plus a must-not clause forbidding: (a) telling sessions to stop bundling, (b) moving the check to PostToolUse without naming the weaker promise, (c) fail-closed widening.

derived: `python3 gates/requirement_met.py 2705 2872` and `python3 gates/requirement_met.py 2705 2774`, both this session — result: identical for both PRs — every criterion advisory `[UNKNOWN]` (the tool cannot auto-judge judgment-shaped criteria) plus a hard block on criterion 3 only: "PR diff 에 before/after 수치 증거(예: '341 -> 41')가 없다" (no before/after numeric-count evidence in the PR diff itself). This mechanical block is diff-local and identical for both PRs regardless of enumeration completeness, so it does not distinguish them; my own reading below, backed by live re-execution, is the basis for the criterion-3 verdicts.

derived: `git fetch origin pull/2774/head:pr-2774-review pull/2872/head:pr-2872-review && git worktree add /tmp/wt/pr2774 pr-2774-review && git worktree add /tmp/wt/pr2872 pr-2872-review`, this session — result: both worktrees created cleanly at the PRs' current heads (`0d9858b0`, `b3091bfb`), used to run each PR's own shipped code directly rather than accepting either PR's own record narrative. Both worktrees were removed after use: derived: `git worktree remove --force /tmp/wt/pr2774 && git worktree remove --force /tmp/wt/pr2872`, this session — result: `git worktree list` afterward shows only this review branch's own working tree; `git status --short` shows only this record file as untracked.

### Criterion 1 — "The guard fires on the bundled shape" / "show the refusal" / provenance executed-live

**PR #2774 — Surface.** canonical: `on-the-record/hooks/gate-registration-guard.sh`, untracked in this review branch's working tree (this file exists only on PR #2774's own branch; read this session via the temporary `/tmp/wt/pr2774` worktree cited above), new block starting at the PR diff's hunk `@@ -145,6 +159,291 @@`, functions `_new_frame`/`_pending_add_segments`/`_match_untracked`/`_pending_add_targets`. The literal named check passes, re-executed live this session: derived: `python3 -m pytest test/test_gate_registration_guard_bundled_add_commit.py -v` run inside `/tmp/wt/pr2774` (this test file is likewise untracked in this review branch — it exists only on PR #2774's branch), this session — result: `24 passed in 3.66s`, including `BundledAddCommitRefusesUnregisteredGateTest::test_bundled_shape_refuses_new_unregistered_gate` (the literal named check: `git add gates/new_gate_2705.py && git commit -m 'add gate'` against the real shipped hook, asserting `returncode == 2`) and `SeparateCommandsShapeUnchangedTest` (criterion 2, both directions).

But the same code (no commits to this branch since 2026-08-29T20:29:05Z — derived: `gh pr view 2774 --json headRefOid,updatedAt`, this session) still has a live, unfixed gap in the same "bundled shape" condition, independently reproduced by this session rather than trusted from either PR's own claim: derived: in a throwaway clone made from the `/tmp/wt/pr2774` worktree, created an untracked probe fixture (never committed to this repo, deleted with the throwaway clone) and a `sub1/` directory, then ran two things against the same real command text —
```
$ ( pushd sub1 >/dev/null && pushd >/dev/null && pwd && git add gates/probe_bare_pushd2.py && git diff --cached --name-status )
/tmp/probe_2774b/repo
A	gates/probe_bare_pushd2.py

$ python3 /tmp/run_guard_probe.py "pushd sub1 && pushd && git add gates/probe_bare_pushd2.py && git commit -m x" /tmp/wt/pr2774/on-the-record/hooks/gate-registration-guard.sh "$(pwd)"
EXIT: 0
```
(`gates/probe_bare_pushd2.py` above is that untracked probe fixture — it was never committed to this repo, and no longer exists since the throwaway clone was deleted after this probe.) Real bash (bare `pushd` with no argument swaps the top two stack entries, returning cwd to the repo root) genuinely stages it, but the guard exits `0` — silent pass, no refusal — on the identical command text. This matches, and independently confirms with a fresh reproduction rather than a re-read, what `gh pr view 2778 --json state,body` and `gh pr view 2779 --json state,body` (both `state: MERGED`, read this session) already found live against this exact unmodified code: bare `pushd`, `pushd +N`/`-N` rotation, an env-var-prefixed `cd`, and unmodeled `$CDPATH` all bypass it. Rule 1 of `conformance-review-verdict-assignment` applies exactly: the artifact implements the literal check (demonstrated live above) but does not fire on the actual full condition ("the bundled shape," as a general category) the requirement names — Surface, not Present.

**PR #2872 — Incorrect.** Failing clause: "check: run `git add <new gates/*.py> && git commit` as a single Bash call and show the refusal" / "empty state: not applicable — the refusal must be demonstrated." Re-executed live this session, not trusted from the PR's own "Live demo A": derived: in a throwaway clone made from the `/tmp/wt/pr2872` worktree, created an untracked probe fixture (never committed to this repo), then —
```
$ python3 /tmp/run_guard_probe.py "git add gates/probe_2872_bundled.py && git commit -m x" /tmp/wt/pr2872/on-the-record/hooks/gate-registration-guard.sh "$(pwd)"
EXIT: 0
```
(`gates/probe_2872_bundled.py` above is that untracked probe fixture, never committed to this repo.) — no refusal, on the literal example the check names. canonical, independently re-read (not accepted from the PR's own record narrative): `/tmp/wt/pr2872/on-the-record/hooks/gate-registration-post-guard.sh` (untracked in this review branch's working tree — this new file exists only on PR #2872's own branch, read via the temporary worktree) — derived: `grep -n "exit 2\|deny(" /tmp/wt/pr2872/on-the-record/hooks/gate-registration-post-guard.sh`, this session — result: zero matches, confirming structurally (not just by the file's own header-comment claim) that this 435-line script contains no code path that can ever refuse a command. Its only response to the bundled shape is an asynchronous `additionalContext` notice on a *later, unrelated* tool call, after the commit already exists in git history. This is an active substitution of a differently-shaped guarantee (a report) for the one the check demands (a refusal), not a mere omission — rule 2 of `conformance-review-verdict-assignment` (Incorrect, not Absent, when the artifact actively does something that contradicts the stated condition rather than merely omitting it).

### Criterion 2 — "The unbundled shape still behaves as today"

**PR #2774 — Present.** Covered by the same live pytest run cited above: derived: `python3 -m pytest test/test_gate_registration_guard_bundled_add_commit.py -v` run inside `/tmp/wt/pr2774` (untracked in this review branch, PR #2774's own file), this session — result (already quoted above): `24 passed`, including `SeparateCommandsShapeUnchangedTest::test_stage_then_commit_still_refuses_unregistered_gate` and `...::test_stage_then_commit_still_allows_registered_gate`, both directions of the unbundled shape.

**PR #2872 — Present.** canonical: `gh pr view 2872 --json files`, this session — `on-the-record/hooks/gate-registration-guard.sh` is not among the changed files (structurally byte-unchanged, not just asserted). Re-executed live this session: derived: in the same `/tmp/wt/pr2872` throwaway clone, staged a second untracked probe fixture in one call, then fed the commit alone —
```
$ git add gates/probe_2872_unbundled.py
$ python3 /tmp/run_guard_probe.py "git commit -m x" /tmp/wt/pr2872/on-the-record/hooks/gate-registration-guard.sh "$(pwd)"
EXIT: 2
gate-registration-guard: newly-added gate/hook module(s) missing a spec registration row (issue #441/#684):
gates/probe_2872_unbundled.py: no row in docs/specs/enforcement-boundary.md
```
(`gates/probe_2872_unbundled.py` above is another untracked probe fixture, never committed to this repo.) Confirms the unbundled shape still refuses exactly as before.

### Criterion 3 — enumeration of every other `PreToolUse` hook reading staged/working-tree state

**PR #2774 — Surface.** The PR's own diff contributes no fresh enumeration; its record cites `docs/issue-2705/reports/adversarial-review-249cc937.md:232-267` (sha `becb449d74faf225b65289949fe52e8ac10ce513`, already on `main`, unrelated to this PR's own commits) — derived: `git show becb449d74faf225b65289949fe52e8ac10ce513:docs/issue-2705/reports/adversarial-review-249cc937.md | sed -n '232,267p'`, this session — result: six independently-run grep/wiring commands landing 8 total hits (6 `on-the-record/hooks/` + 2 core, matched against a `diff --cached`/`git status` text-pattern grep). This is real, command-backed evidence, but it enumerates only hooks that already matched that filter — it does not walk the full stated population ("all PreToolUse hooks in `on-the-record/hooks/` and core's `hooks/`") with an explicit verdict row for every hook, which the criterion's own "empty state: no other hook reads staged state — state that as the finding" clause implies is required to rule out a false negative from the grep pattern itself.

**PR #2872 — Present.** canonical: PR #2872's diff (`gh pr diff 2872`), `docs/issue-2705/reports/architecture-interface-contract-shape-952309f5.md`, "Open findings" table (PR diff lines 322-362) — enumerates all 21 `pretooluse_dispatcher.py`-routed on-the-record gates + 1 `hooks.json`-direct hook (`approach-cap-warning.sh`) + 12 core `PreToolUse` gates = 34 total, with a verdict and an establishing command/citation per row, including explicit "not applicable" rows for hooks that do not read staged state. Independently spot-checked this session, not trusted at face value: derived: `grep -n "script=" on-the-record/hooks/pretooluse_dispatcher.py | wc -l`, this session, this review branch's own working tree — result: `21`, matching the PR's claim exactly; derived: `sed -n '436,449p' "$CLAUDE_PLUGIN_ROOT_CORE/hooks/pretooluse_dispatcher.py"`, this session — result: a literal 12-entry `GATES` list, matching the PR's "12 core PreToolUse gates" claim exactly. This is a materially more complete population walk than PR #2774's carried-forward citation.

### Must-not clause

Neither PR tells sessions to stop bundling — derived: `grep -in "stop bundl\|don't bundle\|do not bundle"` against both full diffs (`/tmp/pr2774.diff`, `/tmp/pr2872.diff`, saved this session via `gh pr diff`), this session — result: no matches in either; both diffs reference the batching guidance (#2135) approvingly, never reversing it.

Neither PR silently widens to fail-closed. PR #2774: canonical: `on-the-record/hooks/gate-registration-guard.sh` (untracked in this review branch — PR #2774's own file, read via the `/tmp/wt/pr2774` worktree), new header comment at the PR diff hunk `@@ -33,6 +33,20 @@` — "No new fail-closed surface: a `git add` segment this guard cannot parse... simply contributes no pending targets, same fail-open posture as every other environment gap here"; confirmed structurally by re-reading `_pending_add_segments`/`_pending_add_targets` in that worktree this session — every unrecognized-shape branch is a `continue` (no-op), never an abort. PR #2872: `gate-registration-guard.sh` is untouched (see criterion 2 above). derived: the same `grep -n "exit 2\|deny(" /tmp/wt/pr2872/on-the-record/hooks/gate-registration-post-guard.sh` (untracked in this review branch — PR #2872's own new file) already run for criterion 1 above, this session — result: zero matches, i.e. zero denial paths at all, let alone a new fail-closed one.

PR #2774 does not move the check to PostToolUse — not applicable. PR #2872 does move (half of) the check to PostToolUse and explicitly names the weaker promise in multiple independent locations: canonical: PR #2872's diff (`gh pr diff 2872`), `docs/handbooks/hooks.md` new "gate-registration-guard.sh / gate-registration-post-guard.sh (issue #2705)" section, and `/tmp/wt/pr2872/on-the-record/hooks/gate-registration-post-guard.sh` (untracked in this review branch — PR #2872's own new file) lines 1-60, read this session — "What this pair does NOT restore: the strong guarantee — refuse the write before it happens — for the bundled shape specifically." This complies with the must-not clause's specific, narrower requirement (name the cost, don't present it as the same guard fixed) even though it does not thereby satisfy criterion 1's separate, literal "show the refusal" requirement — these are two different acceptance items, scored independently above.

## Why

Builder-blind grading means the two PRs' own "Closes #2705" framing (both PR bodies end with this trailer) is not evidence of anything on its own — canonical: `gh pr view 2774 --json body` and `gh pr view 2872 --json body`, both read this session, confirming both bodies end with "Closes #2705" — each PR is scored against the issue's frozen text and against directly re-executed or independently-citable evidence, preferring the issue's own comment thread (operator-authored, not either PR's narrative) wherever it bears on the must-not clause or on whether a PR's approach is already known-superseded.

canonical: `gh issue view 2705` comment thread, read in full this session — a "seam consult result" comment (posted after PR #2778 and PR #2779, both `state: MERGED` per `gh pr view`, found fresh live bypasses in PR #2774's just-shipped pushd/popd fix) put PR #2774 on hold; a later "hold lifted" comment clarified the must-not clause is a *condition* (name the cost) not a *prohibition* on moving to PostToolUse, and stated: "What is not available is the third thing this issue was stuck on: continuing to parse harder." PR #2774 is exactly that — a further round of incremental PreToolUse text-parsing, dated before the seam consult and never updated afterward (confirmed above: no commits since 2026-08-29T20:29:05Z, and this session's own fresh bare-`pushd` reproduction shows the gap is still live). This is why PR #2774 is scored Surface rather than Present on criterion 1 despite genuinely, verifiably refusing the literal named example: the issue's own subsequent, canonical history — and this session's own re-derivation of it — shows the same unmodified code still has an open, live, ground-truthed gap within the condition the criterion names.

PR #2872 is the PR that materializes the design the seam consult and hold-lift comment pointed toward (via PR #2864, closed onto this branch — canonical: `gh pr view 2864 --json state,headRefOid`, this session, `state: CLOSED`, head `3db2ebeebb72f5e39bf2b214c2e4ada412975371`, matching PR #2872's own stated upstream basis). It is the more thorough, more honestly-labeled artifact by the must-not clause's own standard, and its criterion-3 enumeration is genuinely superior — a full population walk, spot-checked in this session and found accurate. But the frozen acceptance criteria's literal text for criterion 1 asks for a demonstrated refusal, not a demonstrated report, and this session's own live probe against the unmodified guard, plus a `grep` finding zero denial paths in the new companion script, shows exactly what the design does not do: refuse. Grading the frozen text literally, this is Incorrect.

**Complementary or overlapping?** derived: `gh pr view 2774 --json files` and `gh pr view 2872 --json files`, both this session — the two PRs touch entirely disjoint file sets (PR #2774: `on-the-record/hooks/gate-registration-guard.sh` and its test file only; PR #2872: `docs/handbooks/hooks.md`, `docs/specs/enforcement-boundary.md`, `docs/specs/generated-paths.md`, a new `gate-registration-post-guard.sh`, its own test file, and `hooks.json`). No merge conflict. They are architecturally complementary in a defense-in-depth sense: PR #2774 closes several real PreToolUse-side gaps for the common case (though this session confirmed at least one, bare `pushd`, still open); PR #2872 adds an async safety net that will eventually surface any bypass the PreToolUse side misses, including ones PR #2774 does not close, since it does not predict from command text at all.

**But combining them does not close the acceptance gap.** Landing both still leaves criterion 1's literal "show the refusal" unmet for exactly the shapes PR #2774's parser does not cover (this session's own reproduction: bare `pushd`; PR #2778/#2779's merged findings: `pushd +N`/`-N`, env-prefixed `cd`, `$CDPATH`) — for those, the combined system's only response is PR #2872's async report, which this session confirmed structurally cannot deny (the `grep -n "exit 2\|deny("` zero-match result cited above). Landing order does not matter technically given the disjoint file sets; if landed, PR #2774 first (the older, narrower hardening) then PR #2872 (the newer, broader companion) is the more legible commit history, but that is a preference, not a requirement.

**Would merging either alone close the issue while leaving a criterion unmet?** Yes, for both, differently, per the verdicts above:
- PR #2774 alone: criterion 1 Surface (a live, unfixed gap this session independently reproduced), criterion 3 Surface (narrow carried-forward citation, no full-population walk in this delivery's own diff) — both fall short of Present.
- PR #2872 alone: criterion 1 Incorrect (no refusal ever demonstrated, by design, confirmed live and structurally this session) — a more severe shortfall than PR #2774's, since the criterion's own "empty state: not applicable — the refusal must be demonstrated" language treats a refusal as non-optional.

Recommend against merging either as a sole, issue-closing delivery. If forced to choose a single closing PR, PR #2872 is closer to the issue's own later-stated intent (the must-not clause's naming requirement, and criterion 3) but a human should explicitly decide whether "show the refusal" in criterion 1 is satisfiable at all under the PostToolUse design the operator's hold-lift comment sanctioned — if not, the frozen criterion 1 text itself may need an operator amendment, not just a different implementation.

## What did not work

None — this was a read-only grading session. derived: `git status --short` (this session, after removing both review worktrees) — result: only this record file listed as untracked; no code file was edited, no PR was merged, no PR was edited. derived: `git worktree list` (this session) — result: only this review branch's own working tree remains, confirming the two temporary worktrees used for live re-execution were fully removed.

## Upstream basis

- canonical: `gh issue view 2705` (body + 17 comments), read in full this session — the frozen acceptance criteria, the must-not clause, and the seam-consult/hold-lift comments establishing PR #2774's approach was later found incomplete and superseded as an ongoing strategy.
- canonical: `gh pr diff 2872` and `gh pr diff 2774` (1179 and 1571 lines respectively), read in full this session.
- canonical: `gh pr view 2778 --json state,body` and `gh pr view 2779 --json state,body`, both `state: MERGED`, this session — independent, already-landed verification of PR #2774's own shipped code finding fresh live bypasses; independently re-reproduced (bare `pushd`, cited under Criterion 1 above) in this session rather than trusted at face value.
- canonical: `docs/issue-2705/reports/adversarial-review-249cc937.md` at sha `becb449d74faf225b65289949fe52e8ac10ce513` (on `main`), read this session via `git show` — the enumeration PR #2774's record cites without re-deriving.
- derived: `python3 gates/requirement_met.py 2705 2872` and `python3 gates/requirement_met.py 2705 2774`, both run this session.
- derived: two temporary git worktrees of PR #2774's and PR #2872's branch heads (`/tmp/wt/pr2774`, `/tmp/wt/pr2872`, both created and removed this session) — used to run each PR's own test suite live and to fire independent bypass/refusal probes against the real shipped hook scripts, rather than accepting either PR's own record narrative.
- derived: `on-the-record/hooks/pretooluse_dispatcher.py` (this review branch's own working tree) and `$CLAUDE_PLUGIN_ROOT_CORE/hooks/pretooluse_dispatcher.py`, read live this session to spot-check PR #2872's 34-hook population count (21 + 1 + 12).

## Open findings

- Flagging for a human, not an open item this review can resolve: whether criterion 1's "show the refusal" is meant to survive the operator's own hold-lift ruling that a PostToolUse-based report is an "honest" alternative. canonical: `gh issue view 2705`'s "hold lifted" comment, re-read this session — as currently worded, the frozen text and the later ruling point in different directions, and neither PR in front of me resolves that tension; it is a spec question, not an implementation gap either PR failed to fill.
- Carried forward, not re-walked hook-by-hook a second time in this session: PR #2872's criterion-3 table's per-hook verdicts (only the two population counts, 21 and 12, were independently re-derived above, cited under Criterion 3).

## Next steps

None — `loop_state: complete`. derived: `git status --short` (cited above, this session) confirms this record is the only change in this session's working tree; no code changes follow from it per the task's explicit scope (grade only, do not merge, do not edit either PR).

## Skill verdicts

- skill-verdict: conformance-review-verdict-assignment — applied: invoked; used rule 1 (Surface vs Present) to score PR #2774's criterion 1, rule 2 (Incorrect vs Absent) to score PR #2872's criterion 1, and rule 4's carry-forward framing to evaluate PR #2774's criterion-3 citation, all cited by rule number above.
- skill-verdict: conformance-review-traceability-and-evidence — applied: invoked; every verdict above cites file path plus line range or hunk offset, the head sha of the PR the evidence came from, and — where evidence came from outside either PR's own diff (the issue comment thread, PR #2778/#2779, `docs/issue-2705/reports/adversarial-review-249cc937.md`) — the specific external artifact, its sha, and how it was fetched or re-executed, not a bare restatement of either PR's own claim.
- skill-verdict: adversarial-review — applied: invoked; the core discipline of this review (judge the diffs and the issue's canonical comment thread, not either PR's self-narrative; re-derive PR #2872's population counts and re-run PR #2774's test suite rather than trust either PR's own numbers; independently reproduce a live bypass in PR #2774's shipped code and a live non-refusal in PR #2872's shipped code by executing both against real worktrees) is this skill's blind-evaluator posture applied to a conformance review rather than a from-scratch artifact critique.
- skill-verdict: merge-gates — not-applicable: this task is grading two competing PRs against a frozen spec, not designing or auditing a CI/branch-protection merge gate; the "complementary vs overlapping, landing order" analysis above answers a landing-sequencing question the task asked directly, not a gate-shape question this skill's procedure (Steps 1-8, binary/machine-evaluable/fail-closed/combined-state) addresses.
- other mounted skills: work-in-english — not invoked via the Skill tool (guidance-only, enforced by core hooks rather than requiring invocation, per this session's own skill-obligations note); followed throughout by writing this record, all commands, and all citations in English while the conversation itself was addressed in Korean.
