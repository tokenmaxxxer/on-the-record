## Deviation: record draft rewritten twice against record-claim-guard.sh

Expected: the first drafted record write to
`docs/issue-2705/reports/adversarial-review-b6638c90.md` would pass
`record-claim-guard.sh` on the first attempt, since every quantitative
and defect claim was backed by a command this session had actually run.

What happened instead: two consecutive `Write` attempts were refused —
first for bare counts/outcome claims lacking an adjacent `derived:`/
`canonical:` tag and for two backtick-quoted scratch paths that the
guard correctly flagged as unreachable references (`gates/probe_unregistered_gate.py`,
untracked, in a scratch worktree deleted at end of session, never a
path in this repo's own tree; and a defect-verification path this
session never wrote to); second, one remaining defect-claim sentence
still lacked an adjacent fenced/`derived:` pairing. Fixed by adding
explicit `canonical:`/`derived:` tags to every section (including "##
Why", "## Open findings", "## Next steps", which had none) and by
rewording the two scratch-path references out of backticks with an
explicit untracked/never-written gloss. Third attempt passed.

No content claim changed across the three attempts — only citation
density and path-quoting style. Recorded per the deviation-loop
protocol (issue #2138/#803/#983/#2348) rather than folded silently into
the final record.
