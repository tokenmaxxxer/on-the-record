inline-fix: built and live-verified a full PreToolUse-side fix for `gate-registration-guard.sh` (a `git add --dry-run`-based projection of the bundled `git add` pathspec, ported from `handbook-trigger-gate.sh`'s issue-141 D2 pattern) before discovering, via `gh issue view 2705 --comments`, that this same class of fix had already been attempted and formally ruled undecidable on this issue across four adversarial rounds (`cd`/subshell resolution, `pushd`/`popd`, symlinks, `CDPATH`), and that my version was additionally weaker (no `cd`/`pushd` tracking at all). Reverted the file to byte-unchanged (`git checkout -- on-the-record/hooks/gate-registration-guard.sh`) and instead materialized the already seam-consulted, CHANGES-round-fixed `gate-registration-post-guard.sh` from PR #2864's branch onto this one. Full account: `docs/issue-2705/reports/architecture-interface-contract-shape-952309f5.md`, "What did not work".
