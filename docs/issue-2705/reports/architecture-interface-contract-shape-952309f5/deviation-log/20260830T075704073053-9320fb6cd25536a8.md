inline-fix: the first draft of the ad hoc `/tmp/otr-2705-timing2.py` re-measurement script staged a new gate file and a separate `enforcement-boundary.md` registration row as TWO commits per "clean" session, then fed only the first (unregistered-at-commit-time) commit's `tool_response` to `post` — mislabeling a genuine violation as a clean bundled commit. Caught by the resulting timing numbers not matching the fix's expected shape (dirty-from-ordinary-use showing ~32ms/call instead of returning to baseline). Rewrote it to stage the gate file and its registration row in the SAME `git add ... && git commit` call, matching the actual "clean bundled commit" definition the shipped test suite (`test_bundled_add_and_commit_with_row_already_staged_is_clean`) already uses. Re-ran; numbers then matched the fix's expected behavior (see docs/issue-2705/reports/architecture-interface-contract-shape-952309f5.md, item 1). No delivered code or test file was affected — the mistake was confined to the scratch `/tmp` measurement script, never committed.
