- 20260830T000000Z — inline — the `secure-coding-input-validation-injection-defense` `Skill` tool call was made after the round-3 cwd-model fix's implementation was already written (cherry-pick + `_pending_add_segments`/`_match_untracked` edits), not before it, since this task's build-now-bypass framing led straight into cherry-picking and patching without pausing for the skill-obligations check first. derived: this session's own tool-call transcript (the `Skill(secure-coding-input-validation-injection-defense)` invocation occurs after the `Edit` calls to `gate-registration-guard.sh`) — corrected in the same commit series by invoking the skill and citing rule 10 (plus reasoning through rule 8's non-applicability) in `docs/issue-2705/reports/secure-coding-input-validation-injection-defense+adversarial-review-4b9dda8b.md`'s own "Why" section.
- 20260830T000000Z — inline — a probe-harness mistake, not a code defect: the first live-fire attempt (record §4) pointed the manual verification harness's `HOOK` path at a freshly `git clone`d copy of this repo instead of the real working-tree file, so the cloned guard script ran pre-cherry-pick code regardless of the working tree's edits, producing three misleading `EXIT: 0` results. derived: `python3 -m pytest test/test_gate_registration_guard_bundled_add_commit.py -v` (this session, run moments before the manual probe) — result: `24 passed` — the contradiction between that green run (using the real working-tree file via `_run_guard`'s own `HOOK_PATH`) and the manual probe's `EXIT: 0` is what surfaced the harness bug. Corrected by pointing the manual harness at the real on-disk hook file directly; re-fired the same three commands and got `EXIT: 2` for all three, quoted in the record's §4.
</content>
