- Expected: the record file's first `Write` to `docs/issue-2705/reports/architecture-interface-contract-shape-3f3d4ef5.md` would pass `record-claim-guard.sh` on the first attempt. Actual: it was rejected twice — bare count claims ("7 tests", PR numbers) with no `derived:`/`canonical:` tag nearby, `canonical:`-missing status/defect claims in the "Why"/"Upstream basis" sections, and OUTCOME-shaped `acceptance: ... — result: ...` lines followed by inline backtick text instead of a fenced code block, plus one defect/cause claim in "What did not work" citing a refusal message without a `derived:`+fence pairing. Fixed by adding explicit `derived:`/`canonical:` tags next to each flagged claim and replacing inline result text with real fenced pytest/hook output captured this session — no claims were weakened, only citation placement/formatting changed.
- Expected: `git commit -m "<title>" -m "<body>" -m "Acceptance-recheck-N/A: <reason>"` (one `-m` per paragraph, per this session's own hook-contract guidance) would satisfy `acceptance-command-real-run-guard.sh`'s and `live-fire-claim-real-run-guard.sh`'s documented `Acceptance-recheck-N/A:`/`Live-fire-recheck-N/A:` escape-hatch trailers, needed because `docs/specs/enforcement-boundary.md`'s own pre-existing rows for those two gates quote their trigger-citation shape as illustrative example prose and false-positive-matched as a live done-claim when that file was staged. Actual: both gates' trailer regexes are `re.search(r"(?im)^\s*<Trailer>\s*:\s*\S", cmd)` against the RAW, single-line Bash command text — a later `-m` argument's content is never preceded by an actual `\n` in that raw text, so `^` never matches it no matter which `-m` it's in. Fixed by putting both trailers on their own lines inside one `-m` argument's value (a literal embedded newline within that one quoted string), which does put a `\n` immediately before each trailer line in the raw command text the gate inspects.
