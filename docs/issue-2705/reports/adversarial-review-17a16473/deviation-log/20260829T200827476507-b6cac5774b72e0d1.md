---
issue: 2705
role: adversarial-review-17a16473
---

Baseline-selection correction mid-review: the first attempt at
re-deriving PR #2763's "each of the three shapes fails against the
pre-fix parser, passes after" claim used `main`'s own tip (`1d6e746c`)
as the "pre-fix" guard script. That baseline has zero
`_pending_add_segments` code at all — PR #2753's own fix was never
merged to `main` (PR #2753 was sent back for CHANGES, not merged) — so
it tested "no bundled-add parsing whatsoever," a materially coarser
condition than the intended "PR #2753's first cut minus this PR's three
follow-up fixes."

derived: `git show 1d6e746c:on-the-record/hooks/gate-registration-guard.sh
| grep -c _pending_add_segments` — result: `0`, confirming the wrong
baseline. Against it, two of the exclude-pathspec regression tests
appeared to pass "pre-fix" for the wrong reason (no parsing at all
trivially allows everything, coincidentally matching those two tests'
own assertions), which would have understated the real fail-before/
pass-after evidence if left uncorrected.

Resolution: fetched PR #2753's actual head commit
(`c6068dcf496c11d1423814e22ab9975fb686aff7`, still reachable via `git
fetch origin c6068dcf496c11d1423814e22ab9975fb686aff7`) and re-ran the
same 9-test selection against that true first-cut baseline instead —
result: `7 failed, 2 passed` (matching the two tests' own class
docstrings, which describe them as defensive no-regression tests, not
shape-pinning tests). Restored the shipped fix and re-ran the same
selection — result: `9 passed`. Both transcripts are quoted in full in
this session's own record,
`docs/issue-2705/reports/adversarial-review-17a16473.md`, under "What
was done" §2 and under "What did not work".
