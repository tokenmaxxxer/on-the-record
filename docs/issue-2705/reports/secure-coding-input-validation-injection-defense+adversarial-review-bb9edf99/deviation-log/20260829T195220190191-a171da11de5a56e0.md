---
issue: 2705
role: secure-coding-input-validation-injection-defense+adversarial-review-bb9edf99
---

`git stash push --keep-index -- on-the-record/hooks/gate-registration-guard.sh`
followed by `git stash pop` (used to prove the new regression tests fail
against the pre-fix parser before trusting them as fail-before/pass-after
pins) produced a self-merge conflict: `--keep-index` left the fixed
version staged in the index while the working tree held the pre-fix
content, and `git stash pop` tried to reapply the same fix as a diff
against that already-fixed index instead of cleanly restoring it.

derived: `grep -n '<<<<<<<\|=======\|>>>>>>>'
on-the-record/hooks/gate-registration-guard.sh` located the conflict
hunks; reading one hunk directly confirmed the "Stashed changes" side
was the fixed version (contained `_pending_add_segments`'s cwd-stack
logic, absent from the "Updated upstream" side). Resolution: `git
checkout --theirs on-the-record/hooks/gate-registration-guard.sh`
followed by `git stash drop`.

derived: `python3 -m pytest test/test_gate_registration_guard_bundled_add_commit.py -q`,
run immediately after the conflict resolution above — result: `20
passed`, confirming the fixed file survived the conflict resolution
intact rather than silently losing part of the diff.

Already logged in the session's own record under "What did not work"
(`docs/issue-2705/reports/secure-coding-input-validation-injection-defense+adversarial-review-bb9edf99.md`);
this entry is the deviation-log-path copy per the deviation-loop
obligation.
