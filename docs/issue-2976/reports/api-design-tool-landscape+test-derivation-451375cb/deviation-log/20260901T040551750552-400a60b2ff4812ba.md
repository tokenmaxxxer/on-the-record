2026-09-01T04:05Z inline — implemented `--body-file -`/stdin changes to `gates/check_runner.py`'s `post_comment()` and `relay.py`'s `ensure_pushed()` PR-create call, mirroring the spawn.py stdin fix for symmetry with the `gh_body_not_heredoc` acceptance criterion, then reverted both via `git checkout -- relay.py gates/check_runner.py`. Reverted because neither call site ever runs through the Bash-tool shell string that `heredoc-command-refusal-gate.sh` inspects (both already used `subprocess.run([...])` argv lists), so the change addressed a caller shape the gate cannot reach, while breaking `test/test_branch_skill_field.py`'s `PrBodyTrailerWriteShapeTest` class (its fake-`gh` shim reads `argv[argv.index("--body") + 1]`, which raises once `--body` becomes `--body-file -`). Replaced with a regression-guard test (`tests/test_gh_body_not_heredoc_2976.py`) that checks the acceptance criterion directly instead. derived: reproduced the break with `python3 -m pytest test/test_branch_skill_field.py -k PrBodyTrailerWriteShapeTest -q`, result: `FAILED ... AssertionError: 'pr-create-failed' != 'pr-opened'`; re-ran the same command after `git checkout --`, result: `1 passed in 1.01s`. Full account in `docs/issue-2976/reports/api-design-tool-landscape+test-derivation-451375cb.md`'s `## What did not work` section.
