{
  "role": "ml-engineering",
  "source_standard": "Model Cards (Mitchell et al. 2019), https://arxiv.org/pdf/1810.03993",
  "required_fields": [
    {
      "name": "model_id",
      "type": "ref",
      "required": true
    },
    {
      "name": "intended_use",
      "type": "string",
      "required": true
    },
    {
      "name": "training_data",
      "type": "string",
      "required": true
    },
    {
      "name": "eval_data",
      "type": "string",
      "required": true
    },
    {
      "name": "ethical_considerations",
      "type": "string",
      "required": false
    },
    {
      "name": "verdict",
      "type": "enum",
      "required": true,
      "enum": [
        "pass",
        "fail"
      ]
    }
  ],
  "reference_resolution": {
    "rule": "model_id must resolve to an actual model artifact or registry entry — no orphan references (issue-515 invariant 2).",
    "checked_by": "on-the-record/hooks/role-spec-reference-guard.sh"
  },
  "recomputation": {
    "rule": "verdict is recomputed from eval_data's current metrics, never a standalone asserted field.",
    "checked_by": "TBD (follow-up — issue-521 out-of-scope note: per-role recomputation enforcement is a follow-up once evidence from real usage shows which roles need it)"
  },
  "write_scope": [
    "docs/issue-<n>/reports/ml-engineering.md"
  ],
  "loop_state": {
    "progress": [
      "training",
      "evaluating"
    ],
    "terminal": [
      "landed"
    ],
    "refusal": [
      "intended-use-undeclared"
    ],
    "error": [
      "model-unreachable"
    ]
  },
  "use_when": {
    "board_condition": "a model artifact is proposed for landing on the branch AND no ml-engineering record exists yet for that model_id"
  }
}
