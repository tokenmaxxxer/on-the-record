{"id": "dicequest-72-monster-scaling", "role": "implementation", "task": "Derive and implement per-stage monster damage/HP scaling per issue #72 (cut-6 finding 1): balance derivation first, then the field-sim wiring; keep first-Awakening median inside the 8-12 band. (Targets R1, R3.)", "expected": ["game-growth-system-design", "game-design-core-loop-and-progression"], "note": "Live gold case from the 2026-08-24 tm-dicequest spawn (issue #2124 comment) — pre-fix Recall@8 was 0. Progression-balance task on a game repo; both game skills' descriptions now name stage/monster/HP scaling."}
{"id": "fixture-version-flag", "role": "implementation", "task": "Add a --version flag that prints the tool version and exits.", "expected": [], "note": "Canonical trivial-fixture task: no cross-family expertise is needed; a good recommender injects nothing."}
{"id": "otr-2068-returned-pr-respawn", "role": "implementation", "task": "Observed repeatedly tonight (2026-08-23): the returned-PR / stranded-relay machinery keeps re-opening PRs and re-spawning sessions for issues that are ALREADY CLOSED with their delivery merged — each needing manual close. Root cause to verify: the relay/returned-PR path checks branch existence but not the subject issue's open/closed state. Fix: before re-opening a PR or re-spawning from a returned branch, check the subject issue state — closed issue => delete/ignore the stale branch and never respawn; and a respawn must carry the CURRENT task text, not the original spawn's.", "expected": [], "note": "Real spawn task (on-the-record #2068 body). Pure in-repo orchestration bugfix; no cross-family domain skill applies — judgment: expected empty."}
{"id": "otr-2100-admission-checklist", "role": "implementation", "task": "A deterministic pre-spawn admission checklist in the spawn gate: before any session is spawned, verify and refuse-on-missing: required capability tokens exist; all directive-completeness items are injected; a live watch/monitor subscription for the session's terminal event is registered; session budget caps are set. A deterministic gate rejection mid-flight is non-retryable.", "expected": [], "note": "Real spawn task (on-the-record #2100 body, condensed). Infra gate work inside this repo; no cross-family domain skill clearly applies — judgment: expected empty."}
{"id": "otr-2101-watch-hardening", "role": "implementation", "task": "Watch-layer hardening: lease/TTL auto-requeue, progress-carrying renewal, level-triggered reconcile sweep, dead-man's switch. Shift from death-detection to self-correcting structures: an in-progress board item carries a lease with expiry; an expired lease automatically returns the item to the dispatchable pool. The monitor emits a periodic coverage-OK signal; absence of the signal is the alert.", "expected": [], "note": "Real spawn task (on-the-record #2101 body, condensed). Watch/monitor infra; observability-* skills are about metric methodology for product surfaces, not this repo's watchdog — judgment: expected empty."}
{"id": "otr-2102-directive-diet", "role": "implementation", "task": "Orchestrate directive diet: byte-stable <=2.5KB index + progressive disclosure + prohibitions-to-gates (from 23KB/turn). Diff the injected blob across consecutive turns and eliminate every varying byte; keep the top always-relevant invariants inline; move each remaining section into an on-demand file; any directive prohibition a gate already enforces is deleted from the injection.", "expected": [], "note": "Real spawn task (on-the-record #2102 body, condensed). Prompt/cache engineering on this repo's hook — judgment: expected empty."}
{"id": "otr-2103-board-read-efficiency", "role": "implementation", "task": "Board-read efficiency: single GraphQL query, delta reads with cached snapshot, event-push substrate. One gh api graphql --paginate query returns N issues/PRs with state/labels/comments — replaces per-item gh issue view loops. Persist last_sweep_at; steady-state poll = one search updated:>last_sweep merged into a locally cached board snapshot.", "expected": [], "note": "Real spawn task (on-the-record #2103 body, condensed). API-call batching inside this repo — judgment: expected empty."}
{"id": "dicequest-upgrade-cost-curve", "role": "implementation", "task": "Design the shop upgrade cost curve so mid-game players face meaningful choices: set the per-level upgrade cost curve and currency sources and sinks so the growth system balance holds through stage 30 without a hard wall.", "expected": ["game-growth-system-design"], "note": "Derived from tm-dicequest economy work (DiceQuest program). Currency sources/sinks + cost/pacing curve is the declared trigger of game-growth-system-design; single-run reward loop is not in play, so core-loop skill is not expected."}
{"id": "dicequest-hp-bar-colorblind", "role": "implementation", "task": "Pick the HP bar, mana bar, and damage number colors so the states stay distinguishable for colorblind players and the text passes contrast on both light and dark backgrounds.", "expected": ["ux-engineering-color-visibility"], "note": "Derived from tm-dicequest HUD work. Color-only distinctions + text contrast is the declared axis of ux-engineering-color-visibility; brand palette strategy (brand-design-color-visibility) is not what the task asks."}
{"id": "release-semver-changelog", "role": "implementation", "task": "Cut the hotfix release: pick the semver bump for this backward-compatible bug fix and write the changelog entries, categorizing each change correctly.", "expected": ["release-engineering-semver-bump-selection", "release-engineering-changelog-entry-categorization"], "note": "Derived from recorded arcade-dodger release sessions (real invocations of both skills in session logs). Both declared triggers apply verbatim: semver bump selection and changelog entry categorization."}
{"id": "issue-525-cross-family-off-domain-fp", "role": "implementation", "task": "Follow-up E of #515. After batches 1-2, realize the remaining 33 roles. Phase 1 must propose its own family split and per-family scouting (MADR/Spectral/oasdiff/dbt-contract; SRE/ITIL/KCS/Diataxis; MEDDPICC/Dunford/SRM/NIST 8286 lineages), with sources cited per role; order not pre-committed. Same template, deployment, and minimal-fields rules as follow-ups A/D. May split into multiple delivery PRs per the phase-1 proposal.\n\n## Acceptance\n- check: same schema-validation pytest pattern over all new `roles/specs/*.spec.json`; final state = all 43 roles have a spec file, `ls roles/specs/*.spec.json | wc -l` equals 43.\n- check: no `roles/*.json` retains empty write_scope without an explicit verdict-role contract, and none retains a single-state loop_state — verified by a python -c sweep exiting 0.\n- provenance: executed-unit for sweeps; spec substance is human PR review per delivery PR.\n- empty state: new spec files; absence today is the documented starting state.", "expected": [], "note": "issue-525's real GH issue body (fetched gh issue view 525 --json body), the exact production task text issue #2166 used to investigate the two live false-positive mounts named in issue #2205's acceptance (market-analysis-mece-proposal and work-in-english). Measured live against the real skill-repository corpus (2026-08-24): market-analysis-mece-proposal ranks 10/269, work-in-english ranks 13/269 -- both outside _CROSS_FAMILY_CONSULT_TOPN=8, so neither ever reaches the judge or the exact-phrase fast path; precision@mount on this case is the regression guard for issue #2166's BM25-rank-based exclusion."}
{"id": "work-in-english-declared-phrase-self-inflation-fp", "role": "implementation", "task": "Diagnose and patch the B-tree index fragmentation regression in the query planner: the cost estimator undercounts page splits after a bulk insert, causing the optimizer to pick a full table scan over the covering index for range queries above 10k rows. Add an EXPLAIN-based regression test that pins the expected plan, profile the estimator against the production statistics snapshot, then fix this bug and open a pr once the benchmark suite is green.", "expected": [], "note": "Constructed (not a literal historical GH issue -- issue #2166s own Open findings could not locate the real #527/Korean-project session that first mounted work-in-english; gh search issues for its own declared phrase across on-the-record and tm-dicequest returns zero hits) but real-corpus reproduction of a residual bug found while building this gold case: work-in-english declares the illustrative trigger phrase \"fix this bug and open a pr\" (skill-repository/skills/work-in-english/SKILL.md); because _skill_bm25_document indexes that same quoted text (issue #2124 part 1), a task containing the phrase verbatim ranks work-in-english 1st/269 on this DB-indexing task that has nothing to do with language policy -- issue #2166s topN gate (consult.py, rank-outside-topN case) does not catch this because the phrase inflates the skill INTO topN, not out of it. Pre-#2205 consult.py auto-picks work-in-english via the exact-phrase fast path (current selection fails this negative); the #2205 fix re-scores with the matched phrase stripped from the task text and requires the skill to independently stay inside topN before trusting the phrase, so the fixed selection correctly mounts nothing (verified via git-stash before/after in the record)."}
